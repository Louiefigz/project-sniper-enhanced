# Two-hour editor implementation and qualification log

Date started: 2026-09-06. Status: active implementation; not complete or release-qualified.

## Goal and authority

The user authorized implementation of the [full enhancement plan](TWO_HOUR_VIDEO_EDITOR_PLAN_2026-09-06.md), tests, actual audiovisual quality control, and measured timing, with all user workflows exercised through the HyperFrames UI. A persistent Codex goal is active for this work.

Preserve the substantial existing dirty worktree. It includes the Studio implementation and command-driven-editing authority work; those changes are not attributed to this task. No user project or approved master may be overwritten for testing. Qualification uses isolated candidates/copies and the normal approval controller.

The UI target is HyperFrames Studio integrated with Sniper's existing Producer controller. HyperFrames is the graphics/review surface; the canonical FFmpeg assembly remains the deliverable renderer. Palmier is not required for this target. A CLI/unit success does not count as a UI end-to-end success.

## Completion checklist

- [ ] A: Complete request/attempt/stage timing, failure/queue attribution, report/UI visibility and baseline trace.
- [ ] B: Explicit guided/autopilot policies, durable cut/treatment authority, MP4-only launch, stop/resume and clause persistence.
- [ ] Intro: Playable sound-on intro preview, natural-language local revisions, exact user/system approval and body handoff.
- [ ] Studio: Open/reuse/readiness/status/diff; safe parent-bound sync through shared mutation authority and complete QC before delivery.
- [ ] C: Context/evidence reduction with defect-recall tests, mechanical issue routing and decode reuse.
- [ ] D: Catalog schemas/properties/eligibility, preview assets/layout and source-footage qualification.
- [ ] E: Resource-aware scheduling, cache/proof dependency validity, local repair and durable request budgets.
- [ ] F1: One float pre-master/master path, whole-program analysis and actual listening.
- [ ] F2: Source-aware color, whole-timeline grouping, transform history, UI and preview/export parity.
- [ ] F3: Validated audiovisual reviewer with explicit automatic versus assisted qualification.
- [ ] ZIP: Selected analyzer/audio/QA/CLI improvements ported only after identified compatibility risks are tested.
- [ ] G: Cold/warm short and produced long runs through UI, full-output quality comparisons, scoped revisions and fault recovery.
- [ ] Adversarial review, standards/security checks, full relevant test/build suite and documentation updated.

Nothing in this checklist is satisfied solely by a mock, synthetic fixture, diagram or existing historical benchmark.

## Current implementation slice

### Placement coverage correctness

Existing `placement_verify.py` exempted a whole graphic after any b-roll/title-card overlap. The first fix partitions exact covered/uncovered intervals, records interval-specific evidence, resolves geometry once, and verifies every uncovered span even after another probe fails or skips. Malformed recognized intervals fail validation before the measurement exception handler. Existing PiP exemption, explicit-pin WARN and calibration policy are unchanged; these policies are not newly certified as sufficient for unattended quality.

Files owned by this slice:

- `scripts/producer/graphics/placement_coverage.py` (new)
- `scripts/producer/graphics/placement_verify.py` (minimal extension of existing dirty work)
- `scripts/producer/tests/test_placement_coverage.py` (new)
- `scripts/producer/tests/test_placement_verify.py` (partial-overlap bug expectations replaced with actual full-coverage expectations)

The source/test seams before editing were archived, along with timing/UI launch seams, at `/private/tmp/sniper-implementation-baseline.o7s26E/initial-owned-seams.tar.gz`. This is a local task backup, not a full repository or output-quality baseline.

### Timing and test observations

Wall times below include Python startup/imports and were measured with `/usr/bin/time -p`. They are test execution times, not video-generation throughput.

| Observation | Result | Wall time |
|---|---|---:|
| Existing placement verification suite before changes | 20/20 pass; two tests encode the partial-overlap exemption bug | 3.27s |
| New adversarial coverage tests against original code | 10 test methods; 23 subtest/assertion failures and 3 errors, confirming defect | 2.86s |
| All `test_placement*.py` after first fix | 94/94 pass; unittest execution 1.601s | 4.48s |

Decoded-frame follow-up found that a positive wall-clock gap can expose no displayed frame: the compositor's inclusive, serialized time windows covered the two adjacent PTS. The verifier now lazily indexes packet PTS once per verification pass, samples only uncovered displayed PTS (including first/last), checks the actual decoded seek timestamp, and records sampled PTS. Index failures are memoized only within that pass to avoid repeating a 30-second timeout for every span. Nonfinite timestamps fail explicitly. Existing no-occlusion sampling, median-face aggregation, PiP policy and frame-sharded card behavior remain unqualified limitations.

After the frame-aware correction, 98 placement tests passed in 3.33s. Follow-up added two more decoded-media cases and 17 index/sampling tests. A combined placement/timing/baseline run passed **159 tests in 5.73s wall** (2.451s unittest). This is not creator-footage or end-to-end qualification.

### Timing v2 implementation

TS/Python spans now carry run, attempt, writer and span identity, parent linkage, terminal execution status and own-clock elapsed time. The baseline reader pairs exact identities, reports incomplete/invalid records and retains legacy attribution explicitly. Inclusive stage sums are not presented as request wall time. The worker and its primary subprocess dispatch carry the new context. This is a partial Package A implementation: launch/claim preparation, signal termination, some gate spawns, nested Python propagation, durable request budgets and full UI reporting remain open.

Adversarial review also corrected three telemetry failures: nonzero SystemExit was mislabeled interrupted; huge integer metadata/Unicode write errors could escape best-effort logging; and enclosing baseline capture dropped the new coverage event. Timeouts now retain the attempted run, captured partial-stream hashes and bounded tails, diagnostic events, null exit code and explicit incomplete state. No immediate repeat runs after a timeout because descendant cleanup is unverified. Failed current-path records no longer require a successful repeat-picture oracle, but cannot claim completion. Pre-timeout subprocess buffering and unflushed child diagnostics remain limitations.

Focused timing/baseline tests: 42 Python tests passed in 4.33s wall; TS legacy/v2 passed in 0.10/0.15s. Latest combined result above supersedes isolated counts; do not sum overlapping suites.

### HyperFrames GUI and migration safety

The four GUI launch paths now explicitly request MP4-only controller delivery, without requiring Palmier startup. Saved-plan actions still save/review the plan and use isolated render + full current controller gates/critics/promotion. Legacy request hashing/defaults remain unchanged; a hybrid checkpoint cannot silently resume as MP4.

Adversarial review caught a migration risk: bypassing Palmier adapters could still promote a new Sniper final from an older Sniper plan in a human-owned Palmier project. A backend guard now rejects ambiguous/manual/promoted/orphaned state before refit or job creation. A genuine unchanged non-authoritative legacy bootstrap requires bounded canonical readback. Explicit policy-aware legacy Resume UI is being added; no automatic migration/fresh fallback is allowed.

The delivery receipt now uses schema v2 and scopes zero Palmier calls to render/delivery adapters. Legacy launch preflight is explicitly outside that receipt; older immutable receipts are not rewritten.

New Studio API supports read-only status and guarded open/reuse only. It holds the shared project lease through readiness, validates parent/base/copied-media bindings, preserves pending edits, and refuses regeneration beneath an active stale session. The editor has a real embedded Studio panel with dirty/active-writer guards and explicit timing/copy-only caveat. Safe sync/apply, parent-bound approval and full spatial preview parity are not yet implemented.

`SNIPER_WORKSPACE_ROOT` permits process-local test isolation without altering personal config. Its registry lives under that test workspace; absent override retains the original paths. Invalid/relative/root/traversal values fail rather than falling back to personal state.

### Actual browser observations (ongoing, synthetic only)

Test server: loopback port 3327, workspace `/private/tmp/sniper-ui-qualification.KsUcgq`. Current owned server session 29532. Fixture factory: `scripts/producer/tests/fixtures/create_studio_ui_fixture.py`. It creates 12 seconds of 1080p synthetic blue footage, a quiet test tone and three real catalog components. No final approval or delivery receipt is fabricated.

| Observation | Actual result | Timing |
|---|---|---:|
| Synthetic fixture preparation | New isolated project; original projects untouched | 0.67s wall |
| First Studio open through editor button | Failed readiness even though preview server started; macOS lsof reverse-DNS mismatch | 10.6s UI |
| Readiness correction | Require numeric lsof output; regression added | 23 backend tests passed in 0.514s |
| UI retry | Real embedded Studio, base picture, 3 components and audio timeline loaded | 1.0s UI |
| Playback | Played to end; inspected callout at ~6s and quote at frame270/9s in actual canvas | Functional observation, not render throughput |
| Opening-only file mutation | Upstream auto-assigned IDs rewrote JSON attributes; false pending edit + lint errors | Defect found, fresh-fixture retest pending |
| Upstream Export/Render controls | Visible despite review-only integration | Server-side denial and guard-identity work underway |

The generator now seeds stable editor IDs; an actual pinned-runtime persistence test proves zero automatic writes on a fresh fully seeded host (and one write if an ID is removed). The first fixture is intentionally preserved with its observed rewrite. It is not force-reset to make tests look clean.

### Audio headroom regression (ongoing)

The opt-in private dialogue-program mixer converts its float sum to signed integer PCM before any downstream master. Two legal 0.8-peak stems yielded a float sum of 1.36568546 (+2.71dBFS), but the current integer program saturated at 1.0 (maximum sample error 0.36568546). Applying the existing master filter afterward met -14.0 LUFS while retaining clipping distortion (12.1079% residual-to-total RMS ratio on a synthetic 1kHz tone; about 1.466% residual energy fraction). A passing LUFS figure alone therefore does not establish clean audio.

This is the private program lane, **not** proof that normal assemble uses that boundary, and it does not automatically call the master. Four real-media tests initially produced two passing controls and two failing acceptance cases in 1.40s. Float program output + versioned policy/receipt changes are underway; source/auxiliary s32 authority stays unchanged. A separate normalization headroom issue is under investigation.

### Test-runner finding

The first `npm test` failed its skill-surface preflight but printed `all tests passed` and exited 0 because the final shell echo was outside the conditional chain. The runner now groups the loop and gates the success echo. Its stale command list was updated for the already-existing `produce-studio` command. The first correctly failing full run stopped at the missing Studio route-matrix disposition in 57.14s. Adding that route as explicitly short/long **unqualified** and regenerating the executable matrix fixed the contract gap. The next full `npm test` passed in **247.34s wall**. This excludes the separately located Studio API tests and all Python/media qualification. The first misleading green result remains a failure, not counted as a pass.

### Fresh Studio / timing follow-up

The second fresh fixture opened through the actual GUI in **1.4s**, with no false pending edits and no JSON-attribute errors. A remaining informational base-video lint warning led to a generator `clip`-class fix; a fresh third-fixture test is pending. Actual upstream Render/Export clicks returned 403 and created visible failed-export UI rows, confirming the server-side guard but exposing confusing UX. Audited, pinned-shell control disabling is underway; the server guard remains authoritative. No final video was created or approved by these tests.

Studio verification: 86/86 Python generator/sync/edge-case/review/ID tests passed in 5.038s (authorized loopback/process checks, no skips); 29 backend tests passed in 0.490s before the additional legacy-ID blockers, then 31/31 in 1.011s. The player Puppeteer suite was not run; actual UI uses CUA. Missing/duplicate host IDs now block read-only admission rather than rewriting existing views.

Policy-aware Resume now derives explicit hybrid/MP4 delivery only from the matching validated durable job. Seven new tests, actual status JSON, rendered labels, seven related suites and type-check passed. New suite: 0.41s. Missing/invalid/wrong-project journals cannot fall back to fresh generation or silently change delivery mode.

A read-only job timing panel is being integrated into project details. It retains the original job wall start across resume, freezes stopped jobs, pairs TS/Python spans by exact identity, excludes conflicting pairs, exposes missing/legacy data, and distinguishes overlapping stage work from elapsed time. This is observation only: request-wide active/user-wait/generation budgets and pre-launch preparation remain unimplemented. Initial parser/real-writer/Python-oracle/UI-copy tests passed in 0.30s; expanded actual status-route checks are pending.

### Third fixture and actual Elements correction

The third new synthetic fixture prepared in **0.70s** and opened Studio in **1.3s**. The real player showed its base and all three components, with zero lint findings, no false pending edits, and disabled render/export controls. The server continues to deny export independently. A later visual-only correction mutes the disabled controls; that presentation change still needs a fresh browser check. These are review-only UI observations, not delivery qualification.

The first Elements optimization passed HTTP dependency tests but **failed in the actual opaque sandbox iframe**: the statement card rendered an empty container. Shared runtime/CSS API requests were blocked by the existing local-request/CORP boundary. We preserved the sandbox and security policy, reverted delivery to safely inlined fixed runtime/CSS, and withdrew the apparent shared-CSS performance benefit as a functioning optimization. All 53 generated documents now have no external runtime/CSS dependencies and their scripts parse (4 route tests, 0.807s). Actual CUA remount displayed the statement text. Referenced icon/image embedding remains in progress and needs separate browser qualification.

A parent/iframe nonce-bound health handshake now exposes runtime/asset failure instead of a silent blank preview; the actual gallery displayed `gsap is not defined` on the failed document. The restored document rendered readable white text, but visual inspection caught a dark-blue catalog default overriding the source template's intentionally light accent. Only the sample/default is being corrected; existing authored projects are not rewritten. Health readiness proves runtime initialization, not animation/readability quality. The protocol tests, type-check and focused lint passed; the player-overlay integration is undergoing follow-up checks.

### Timing propagation and readback extension

Timing context now propagates through planning/audit gates, current render graph, assemble/render/draft and Studio subprocess seams. A real three-process test proves lineage without global environment mutation. Five Python propagation tests passed in **3.08s wall**; two TS tests passed in **0.17s**. Related Studio tests required authorized process/loopback access and then passed (31 tests, 2.74s). Pre-cut ingest and deliberately isolated container environments remain outside this propagation slice.

The project timing endpoint now pairs the exact root and `base_work` journals, bounded to 4 MiB/20,000 rows combined, and explicitly reports missing/rejected sources. Conflicting duplicate identities, foreign runs, symlink/hardlink inputs, malformed clocks and oversized journals cannot yield apparently complete totals. Actual status-route, resumed-job and cross-process writer tests passed. Five related suites reported 12 tests in **0.47s**; type-check passed in **1.69s**. Node path checks are not an openat-grade defense against hostile ABA directory replacement. No active/wait/preparation/generation budget or two-hour throughput claim is implemented by these summaries.

### Audio implementation and remaining delivery findings

Private dialogue-program output now uses a versioned float PCM boundary instead of clipping the sum to signed integer PCM. Channel normalization also retains float headroom while explicitly preserving the previous multichannel matrix gain. Source/channel-choice authority is unchanged; new output-policy receipts cannot reuse historical integer bytes. Music mastering reuses the shared linear/static/dynamic dispatch and measures the exact program-plus-bed sum. Full encoded-candidate LUFS/true-peak checks reject an out-of-policy result before replacing a public output. The initial combined **99 tests passed in 46.71s wall** (43.810s suite); subsequent delivery tests are not included in that count.

The high-crest stress case still lands outside policy (~-20.3 LUFS). It now fails closed and preserves the previous public bytes; this is correct rejection, not a successful quality improvement. High-LRA material retained its quiet/loud ratio in the synthetic test. The optional without-music output was found to remux PCM into MP4; its new AAC-copy-or-audio-only-encode route passed five real-media tests in **9.14s wall**. It preserves original picture packet payloads and PTS in those cases. A separate with-music regression retains all 72 coded pictures but shifts some 24fps timestamps by **0.333ms** through Matroska; correction and exact-clock tests remain open. Full-decode failure handling is being independently reviewed so partial loudness statistics cannot qualify damaged media.

### Guided workflow contract and benchmark access

Current cut geometry already survives visual-plan lane changes, but full treatment intent is included in cut-review authority. Narrowing that existing digest in place would weaken legacy approval. The implementation must introduce versioned cut/treatment authority alongside full current intent, preserve original receipt/lock bytes, and classify ambiguous brief/reference changes as cut-affecting. The first backend slice is a real durable `awaiting_cut_approval` outcome; ordinary Resume and fresh render must not approve it or overwrite it. Acceptance additionally needs an exact, playable cut preview—none currently exists at this pre-visual boundary. No guided/autopilot workflow is advertised as connected yet.

The requested real ten-minute source is still unavailable: macOS denies read-only ffprobe of the existing Downloads `C0679.MP4`, including the authorized retry. The user was asked for an accessible source or file-picker selection. An existing 91.967s rendered derivative can be probed, but is not raw footage or a ten-minute benchmark. No protection was bypassed and no original project was changed.

## Newly inspected integration constraints

### Follow-up implementation and actual UI findings

All **53/53 Elements catalog previews** reached nonce-bound runtime readiness through the actual embedded UI. Representative visual checks covered the statement card, kinetic quote, SVG stroke/icon badges and image-backed UI zoom. The final statement-card screenshot shows readable light-blue emphasis, and Enter successfully opens its showcase. This is runtime coverage, not complete visual/motion qualification of all 53 assets. The catalog/backend asset suite passed **45 tests in 1.046s**; type-check and focused lint passed. Exact declared local assets are safely embedded; no iframe sandbox or local-request policy was weakened.

The third Studio fixture re-opened in **0.5s**, with muted disabled export controls. The fourth new canonical-ID fixture prepared in **0.73s wall** and opened in **1.5s UI**. The first import preview returned unchanged in **0.8s**, preserving the synthetic project's known lint warnings. No approved final exists in this fixture.

Actual Inspector testing found another defect that backend-only tests did not expose: selecting the statement-card timeline wrapper and changing End from 3.50s to 4.5s fails with “Couldn't find this element in the source file.” The pinned Studio UI attributes the host wrapper `gfx-01` to its composition file, while the wrapper is in `index.html`. The failed fixture is preserved; source-attribution correction and a fresh actual-UI retest are required. Native copy-control discoverability is also under investigation. Safe backend import tests are not counted as successful end-to-end human editing.

Studio import now has explicit preview/apply-to-draft controls, immutable original/proposal evidence, exact current-plan compare-and-swap and replayable pre-save receipt intent. It supports repeated timing/copy imports and undo-to-original without regenerating the Studio host or declaring the draft delivered. **10 backend import tests passed in 7.12s**; six client tests passed in **0.024s**, with type-check and focused lint. Client tests cover pre-request preview invalidation, canonical reload after success or ambiguous failure, exact proposal retry, cancellation and escaped diff display. Full actual edit/import/reopen qualification remains open because of the Inspector defect above.

The guided cut-first backend now durably pauses at `awaiting_cut_approval`; a normal resume or new render cannot approve or overwrite that state. Exact receipt/plan/source authority is frozen in a closed request contract. **32 tests across 14 suites passed in 5.02s**, plus two launch tests ensuring rejected launches do not mutate intent, plan, job history or snapshots. A playable exact cut preview and explicit acceptance UI are still being implemented; the workflow is not yet advertised as connected.

The expanded audio regression cohort passed **111 tests in 62.40s wall** (59.528s suite). Both with- and without-music paths preserve original picture payloads and exact rational timestamps in covered cases, eliminating the earlier 0.333ms intermediary-container drift. Full AAC decode must succeed before loudness statistics can qualify the output. Corrupt-tail, nonzero/VFR-clock, silent-audio and high-crest cases fail closed; the high-crest fixture remains out of policy rather than being called improved. A three-second dual-variant test took **1.919s**, including 16 FFmpeg calls, 16 total ffprobe calls and seven full-media hashes; that cost is recorded, not extrapolated into a ten-minute SLA.

An additional ordinary Audit B regression reproduced a corrupt AAC tail yielding plausible -14.0 LUFS/-13.1 dBTP statistics despite decode failure. Versioned full-decode evidence and an early controller stop for mechanical audio failure are being integrated. Passing loudness numbers alone must not send a damaged candidate into expensive visual review or promotion.

Three hard-coded “~3 min” rebuild expectations were removed from UI copy because no measured workload-specific estimate supported them. Current run timing is shown instead. Ten-minute cold/warm generation, full listening/visual comparison and two-hour throughput remain unqualified.

### Native edit, strict audio gate and preview boundary follow-up

Fresh fixture 5 prepared in **0.64s wall** and opened in **1.4s**. A generator-owned, exact-ID provenance shim corrects the pinned Studio runtime's attribution of host wrappers to child composition files. The actual Inspector now saves End 3.50s → 4.50s, with `index.html` reflecting a 3.50s duration beginning at 1s. Actual frame120/4s playback shows the statement still visible. This also triggered upstream serialization of JSON attributes into valid HTML entities, which its own static JSON reader incorrectly rejected. Guard v3 applies an exact hash-pinned, in-memory decode-once interoperability patch using the pinned CLI's parser; it does not modify vendor files, project files or waive lint rules. The exact old fixture-5 process was stopped, and the same pending edits reopened in **1.3s** with zero JSON lint findings. Earlier failure fixtures remain preserved.

Adversarial real-fixture review reproduced three import defects: an inconsistent re-sealed original object could import an unlisted cut change; an inconsistent first proposal could write then falsely report not committed; and a failed pre-write save followed by another edit could trap the stale proposal. Full root/proposal internal consistency and explicit safe pending-intent supersession corrected them. All three independent reproductions passed on the fixed source in **4.71s wall**. Owned Studio/import/Elements coverage passed **63 tests in 7.007s**, with type-check/lint. Actual full GUI import/repeat/reopen qualification is still pending; native copy controls are not yet provided.

The Sniper Undo integration had a separate version problem: importing v2 then restoring a v1 history snapshot made Save correctly reject stale authority. History restoration now restores content while preserving the current saved version. The actual save transaction regression exercises import v2 → Undo/Save v3 → Redo/Save v4, preserves cut content and still rejects a genuinely stale external version. Two tests passed in **0.197s**; actual GUI Undo remains to be exercised.

Strict Audit B is now versioned and requires successful whole-audio decode plus finite in-policy metrics bound to the exact candidate. Mechanical audio failure stops before visual critics/creative repair and cannot promote the candidate. The full **111-test audio regression passed again in 63.03s wall**; **105 affected audit/audio-quality/Palmier tests passed in 10.18s wall**. Seven TS gate/policy/controller/approval/promotion/Palmier suites passed in **9.18s**, with type-check/lint. One intermediate test failed because its synthetic fixture lacked assembly proof; the fixture was corrected without weakening production. Routed strict audit adds **three** full-media hash reads: two inside Python and one at the direct gate. Held controller/approval hashes are reused. This cost is not yet a ten-minute benchmark.

Production worker wiring now passes its actual held lease into private cut-preview generation, keeps system time active throughout preparation, and atomically stores request + preview pointer when waiting starts. New worker settlement rejects pointerless pause outcomes; historical pointerless checkpoints remain readable but cannot be played/accepted. Read-only cut-review descriptor/video routes derive paths only from exact paused request/attempt/receipt identities. Range playback hashes and streams the same open descriptor, rejects links/changed bytes, and cleans up cancellation. They do not accept a cut, change a plan or render a final. Nine state/boundary tests, including malformed/backwards clocks, passed in **3.624s**; six media/local-policy and two history tests passed together in **0.253s**. Actual preview UI/acceptance/continuation is not connected yet.

Source-derived audio oracles rejected two tempting preview shortcuts: duration-normalized concatenated AAC still shifted an interior impulse by **8.83ms**; an identity-speed `atempo=1.0` filter shifted another by **12.535ms**. Private preview audio now derives from admitted sources in float, omits the unity no-op, and encodes once. An additional **21ms** copied-picture start offset required exact private timestamp-origin correction with unchanged packet content/relative timing proof. Nonunity speed exposed a separate inherited CFR discrepancy and remains outside the first private preview class. These are synthetic engineering findings, not listening evidence or preview/final parity. Ordinary final rendering still needs the corresponding audio/clock work.

Readback efficiency remains open: each current cut-review request repeats whole preview/toolchain verification and four pipeline-authority traversals; a video range then hashes its media again on the streamed descriptor. A scoped evidence/media split and per-observation authority reuse are under review. No stat-only cache or reduction of required source/media proof has been introduced.

### Actual supported Studio import, cut-review UI and reduced media reads

Fixture 5's first actual import attempt correctly failed the full plan checks: the synthetic 12s produced-longform plan was below the mode floor, lacked required transcript/style evidence, and extended a hold-to-cut graphic into a mid-shot hard exit. That failed fixture is preserved. Its Inspector/JSON fixes were necessary but did not prove a successful valid import.

New fixture 6 is a genuinely valid **18s short/light portrait** project, with a baked opening hook and a truthful empty transcript for its synthetic tone. Setup took **1.86s wall / 1,761ms reported**. Actual production lint has zero errors; two warnings are retained: no face box on this no-person plate and the callout's measured terminal partial fade. The fixture is not audiovisual-quality or production-throughput evidence. Four real fixture/bridge tests passed in **2.874s**.

Actual HyperFrames UI sequence on port3996:

| User action | Observed result | UI request timing |
|---|---|---:|
| Open Studio | Real base, three components, zero native lint errors; exports disabled | 1.3s |
| Inspector title End 3.50→4.50, Preview changes | Exact sole `g-00000001.outEnd` diff, no blockers | 1.0s |
| Apply to Sniper draft | v1→v2, outEnd4.5 | 0.5s |
| Native Undo, Preview / Apply | Reverse diff4.5→3.5, v3 | 0.5s / 0.5s |
| Native Redo, Preview / Apply | Diff3.5→4.5, v4 | 0.5s / 0.5s |
| Close and reopen | Existing session and native Undo preserved | 0.6s |
| Preview after reopening | No changes; no duplicate apply | 0.5s |
| Sniper Undo then Save | v5, outEnd3.5; actual GUI CAS succeeds | Not separately instrumented |
| Sniper Redo then Save | v6, outEnd4.5; actual GUI CAS succeeds | Not separately instrumented |

Read-only disk checks after these actions prove cutTrack unchanged and no final.mp4 created. The native source is retained. Sniper's independent save intentionally creates a new canonical version outside the earlier import chain; future import must requalify, not ignore that change. Reopening exposed misleading banner copy claiming already-imported changes were unapplied; it now correctly says Studio differs from the original build and requires Preview changes to determine outstanding imports. Native copy editing controls are still not validated/provided; backend-supported copy tests are not counted as actual human copy editing.

The cut-review screen now displays only a verified historical preview, with playback metadata checks, exact local identities, explicit non-delivery scope and a waiting clock. Project cards route waiting jobs to Review cut; ordinary Generate/Resume are not shown, and timeline mutation stays locked. The screen deliberately does not yet enable acceptance while its separate transaction is being implemented. Ten state/real-lease boundary tests passed in **4.312s**. Twelve client/media/project-state suites/tests passed in **0.397s**; after adding SSR coverage, the five-test client/editor cohort passed in **0.276s**. Type-check passed. Initial focused lint found cascading effect state updates; keyed asynchronous results corrected them and focused lint passed. Rendering or metadata readiness does not attest that anyone watched or listened.

The metadata/media split is now wired only for video/HEAD. Proof, manifestation and toolchain checks remain; the streamer still hashes the entire movie on its exact opened descriptor before serving any byte range. Descriptor, rendering and acceptance retain the strong media reader. This removes exactly **one N-byte movie traversal per playback request**, with no cache/stat substitution. Tests prove missing or same-size-corrupt media is never strongly qualified, metadata cannot hide the movie as a toolchain file, and HEAD/short ranges reject corruption outside the requested range. Four pipeline-authority traversals and remaining full toolchain verification are still unoptimized. No ten-minute wall-time benefit is claimed.

Explicit alternative SSE terminal events now allow a guided pause without misreporting a missing final output; output-only callers still reject that pause as delivery. Abort releases a pending body read and handler errors propagate. Three tests passed in **0.187s**; these transport tests do not prove a connected creator workflow. Exact human acceptance, launch-pending recovery, post-cut treatment authority and intro approval remain under implementation.

### Exact cut acceptance, cross-session recovery and actual preview UI

The cut checkpoint now has an explicit exact-v1 human transaction and a separately requested continuation retry. Four attestations are initially unchecked. Verified playback metadata is a prerequisite, not evidence of watching/listening. The first transaction continues the **already bound brief**, not a new post-cut treatment. It records immutable human cut acceptance before launch; a worker is running only after its OS start identity is confirmed. `cut_accepted` means launch pending, never delivery approval. Lost-response replay is read-only and cannot spawn again. Accepted failed/interrupted Resume returns the original context unchanged and cannot reconcile new intent or recut. Ordinary fresh/saved-plan shortcuts are hidden for cut-first recovery.

The HTTP boundary checks local origin before body or authority reads, limits actual JSON bytes to16KiB with a5s body deadline, rejects caller timestamps/paths/plan/policy fields, and projects closed result/status DTOs without process IDs or worker tokens. The browser sends one explicit POST, preserves decision identity, and never retries automatically. A300s response ceiling reserves headroom beyond the120s source verifier; this is not a measured normal response or a delivery-time claim. Read-only accepted status distinguishes pending/running/failed/interrupted/complete without upgrading scope.

Adversarial review found a real recovery blocker: a crash during verification left the UI permanently disabled. Recovery now explicitly reads and replays the exact previously submitted decision from immutable server evidence, so another browser tab can recover it without inventing an idempotency key, overwriting local storage or inferring new human approval. A live lease still rejects overlapping work. An orphaned verification retry preserves the original submission/wait-stop time and classifies the unobserved gap as **not human wait**; the incomplete prior attempt is retained. New-key replacement is rejected before attempt writes. A failed verification starts a distinct new waiting interval; source hashing remains system work.

Measured engineering tests, not creator qualification:

- Initial actual acceptance cohort4/5 passed in17.83s; the remaining case correctly rejected PID-only identity under the restricted sandbox. With real OS identity probes,5/5 passed17.96s. No identity rule was weakened.
- Expanded pinned-source acceptance cohort7/7 passed39.28s; final recovery/readback-expanded7/7 passed42.63s. Cases include silent-source same-size mutation, orphan immutable-fact recovery, visual-plan resume without recut, context/policy forgeries, short lock/CAS ownership and incomplete verification recovery. Full process/power-loss crash coverage remains open.
- Main API/client/SSE/matrix/editor cohort31/31 passed0.58s. Further recovery/client/card checks14/14 passed0.33s; cross-tab API/client/matrix cohort16/16 passed0.35s. These overlapping cohorts are not summed as unique tests.
- Type-check exposed an invalid test-only intent.scope field and a test-header union containing undefined; both were corrected. Latest clean type-check took1.56s; focused lint passed. No runtime claim is based only on these checks.

Actual UI fixture `/private/tmp/sniper-ui-qualification.KsUcgq/test-only-cut-review-da08773a/producer` was created/registered in3.97s, only in the isolated workspace. It explicitly declares TEST ONLY model/critic stubs. Its real receipt-bound90-frame160×90 NTSC preview is3.003s with144144 presented stereo samples. The actual browser loaded matching dimensions, played to3.003s/ended without a media error, remained unmuted, and left all four attestations unchecked and Accept disabled. **Unmuted playback is not listening evidence.** No acceptance or final was created in this UI fixture. The native dialog and dedicated editor checkpoint preserve their non-delivery caveat. An actual card-copy discrepancy was corrected: waiting/verifying cut jobs no longer say nothing is happening or offer a Palmier ownership change/cutaway reconciliation beside the checkpoint.

### Ordinary audio clock and private color diagnosis: newly measured limits

The shared limiter now compensates its actual4.990573ms lookahead and preserves the final3ms of content. Mastering policy3 invalidates old or unproved caches; it never relabels them current. The existing strict Audit B policy/targets remain unchanged. The original111-test audio cohort passed64.33s wall/61.461s suite. A later142-test audio/Audit B/J-cut/preview cohort passed85.80s wall/82.275s suite; a separate83-test F1/candidate/cache/provenance cohort passed16.50s wall. Cohorts overlap.

The first ordinary `source-float-v1` lane is explicit, **not the default GUI lane**. It supports its tested unity-speed/no-enhancement/no-gain/no-SFX/no-music class and rejects unsupported features early. Real NTSC four-cut output uses48kHz stereo, a44.1kHz right-only source repaired through its channel receipt, and a silent source. Five interior/J-cut/end events match an independent decoded-source oracle within3ms; final presentation is192192 samples. All120 encoded picture packets and their timestamps match the legacy output. Strict whole-output decode/loudness/true-peak checks pass. This improves a measured clock defect without establishing perceptual quality equivalence. Ignored legacy intermediate AAC still costs work, though it is excluded from the new audible final path.

The pulse-only high-crest fixture still failed strict loudness at−15.17LUFS (residual−1.17) in6.75s; it remains at `/private/tmp/sniper-f1-ordinary-q7t8zago/`. A separately declared normal-level synthetic bed with isolated clock events passed; the negative case was not erased or reclassified. Positive baseline/changed finals and full Audit B evidence are retained at `/private/tmp/sniper-f1-ordinary-lqirwzk3/`. Missing J-cut channel evidence, source mutation, re-sealed nonfloat bus, under-level final and candidate mutation during measurement all reject while preserving the prior final/sidecar. Actual listening and wider source/effect classes remain unqualified.

The private source-color diagnostic now samples across retained source/lighting groups, including late frames through89.5s, with explicit profile/history/lighting intent. It writes new private evidence only. An existing attested networkless container (FFmpeg4.4.2, unlike host8.0) completed10/10 real synthetic samples: final pre-review run0.978s diagnostic/2.717s including source creation and admission. Intentional dark/colored and unknown-profile cases emit no correction. The only possible numerical result is unreviewed encoded-luma screening, **not an applicable grade**. Missing color tags correctly produced partial/no-suggestion evidence; source-hash mismatch produced retained failure.

Independent review then found deadline overrun and per-frame metadata weaknesses; the Color UI launch was paused for fixes. It also found FIFO-open hangs in inherited admission/result readers. Nonblocking open followed by existing file-type checks now rejects actual FIFO/socket/device substitutions without hanging;36 reader/ingest/VFR checks passed0.70s after the retained failing baseline (three FIFO timeouts plus a restricted-socket test environment failure). Final color deadline/frame-class/cleanup reporting qualification and its API/UI are still in progress. Repeated full-source hashing remains an unresolved cost (four host plus two worker reads per used source in the observed diagnostic path); subsecond tiny-fixture results are not a large-source promise.

### Integrated regression checkpoint and remaining activation timing correction

The first complete `npm test` attempt stopped after **66.24s** on an obsolete source-shape assertion expecting three Resume arguments. The corrected assertion now explicitly requires the fourth persisted workflow argument; functional tests continue proving legacy policy preservation and guided-pause behavior. The next complete command passed in **317.28s** (221.75s user,58.00s system). This is regression execution time, not video production time. New Color route/UI and activation-timing work landed during/after that run, so it is not a final frozen-tree build or complete Color qualification. The later route-matrix/client/card/Resume cohort passed21/21 in0.48s; scoped lint and whitespace checks passed.

Further adversarial review found that immutable-fact-before-journal recovery could underreport a later waiting interval. A separate content-addressed activation receipt now binds the actual successful verification execution and wait-stop clock, while keeping the original human-decision fact byte-identical. Historical acceptance without activation evidence remains accepted **with timing explicitly unavailable/null**, not zero or the first submission's timestamp. Final seven-test actual-source/process recovery cohort passed42.84s; type-check1.61s. Main status/API tests verify the distinct original-decision and last-wait-stop fields.

Actual browser evidence checks took **0.5s** in the card dialog and **0.4s** in the dedicated editor cut-review view. These exclude separate player loading. The second view also showed all four confirmations unchecked and Accept disabled. The browser briefly initialized the ordinary locked editor while project status loaded, then showed the dedicated checkpoint; those unnecessary initial read hooks are a remaining UI-efficiency opportunity, not a claimed optimization. An older Palmier-approved state could hide the card's Review cut action; the checkpoint now overrides that legacy visibility rule without changing authority, with a full ProjectCard render-matrix regression being added. Scoped helper extraction brought the modified stage-actions/project-card/project-state modules below300lines; the new acceptance controls remain below the function-size limits.

### Broad regression and actual Color UI checkpoint (11:07 UTC onward)

The audited broad Python run selected444/445 modules and executed4,102 cases in380.791s test time /386.88s wall (6m26.88s). It reported18 failure records and8 errors, including subtest failures; do not subtract those records to infer a passed-method count. No skips or xfails were added. The one excluded module, `test_studio_player_view`, needs separate real-browser qualification because it launches a local preview server/Chromium, writes shared screenshots, and can leak its server on setup failure. Full output: `/private/tmp/sniper-producer-safe-full-20260906.log`; classification: `/private/tmp/sniper-python-full-failure-classification.md`.

Failures include current F1 integration mistakes (an unnecessarily expanded frozen import closure and incomplete mix ownership registry), retained media evidence stale against current source/tool identities, existing caption-page discovery confusion, placement dependency expectations, and Palmier fixture/current-authority mismatches. This is **not a green full regression**. The narrow F1 correction reuses the existing producer configuration for the same mastering-policy constant, restores exact registry ownership, and rejects unknown audio roots through the existing closed plan validator. Its77-case focused cohort passed14.532s test /17.57s wall. Historical frozen-v1 catalogs and retained media proofs were not rehashed. Remaining failures require actual reruns or owner-directed contract review.

Color API/UI now has16 discovered tests covering private ownership, malformed projections, cleanup uncertainty, source binding, rapid double-click, storage failure and lost-response retention (0.905s before the additional declaration-drift test). Type/lint passed. The real source diagnostic uses the approved local sealed image; no installation or persistent environment change was made. An admitted fixture was created directly in `/private/tmp/sniper-ui-qualification.KsUcgq/color-ui-synthetic-1/producer` (3.361s setup/admission). It is explicitly synthetic, with a90s source and an18s saved cut retaining source55–73s, not creator-quality evidence.

Actual CUA steps opened that project, selected Color, verified allthree default declarations were unknown, and clicked **Run private color check**. Controls disabled while the owned worker ran. The result displayed **0.9s** (durable API elapsed929ms; worker envelope618ms, decoding317ms). Five actual samples were at55.5,59.5,64,68.5,72.5s, all within the retained interval. The result reported luma variation57–181/255, explicitly unknown profile/history/lighting, no numeric correction, and verified cleanup. The saved plan hash remained identical, no final existed, and the active resource claim was removed only after cleanup. Closing/reopening Color restored the same result without a new job. Job `c6ef9a72-0918-4027-8bbd-a8cb3dc75933`; diagnostic `8c27fbbd-ab95-4345-810e-05249d3ad470`. The earlier direct service run was986ms wall/950ms terminal and remains a separate attempt, not this UI measurement.

The UI now identifies the declarations attached to each result and warns when profile/history/lighting/notes in the controls differ; editing controls never relabels old evidence. Actual CUA changed only the synthetic source-profile control to Known SDR BT.709 and observed the warning while the retained result still identified unknown declarations. No new diagnostic or grade was triggered by that edit. Eight client/flow tests including declaration drift passed0.21s. These are diagnostic/plumbing checks, **not** grading, before/after visual comparison, skin-tone review, browser/export parity, listening, or a ten-minute throughput qualification.

The post-cut treatment/intro implementation is proceeding under a separately typed v2 launch/decision. Existing v1 still means continue the exact saved brief. Internal v2 waiting/admitted/failed states now fail closed in the project-card/editor presentation: old Palmier approvals, v1 Accept, ordinary Generate and Resume cannot continue them. No v2 launch selector or new unusable user pause is enabled yet. The initial editor view checks status before mounting the media/timeline surface while preserving the controller's local draft. Independent review caught raw in-app Back callbacks bypassing the unsaved-edit confirmation in checkpoint views; all such branches now reuse the ordinary editor confirmation, with explicit cancellation/clean/discard tests. Initial82-case UI cohort passed0.50s; one subsequent type-check caught an invalid synthetic test field (`version`), fixed without changing production schema. Then80 cases passed0.35s and type-check1.59s; the new Back regression plus existing lock test passed9 cases/0.36s. Overlapping cohorts are not additive.

### Checkpoint write barrier, v2 treatment authority and reviewer telemetry

Independent adversarial review reproduced eight real legacy `save-plan` requests writing a new plan while a guided checkpoint owned the cut. The common server mutation guard now rejects these writes under the real project lease, including a streamed request whose body finishes after the cut pause commits. Only a closed internal action/version/status/token/exact-journal-hash capability can enter checkpoint verification; ordinary operation labels cannot bypass it, and promotion/cut-repair reconciliation remains mandatory. The focused guard cohort passed28/28 in1.64s. A v1 acceptance cohort initially passed9/10 in56.25s under the sandbox, with the lone failure caused by unavailable child process identity. With actual OS process visibility, the same cohort passed10/10 in54.87s. Evidence: `/private/tmp/sniper-checkpoint-mutation-guard-fix.md`. Later the separately authorized v2 proposal action was added with its own guarded-lease regression; guard-only27/27 passed1.31s (not the same test selection as the28-case cohort).

The internal, still-disabled v2 handoff now accepts the exact cut into a real unapproved PICTURE_LOCKED revision, retains a separate treatment request, validates the full referenced-object closure and fixed original generation clock, and uses the same guarded lease/CAS/replay discipline. Unsupported earlier clauses cannot disappear from a later request without explicit supersession. Tests passed34/34 in50.70s, with existing job/cut/launch/pause/SSE regressions17/17 in5.31s; type-check2.56s and scoped lint2.07s passed. This proves admission mechanics with actual synthetic source/preview and test-only creative stubs, not treatment execution, intro review, body generation or quality. Evidence: `/private/tmp/sniper-v2-treatment-handoff-qualification.md`. Genuine natural-language intake/proposal compilation is the next slice; users will not be asked to supply precompiled binding placeholders.

The caption-page manifests are now excluded from asset-manifest discovery by exact file+field pairs; unknown actual asset roots and stale exceptions still fail. Global face geometry now correctly declares scene/composite dependencies as well as base/final. The obsolete list-identity test now requires equal non-mutating projection instead. The focused render-effect/Punch/placement cohort passed135 tests in8.556s. No retained media proof was rehashed. Read-only workspace resolution also no longer creates a personal workspace config when none exists; isolated read-only test0.25s, type-check1.93s and lint1.94s passed.

Reviewer timing now separates exact-input preparation, the actual provider call, and result/authority checking under one inclusive critic span. The actual prompt string, full doctrine, model/effort settings, read-only tool scope and QC verdict policy are unchanged. Only bounded diagnostic scalars are recorded: exact UTF-8 prompt bytes (not estimated tokens), listed image count, provider/effort/lens and the existing hard process ceiling. The UI labels the ceiling as a ceiling, not an estimate, and shows missing input measurements explicitly. Provider failures and malformed results remain failures; a completed `revise` verdict is never interpreted as QC passing. Existing401-line review/revision logic was split into focused execution/settings modules while keeping public imports stable. The first11-case cohort had one incorrect test expectation about legacy rendered doctrine fallback; the corrected isolated-plan failure test and the full cohort then passed11/11 in0.63s, type-check2.36s and lint2.22s. Providers were stubs in this cohort; it is not measured production-model latency or quality equivalence.

### Actual native copy test and bounded catalog overrun investigation

Actual HyperFrames Studio opened the synthetic Color fixture in1.5s. Selecting the `text-element` composition and its `te-text` native Text/Content field, then typing `Review timing clearly`, changed the retained composition DOM through the real UI. Two clipboard-based browser input attempts failed at the interaction layer; direct native keyboard input succeeded. `Preview Studio changes` took1.1s and rejected the native copy as an unsupported composition code/structure change. Inspection confirmed the native inspector edits a DOM leaf and serializes the declarations attribute, while the previous importer only accepted paired declared-variable changes. A narrowly scoped plain-text selector-to-variable proof is being implemented and adversarially tested; no arbitrary DOM/code reverse-mapping or visual-quality approval is permitted. This failed GUI attempt remains retained and is not counted as successful copy import.

The new OCI catalog/rate cohort presealed424 inputs (53 compositions ×8 rates). Early measured projections were23–25minutes. It stopped after340 completed passing rows when `stroke-draw-badge` took48.4–48.6seconds per four-frame probe, projecting an overrun of the declared30-minute soft cohort budget. The stop reached one newly started probe; that uncompleted row is not counted as a pass. Exact container die/destroy evidence and empty labeled-renderer inventory proved cleanup. All340 completed source/default/archive/media/runtime bindings were subsequently deep-revalidated in17.679s. No full matrix, compact current proof or perceptual-quality claim was exported. Retained evidence: `/private/tmp/sniper-rate-oci-20260906-1/partial-summary.json`. A future runner now needs programmatic safe-boundary budget enforcement and startup self-capture; phase-level fast-control/slow-template diagnostics precede another large run. Test throughput was observed while other implementation tests were active, not a controlled production p95.

### Native copy persistence and render-overrun root cause

The first native-copy bridge correction passed49 focused Python checks in11.32s wall, but a fresh actual Studio test on fixture7 exposed a second defect: returning to Master and reopening the composition displayed the old text even though the new leaf text was still on disk. The template's startup assignment overwrote the native leaf from the original variable. The generator now seeds only the registered `text-element` plain-text leaf and uses a source-bound assignment: an unchanged seed follows host variables; an edited native leaf, including an empty string, survives startup. Literal text and script data use separate escaping. The importer proves the exact original assignment/default, all remaining code/structure, and rejects conflicting native/host changes. No arbitrary DOM import or final-template modification was introduced.

The first local test invocation failed because its producer PYTHONPATH was omitted; it is retained as an execution failure, not a behavior failure. After the corrected invocation,31 native/UI/sync checks passed31.06s wall under concurrent host load, two pagination tests passed1.22s, and type-check passed4.49s. The expanded native/UI/sync/provenance cohort passed33/33 in10.79s wall;15 actual Python-bridge/service/route/file tests passed9.35s. Focused UI/timing lint passed1.83s after correcting an invocation that referenced the review files in the wrong directory. Cold-start assignment tests execute the actual generated JavaScript for host-variable changes, native copy, blank copy and escaped special characters. These are not substitutes for a fresh actual Studio reopening/import test. That live retest is currently paused because the Mac is locked; the user has been asked to unlock it.

Actual project selection also exposed an eight-project UI cap: the "expanded" list always sliced the first eight entries, making fixture7 (the ninth registered project) unreachable. Progressive five-project pages now expose every entry with an explicit collapse control. The actual UI then opened the ninth project. Rendering tests cover3/8/13/18/19 entries and a shortened registry. This fixes reachability; the existing all-project status request cost is not claimed optimized.

The stroke slowdown was not expensive drawing. Phase probes measured48.13s waiting for render versus89ms ffprobe and73ms decode. An undeclared preview fallback requested a missing YouTube icon, raised a page error before timeline registration, waited45seconds, and still produced a decodable movie. Thus the four earlier stroke codec/decode passes are not usable runtime proof. The corrected explicit diagnostic sample and source-default asset resolution produce four-frame30/30000:1001 MOV probes in about5.2s with timeline readiness200/286ms and no page error/404. A copied historical bad input is rejected in3.271s with no output and exact container cleanup. A30-frame H.264 NTSC smoke passed decode/rate/cleanup in15.178s; the explicit diagnostic icon/label was separately visually inspected.77 scoped tests passed. A nonblack-white-background statistic is not counted as icon-ink or perceptual quality. The new runtime-error guard participates in cache identity; historical proof closures remain immutable. A fresh complete catalog/rate matrix is still required after source freeze.

Audio integration testing independently found a short ducked-music stem that a whole-program loudness pass did not detect. One retained192192-sample program received only188416 music samples. The new opt-in exact-sample ducking path passed an8-case actual AAC cohort in18.04s wall: dialogue/bed both192192 samples, the unmastered sum matches raw dialogue+bed within5e-8, final presented audio has192192 samples, and ordinary-picture packet bytes/timestamps are preserved. Earlier delivery-origin failures, source-drift interruption and failed short-stem runs remain retained. Full assembly/cache integration, correction of remaining legacy callers and listening qualification are still pending.

### Native full-save regression, legacy fixture corrections and proposal review

The new actual Python-bridge/save test initially failed: `text-element.text` was still absent from the shared copy-field allowlist, despite the isolated native-leaf proof passing. Adding that exact declared field fixed the complete backend roundtrip: native text edit → ordinary draft save → entity/special-character edit → undo to original, while host/Studio manifest/cut bytes remain unchanged and no final is created. The roundtrip passed in8.10s wall; type-check2.54s and focused lint2.14s passed. Native carriage returns are now HTML character references so parsing does not change the seeded value; NUL is explicitly unsupported. A first multiline test-helper mismatch was corrected, then11 native tests passed0.26s. This still does not claim the locked Mac's fresh actual UI reopening/import test has run. Independent native-copy and reviewer-timing reviews found no blocking issue in their scoped code; the reviewer extraction retained its prior prompts/effort/tools/QC semantics.

The broad regression's obsolete Palmier fixtures were corrected without changing Palmier production or invoking its UI: keyframe tests now authorize a real bound worklist and return actual keyframe rows, with scalar-scale substitution explicitly rejected; graphics tests require the current one-track-per-normalized-full-canvas contract; exact-master dependency mocks and later-stage frame references now exist where those unit tests require them.25 tests passed3.51s wall. These are offline contract/mocked tests, not Palmier or HyperFrames visual qualification. Historical media-evidence freshness failures remain unresolved and are not erased by this fixture correction.

Main review of the new unapproved raw-treatment compiler found a genuine grade-only materialization bug: absent `baselineLook` would pick up the existing1.28×/1920×1080 defaults and change framing. Grade intent is being retained as blocked pending F2. Whole-cut-segment graphic/beat timing was also too coarse for fast opening graphics over continuous shots; controller-owned word/frame boundaries and source-occurrence coverage are required before range execution. A suspected missing-transcript hole was already blocked for used sources by the shared reader; explicit mixed-source coverage tests are still being added. Mode-only target dimensions are already rejected earlier in v2, but independent exact destination/canvas validation is being added to avoid a local NaN comparator hazard. No unapproved candidate is presented as a rendered or fulfilled treatment.

### Native browser roundtrip and remaining runtime failures

The final focused native/Studio Python cohort initially recorded103 passing cases and5 sandbox execution errors (two loopback binds and three process inspection calls). The same108 cases passed with the narrowly required permissions in13.18s wall (9.894s suite); the18-case TS import/native/pagination cohort then passed9.40s wall. This is a focused cohort, not the final full regression/build gate.

A new bounded headless browser harness exercised normal Producer/HyperFrames controls while the Mac remained locked. The first attempt stopped cleanly at19.4s because the harness clicked a project before its asynchronous openable status arrived; fixture8 remains untouched. Fixture9 initially appended text because headless Meta+A did not select the existing field. The incorrect edit and evidence remain retained. A corrective real-keyboard attempt then replaced the field at4.453s, re-opened the composition via Master at5.846s, reloaded/reopened Producer at8.600s and applied the native copy to canonical draft v2 at10.647s. Total10.696s; browser closed; cut unchanged; no final or approval. This proves persistence of the actual active-player DOM and draft import, not painted copy: the paused frame0 entry animation had opacity0. A visible playback check and native Undo remain separate required checks.

The harness intentionally reports overall failure because exact-loopback network policy blocked upstream MotionPathPlugin CDN requests and Google Fonts; page errors remain unresolved. It also observed a denied `/renders` request and a missing `captions.srt` request. These are under investigation, not suppressed or counted as a passed runtime. Artifacts: `/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-native-headless-AYdDjs/result.json` and neighboring screenshots. The earlier fixture7 remains untouched.

Independent integration review found that the new audio-only picture-reuse receipt must bind the prior graph's exact final and visual input key, not merely recomputable fields in the mutable public pointer/sidecar. The correction will decline unproved direct-assembly reuse and preserve graph-held input authority. Review also confirmed that support files could be published before the second promotion guard; staged support and rollback qualification are required. No public/default audio release is inferred from the earlier passing synthetic assembly tests.

The first generation-deadline slice has five pure tests passing in0.23s Node-reported time. It keeps the original120-minute request origin and leaves80minutes of declared downstream clean-work/variance allocation after proposal work. The existing ten-minute proposal-attempt ceiling includes preparation and checking, and neither a retry nor clock rollback creates a fresh request allowance. This is not a measured forecast, complete repair ledger, connected worker cancellation policy, or two-hour throughput proof; integration is still underway.

### Request-deadline integration and pinned Studio runtime correction

The first actual proposal/frame/deadline integration cohort passed24/24 in52.56s wall. It retains a proposal-admission record and precommit timing proof in the immutable execution/receipt, uses the original raw request clock, and reduces the existing provider ceiling by preparation already spent. Expired work is retained without committing a proposal. Legacy proposal receipts remain readable with unknown budget qualification; they are not retroactively upgraded. The whole workflow, shared30-minute repair ledger, qualified user-wait subtraction and cancellation of every non-provider subprocess remain unfinished.

Independent review caught two real deadline holes. First, a delay after the budget check but before canonical journal CAS could commit late. The repeatable lease/time guard now runs again immediately before the synchronous write, and stored receipt creation must precede the attempt deadline. Second, per-process monotonic checks did not prevent a later retry's wall clock moving backwards above the original request start. A bounded append-only original-clock observation history now prevents that recovery of already-observed time. Its first tests exposed a noncanonical macOS temp path and a real canonical-hash-versus-pretty-JSON writer mismatch; the fixture now resolves its own temp directory and the implementation uses the existing canonical content-addressed writer.15 actual proposal/deadline/high-water tests passed72.50s wall under concurrent media/readiness load. A follow-up adversary noticed that a provider throwing late also needs its healthy-lease outcome observation persisted; this is now corrected with four high-water tests plus six policy/proof tests passing0.516s Node time. The new actual late-exception/retry regression still needs its next frozen integration run. Unavailable/invalid timing is not zero time, and no deadline record is quality approval.

The Studio issue is two eager upstream paths: every player iframe requests MotionPathPlugin even when no timeline uses it, and native font-family control mounting requests Google CSS plus a server-side Google font metadata fetch. The pinned shell now serves an in-memory, exact-bundle/function-hash correction; original vendor and project HTML stay unchanged. Ordinary timelines no longer request a nonexistent plugin; actual motion-path vars/soft reload remain explicitly unsupported, with that native control disabled. Native family changes are disabled, and shell font previews load only the four exact local font faces already in motion tokens—not scene/layout CSS. Google discovery is removed from that disabled control. Route-launched CLI telemetry is disabled using its supported per-process environment variables. The server's render/export deny remains unchanged. This is a narrow runtime interoperability correction, not a global network-isolation claim or a new rendering capability.

The first shell test run failed two cases because the local tokens path traversed one parent too far; the corrected path passed the three tests. The expanded16-case shell/runtime/guard/CLI-interoperability cohort passed0.770s Node time; the complete transformed ESM parsed successfully. Focused lint passed. A concurrent whole type-check observed in-progress readiness errors rather than a frozen complete tree; later whole type-check passed. Independent runtime review then found that a cache-query or encoded asset alias could fall through to the original bundle. Reserved-resource path normalization now serves the same corrected bytes for canonical/query/encoded/dot-segment aliases; eight focused tests passed0.613s Node time. Actual HTTP alias and fresh browser playback/reopen/import/Undo checks remain required. New review policyv4 rejects old live sessions rather than silently claiming their runtime changed.

Latest audio results:238/238 expanded audio/source/master/QC tests passed130.19s wall (125.854s suite);25 held-source/actual graph/assembly/publication tests passed89.89s wall. The old ducked-music path's421.67ms loss on a3.003s fixture was reproduced before correction; initial equal-block framing alone still failed two cases. The final decoded-tail/sample/clock cohort passed, without padding a missing tail. Resealing replacement PCM and matching WAV/receipt/pointer was also reproduced as an invalid cache acceptance; reusable source buses now require a separately held execution/pending/graph receipt. Standalone unproved v2 reuse rejects, while legacy defaults are unchanged. These tests do not establish whole-output listening or default release quality.

The F2 worker's first full-source smoke decoded all180 frames of the existing90s/2fps synthetic source through89.5s in1.088s, using the pinned networkless image and exact source/tool/worker/cleanup evidence. FFprobe4.4.2 does not expose per-frame corruption flags; the new policy retains them as unavailable and separately requires strict full decode, clean EOF and no warning/error stderr. The pure validator alone still cannot establish decoder execution. Corrupt/truncated/late-metadata adversarial media tests and actual creator-source color/appearance qualification remain open; no grade was applied.

### Painted native roundtrip, full-source negatives and readiness timing

The final pinned runtime cohort includes a real loopback HTTP alias test:17 shell/runtime/guard/interoperability tests passed0.78s wall. The first fresh fixture10 browser attempt stopped20.350s before Studio mounted because the free-port probe could not bind the recently stopped fixture9 port. No edit or import occurred. After an independent bindability check, the same untouched fixture10 was reused without rewriting its files.

The next actual headless Producer/HyperFrames flow passed33.003s. It edited native text, played/paused a visibly painted frame, returned through Master (21.651s), fully reloaded/reopened Producer and showed the same painted text (26.899s), imported draftv2 (28.920s), edited/imported a second valuev3 (31.417s), then used native Undo and importedv4 (32.925s). Main independently inspected the cold-reopen screenshot: the new words are visible, not opacity-zero DOM. Cut bytes stayed unchanged; no final or approval was created; the browser closed. No page errors or MotionPath/Google requests occurred. Residuals remain explicit: two blocked PostHog telemetry requests, absent-caption404s and deliberately forbidden render-history403s; this is not a globally clean console, desktop/manual audition or final audiovisual parity claim. Evidence: `/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-native-headless-fpCYqw/result.json`; startup failure: neighboring `sniper-native-headless-gyGm8O`.

The original-clock late-provider exception/retry regression, after-receipt expiry/rollback, and actual raw-proposal positive passed3/3 in38.77s wall. A new shared deadline helper preserves the existing compiler receipt policy while adding a distinct readiness budget: both independent critics, source verification and persistence share one20-minute ceiling inside the original proposal phase, with the same80-minute required-finish reserve and append-only clock history. This is a conservative interim guard, not a new free planning allocation: the10-minute clean proposal target remains unproved, excess consumes original phase allowance, and the complete shared repair ledger remains unfinished. Readiness receiptv2 binds admission/precommit; historicalv1 is explicitly budget-unqualified and grants no new time. Fifteen pure deadline/high-water tests passed0.55s wall, whole type-check2.75s. The new actual readiness-budget fixtures await a stable source window.

The prior independent-readiness implementation's first actual cohort passed29 tests and failed2 in25.33s because its closed critic metadata validator compared sorted keys to an incorrectly ordered expected array. After correction,32/32 passed37.53s wall, including real synthetic source/proposal admission, two independent configured-critic stubs, exact isolated TREATMENT_DRAFT closure, a negative verdict, source drift, journal race, lost lease, timeout, orphan receipt and explicit retry. No real creative provider or final plan/render approval was exercised. This result precedes the new original-phase budget integration.

F2's final actual all-source cohort passed3/3 in4.701s across four bounded container invocations: complete180-frame decode, corruption after50s, truncation, and actual lateBT709→PQ frame metadata drift. A first3-case attempt had2 passes and one synthetic fixture-generation error: remuxing incorrectly made its global headerPQ, causing an earlier correct source-admission rejection rather than exercising late-frame validation. That failure remains recorded. The corrected synthetic header retains initialBT709 while exposing laterPQ at frame validation. Forty-three pure worker/contract tests passed0.297s. Evidence: `/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-grade-media-regression-ou0i587b`. This proves strict input observation/cleanup, not actual grading, creator appearance or a ten-minute benchmark.

An actual1080pNTSC/J-cut caption+music final passed2 integration tests and full AuditB in33.43s wall, with192192 presented audio samples and early/late caption pixels. Visual inspection nevertheless caught portrait safe margins placing requested bottom-center captions near the middle of the widescreen frame. The pass establishes visible captions and audio/assembly plumbing, **not correct caption placement**. A bounded aspect-aware correction and decoded placement regression are now required. The separate v2 visual cache adversary reproduced5 failures across4 tests in1.30s: graphics/transcript inputs could change without invalidating reuse. The conservative correction declines unproved graphics/explicit-caption whole-final and picture reuse while retaining fresh composition and full QC. Both issues are retained rather than hidden by the technical pass.

### Integrated readiness deadlines and caption placement correction

The latest source-bound compiler/readiness/budget cohort passed14/14 in91.73s wall (78.01s user,80.25s system) while the independent F1 media regression was running. This slightly exceeded the provisional60–90s test-window estimate; it is engineering-test time under concurrent host/filesystem load, not a generation SLA measurement. It includes expired original-phase admission before source verification/provider work, provider/critic exceptions and explicit retries, final-receipt expiry, rollback, source/journal/lease drift, isolated draft creation, exact budget-proof reopening and explicit historicalv1 budget-unqualified display. No creative provider or final video approval ran. Original compiler behavior remained covered after extracting the common deadline clock.

The existing deterministic advisor children (30s maximum each) and full source reobservation child (120s maximum) now additionally inherit the caller's remaining attempt time; old callers retain their original ceilings. They cannot start with invalid/expired remaining time or receive a fresh full ceiling after preparation spends it. The existing owned process-group runner still performs bounded cleanup. Sixteen pure deadline/high-water/child-bound tests passed0.52s wall, whole type-check3.69s and focused lint2.20s. This does not preempt every synchronous read or implement the complete request-wide scheduler, repair ledger or operator-wait accounting.

The aspect-aware caption correction changes only destination safety insets in the existing compiler. It preserves portrait margins, styles and declared placement ratios (bottom-center70%, lower-third76%, top20%). Existing landscape layout values no longer reuse the portrait520px bottom exclusion. The actual corrected1080p media passed3/3 in34.99s wall: early glyph boxy706–749 and latey711–744 both respect the declared756px baseline. Main separately inspected the decoded screenshot. The preserved pre-fix early glyph top510 fails the new position assertion. An initial retained-before unittest attempt unnecessarily ran32.86s of setup; it is recorded separately and is not a fresh pre-fix render. The direct old-output assertion required no rerender. Forty-seven compatibility cases passed13.37s wall. Evidence: `/private/tmp/sniper-f1-caption-final-m7h5l5cr`; logs `/private/tmp/sniper-caption-aspect-final-media-20260906.log` and `/private/tmp/sniper-caption-aspect-direct-before-media-20260906.log`. This is synthetic placement/clock qualification, not creator aesthetics or listening.

The runtime guard required an explicit new current render-build contract. Live build/receipt/admission writers and readers now use wireV2/policy`sniper-headless-render-build-v3` and its101-path closure, while the old94-pathV1 catalog/parser and deterministic historical digest remain unchanged from this slice's entry state. Existing user changes before this slice were preserved. Historical archive roles were not upgraded. Current and historical version cohorts passed73 tests in6.179s and126 tests in5.426s; the independent registry cohort is now53/53 green in12.49s wall, including the two previously failing current render-closure tests. Main and the quality agent independently inspected the version boundary. This is not an actual V2 renderer/matrix run or retrospective validation of old render outputs.

The final F1 regression union then passed276/276 in250.856s suite /255.70s wall (230.71s user,68.45s system). It includes actual source/base/graph/assembly/music revision, float headroom/master/sample clock, AuditB, held-cache attacks, publication rollback, conservative visual reuse and corrected1080p caption position. Full log: `/private/tmp/sniper-f1-final-expanded-20260906.log`; fresh graph: `/private/tmp/sniper-v2-graph-cli-kjxiydph`; final caption fixture: `/private/tmp/sniper-f1-caption-final-lg4l9f59`. It does not qualify a graphics-heavy complete final, default/GUI audio release, creator listening or ten-minute throughput.

Main's standards pass split the new compiler operation into a35-line guarded execution and17-line candidate helper without changing output/fault/check order. The reviewed deadline/readiness functions are all within50lines/four parameters and their modules within300lines. A concurrent type-check caught an in-progress schema3 binding error in an independently edited file; this is not a frozen full-tree pass. A subsequent timing audit found that source verification overwrote its enclosing execution's attemptId with the shared job token. It now uses a job fallback only when no parent timing context exists, and compiler traces use the immutable operation executionUUID rather than the retry-shared idempotency key. Six timing/context/actual child-propagation checks passed0.48s wall; a new actual compiler/readiness lineage fixture awaits the next stable schema/source window. Readiness subprocess failures now retain the existing owned-process cleanup evidence as compiler failures already did.

### Terminal-only deterministic QC routing

A narrowly bounded review-efficiency change now stops before the two paid visual critics when the last allowed QC render round already has a material deterministic failure. Existing material`motion_pacing` warnings count exactly as before. At that point a visual vote cannot approve the failed candidate and the existing round cap forbids another repair. Earlier repairable rounds still receive both critics, and audit-clean last-round candidates still require both critics before normal approval. There is no reduction of review coverage on an approvable output, no generic skip on every warning/failure, and no higher-effort fallback or quality-policy waiver.

The branch settles the overlapping render checkpoint and retains a separate`terminal-mechanical-routing.json` with the failed evidence, exact candidate/input authority,`renderedCritics:not-run` and`approved:false`. It is not a quality-summary or critic approval. The prior final/approval, current plan and failed candidate stay unchanged. The existing terminal error/event remains actionable. A failed checkpoint barrier propagates before writing the routing receipt.

The controller and real-FFmpeg audio-policy test modules passed together in4.04s wall. Added last-round clean approval/preserved-master checks passed the controller in3.33s; the exact max-minus-one boundary rerun passed3.18s. These Node entry counts represent test modules with many assertions, not a fresh whole-video cohort or measured provider latency saving. Whole type-check passed3.03s and focused lint2.20s. Independent final code inspection found no blocking issue, including the checkpoint barrier, unchanged normal/clean-final review, and absence of an optimistic skipped-review quality summary. The broader compiled-doctrine, shared media scanning and scoped repair optimization packages remain open.

### Schema3 opening boundary, exact audio and conservative admission

The stable schema3 compiler/readiness/deadline/timing cohort passed11/11 in86.44s wall (63.96s user,61.10s system). The actual source-verification lineage case took17.31s and retained the enclosing immutable execution identity and parent spans. A separate26-entry timing/deadline/child-propagation cohort passed0.70s wall; these counts overlap earlier tests and include module entries. The schema3 unavailable-opening cohort passed4/4 in21.78s wall: actual synthetic cut/raw proposal/two configured critic stubs/readiness, exact frame/presentation binding, journal/CAS/replay/orphan negatives, and explicitly unsupported opening. It performs zero source-verifier/provider/renderer calls when the renderer is known unavailable; it makes no current source-byte, media, playback or approval claim. Historical V2/V3 snapshots remain separately read-only and execution-unavailable; their closed metadata/actual byte-count/link tests passed3/3 in0.30s after a test-only assertion-order correction.

Actual full-master PCM excerpt tests passed19/19 in50.20s wall (49.856s suite), including absolute fractional-frame sample boundaries, exact full-master content, late global music changes, mutation at publication, and owned child timeout/reaping. Strong readback now requires the separately held successful receipt and current whole-program selection; a result file left by a failed post-write attempt is unselectable. A later combined audio/master/source-cache/build cohort ran in the wrong sibling Python environment:72 tests passed and one registry import failed because that environment lacked scipy,73 reported cases in74.85s wall. The missing registry was rerun in PROJECT_SNIPER/.venv:8/8 passed7.45s wall. The wrong-environment attempt is retained, not described as a full green cohort. Logs: `/private/tmp/sniper-opening-audio-final-cohort-20260906.log` and `/private/tmp/sniper-opening-audio-effect-registry-project-venv-20260906.log`. Browser A/V, the private full-base/compositor adapter and creator listening remain unfinished.

The new opening deadline is a deliberately conservative scheduling guard, not a throughput prediction. It captures wall and monotonic time immediately after request parsing, before strong metadata reads or project-lease acquisition, and consumes that same25-minute attempt after the verified original request clock becomes available. Its protected80-minute downstream allocation includes full body assets/captions, planning, assembly and quality review; a full base/audio master earns zero unproved body-asset credit. Consequently the whole25-minute attempt must begin no later than15minutes after the original generation origin, with a phase end at40minutes. A slower proposal/readiness chain can be rejected before render; this is not evidence that common jobs are already usable within two hours. Independent adversarial review caught and rejected an earlier55-minute reserve idea because it would hide remaining body assets. Twenty-one combined deadline/high-water tests passed0.77s wall; independent final module review found no blocker. Actual executable caller integration and the full repair/user-wait ledger remain required.

### Actual color service and corrected timing UI

The new internal F2 project adapter completed a genuinely admitted90-second/180-frame synthetic source in2.910s service time,2.930s caller wall, with150ms exact cleanup; fixture setup/admission took2.943s separately. It preserves canonical project/plan/manifest and creates no grade, final or approval. Fixture: `/private/tmp/sniper-grade-project-live-a9ev1378/synthetic/producer`, job`e716fa8b-56a4-4d1e-a49a-16a447b004d4`. Before this run main review identified four concrete process/publication problems: a late durable complete status, never-settling unknown child identity, unbounded retained output after invalidation, and lack of an independently enforced original work deadline. The corrections retain candidate-only durable evidence, bounded unknown outcomes/claims, capped pipe data and a parent work-deadline cancellation with protected exact cleanup. Twenty-one Python fault tests and14 TS lifecycle/lease/clock/request tests passed. Main re-read both production boundaries; actual grading/UI adoption is not inferred.

The timing UI no longer implies that all work stopped at the job's last status update. Valid job-bound completed or unclosed spans may extend a separately labelled observed work lower bound; it is not a new generation clock, liveness assertion, inferred wait or approval. Foreign, duplicate, future and wall-clock-inconsistent records cannot extend it. Unclosed private executions are labelled possibly active/interrupted/lost rather than automatically stopped. Seven focused test entries passed0.78s wall, including the existing full timing-summary module; whole type-check passed3.89s. Initial lint found one test-only prefer-const issue; corrected focused lint passed2.89s.

The actual headless Producer panel test first failed at12.292s: the external ESM fixture and TS CommonJS dependency graph instantiated different timing contexts, producing correctly excluded standalone rows. The app did not assign those rows to a job. After using the same loader for both fixture timing modules, a NEW fixture passed2.292s end to end (2.82s command wall), including an actual intentionally failed1.2-second synthetic span, visible lower-bound/failure text, real status API readback, unchanged journal, no final and closed browser. Main inspected the screenshot. Passing evidence: `/private/tmp/sniper-ui-qualification.KsUcgq/timing-ui-synthetic-ucXTyi/timing-ui-result.json` and `timing-panel.png`; first failure retained at sibling`timing-ui-synthetic-02STTO`. No media/provider ran; this is UI/telemetry qualification, not video quality.

An isolated review-frame batching experiment, not a production change, reduced six640×360 JPEG extractions from0.40–0.43s to0.089–0.096s across24,30000/1001 and60fps. The first seek-relative implementation selected a wrong frame at1.000s/24fps despite appearing faster. Global-PTS selection matched all18 decoded JPEG pixel comparisons. Both attempts and source media remain retained under `/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-frame-batch-spike-j3_rb3bh` and `sniper-frame-batch-spike-y5vm_0b8`. This does not qualify nonzero/VFR sources, full-size longform behavior, failure publication or production integration; no eight-hour/two-hour inference is made.

### Actual sealed render, cleanup overrun and readability gap

The first actual current V2 fixed30 section-marker attempt failed after74.974s, despite the media/proofs being ready after14.845s. Exact-name cleanup kept resetting its absence window because this Docker version returns success for removing an already-absent container. The initiating result-contract failure was also real: the current renderer emits an exact`fps` field, while the current lane still expected the older six-key result. The failed attempt remains unchanged at `/private/tmp/sniper-render-build-v2-live-4fhk2y9d`; later absence evidence is not retroactive approval.

Cleanup now relies on observed exact presence/absence, not removal-command success. A direct absent-name regression completed3.458s. The current fixed30 lane requires its seven-key result and canonical`fps:"30"`; historical parsers and other ordinary exact-rate render paths are not coerced to30. A NEW actual sealed run passed16.816s total (119ms preparation,16.563s launch):75 frames,2.5seconds,1080×1920 ProRes4444 alpha, current101-file build unchanged before/after, actual media/runtime/assets/copy/sidecar/alpha validation and exact container absence. Evidence: `/private/tmp/sniper-render-build-v2-live-i70jnf0m`. This is one current template/rate, not the424-case catalog matrix.

Main inspected a decoded midpoint and found low-contrast blue copy over the navy scrim. Independent actual black/navy/white full-composite checks then exposed two gaps: existing static/composite checks emit no contrast finding for this treatment, and the UI incorrectly marks this transparent section-marker as own-screen. Reference/presence checks passed, but full Audit B did not pass overall; missing contrast output is not readability approval. Evidence: `/private/tmp/sniper-section-contrast-audit-bjfyxnws`; review: `/private/tmp/sniper-section-marker-contrast-review.md`. No palette change or aesthetic approval occurred in that audit; correction and actual readability qualification remain in progress.

### Qualified nearby-frame extraction integration

Following the isolated failed/passing experiment above, ordinary Audit B now passes its already-observed probe metadata into bounded nearby-frame extraction. The opt-in preserves every label, note, original timestamp, duplicate event and JPEG quality/dimensions. It admits only the qualified zero-origin, square-pixel H.264/yuv420p single-video class at eight declared frame rates. Unqualified media retains the ordinary individual extraction path. Groups have at most six outputs,1.5seconds total span and750ms neighboring gaps, avoiding whole-longform decode for sparse samples.

Each batch writes a new private directory and validates every complete JPEG before publication. A missing/partial result fails every group member; it neither silently retries costly work nor selects older JPEGs. The original source-byte and overall audit/QC guards remain unchanged. A separately instrumented extraction span reports the actual selected path and evidence count, not approval. Independent code review found no concrete blocker, while noting that rate metadata alone is not an all-frame CFR proof; the global-PTS selector does not invent frame numbers.

The first production module cohort passed8/8 in16.57s wall, including110 decoded-pixel comparisons across eight rates and full-size landscape/portrait/4K. Ordinary Audit B integration plus real audio/audit fault checks passed24/24 in24.09s wall. The later qualification/partial-publication/registry cohort passed17/17 in27.43s wall, with121 decoded-pixel comparisons including time zero. Counts overlap; these are not a summed full-suite result. Evidence roots: `/private/tmp/sniper-review-batch-media-a9r2jy8g`, `sniper-review-batch-media-ct1nb5g6`, and `sniper-review-batch-media-ou2v29w5`, each retaining actual media and measurements. Representative ten-sample full-size extraction improved0.962→0.391s landscape,0.992→0.382s portrait and1.878→0.827s4K. These are local single-trial extraction measurements, not ten-minute job throughput, creator-quality equivalence or provider-review latency savings.

### Opening construction and actual lower-level A/V work

The first actual own-screen worker-input construction test failed0/1 after11.49s because statement-card's required empty`iconFile` default was rejected as an asset path. The schema3-only correction permits an exact empty string only when that known catalog field also defaults to empty; nonempty/whitespace/path/URL/default-asset values remain blocked, and schema2 reproduction is unchanged. Seven focused contract cases passed0.29s, whole type-check3.11s and lint2.81s. The original160×90 synthetic fixture also cannot qualify a1920×1080 base; the next positive fixture generates actual1080p before source admission instead of silently upscaling or weakening the canvas check. Corrected authenticated input/media construction remains pending.

New lower-level opening preparation/composition/mux modules passed8/8 in19.40s wall: actual ordinary base, actual source-derived whole music master, exact0–10/0–110-frame PCM excerpts, H.264/AAC with NTSC sample/packet clocks and wrong-canvas rejection. Reversed overlay order, equal-start collisions and nonzero[3,9) range tests also passed. Evidence: `/private/tmp/sniper-opening-preparation-vnq54_d3` and `/private/tmp/sniper-opening-compositor-awg9cfu1`; log `/private/tmp/sniper-opening-preparation-path-corrected-20260906.log`. An initial omitted-PYTHONPATH invocation failed two imports in0.12s and is retained separately. These tests are below the authenticated TS opening gate and do not establish real catalog playback, user approval or listening.

### Independent review corrections and first integrated opening attempts

The subsequent mechanical pass split three frame-batching helpers without changing requested frames, timestamp arithmetic, validation or publication selection. Six pure contract cases passed0.26s wall. The121 actual pixel comparisons above precede this extraction; a fresh actual rerun remains required before describing that exact final source as media-qualified. The13 new `guided_opening*.py` modules independently passed the repository's mechanical file/function/parameter/nesting limits by AST inspection. This is not behavioral or audiovisual approval.

A focused read-only compatibility rerun of the already-corrected offline Palmier authority/revision fixtures, exit-on-cut projection and face-aware placement passed47/47 in3.55s wall (0.309s suite). Main made no new source/test change for that run. This closes those historical fixture failures for this current cohort, not the still-stale retained media proofs or a real Palmier UI workflow.

The first integrated opening driver invocation failed10.83s before any opening input, renderer claim or media worker. Its inherited TEST fixture cleanup removed the early fixture, so only the terminal failure survives; this is explicitly lost test evidence, not retained media proof. A TEST-only retention option now preserves actual opening-driver failures and bounded process diagnostics while leaving existing ordinary test-cleanup defaults unchanged. Focused lint passed3.48s.

The fresh explicit attempt failed11.98s, with exact cause retained at `/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-human-cut-rJjmn3/test-only-cut-review-07df4ff4/TEST-SETUP-FAILURE.json`. The failed child was the proposal catalog loader, not a cut render or opening compositor: `matrix is stale: motion source digest changed`. Accepted synthetic cut/source records exist, but opening input/claim/media do not. Owned child stop was verified; timeout was false. The shared process runner's generic “cut preview exited1” label initially obscured the failed operation. No more fixture retries are being made against this known-stale catalog.

The immediate blocker is `comp_capabilities.json` schema2, separate from the eight-rate matrix. Its global digest covers every registered HTML and shared motion runtime/icon source, so the new section-marker treatment correctly invalidates the old aggregate artifact. Genuine regeneration requires53 actual default/probe-spec renders with terminal alpha and settled content measurements. A bounded25-minute current cohort is planned after the narrow readability correction, using owned resources and before/after source/build checks; all53 valid rows are required before publishing the new capability artifact. Merely changing its source digest is prohibited. The424 composition/rate acceptance matrix remains a separate outstanding gate.

Independent adversarial inspection found that the first local-plate checker scanned the whole final image and could credit lookalike text elsewhere. The exact counterexample reproduced in0.491s: the intended marker rectangle was absent, but a translated lookalike produced three role PASS rows. Reproducer: `/private/tmp/sniper-plate-attribution-counterexample.py`. This is a known blocking false positive, not completed readability qualification. The correction must bind the existing actual placement evidence, handle ordinary executed overlay ordering, and reject ambiguous occlusion. A separate review found duplicate paired-JPEG decoding in the new checker; the same sampled pair will be shared between presence and plate checks instead of decoding twice.

A read-only preview-verification buffer experiment hashed every byte of the same140,055,576-byte existing derivative nine times. All complete SHA-256 results matched and the source stamp remained unchanged.256KiB reads took122.8–311.7ms,1MiB130.2–139.0ms,4MiB183.9–410.1ms; warm-cache ordering/noise prevents a robust route-speed claim. The experiment completed1.98s wall. No preview route, verification policy or buffering default changed; this does not justify weakening whole-byte checks or extrapolating a two-hour saving. Runner: `/private/tmp/sniper-preview-hash-buffer-spike.mjs`.

### Fresh final batch check and opening read-only UI

The final mechanically extracted review-frame implementation passed9/9 actual tests in29.42s wall (29.128s suite,54.95s user,12.50s system), with all121 decoded-pixel comparisons identical. Evidence: `/private/tmp/sniper-review-batch-media-n_yr7f_e/measurements.json`. This run includes11 samples per class, not the earlier ten-sample full-size timing example. Actual full-size measurements were1.738→0.819s landscape,1.801→0.687s portrait and3.673→1.755s4K. Workload/load differs from earlier measurements; no whole-job or controlled-distribution speed claim follows. Main stopped media tests before the new catalog render cohort.

The new read-only HyperFrames opening panel is wired into the existing editor only for a verified workflowV2 `treatment_admitted` checkpoint. Existing timeline locks, the unreleased workflow notice and the mounted dirty-plan controller remain. A failed project-status read hides playback; observed token/timestamp/project changes remount the evidence reader. Opening readback is on demand, not a repeated expensive media-verification polling loop. Changing/rechecking a selection hides the previous player until fresh readback completes.

The closed browser DTO rejects invented approval/listening, hidden fields, foreign media URLs, extra/duplicate query fields, invalid identities, unqualified timing and inconsistent frame/sample boundaries. Only the ready branch can contain media. Pending ownership never asserts a worker is alive; stopped-process time and resource-cleanup time are distinct and cover only that recorded attempt. The player defaults to opening-plus-following-context, has no autoplay/muting/approval action, and screens actual browser dimensions/duration without claiming that playback is human review. The ready branch is tested with an explicitly synthetic display-only DTO; no actual server-selected opening is qualified by those fixtures.

Ten new pure client/SSR cases passed0.95s wall (0.878s suite). They cover all five status states, exact ties-to-even48kHz boundary arithmetic, body/URL limits, abort/error behavior, playback metadata mismatch and escaped server text. Actual browser lifecycle/playback, current successful media selection, scoped revision and human approval/body handoff remain outstanding. This is not the completed requested guided workflow.

### Catalog release failures and adversarial recovery findings

The first genuine capability-catalog attempt stopped after49.838s at its first composition, after actual media/proof creation but before measurement: the local measurement path required an explicit `SNIPER_NODE_PATH`. Exact owned resources were removed, and the old catalog was not changed. Evidence: `/private/tmp/sniper-capabilities.eScwuG/run`. All four local measurement tools were then checked by canonical path, SHA-256 and version; retained-media measurement succeeded2.443s. That diagnostic result does not retroactively pass the failed attempt.

A second fresh capability attempt stopped46.961s at evidence serialization: the canonical JSON writer rejected a non-JSON-domain proof/measurement value after real media measurement. Exact resources were again removed; the catalog remained unchanged. Evidence: `/private/tmp/sniper-capabilities.e5YNnH/run`. The complete evidence-writing path must be exercised against retained observations before a further render. Failed/configuration/debug time remains part of the catalog-release record; starting another private attempt does not make prior cost disappear. No53-case completion or first-five projection is claimed yet.

Independent opening-process review reproduced acceptance of fabricated internally consistent stopped-process sidecars with zero actual processes, in0.63s. It also found a final cleanup-CAS clock rollback gap, newly hashed mutable sidecars instead of held actual-return bytes, and a production Python launch that canonicalized away the project virtual environment. The project venv contains cv2/numpy/scipy; the resolved base Homebrew interpreter does not. Corrections are in progress before any cleanup recovery or selectable-opening release. Historical cleanup must never outrank a newer active claim. Detailed review: `/private/tmp/sniper-opening-process-review-20260906.md`; reproducer: `/private/tmp/sniper-stopped-process-forgery.ts`.

### Operator-requested handoff checkpoint

The operator requested a pause and exact instructions for a successor because of credits. Start with [the self-contained resume handoff](RESUME_HERE_TWO_HOUR_EDITOR_2026-09-06.md), then its three owner handoffs. No new implementation, provider or opening/creator media job was launched after that request. The already-running catalog independent readback completed safely; all owner jobs are stopped and parent UI servers were preserved.

The genuine53 capability rebuild is now published and independently read back53/53: artifact SHA256 `e65e1e6d45d809446ed7b7b4670355aa7f941d0600450aadd73afd18736e98ce`; final cohort `/private/tmp/sniper-capabilities.SHd8Fp/run`; readback `/private/tmp/sniper-capability-final-readback-lh1r_clp/result.json`. Original-total2422.224s includes failures/debug and earlier25/35-minute budget misses; the final45-minute total envelope completed. Independent readback took another66.660s. Final continuation was45 fresh+8 exact-current revalidated rows. All exact owned containers were verified removed; source freeze released. This is default/probe-spec30fps capability, not424-rate or per-input visual approval.

The four opening-process source blockers above were corrected and focused protocols/actual-venv regressions passed; actual whole opening/cleanup-CAS faults/strong-readback integration is still unrun. A newly identified detached ffprobe session means outer-group stop plus Docker absence does not qualify comprehensive hard-kill recovery. Ready selection/media GET, approval and full-body integration remain unfinished. The handoffs specify the next exact fixture/runtime, conservative ownership response and selection contract.

Opening UI lifecycle passed against explicitly intercepted TEST DTOs and actual small synthetic media in1.400s test/1.68s command, at `/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-opening-ui-1snm1d/result.json`. The subsequent ready-only `selectionQualifiedAt`/`sourceFreshness:not-rechecked-by-status` amendment passed10 pure tests0.40s wall but still needs fresh browser/type/lint. Neither UI test is authentic server-selected media or human review.

The newly accessible raw `/Users/aaronfigueroa/Downloads/C0679.MP4` was actually probed in0.119s:10,280,473,262 bytes,834.335s,4K23.976 H264, stereo48k PCM, BT709 primaries/matrix but `iec61966-2-4` transfer. No full hash/decode/admission/edit was done. Current private F2 >8GiB/transfer limits do not qualify this file; an explicit real large-source/xvYCC class is necessary, not a silent proxy/grade substitution.

Main reproduced all three old glitch detectors returning PASS on failed/partial FFmpeg output. New strict separate scans and fail-closed results passed11 pure tests0.16s wall. The patch remains deliberately unfinished at handoff: ordered freeze start/duration/end validation, prior-span plus terminal-tail reporting, nesting cleanup, the Palmier fixture and actual decoded-media/threshold parity are pending. See resume §5A before trusting this slice. No shared-scan speedup is claimed. The overall workflow, complete regression suite, creator audiovisual quality and two-hour target remain unqualified.

### Original audit constraints (historical; current slices are described above)

Three existing adversarial agents independently inspected timing, placement quality and Studio integration, then received bounded ownership for fixes/tests. Their changes are integrated above; the initial user-owned Studio/authority work remains distinct from this task's additions.

- Existing TS/Python timing rows have no unique span identity. Concurrent same-name critics are paired by a stage-name stack in the old baseline reader, risking misattribution. Add identity/status fields compatibly and update the reader before relying on concurrency measurements.
- Stable telemetry run identity can reuse `artifactToken ?? token`; fencing `token` identifies the attempt and `attempts` gives its number. Request keys change on resume and must not be repurposed as stable run identity.
- Current Studio CLI exists in user-owned untracked `scripts/producer/studio/`; no Studio integration currently exists under `src`.
- Studio direct `sync --apply --assemble` does not run complete current controller QC and writes a public final. Do not wire it as the GUI delivery shortcut.
- Studio sync needs the shared project lease, immutable diff inputs and exact parent/base binding. Baseline-diff lint acceptance is draft-edit permission, not final delivery approval.
- Opening Studio must preserve unsynced edits and reuse an existing valid session. Preview process launch is not proof of HTTP/player readiness.
- Current Studio projection omits some renderer-resolved placement transforms. Do not describe it as full spatial WYSIWYG before actual parity tests/implementation.
- Elements iframe asset failures and Studio are separate surfaces. Studio already stages local scripts; the Elements route still needs independent fixes and tests.

## Qualification rules

- Track total request, preparation/cut, generation, genuine user wait and later revisions separately. No phase/retry/resume resets spent time.
- Initial target remains 120 minutes from reviewed cut + accepted treatment, with preparation visible separately; not a proven raw-to-final promise.
- Include failed, overrun and unsupported requests in results. Preserve the last approved master.
- Compare complete baseline/changed outputs on story meaning, readability, motion, sound comfort/intelligibility and color—not solely technical check counts.
- Report missing telemetry, skipped checks, unavailable listening and unqualified source classes explicitly.
- Any changed rendering, caching or review policy requires proportionate equivalence/defect-recall evidence before claiming quality preservation.
- Finish the full UI/cold-warm/creator-footage cohort from the plan before marking the goal complete.

## Next work

1. Complete exact human cut acceptance/continuation and its real UI, failure/replay and source-currentness tests. Introduce distinct versioned post-cut treatment authority before accepting a changed brief.
2. Connect guided/autopilot selection, a playable fast-paced intro approval and the full-body handoff without restarting generation time.
3. Finish ordinary audio/master clock parity and source-aware color diagnosis/correction/UI, with honest unsupported-source decisions and actual listening/color comparisons.
4. Reduce repeated doctrine/review/decoding work; finish resource admission, scoped repair and proof-reuse qualification. Preserve critical defect coverage.
5. Run the stable integrated regression/build/standards/security cohort, then fresh short/ten-minute cold/warm creator-footage jobs through the actual UI. Accessible raw footage and real listening evidence remain required; synthetic tests do not satisfy this exit gate.

### Successor session (2026-09-06, after the handoff pause)

Audit B glitch fix was closed on evidence, not fixtures alone. Real ffmpeg 8.0 log shapes were captured (ordered freeze start/duration/end triples; start-only EOF tails), and the shared-stdout hazard the handoff suspected was reproduced: `metadata=print:file=-` plus `-progress pipe:1` tore 46 lines in one 10,796-frame scan, so the luma sink moved to a private file parsed as a stream. Ordering uses integer `pts`; frame counts and `out_time_us` from the terminal progress block bind the decoded length to the audited duration. An independent adversarial review then confirmed four blocking defects (fixed 300 s deadline failing flash past ~20 min; a 16 MiB sink budget acting as a ~133 min ceiling; missing option-level apostrophe escaping that killed scans under an apostrophe TMPDIR; a declared static hold reaching EOF that could never be authored) plus metadata/filename event injection, a wrong rounding regime, and a narrow read guard. All were corrected: media-scaled scan budgets that still honour the run deadline, detector lines anchored on the av_log prefix, 2 ms freeze tolerance for six-decimal clocks, declared-hold-to-program-end exemption, and full escaping. Final: pure/Palmier 31 pass 3.44 s; decoded-media 12 pass 3.49 s (retained `/private/tmp/sniper-glitch-media-*`); modules within 50-line/4-param/2-nesting limits. Measured scan cost: 1080p30 ~5.4 ms/frame, 2160p24 ~21.6 ms/frame for the signalstats pass; a 10-minute 1080p final costs ~2 min across the three separate decodes, a 10-minute 4K final ~7 min. No shared-decode optimisation was attempted.

The first real opening media fixture ran end to end through claim → owned Python worker → durable failed outcome → exact cleanup (371 ms, both groups stopped, no containers left) in 17.88 s, the first attempt to pass the previously stale catalog loader. The worker failed honestly inside the ordinary full-base render at the real `plan_lint` bundle: the TEST fixture's 3-second candidate broke seven gates (300 s longform floor, missing `target.graphicsStyle`, missing graphic `reason`, unread classic spec fields, 2.5 s completion floor, EXIT LAW seam, zero strong transcript beats for the 4-graphic floor). Retained root: `/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-human-cut-18zxhT/test-only-cut-review-f35b14d4`. Root cause is architectural, not a fixture typo: the guided v3 treatment proposal cannot carry `graphicsDecisions`, `semanticBeatId`, `reason`, or `graphicsStyle`, and proposal readiness runs critics only, so an unlintable draft reaches a 25-minute-class opening attempt. The engine already has what is needed (planner `introSemanticBeats`/`formAllocation` in the guided evidence, the autopilot gate bundle). Work in progress in parallel: proposal contract v4 with those fields compiled into the candidate; the deterministic gate bundle at readiness (the old 3 s draft becomes a negative test); a 96 s cue-rich, jump-cut synthetic long-form program declared as an operator excerpt; one precision fix so empty measured-default `headlineLines/statements/statementLands` do not fail the classic statement-card contract. No gate threshold was lowered.

Hard-kill ownership (resume §5C) now has a proved conservative bound: any outer-group stop the owned runner itself performed is recorded as `forcedStop:true`, can never be a complete outcome, keeps the execution claim and outcome pointers after exact Docker reconciliation (`claimRetained:true`), is refused by the committed-cleanup reader as selectable, and reports `pending-owned-execution` with a manual-resolution detail. Opening server cohort 22 pass; owned-runner tests 6 pass with the deadline kill asserting `forcedStop`. No PID is ever guessed; a qualified nested-session ledger remains future work, and no real hard-kill media run was performed.

#### Guided v4, deterministic readiness gates, and the long-form synthetic program (successor session, continued)

Three bounded owners delivered in parallel and their work was integrated on the same tree. The Python owner made `template_contract` accept the exact empty measured defaults of `headlineLines/statements/statementLands` for classic statement cards (non-empty values still fail), and authored a deterministic 96-second long-form TEST program: 26 utterances, 222 kept words, 25 dead-air removals, jump-cut seams every 3.1–3.9 s, output 95.019 s, `transcript_cut_contract --previsual` clean, and eight deterministic transcript beats whose maximum-distinct allocation lands on eight different own-screen scalar kinds. 1080p media synthesizes in 4.7 s; the default 3-second fixture stays byte-identical. The TypeScript owner added treatment proposal v4 (graphics style with rationale, per-graphic reason, per-beat decisions bound to the planner's semantic beats, clean-hook seam decisions) and compiled it into the candidate's `graphicsDecisions`, `semanticBeatId`, `reason`, target style and transition receipt, with a parity test that feeds the compiled candidate into the Python binding and clean-hook validators. The gates owner made proposal readiness run the renderer's own deterministic gate bundle before any critic, retaining the plan, manifest and bundle it evaluated; the previous 3-second draft is now blocked at readiness with four operator-intent findings and never reaches an opening claim, and receipts that predate the gates are rejected. After version pickups (opening authority, media input, history readers, fixtures), the consolidated TypeScript cohort passed 85/85 in 88.6 s with a clean repo-wide type-check and lint, and the headless UI regression passed with the extended ready contract.

The opening-to-master vertical slice gained its next two pieces on the server side: an exact playback selection pointer (`openingMediaSelectionHash`, committed under the same lease as the strong readback and validated cheaply by status with `sourceFreshness: not-rechecked-by-status`), a guarded media route that hashes the selected artifact through the same descriptor it streams from, and an explicit local-operator opening approval (`openingApprovalHash`) that re-runs the read-only requalification child under a separate protected allowance before recording a closed attestation. Status now reports a verified approval object; the browser accepts `openingApproved` only with that object. Forced outer-group stops keep their claim after exact Docker reconciliation. None of this is yet exercised by an authentic long-form opening run; that run, the manifest resolution rows the geometry gate needs, and the per-beat card copy validated against the template, claims and timing contracts are the immediate next steps. The C0679 original was hashed read-only (26.3 s) and fully decoded (20,004 frames, 136.4 s, 6.1× realtime) as admission baselines; it remains unedited.

#### Own-screen budget versus the semantic floor, and the genuine 300-second class

Authoring gate-clean card copy for the 96-second excerpt exposed a structural contradiction rather than a copy problem: produced/full long-form requires at least four unique first-minute graphic bindings and every strong early beat must resolve to a graphic when b-roll is off, while `plan_lint_motion` caps own-screen takeovers at `max(3, own_screen_cap(duration))`, which is 3 below about 120 s. Five of the eight required cards therefore had to ride `free-band`, the one presentation the first private opening profile explicitly refuses. Rather than widen the profile or waive a gate, the fixture program was extended to the product's real class: 32 inert body sentences screened against the trigger and cue detectors take the output to 311.7 s (82 cuts, no `excerpt`), where the same budget function grants eight takeovers and every designed beat stays own-screen. The program check now mirrors lint's 180-second retention window (seams under 4 s there). A second contract gap was closed with a precision fix: the measured catalog declares `moduleLands` with an empty-string default and the v4 compiler carries every default key, so plan lint now treats that exact empty string as absent. `chart-story` remains unauthorable on a program that speaks no numbers near its beat, so the final comparison beat uses a second scoreboard (seven distinct kinds across eight windows, variety-legal). At 312 s the assembled plan passes `plan_lint` (two pacing warnings: front-load 1.06×, 16% of the 180 s intro dressed against a 28% envelope floor), `hook_contract`, `claims_contract` and `operator_intent_contract`. Synthetic manifest rows now carry probed `fps`/`frameRate`/`vfr`/`resolution`/`rotation`/`audio` exactly as ingest writes them, which the geometry gate requires. The first real run of that program through readiness gates, the sealed renderer, cleanup faults, readback, selection and TEST-only approval was launched at this point.

#### The 82-part long-form preview exposed a millisecond join clock (rendering policy change, with evidence)

Run #1 of the genuine 300-second class failed before readiness, inside the private cut preview: every part was CFR and the compiler drift was 3.08 frames inside its 42-frame tolerance, yet the joined proxy probed `avg_frame_rate 280380000/9354877` against `r_frame_rate 30000/1001` and the exact-fps gate in `cut_preview_media.observe_media` refused it. Five stream-copy experiments on the retained parts isolated the cause: the concat demuxer offsets every file by its container duration, and MP4 stores that duration in milliseconds, so NTSC parts, which are never a whole number of milliseconds, accumulate ±1-tick seam gaps in the joined video `stts`. Audio was not the cause (the preview already rebuilds source-derived float audio); the earlier AAC part clamp had only hidden the symptom at small part counts (LL-038 in the failure ledger).

The fix changes the cut mezzanine's rendering policy and is recorded as such. Parts now encode without B-frames (`ENCODE["cut_part_bframes"] = 0`, so packet order equals presentation order) and carry float PCM audio in MP4 (`pcm_f32le`, supported by the pinned ffmpeg 8.0); each part's audio is pinned to the cumulative sample clock round(cumulative frames ÷ fps × 48000) instead of a per-part seconds trim. `concat_parts` stream-copies the video but rebuilds every packet timestamp from its index (`setts`, explicit `video_track_timescale`), decodes the PCM once, re-times it contiguously, and encodes the mezzanine AAC exactly once (previously twice: part encode plus clamp re-encode), with the movie timescale set to the sample rate so the presented audio clock is sample-exact rather than millisecond-rounded. The elementary-stream proof that the manifestation authority seals is unaffected, because `setts` touches timestamps only.

Evidence: a prototype on three parts (37/53/29 frames) and the new regression `tests/test_cut_speed_concat_exact.py` (twelve NTSC parts through the real stage, work directory containing an apostrophe and a space) show `start_pts 0`, `duration_ts = frames × 1001`, uniform 1001-tick steps with `pts == dts`, `avg_frame_rate == r_frame_rate == 30000/1001`, presented audio exactly `round(frames × 1601.6)` samples, and decoded padding inside the existing ≤2048-sample rule. Fidelity at CRF 12 was measured on a 1080p NTSC synthetic: B-frame-free parts are 2.1 % larger, 0.21 dB lower average PSNR and 0.51 dB higher worst-frame PSNR, SSIM 0.99941 versus 0.99947; both are far above transparent, the mezzanine is re-encoded by every later stage, and no delivery-quality claim is made from this synthetic. The sixteen cut-dependent Python suites pass (142 tests) after one honest test update: the speed-1.25 preview no longer fails incidentally at the CFR check when admission is patched out in a test, so that test now asserts admission is the only speed gate and that a bypassed admission seals the retiming explicitly (speed rows in the manifestation and audio clock). No gate was weakened; `assert_supported_cut` is unchanged.

Run #2 of the 300-second class (114.9 s wall) then sealed the private preview receipt for the first time (9346 frames, exact profile, full decode passed) and failed one verifier later: the TypeScript evidence reader requires the sealed float audio clock total to equal the AAC presented sample count exactly, and they differed by six samples (14968554 versus 14968560). The preview mux wrote its AAC priming edit list at the MP4 default movie timescale of 1000, so the presented duration is millisecond-rounded; the three-second fixtures had only ever passed because ninety NTSC frames are a whole millisecond (LL-039). The mux now uses the sample rate as the movie timescale, which makes the presentation sample-exact; the verifier's strict equality was left untouched, and the tempo test now covers a non-whole-millisecond total. The one TypeScript failure seen in the same window (legacy cut deadline group-stop verification) reproduced zero times in three idle reruns after failing once while an ffmpeg render competed for CPU; it is recorded as load-sensitive with a diagnosis debt (the runner swallows the thrown error instead of recording it).

#### Run #3 reached the real gates, and the operator's review round landed before any further attempt

With both clock fixes, run #3 (123 s wall) sealed the private preview and, for the first time on the genuine 300-second class, ran the real deterministic readiness bundle. `operator_intent` blocked it in 93 ms: the synthetic program's accepted cut target carried no lane directives, so guided V4 inherited a lane-less target into the candidate and every waived lane resolved to auto against a stored intent that requires off. The program's cut plan now mirrors the stored intent's lanes exactly (the same object the fixture stores as project intent), and the opening fixture stops on a blocked readiness with the retained gate findings instead of tripping a pointer parse.

The operator's independent review of the session arrived at the same time with six reproduced findings and two QC edge cases, and all landed before the next attempt rather than after it. Readiness gates now receive the remaining request budget and, under a configured sealed renderer, one owned container name per gate, with those exact names reconciled and recorded on the gate bundle (an unverified absence fails the bundle). The selection reader accepts the worker's legitimate reuse of `core.mp4` for an identical opening/context range and still rejects any divergence. Recovery gained a non-guessed instrument: the Python process runner records every new-session child it starts in an owned ledger when the controller names one, the opening launcher names it, and the stopped-process reader treats a recorded pid that is still alive with the same program and start time as unresolved ownership, which retains the claim and is reported in status; nothing is ever signalled from that evidence. The browser parser now accepts the server's forced-stop and live-descendant statuses instead of hiding the recovery message behind a parse error. The beat-coverage validator is half-open on the frame clock with a boundary regression, the approval endpoint enforces its 16 KiB limit while streaming under a 10 s deadline, the glitch scanner refuses to be colourized (and declares any stripped codes), and decode completion is bound to the terminal progress block alone. The test launcher now propagates the inner exit code. All three delegated agents were cut off by the session limit mid-run; the two implementation owners' on-disk work was completed and tested by hand, and the concat adversarial review must be re-run because its finding was lost.

#### Run #4 reached the full deterministic bundle and exposed a gate contradiction

With the review-round fixes in place, run #4 (196 s wall) sealed the preview and ran the complete deterministic bundle in under a second: operator intent, transcript cut, plan lint, hook, claims and geometry all passed for the first time on the 300-second class, and the owned readiness containers were reconciled and verified absent. Two blockers remained. `template_usage` demanded an eighth distinct kind, chart-story, for the closing contrast beat, while `claims_contract` can never accept chart-story there because the beat speaks no number and chart-story paints numbers; the feasibility gate and the truth gate contradicted each other, and no plan could satisfy both. That is fixed at its source rather than in the fixture: the catalog contract now names the number-painting kinds, and the semantic contract keeps them compatible only when the beat's card window actually speaks a value, with a regression that flips on a single spoken "12" (LL-040). The second blocker was the sealed renderer refusing every `comp_size` render because the readiness environment named the owned container but not the pinned proof tools; readiness now resolves ffmpeg/ffprobe from the accepted cut's hash-verified toolchain, lazily and only when a sealed renderer is configured. One unrelated pre-existing test failure was found and left alone: `test_template_usage_approval` pins a call string in `assemble.py` that an earlier refactor moved into `assemble_arguments.py`.

#### Second adversarial round: a real seam defect in the concat fix, and the renderer's name grammar

The re-run adversarial review found the defect its predecessor had started to characterise before the session limit cut it off: the cumulative-sample clamp trims each part to its picture-exact window, which can end up to a frame before the nominal length on which the 15 ms declick fade had been placed, so the fade was cut off and seams ended at full amplitude (measured at two of three seams on the change's own fixture). The same shape existed in the private preview's float parts and in the delivery bus fit. All three now re-apply a sample-exact fade on the trimmed window, and the concat regression asserts every part and every seam ends inside the fade. The review's second blocker was that my readiness container names did not match the sealed renderer's `sniper-render-<32 hex>` grammar, so under the sealed lane every `comp_size` render was refused and the reconciliation evidence was vacuous; run #6 reproduced exactly that. Names now derive from the execution id and gate in the renderer's own grammar. The remaining suggestions landed as well: the joined mezzanine must carry exactly the parts' frame sum; the process runner records an intent before each fork so a child whose pid was never recorded is unknown rather than absent; each child invocation keeps its own ledger; program matching is boundary-safe for paths with spaces and a process older than the recorded spawn is a pid reuse; the readiness store rejects an ok bundle with an unverified renderer; the Python mux reuse key now includes the exact ranges. One consequence is documented rather than hidden: the ordinary mezzanine now aligns with source content within 0.85 ms on the fixture that previously pinned it as misaligned by more than 3 ms, so that test became an alignment guard. Still owed from the review: an explicit ffmpeg capability probe for PCM-in-MP4 parts, and a real (non-vacuous) source-length check on the preview's float parts.

#### Runs #7 and #8: the sealed renderer measures the plan, readiness passes, the worker's own lock check is the next wall

With the renderer-grammar container names and pinned proof tools, run #7 was the first readiness in which the sealed renderer actually rendered the candidate's comps: every deterministic gate passed except `comp_size`, which refused three cards at render time because the test treatment had authored `|`-lists where the module comps require `num~label`, `key~value` and `value~label` pairs. The gate did its job, but late and at sealed-render cost; the absence of a plan-time slot-grammar contract for those comps is recorded as a product gap. With the copy re-authored in grammar from spoken words only, run #8 passed readiness for the first time on the 300-second class under the sealed renderer (gates 24 s, readiness 29 s), committed a treatment draft, claimed the opening, and then the owned Python worker refused its own input in 133 ms because it required the candidate target to equal the accepted target exactly while guided V4 legitimately adds the two style keys the TypeScript input builder already tolerates. The Python rule now mirrors that tolerance and nothing else. Cleanup reconciled the claim on its own and left no container behind.

#### Runs #9 to #11: through the worker's admissions into the first whole-program base render

Run #9 tripped the worker's second exact-target comparison on the same V4 style keys; both comparisons now use the style-free rule, and the retained inputs prove the frame-binding hash already equalled the style-free target on both runtimes. Run #10 passed admission and failed in the frames stage because the executor qualified placement and effect intent for every bound graphic, including the body-only takeover at 78 s that the first opening profile can never composite; qualification is now applied only to graphics the opening executes, while body-only graphics keep their exact identity and endpoint binding. Run #11 then went further than any attempt so far: admission, frames, pinned code and tools, sealed-renderer admission, and the ordinary whole-program base render of the 312-second program (82-part cut, 9346-frame visual bootstrap, 162 s) all completed, and the attempt failed only at the final loudness policy, 1.34 LU below target, because the synthetic audio is 20 ms pulses in silence that no master can lift to −14 LUFS under the true-peak ceiling. The long-form program now carries a continuous voice-like bed under quarter-scale pulses (measured −15.1 LUFS, −3.3 dBFS peak), which is what real speech looks like to the master; the byte-pinned three-second fixture is untouched. Readiness itself dropped from 29 s to 8.5 s between runs #8 and #11 because the sealed comp renders are content-hash cached.

#### Run #12: the first mastered whole-program base of the 300-second class

With the voice-like bed under the program's pulses, run #12 rendered and mastered the entire 312-second program inside the owned worker (262 s: 82-part exact cut, 9346-frame visual bootstrap, master at −14.0 LUFS within tolerance, graphics-free base sealed), then cut the exact core and review master excerpts (11.5 s), and failed at the very first graphic's intent seal because that seal hashes the sealed renderer's cache identity, which reads the pinned proof-tool paths, before any container name or proof environment exists; no earlier attempt had reached that line. The owned graphics phase now keeps those pinned tool paths in scope from its first attempt to its last. Readback and result verification compare stored digests only, so they are unaffected. The retained base from this run is the first mastered whole-program render of the genuine long-form class in this project.

#### Run #13: the first opening graphic rendered inside the sealed container

Run #13 carried the whole-program base and the exact master excerpts through again and then, for the first time, rendered an opening graphic (the statement card) inside the sealed OCI container in 7.9 seconds with its runtime receipt, network proof and container removal recorded. Its proof verification failed on a shape mismatch rather than on the media: the receipt binder keeps the retained input archive's member list as a tuple in memory while the validator compares against a list, and every previous consumer had read the proof back from its JSON sidecar, where the tuple had already become a list. The opening worker is the first consumer to validate the bound proof before any JSON round trip. Both sides now compare the archive proof in its JSON shape. The retained attempt is the first sealed-container render of an opening graphic in this project.

#### Run #14: the first complete opening on the 300-second class, with audiovisual evidence

Run #14 exited 0. Inside the owned worker (387.7 s) the whole 312-second program was cut (82 exact parts), mastered (−14.0 LUFS) and retained as the graphics-free base, the exact core and review master excerpts were cut, seven opening graphics were rendered one by one in the sealed container (4.1 s to 10.0 s each, one owned container each, every one removed and its removal observed), the exact ranges were composited, the picture was copied and the AAC muxed, and the whole input and result were revalidated. Four cleanup attempts with injected CAS faults all reconciled in about 0.66 s each, two current-media readbacks took 41 s each, selection committed, and the TEST-only synthetic attestation recorded an approval bound to the exact selection while leaving body generation and delivery explicitly unstarted. The produced media are real: core.mp4 (1853 frames, 61.8 s) and review.mp4 (2013 frames, 67.2 s) at exactly 30000/1001 from a zero origin, both at −14.0 LUFS with −5.1 dBFS peaks; the fixed Audit B screens pass freeze and flash and warn on the intentionally dark ledger card once the plan's own windows are declared; stills at 1.8, 15.5, 36.5 and 56.4 seconds show the statement card, the bullet-bars card, the dark ledger and the section takeover composited over the synthetic footage. The whole fixture took 617.7 s of wall time, of which the cut preview took 98 s and readiness 9 s. None of this is a creator-footage or quality claim: the footage is a test pattern, the audio is a synthetic voice-like bed, the critics are TEST stubs and the approval is a synthetic attestation. It proves that the opening workflow's mechanics and exactness hold on the genuine long-form class under the sealed renderer.

#### Authentic route probe on the retained opening

The running test server on port 3327 answered one status request with 409 and then stopped answering altogether; it is the user's server and was left alone. The real route handlers were instead invoked in-process with genuine `NextRequest` objects carrying loopback host, origin and same-origin fetch headers, and with the workspace root override pointed at the retained test root, which is the only reason the first attempt had been refused (the canonical-directory policy, not the opening evidence). Status returned 200 in 67 ms with ready-for-review, the TEST attestation's approval and the journal identity, and the browser client's parser accepted it unchanged. The media route served a 1 KiB range as 206 with the exact bytes of core.mp4, the whole file as 200 with a SHA-256 equal to the sealed selection, 409 for a wrong media hash and 416 for a range past the end. This is a handler-level test, not a browser session; the browser-side evidence remains the UI regression with intercepted DTOs.

#### C0679: the real-source admission class opens

The real long-form source (10.28 GB, 834 s, 4K 23.976, BT.709 with an xvYCC transfer, stereo PCM) is ingested through the real ingest route handler in-process, into an isolated qualification workspace with local whisper as the only transcription provider, so nothing paid is reachable and the original file is referenced in place and never modified. Two bounds stood in the way and both were raised deliberately rather than bypassed: the external-media snapshot bound and the sealed probe's own size ceiling were 8 GiB, below this source's 9.58 GiB; they are now 16 GiB, the change is recorded on the constants and in the test that pins them, and every other probe ceiling (decode time, duration, frames, dimensions) is unchanged. The ingest admission also turned out to require the sealed Docker controls that the render lane uses, which the local environment file does not carry; the qualification run passes them explicitly. The source clears the SDR admission rule as written, since only PQ, HLG and BT.2020 are refused.

#### C0679 admitted, transcribed and previsually cut

The third ingest attempt completed: the source was referenced in place, snapshotted with a running hash, and fully decoded by the sealed probe container in about 17.8 minutes (89 % of the probe's 20-minute ceiling, so a source only slightly longer than this one would be refused on time rather than size), then local whisper transcribed 2232 words in 34 seconds and the route wrote the manifest and the long-form intent into `~/ProjectSniper-qual-2026-09-07/c0679-20260907`. Two honest limitations of the local-ASR path surfaced immediately: whisper's word timings carry no inter-word gaps at all (maximum 0.21 s over the whole take), so the pause scan is blind on this path and the deterministic cut removed only the four retakes it could prove, and whisper absorbed the leading silence into the first word. The previsual cut plan was then authored deterministically from the retake evidence, the opening restart and the hard stutters, with a verbatim-evidence removal decision for every gap, and the previsual transcript-cut gate passes it at 725.7 s of output. Along the way the gate itself was wrong once: it read "do it. It's" as a stuttered "it it" because it compared normalized tokens rather than whole words (LL-041), which is fixed with regressions. The next steps for this source are the human cut review in the UI (the dev server was unresponsive at the end of this session), the treatment proposal and readiness critics driven by Claude Code subagents rather than any paid provider, the sealed opening, and a real human approval; none of that is claimed here.
