---
description: Produce or revise a short or long-form video
argument-hint: <source-or-project> <what you want changed or produced>
---

Invoke the `producer` skill.

The operator's request is: $ARGUMENTS

Honor the requested destination and scope. A rendered-file request uses the
deterministic Producer pipeline. An editable-Palmier request uses the staged
Desktop-native branch. A trim-only request must not gain graphics, motion,
music, or other unrequested treatment. Run the applicable gates and QC before
claiming completion.
