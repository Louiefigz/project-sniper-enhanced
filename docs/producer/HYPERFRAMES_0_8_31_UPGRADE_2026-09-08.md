# HyperFrames 0.8.31 upgrade — implementation and evidence

Status: installed; all 53 native catalog measurements published; reviewed route,
cleanup and audio correctness fixes integrated live. Further workflow
qualification remains. This is not delivery
approval, completion of the video-editor goal, or proof of a two-hour master.

September9 06:31UTC: all16 non-browser Studio TEST modules now pass191/191,
zero skips, in40.66seconds mirror/40.30live. This includes the separately fixed
original-version import reconstruction, retained SplitText head, and both
SDK-specific probes. The actual browser player module is explicitly excluded:
its offline controls/visibility failure remain a separate unresolved qualification,
not a passing skip. All four final live follow-up hashes match the tested mirror.
See `live-studio-original-readback-integration.json` in the upgrade workspace.

Latest integration checkpoint (supersedes earlier pending-live wording below):
the split TypeScript run reached all 445 original file paths. Its retained
suffix has 13 import-cycle failures and one metadata-child timeout; reviewed
cleanup fixes and focused unchanged-deadline retry evidence resolve those
observed cases. Three earlier fixture failures remain in the historical logs.
Five explicitly skipped cases are not claimed tested. See the exact per-file
`typescript-regression-445-file-inventory.tsv` and completed coverage report in
the upgrade workspace; there was no single uninterrupted all-pass invocation.

All 23 reviewed route/cleanup/Python-fixture files now match the tested mirror
byte-for-byte. Final live cleanup/import/metadata checks passed 32/32 in
26.30 seconds wall; TypeScript passed in 7.79 seconds. The frozen audio package
plus separate ACTIVE-shape correction is now live, with all 29 source/dependency
hashes matching the independently checked mirror and 43 live metadata tests
passing in 3.00 seconds. The separately reviewed audio dependency follow-up is
also live after the native revision cohort below. Guided finishing profile
activation remains outside those completed integrations.

The final live ordinary audio render/assembly cohort also passed all seven
real tiny-media cases in 37.75 seconds wall (36.764 seconds suite), retaining
`/private/tmp/sniper-finishing-assembly-i6655zz3` and
`live-audio-assemble-integration.log` in the upgrade evidence directory. This
proves the exercised ordinary four-second synthetic path, not guided finishing,
a graph-backed revision, creator listening or a full-video timing target.

### Audio revision dependency follow-up — integrated

The separate frozen follow-up (`e9a18a0c…`) and additive test-structure correction
(`9038eea2…`) now match the tested mirror across all seven affected files. The
full hashes, original/live/mirror identities and retained failure are in
`live-audio-revision-followup-hashes.json` in the upgrade workspace.

For source-float-v2 only, timeline/base dependencies now use the existing
finishing-free lineage projection. Legacy domains remain unchanged; changed
picture inputs still invalidate picture work; final-master dependencies retain
the complete requested finishing. There is no new renderer or quality setting.

- Mirror digest/registry tests:12PASS,9.20seconds wall.
- Real graph-backed revision cohort:12PASS,134.351seconds suite,
  135.37seconds wall; artifacts and every command log remain at
  `/private/tmp/sniper-v2-graph-cli-s02o0pc2`.
- Final live digest/registry/graph/store tests:20PASS,9.64seconds wall.
- The first native attempt ran0tests and failed in3.60seconds wall because the
  mirror omitted motion `node_modules`. Linking the already installed runtime
  fixed the fixture; no install, source change or weakened check was required.
  Its artifacts remain at `/private/tmp/sniper-v2-graph-cli-nm15w62c`.

The actual four-second synthetic graph used13.407seconds for base generation,
17.107for first assemble,5.593for a no-op,16.098for music revision,15.025for
combined finishing,10.878for retained-master reassembly,13.901for gain-only,
14.039for cleanup change and14.732for music/SFX change. These are elapsed
observations, not matched speedup claims. Audio-only revisions kept exact video
packet signatures and invoked no graphics stage. Composite and final nodes both
read dirty when they alias the same audio-bearing `final.mp4`; source, timeline,
base and raw-dialogue nodes stay reused. Foreign-master, resealed-pointer and
substituted-picture negatives still reject; no creator or whole-video approval
is supplied by this fixture.

Default `npm run lint` now passes with zero errors and 41 retained warnings
(20.88 seconds wall). A prior final invocation accidentally scanned the named
`.next-guided-20260907` generated cache and failed with 734 generated-code
errors; it is retained. The config now excludes `.next-*/**`, matching the exact
named-cache convention already enforced by `next.config.ts`, so no extra CLI
ignore flag is needed. No application-source rule was disabled.

### Measured staging overhead — integrated

The reviewed four-file prepublication batching patch is live and exactly matches
the tested candidate. Agent union158/158PASS includes36new tests; root separately
ran those36in44.00seconds wall and17existing live staging tests in17.96seconds.
Final live TypeScript passed in7.71seconds. Overlapping test cohorts are not
added together. Exact identities: `live-staging-batch-integration.json` in the
upgrade workspace; candidate/report:
`/private/tmp/sniper-staging-batch-20260908.nTn2tC`.

At303pins, direct-class prospective-file stats fell184224→2424; genuine owner
hooks use3636. Original callbacks remain, with one extra complete final check.
The same two actual metadata tests measured9.00→5.06seconds combined in one
before/after sample. This is metadata overhead, not video-render throughput.

All prospective original identities/parents are captured before callbacks;
unpublished cross-pin mutation is now detected at the final batch boundary.
Original held claims/inputs still check throughout. Publication, reentry and
partial cancellation-inventory transfer cannot escape the unfinished batch.
A failed batch retains its failure/leases; phase-only stopping does not prove
durable cancellation or release. No native/source-color activation or media
quality check was added, removed or waived by this optimization.

## Operator decisions

### Ordinary caption-cache revision — integrated and tested live

The isolated source-float assembler now seeds only paired caption media/receipts
named by the original prior caption manifest. It does not copy every obsolete
shard from revision history. Files remain untrusted hints: existing receipt,
font/tool, media-proof and complete-output audit checks still apply. Original
public holds remain, copies are independent inodes, and held guided preparation
and owned caption execution do not use this ordinary cache path.

The 33 focused checks passed in 5.75 seconds wall in the mirror and 6.28 seconds
live (including two tiny native owned-graphics checks). Nine final real caption/
held-preparation tests passed in 87.18 seconds wall in the mirror and 87.69
seconds live. A changed cue reused the unchanged shard (1 hit/1 render)
versus 0 hits/2 renders with seeding disabled as the control. Both resulting
video and audio packet signatures match exactly, both full audits pass, and
the actual audio master content/sample count and base bytes remain unchanged.
This ordinary fixture has no held prior graph: it correctly rebuilt its audio
master in both runs, rather than treating the mutable pointer as reuse authority.

Preparation/first assemble took 22.944 seconds; the cached revision took 11.218
seconds versus 11.252 for its uncached control. That 0.034-second difference is
**not a demonstrated throughput improvement**. The live repeat took 22.963
seconds for preparation/first assembly, 11.322 for the cached revision and
11.292 for its uncached control: cached was 0.030 seconds slower. The fixture is four
seconds of synthetic NTSC/J-cut/right-only/silent media, not a long-video or
human-listening qualification. Caption-free graphics composite seeding remains
outside this patch because its placement-evidence join needs separate work.
All earlier failures, including root's mistakenly removed then restored helper
re-export and incorrect TEST expectation of ungraphed master reuse, are retained
in `mirror-caption-cache-integration.json` in the upgrade workspace. Independent
review found no blocker; all four integrated live hashes match the tested mirror.
The live outcome is retained in `live-caption-cache-integration.json`.

### Studio runtime environment — inherited overrides refused

The existing Studio launcher/context now reject a nonempty inherited
`HYPERFRAME_RUNTIME_URL` before SDK execution. This avoids loading an unpinned
remote/project runtime through an upstream escape hatch. Empty/whitespace-only
settings retain the installed upstream default, and unrelated environment values
are preserved. No SDK bytes, renderer image or runtime override were installed.
The two-file guard passed 46 combined mirror command/ID/declaration tests
(4.712 seconds suite), then the same 46 live tests (4.811 seconds suite).
A broader 110-test run exposed stale V1 generated-format expectations and
sandbox-denied ps/socket tests (1 failure, 9 errors, 10.654 seconds suite).
The seven-file reviewed follow-up is now live: historical fixtures explicitly
remain V1, newly generated views require V2/hf-ids-0.8.31, and the existing strict
HTML declaration parser also supplies quote/entity-aware advisory comparison.
Unmatched defaults still refuse with exact pending plan/manifest/file bytes
preserved; no generic HTML rewrite or new sync permission was added.
All121 tests then passed with local process/socket/tiny-FFmpeg permissions:
10.918 seconds suite/13.83 wall in the mirror, 10.669/13.70 live, zero skips.
The same seven live hashes match the tested mirror. Original failures and the
agent's narrower90-check cohort remain separately recorded. Full evidence:
`live-studio-regression-integration.json` in the upgrade workspace. This is not
deployment or qualification of the separate SDK visibility diagnostic.

### Selected captions — mirror integration, compiled word correction live

V9 caption integration plus strict parser parity and connected-service/history
corrections are in the mirror, not activated by default. Root93 Python/45 TS,
three original-source-holder negatives and two static closure checks passed.
Separate actual landscape/portrait selection tests passed in41.43seconds, with
1hit/1render for the portrait restyle. The actual accepted-cut/raw-admission/
compile/current-history-read/replay TEST path passed13.75seconds in the agent's
candidate; this is not the public four-field/readiness/opening/body chain.

Root visual inspection then caught a real semantic defect despite the two
native tests passing: at frame48, future word SIX is highlighted while current
word FIVE (frames43–53) is white. The old test counted any yellow/white pixels,
not the corresponding word or gap. The compiled current-word correction is now
live across two production and three standalone TEST files. Root independently
replayed all41 focused checks in the mirror (0.557seconds suite/0.77wall) and live
(0.612/0.81). The native tests check every active/gap frame at integer/NTSC rates,
global/local parity and unchanged glyph layout. Root also viewed actual output
PNGs with current FIVE highlighted, no highlight in the gap, then current SIX.
The separate corrected selection2 passed43.351seconds suite/43.91wall; root
independently reran both in43.508seconds suite/43.90wall. Their two TEST-file
changes are mirror-only. Line and phrase ASS bytes remain unchanged;
legacy/phrase timing semantics are not newly qualified. Adding the directly
imported shared formatter to the compiler hash prevents old cached code output
from surviving the upgrade; an actual cache experiment confirmed miss then warm hit.
Exact evidence: `live-caption-karaoke-integration.json` and the frozen package
`/private/tmp/sniper-caption-karaoke-review-20260908.1Z5zCC/README.md`.
Do not treat the earlier green cohort
as caption synchronization or creator-quality approval. Original images and
the defect remain retained; no final gate or timing requirement was relaxed.

The public four-field cut-to-V9/readiness/original14-document TEST now passes
in25.48seconds wall in its frozen private candidate. It runs actual source
admission, cut preview, TEST acceptance, raw treatment, deterministic readiness
gates, TS/Python input/source/pipeline and manual-short geometry readback.
Compiler and independent critic outputs are explicit TEST leaves. It requests
outside-line plus three selected words and an exact crop; outside-off short
remains refused by the existing raw crop gate. Earlier failed attempts remain:
1.96seconds missing runtime configuration;28.67seconds real selected-short
geometry dispatch gap;24.83seconds incorrect preset argument in the new TEST.
The two-module geometry dispatch correction and separate test-oracle fix are
in the mirror. No caption/opening/body/final render or creator approval occurs
in this input-only test. Exact stages, hashes and process/container cleanup:
`/private/tmp/sniper-v9-readiness-input-umKTpc/NATIVE_QUALIFICATION.md`.
No public/default activation is inferred.

### Reproduced Studio preservation gap — correction integrated live

After the live121-test compatibility pass, root exercised actual generated TEST
views through sync/apply and ordinary no-force regeneration (only the inert
media-probe leaf was replaced). In0.903seconds, both a structural instance edit
and a mixed valid timing/index-comment edit were marked clean after apply and
discarded on regeneration. The first emitted a warning but no blocker; the second
had no structural warning. No user view was touched. This is a pre-existing
preservation defect, not fixed by SDK ID normalization or declaration advice.
Independent review confirms the documented unsynced-edit invariant requires
those changes to remain pending. The independently reviewed11-file correction
is now live, with every byte matching the tested integration mirror. Unsupported
instance/index edits and changed tracked sidecars/assets remain pending and
block apply; a supported edit cannot silently absorb them into a new baseline.
Known host deletions preserve the full index stream and original head dependency.
Original index/tracked hashes are checked again after the actual plan gate and
immediately before publication. This is not an atomic multi-file transaction
against independent external editors. Existing untracked standalone files remain
preserved and pending. Root also caught two fixed-temp-name file collisions;
exclusive index temps and the existing atomic JSON helper preserve the original
operator-file sentinels and plan formatting.

Root's full expanded Studio cohort passed160/160 with zero skips:23.909seconds
suite/27.00wall in the mirror, then23.822/26.85live. These include actual local
process/socket and tiny-FFmpeg cases, not a browser visibility or video-quality
qualification. The earlier focused50 and agent metadata cohorts overlap these
tests. The separate SDK provenance TEST-only migration is also live: root5/5
passes10.93seconds in the mirror and all5 pass in the subsequent live25-case
cohort. That wider original cohort failed two other cases (three sub-errors):
the old host-ID persistence probe and the read-only UI import bridge. The latter
reconstructed raw instance HTML even for normalizedV2 generation. Both are now
fixed in the separate reviewed follow-up, including a genuinely reproduced
last-SplitText-deletion/import failure. The same original hashes remain mandatory;
V1 launches no normalizer and V2 reuses one shared original-only batch.
All191 tests across every non-browser Studio module pass37.629seconds suite/
40.66wall in the mirror and37.283/40.30live, zero skips. This includes6 new
import cases, actual SDK persistence/resolver functions and tiny-media fixtures.
The independent browser player module remains excluded/unqualified; no visibility
fix is inferred. Exact follow-up identities, failure history and exclusions:
`live-studio-original-readback-integration.json`. Earlier provenance evidence:
`live-studio-provenance-integration.json` in the upgrade workspace.
No old view was force-regenerated. Full exact identities, commands and retained
failures: `live-studio-pending-preservation-integration.json` in the upgrade
workspace and `/private/tmp/sniper-studio-pending-review-20260909.TWRdyp/README.md`.
Original repro:
`/private/tmp/sniper-studio-pending-repro-20260909.1o8K3c/TEST-repro.json`.

V10 cleanup/gain contracts, canonical signed-zero parity and internal post-cut
authoring are now mirror-only. The18-file authoring package rederives original
accepted audio, full original intent and clock, preserving exact request version
and older default5. Root102 tests pass with1 explicit native skip (1.45seconds),
whole TypeScript passes7.02seconds,3 static import/snapshot checks pass1.01seconds,
and5 original-source-holder checks pass93.11seconds. These overlap agent cohorts;
do not add them as unique tests or infer rendering performance. The separate
one-file V8 negative TEST context correction changes no production gate.
The frozen authoring candidate's real three-second synthetic cut-to-V10 stored
compile/current-cold-replay test also passes independently in12.99seconds wall.
It retains the exact four-field accepted plan, validates actual compiler pins
and confirms the still-closed readiness boundary. Source-admission decode/image
facts and creative outputs are explicit TEST leaves; this is not DSP or the
public-bootstrap14-document path. The existing test cleans only its successful
temporary fixture; root confirmed its exact path absent and no matching child
process. Details: `mirror-v10-authoring-integration.json` in the upgrade workspace.
The TS readiness16-file and Python readback7-file additions are now in the mirror,
not live/default activation. Root136 TS tests pass with1 explicit native skip
(1.74seconds wall), and whole TypeScript passes7.43seconds. Both actual audio
fields, their exact presence, original raw clauses, controller-held clock and
accepted cleanup policy replay before prior-lane validation. Fresh TS readiness
uses a detached clone of the full original intent with only cleanup projected;
the Python cold reader authenticates only the held cleanup subset, not a new
independent original-intent document. The original14-document shape remains.
Root62 combined Python cases pass0.79seconds wall, including6 connected-file
positives/negatives (0.43seconds separately) for active/clear requests and changed
gain, no-op presence, raw text and published input bytes. Their media/probe leaves
are explicit inert TEST data, not actual cut/controller/renderer qualification.
Hubble's independent Python review found no actionable defect; the actual Python
closure includes364 modules and refuses missingV10/V9/V7 schema pins. Details:
`mirror-v10-python-readback-integration.json` in the upgrade workspace.
The first actual gate/critic-to14-document attempt failed15.60seconds wall:
the original produced fixture assigned motion/transitions/B-roll but the
audio-only proposal authored none. The actual operator gate correctly refused;
no readiness critic, treatment draft or14-document input was reached. All9
owned-container names are verified absent and no test child survives. Original
workspace `/private/tmp/sniper-v10-input-3Je4yG` and
`/private/tmp/sniper-v10-input-review-20260909.d6E9jD/native-once-result.json`
are retained without retry/rebaseline. A separate explicitly trim-intent fixture
was then declared before capture. Its37 pure regressions pass2.28seconds with2
native skips and TypeScript7.45seconds. The single native attempt failed at the
unchanged300-second longform minimum:15.765seconds wall, zero critics and no
14-document input. All9 owned containers, exact processes and both locks are
absent; sources and both failure workspaces remain unchanged. Report:
`/private/tmp/sniper-v10-trim-input-review-20260909.OZmZJ2/NATIVE-RESULT.md`.
This is a fixture-design failure, not a passing readiness or DSP qualification.
A correct next positive must declare its valid full-length or deliberate-excerpt
semantics before source/cut capture. The old sparse-pulse audio is also known
not to satisfy the ordinary loudness/peak master gate; a DSP positive needs a
new upfront continuous synthetic source, never relaxed QC or changed accepted
source bytes. The one-file supplied-ready TEST helper is in the mirror only:
root9 deadline/default-entry tests pass0.44seconds, TypeScript7.21seconds,
lint1.53seconds. It reuses the existing opening operation/lease/worker under the
original caller deadline; no readiness, approval or fresh clock is manufactured.
The six refusal cases are now permanent in the existing TEST module; all15
root cases pass0.52seconds, combined lint1.37seconds and TypeScript7.10seconds.
No V10 DSP, source2 activation or listening approval is established,
and no original intent/accepted cut is rewritten.

An independent code audit also confirms the produced-workflow gap is authoring
and execution binding, not a missing rendering engine. Guided V5–V10 cannot
author ordinary punch-ins, B-roll or transitions; V4 candidate projection clears
transitions, and both opening profiles reject all three lanes. Existing ordinary
planners/render stages are reusable. Integrate motion first, admitted video
B-roll second, transitions last because full-scene graphics ordering and shared
master SFX must be connected. Presenter layouts and clean-hook decisions do not
silently discharge those lanes. SDK0.8.31 composition edit APIs do not establish
accepted-cut source bindings, actual lane consumption or quality approval.

The three-file ordinary motion endpoint repair is now live. Existing inclusive
`between(t,start,end)` kept the static zoom on frame60@2s; exact90frame counts,
timestamps and142audio records did not detect it. Both static/animated branches
now use half-open predicates, keeping existing six-decimal projections and all
encoding settings. Agent native three-rate batch PASS4.66seconds; independent
root63-case combined cohort PASS7.45seconds (actual media4.496seconds), final
live62 pure PASS3.01seconds. Exact frame counts, PTS and audio packet records
remain unchanged across all9outputs. Root viewed before/after endpoint frames.
All3live hashes match the tested mirror, and exact owned PID/native matches are
absent. Existing stage fingerprints already include the changed primitive.
Evidence: `live-punch-half-open-integration.json` in the upgrade workspace and
`/private/tmp/sniper-punch-half-open-20260909.zMLvmN/README.md`.
A durable integration summary is in
[`evidence/PUNCH_HALF_OPEN_2026-09-09.json`](evidence/PUNCH_HALF_OPEN_2026-09-09.json).
Arbitrary fractional-frame quantization, guided motion and whole-video quality
remain unqualified; no new renderer or lowered quality setting was introduced.

### Studio visibility candidate — diagnostic only, not deployed

An isolated exact-artifact patch changes only the existing raw-node and timed-
ancestor HTML predicates, preserving the runtime's timing math. Both full IIFEs
parse and the inverse patch restores every original byte. The actual complete
runtime test reproduces skipped genuine foreign-realm elements with stock SDK
bytes and passes with the candidate; seven Studio beats also pass. The latter
launch happened to have same-realm hosts, so it alone does not establish repair
of the originally failing Studio launch. Native total: 22.826 seconds; the first
2.271-second TEST baseline-oracle failure is preserved.

No SDK dependency or sealed renderer bytes were changed. A source rebuild did
not match the released artifact, and a proposed preview-only URL override would
leave important sub-composition/fallback consumers using original bytes. That
partial override is not being shipped. The version/hash-bound candidate and
scope/cleanup evidence remain at
`/private/tmp/sniper-sdk-realm-retry-XQLi8K/README.md`. Full Studio and sealed
render qualification are not inferred from this diagnostic. All three exact
test processes are gone; no listeners remain.

### Catalog discovery — integrated with adversarial corrections

The seven owned read-only catalog modules/tests plus the three shared command/
skill documentation changes are live. The separate finite-duration and shape/
ranking corrections are included; 29 final live tests passed in 4.07 seconds wall
(1.086 seconds suite), after the same mirror cohort passed in 4.07 seconds.
The earlier six-file live cohort passed 26 tests in 4.19 seconds. An earlier root test
invocation lacked the existing tests PYTHONPATH and failed before collection
(two import errors,0.07seconds); the corrected mirror cohort passed in4.10seconds.
The skill validator passed using the already installed backend venv; Sniper's
venv lacks PyYAML, so its initial validator invocation failed without installing
anything. Root narrowed the skill wording to distinguish declaration filters
from measured compatibility and to report incomplete as well as missing/stale
measurement rows.

Further adversarial review found exact prefixed references could lose the top
search slot, plus malformed study/lock shape handling gaps. The tested additive
correction is now live. Exact references rank first (all 418 checked; original
candidate missed 13); four retained non-exact JSON searches remain byte-identical.
A focused shared validation module avoids a records/sources import cycle.
Native capability/admission is
unchanged; no full upstream inventory or porting approval is claimed.

### Studio ID prevention — integrated; guided native entry failed

The nine-file SDK ensureHfIds prevention correction is now live and byte-identical
to the tested mirror.
New views have an explicit V2 normalizer identity; old V1 views retain their
original bytes/behavior. Root reviewed it and made a documentation/type-only
sync_apply cleanup (326→295 physical lines). The combined 67 focused ordinary
checks passed in 7.82 seconds wall in the mirror and 7.67 seconds live (4.729
seconds suite). Actual new-view Studio open and seven settled seeks passed the
ID check: all 12 tracked files were unchanged; dry sync was clean with no plan
changes or blockers. The entire synthetic diagnostic took 25.067 seconds,
including 2.148 seconds setup. No apply/rebaseline was used. Both owned PIDs
were gone afterward and no listener remained. Immediate plain bind still
reported closing TCP state and is retained as a failed check. Evidence:
`/private/tmp/sniper-studio-id-native-PpHNne/README.md`.
No installed SDK bytes or existing view files were changed. This proves ID
prevention, not the separate visual timing behavior, which still failed.

The initial inherited-audio entry TEST fixture required a separate correction:
its project intent and plan must be authored together BEFORE capture. Plan-only
authoring genuinely fails the actual operator-intent gate. The corrected seven
pure checks passed in1.12seconds; at03:44UTC September9, root started the actual
five-minute1920x1080 opening/master/body reuse test, retained session11964 and
`guided-audio-native-entry-start.json`. It FAILED in23.63seconds wall after
21.973seconds synthetic preparation: the actual previsual cut gate correctly
rejects populated audioEnhance/audioGain. No opening/master/body work began.
Fixture `/private/tmp/sniper-human-cut-rWJ1OL/test-only-cut-review-5c1b91a7`
is retained. Independent process inspection found no surviving test/renderer;
Docker listed only the three unrelated existing RAG services. Do not weaken the
cut-only gate or retrofit accepted authority to make this fixture pass. The
inherited profile path is not connected fresh-workflow proof; investigate the
existing post-cut treatment path. The public saved-plan route is ordinary,
not a guided V2 adoption route; it cannot substitute for this test. Explicit
post-cut audio needs request-derived authority (V10; V9 is reserved for selected
captions), without rewriting the accepted cut or original intent. This is a synthetic mechanics regression,
not real-dialogue listening, creator approval or the ten-minute/two-hour proof.

Studio playback failure is now reproduced on a fresh port with the original
1800×1100 viewport. A smaller800×600 probe passed. In the wider case the hosts
belong to the iframe document but retain the parent-window HTMLElement realm;
the exact creation/adoption seam has not been established.
The SDK runtime's realm-specific instanceof check skips those hosts' visibility
updates; the2.25second failed screenshot is byte-identical to the earlier
overlap. No visibility fix is enabled yet. Both native processes are gone;
the immediate plain-bind cleanup check failed on closing TCP state while
independent PID/listener/connect checks showed no live server. Evidence:
`/private/tmp/sniper-studio-visibility-matched-c6BPow` and
`/private/tmp/sniper-studio-visibility-realm2-WyClUy`.

### September9 actual Studio/guided integration checkpoint

The new guided inherited-audio candidate plus two separately reviewed corrections
is applied only to the integration mirror:35 TS checks PASS56.76seconds wall,
plus51 distinct complementary TS checks PASS2.10seconds (86 total, not counting
the separate static-closure check). The47 Python checks PASS0.49seconds and whole
TypeScript PASS7.59seconds. One earlier
Python invocation named a nonexistent test module (20 passed /1 import error,
0.52seconds); corrected cohort names, not production changes, resolve that setup
error. The helper is now in the complete shared source2 capture list, including
old-profile first/final replacement negatives; new finishing/source2 remains
explicitly fenced. Actual new-profile native entry/body reuse is still pending.
This carries inherited settings, not new post-cut cleanup/gain authoring.

The separate Studio lifecycle patch and additive resolved-Node correction are
now LIVE across four exact-tested files. Final live41 ordinary tests passed in
3.39seconds wall (0.468seconds suite); the original40-test mirror run passed in
3.59seconds. Custom-basename or whitespace-containing Node executable pins now
refuse before spawn because the owned-process matcher cannot safely stop them;
spaces in the Studio project directory remain supported. Exact hashes and scope
are in `live-studio-lifecycle-integration.json` in the upgrade workspace.
Actual upgraded Studio starts in1.06seconds; an explicit test-only localhost
sandbox starts it in1.20seconds. Exact PID/project/port, context and observed
shutdown work on the installed Node path. The later ID correction above fixes
automatic SDK stamping for new views, but the full Studio round-trip still has
**not** passed: the actual player shows an icon before its scheduled window.
The seven-beat probe's exit0 is not a visual acceptance pass. No visibility fix
is enabled and no pending old views were overwritten or rebaselined.

Ordinary Studio also automatically fetched free Google Font CSS despite Node
update/install opt-outs; the test was stopped and repeated with denied external
network. CLI opt-outs are not an offline browser/asset policy. No paid/model
generation occurred. See retained actual attempts, screenshots, network-test
scope and failures in
`/private/tmp/sniper-hyperframes-upgrade-20260908.mUKXnI/studio-0831-native-checkpoint.md`.

- Upgrade approved; continue the existing implementation goal.
- The `sniper-usage-reset` scheduled task is PAUSED. No reset was consumed.
- Codex/Producer commands are the primary workflow. Custom Sniper UI polishing
  is not a prerequisite for the operator's working path.
- Local compute and verified subscriptions only. No paid inference, model
  downloads, purchases, or billing fallback were used in this upgrade.
- Preserve creator footage, historical evidence and the shared dirty worktree.

## Installed changes

- Root `@hyperframes/sdk` is pinned to 0.8.31; motion `hyperframes` CLI is pinned
  to 0.8.31. Both locks were generated by npm, not hand-rehashed.
- The existing SDK proposal adapter imports the project-installed SDK normally.
  Its 14 tests run in `npm test`, with no SDK opt-in and no skipped cases.
- Bundle readers accept exactly the existing 0.7.33 and new 0.8.31 declarations;
  new parametric scenes declare 0.8.31. Historical bundles were not relabeled.
- The layout observer was ported against the actual installed CLI bytes. The
  initial port retains single-worker capture/acknowledgement checks and keeps
  observer frame deduplication disabled pending real parity evidence.
- The render watchdog now accepts only the new CLI's `(ready)` completion.
  `(timeout)` and `(script_failure)` cannot become successful renders.
- Live scene cache identity includes selected installed CLI/runtime bytes,
  package/lock metadata and shared inputs. It does not claim full transitive
  npm attestation. Declared runtime mismatches reject instead of reusing cache.
- Docker now includes all five observer modules and installs dependencies with
  lifecycle scripts disabled. Base image, Node, Chromium, FFmpeg, codecs and
  quality settings were not silently upgraded or downgraded.
- Version-bound output allowance is now 2 GiB for 0.8.31 and remains 1 GiB for
  retained 0.7.33 receipts. Unknown versions reject. This fixes the new CLI's
  1024 MiB free-space preflight without disabling it or changing media quality.
- Sealed scene rendering also joins the bundle's declared runtime to the
  approved image version before cache reuse, staging and launch. Old bundles
  remain readable but cannot silently render with a different runtime.
- Both existing Producer command entrypoints expose `propose-text`. This runs
  the installed SDK and existing validated treatment handler against explicit
  package/state hashes. It returns a candidate and invalidation receipt, not a
  saved edit, render, or approval. An actual 0.8.31 six-file bundle proposal
  completed in 0.287 seconds with all original inputs unchanged.
- Pipeline HTML initializes the actual timeline registry before its external
  animation script and verifies a paused, seekable timeline afterward. The
  animation JavaScript, copy and timing were unchanged.
- The root now declares exact `@hyperframes/lint@0.8.31` directly. Future
  catalog refreshes run its official explicit-root API once before native
  preflight. Findings are retained; errors and forbidden child/network attempts
  stop work. The original cohort end is preserved. Static setup/read checks are
  cooperative; the owned Node child has a hard timeout and output cap.
- New Studio ID normalization also declares exact `@hyperframes/parsers@0.8.31`
  directly. npm generated the lock change offline in 0.58 seconds; only the root
  direct declaration changed, with all resolved package/integrity entries and
  installed runtime bytes unchanged. Live and mirror package/lock hashes match.
  The 12 Studio ID tests still pass (1.77 seconds wall). npm retained its existing
  eslint-visitor-keys/Node23 engine warning; no runtime or lint tool was upgraded
  implicitly and no scripts/downloads ran.
- Explicit single-worker scene rendering now stays on the caller thread.
  Typed wall/process deadlines and timeouts propagate out of cache reproof
  instead of quarantining valid media and starting another render. Multiworker
  caller-alarm behavior remains unqualified. Quality and cache keys are unchanged.

## Retained upgrade workspace

`/private/tmp/sniper-hyperframes-upgrade-20260908.mUKXnI`

This contains original root/motion package and lock files, original image
approval and UI review policy, the complete old motion dependency tree,
npm logs, actual new image inspection and probe results, and native attempts.
The old renderer image remains available. No creator project was deleted.

Ten verified obsolete test-preview servers were terminated before moving the
old dependency tree. Their temporary project files were retained. The unrelated
Next development server was left alone.

## Actual image identity

Tag: `sniper-motion:hf-0.8.31-20260908`

Image ID:
`sha256:0e88874d6e93efa3035c4cbebdf6d84bddf5a82ddeb180132808f0a464394046`

CLI SHA-256:
`95be44729e244283cb685833a20b94e00c96aaafba7848b9182db01771477c78`

The published image approval is based on actual Docker inspection plus a
network-disabled, read-only container probe. It is runtime identity approval,
not proof that a composition rendered or that a video looks good.

The probe observed Node 22.20.0, FFmpeg 4.4.2 and Chromium 141.0.7390.37.
All local observer/entrypoint/lock hashes matched the probed image. The exact
probe container's removal was verified. No credentials or source footage were
mounted in that probe.

## Observed checks and failures

These are overlapping focused suites, not an additive unique-test total.

| Check | Observed result |
| --- | --- |
| Initial sandbox npm attempts | DNS denied; failed in 0.69/0.14 seconds; retained |
| Approved root / motion installs | Completed in npm-reported 3 / 8 seconds; lifecycle scripts disabled |
| First isolated Docker build invocation | Failed in 0.01 seconds: existing buildx plugin not located |
| Corrected Docker build | Passed in 29.43 seconds wall; used the already-installed plugin |
| Installed native module load / CLI version | esbuild, Sharp, ONNX runtime loaded; CLI reported 0.8.31; no model inference |
| SDK proposal suite | 14/14 passed, 0 skipped; approximately 1.43 seconds suite |
| Root TypeScript check | Passed |
| Container / graphics closure / render log checks | 43 passed in 2.524 seconds suite |
| Scene runtime / contract / parametric / staging checks | 35 passed in 0.35 seconds wall |
| Installed scene identity helper | Passed in 0.0161 seconds; child processes forbidden |
| Expanded scene checks before catalog refresh | 42 passed, 2 stale-capability errors; not waived |
| First fresh catalog attempt | Failed at first agenda before rendering; 7.541 seconds total; no publication |
| Full root `npm test` attempt | SDK and preceding modules passed; stopped at missing test-fixture `tsconfig.json` in cross-language authority test |
| Quota compatibility checks | 78 focused tests passed; four retained 0.7.33 observer receipts still validate unchanged |
| Combined upgraded Python checks | 145 passed in 3.764 seconds suite |
| Actual SDK proposal command | Passed in 0.287 seconds; proposal only; no input changes |
| Actual CLI root-lint of all 53 compositions | 53 passed in 25.198 seconds; zero errors, 26 warnings across 22 compositions |
| Native observer: agenda / pipeline | 75/75 and 104/104 frames; 10.286 / 9.737 seconds; exact cleanup verified |
| Native observer run including preflight | Passed in 20.652 seconds; held build unchanged; no delivery approval |
| Second fresh catalog attempt | 32 passed, pipeline failed strict lint; 515.671 seconds; no publication |
| Third fresh catalog attempt | 53/53 passed and published; 812.118 seconds this attempt, 1335.330 seconds including both failures, within original 2700-second catalog budget |
| Normal V2 overlay launch | Passed, 75 frames, ProRes 4444 alpha, 11.452 seconds; fresh output, held build unchanged, exact cleanup |
| Scene suites after actual catalog publication | 66 passed in 1.913 seconds; previous stale-capability errors cleared without weakening freshness checks |
| Final current TypeScript check | Passed |
| Official linter one-process parity | All 53 identical root findings; 1.21 seconds wall versus 25.26 seconds CLI-copy preflight; zero native/network attempts |
| Two section-marker renders / six real background composites | Visual presence and full-opacity text contrast checks passed on all six; 45.018 seconds total; partial-opacity warning retained in every case |
| SDK-proposed two-unit scene cold render plus revision | FAILED original 120-second work budget; returned after 129.949 seconds; original two units took 86.899 seconds; no revised package receipt or approval published |
| Integrated official root-lint preflight | 53 passed, zero errors, 26 warnings; 1.411 seconds helper / 1.47 seconds wall; 20 focused tests passed in 3.19 seconds wall |
| Caller-thread / timeout-cache fixes | 58 overlapping focused tests passed in 0.580 seconds suite / 3.71 seconds wall, including actual SIGALRM interruption |
| Normal V2 launch after the deadline/cache changes | Passed, same 75-frame ProRes 4444 alpha request; 14.737 seconds total, unchanged build and exact cleanup |
| New independent warm SDK card revision | Passed within original 90-second work cap: 47.973 seconds total; warm original reproof 2.714 seconds, changed-right revision 44.463 seconds; only right rendered fresh |
| Actual V2 cancellation | Deliberately interrupted at 5-second work cap, returned after 8.301 seconds including cleanup; exact create/start/kill/die/destroy events and independent absence readback retained; no media success |

The catalog attempt is retained under `capabilities-0831`. New CLI environment
checks require at least 1024 MiB free at the output directory. The old 1 GiB
tmpfs contains a log before that check, so it falls just below the minimum.
This is an adapter resource-allowance incompatibility, not evidence that the
user's disk is full. Container cleanup passed. The version-bound quota fix is
implemented and has passed actual native renders; the SDK guard is not disabled.

The second attempt is retained under `capabilities-0831-quota2`. Pipeline's
timeline was registered by an external script, but the upgraded CLI's HTML-only
strict lint could not see that registration. The actual registry initialization
fix passed a retained red/green CLI check and 25 focused tests. All 53 files were
then individually checked as root compositions, since linting a compositions
directory alone skips that particular root-only rule. Warnings were retained,
not reclassified as output approval. The third run uses entirely fresh renders;
the 32 partial successes were not recycled as a completed current cohort.

Observer evidence: `observer-0831-5inqa0ia/result.json` in the upgrade workspace.
Returned media/sidecar hashes were read back against held image/source identities
and the original deadline. This verifies encoded-frame observation for the two
native caption-safe profiles, not final composited caption legibility.

The completed catalog cohort is retained under `capabilities-0831-root-lint`.
All 53 rows are genuine fresh 30 fps default/probe-spec measurements. Publication
requires unchanged sources, complete inventory, valid measured rows, exact
readback and cleanup. Published artifact SHA-256:
`5234501a7eddc4155463e23d76f6e0507256ded09725278978e86b7485bdac71`.
This is not a released-rate matrix or arbitrary-input visual approval.

Normal V2 launch evidence: `/private/tmp/sniper-render-build-v2-live-wabjaevg`.
The output remains 1080x1920, 75 frames, ProRes 4444 `yuva444p12le`, with full
decode proof and original high-quality render settings. Nothing was promoted
to a creator's final master.

Composite evidence: `/private/tmp/sniper-section-plates-live-yd3_h9hw`.
Two explicit title treatments were rendered once each and composited over black,
navy and white. Every full-opacity title/eyebrow/qualifier contrast check passed;
each case retains an unscored partial-opacity animation warning. Root inspected
actual frame-37 top-region pixels: both complete titles and the wrapped long
qualifier were readable and unclipped. The useful contact image is
`section-plates-top-composite-frame37.png` in the upgrade workspace. The earlier
middle-region crop missed the title region and is not QC evidence.

All six overall audits correctly remain FAIL: these deliberately tiny silent
fixtures have no timeline map, audio or cover and use fixture-only baseline /
non-faststart encoding. Do not turn their visual-screening PASS into delivery,
audio, arbitrary-footage or complete-master approval. Nothing was waived or
silently modified to clear those delivery failures.

The actual SDK proposal was also exercised through the ordinary scene-package
render command in a private fixture. Evidence is under
`scene-revision-0831-8yi41kjp` in the upgrade workspace. Three exact runtime
receipts exist (original left/right, revised right), all with verified container
removal. The final exact container was independently observed absent. A
refreshed left proof sidecar is normal cache reproof, not evidence of rerendering
that left unit. The original two-unit package took 86.899 seconds; the aggregate
request exceeded its original 120-second work cap and returned at 129.949 seconds.
It remains FAILED. Completed private media cannot retroactively clear that
budget failure or become delivery approval. A 0.287-second SDK proposal is not
an end-to-end scene-render latency claim.

The deadline diagnosis reproduced two existing issues. Explicit `workers=1`
entered a thread pool; exiting that pool could wait for running work after the
caller alarm. Separately, a generic cache-error catch could treat deadline
expiry as corrupt media and start a cache miss. Both narrow fixes are now
implemented and independently reviewed. A real SIGALRM leaf test proves
interruption on the serial caller path; no equivalent claim is made for
`workers>1`. Do not describe all past overshoot as mandatory cleanup.

A new warm revision test copied only the original, proved units into a new
private cache and retained the previous failed attempt unchanged. The installed
SDK proposed different right-card copy; the ordinary package command re-proved
cached media and rendered only the new right unit. Evidence is under
`warm-scene-revision-0831-i159iecz` in the upgrade workspace. All 180 frames per
unit decoded; original left bytes matched exactly; high-quality 1920x1080
ProRes 4444 alpha settings stayed unchanged. Root inspected actual frame90:
"Change one card. Keep the rest." is complete and unclipped. This is not all-
frame arbitrary-copy legibility, a cold-generation pass, or creator delivery.
The first warm invocation stopped in 0.303 seconds because the sanitized PATH
omitted existing Node; that failed attempt is retained separately.

Actual V2 cancellation evidence is `/private/tmp/sniper-v2-cancellation-gxk02jhd`.
The real container started, then was killed/destroyed after the caller deadline.
The normal resource ledger reports its sole exact name REMOVED and no active
names; a separate inspect observed it absent. The complete daemon lifecycle
records are retained in `independent-events-readback-v2.json`; the first compact
formatter failed and its failure remains retained. This tests actual V2 caller
cancellation, not the scene dispatcher itself. The 8.301-second return includes
cleanup and is not described as a five-second total-return guarantee.

The full TypeScript regression run exposed older harness drift: missing fixture
config inputs, an obsolete expectation that a blocked paid-provider stage would
launch, a macOS temporary-path alias, and four implemented routes absent from
the route matrix. These were corrected without enabling provider calls or
claiming the routes are delivery-qualified. The 10,011-value cross-language hash
test hung once; bounded reproductions did not establish a production hash bug.
Its test child now has a 30-second hard deadline and a 10-second diagnostic stack
dump. All 10,011 cases, type-check and scoped lint pass. A separate diagnostic API
test passed all six cases unchanged once allowed to observe its own OS process
identity. A full rerun is still in progress; no full-suite pass is claimed yet.

One full-suite invocation also omitted the installed FFmpeg directory from its
sanitized PATH and stopped at the audio fixture. The failed log is retained as
`npm-test-final-upgrade.log`; the corrected rerun includes that existing binary.

That corrected run reached TS file269 of445, then exposed a getter-only TEST
dependency overwritten by `Object.assign`. The fixture now preserves the actual
getter and only replaces/restores existing test leaves: 9/9 passed in 14.17
seconds wall, with type-check and lint passing. The exact remaining176-file
suffix was run without repeating the already-recorded prefix. It stopped at a
second fixture drift in `guided-opening.test.ts`: the explicitly V4 historical
test was using the now-V5 default proposal factory. Diagnosis/repair remains
separate from historical production-reader behavior. Logs retain both stops;
there is still no uninterrupted full-suite pass claim.

That V4 test now explicitly constructs V4 before compilation, keeps historical
V4 and no-current-authority assertions unchanged, and passes (22.86 seconds
wall). A subsequent music-intent TEST fixture lacked the original manifest,
candidate and closed proposal required by the current authority reader; supplying
that real-shaped test context restored the intended stale-music refusal, with
3/3 passing in0.26 seconds wall. Neither fix changes production validation.
The remaining suffix now collects all failures in one run; no failure is waived.

That suffix also exposed real cold-import cycles in source-color cleanup. An
independent scratch proposal delays existing dependency-object reads without
changing their authority or identity;13 fresh-process imports pass in the
overlay. Live integration and the complete affected regression cohort remain
pending while the suffix's production sources are held stable.

Full ESLint now exits0 with0errors and41warnings. Two headless `.js` workers are
deliberately CommonJS source injected into Docker's `node -e`; they are included
by exact path in the existing CommonJS exception for `no-require-imports`.
No other rule is disabled. The warnings remain recorded, not called clean.

The isolated Next build compiled in16.1seconds, then its generated route-export
type gate rejected custom runtime helper exports. An installed-TypeScript AST
inventory found11 forbidden exports across4 of67 routes. Minimal helper moves
are being checked in the isolated build copy; Next's validation stays enabled.
The first build attempt failed free Google Fonts DNS resolution; the approved
network retry reached this actual type error. Neither attempt is a build pass.

The four-route repair then passed in the isolated copy:29 focused tests in
3.27seconds, scoped lint in2.24seconds and TypeScript in9.33seconds. All29
original route function bodies (including moved helpers) were byte-identical
under an AST comparison. The subsequent full Next build exited0, including
generated route types, static generation and traces; compilation took13.1seconds
(total build wall time was not separately recorded). Live route integration is
still pending the original regression source freeze. A later additional cleanup
resource getter is not covered by this particular build.

The final isolated build, including that resource getter, subsequently passed
in 29.71 seconds wall (14.5 seconds compilation, 24/24 static pages). All 16
route-repair and five cleanup-repair source hashes matched before and after.
The credential-free mirror used existing installs and cached fonts; no network
escalation, provider call or live build-cache write was needed for this run.
Evidence: `npm-build-isolated-final-resource-getter.log` in the upgrade workspace.
Live integration is still pending the original regression source freeze.

A supervised isolated production start was ready in542ms. Read-only HTTP smoke
checks returned200 for `/producer` (114.7ms) and `/api/producer/comps` (27.0ms,
all53 entries). This is server/page/catalog loading, not interactive editing,
Studio byte-patch compatibility or media QC. The temporary3359 server was stopped
and the port independently checked absent. Sandbox-denied startup/fetch attempts
are retained in tool output; approved loopback checks succeeded. No user-project
write or model/provider call occurred.

The isolated cleanup repair subsequently passed333 tests over22 files
(681.556seconds wall), then118 tests in its final affected delta
(202.977seconds wall). These cohorts overlap; their counts are not additive.
All44 relevant production module entry orders passed cold-import checks,
including the14 permanent identity checks. Three first-attempt broader-sweep
failures were the test fence blocking the installed TypeScript compiler's Worker;
warming only that compiler, without pre-importing production modules, fixed the
harness and those three passed. Final TypeScript, lint and mechanical checks
passed. This is still an isolated candidate pending live integration.

Two more Python TEST fixes are ready in the isolated copy. The historical
treatment test now asserts the exact intentional changes (two full-canvas
defaults and pipeline's3.45second completion floor) before comparing a detached
old view against unchanged historical hashes;10 body/caption tests pass in
2.68seconds wall. A preparation-readback fixture now supplies its actual picture
result and the same empty graphic list used by its real compositor, keeping the
unexpected-caption rejection. Root reproduced the missing-picture error in
22.522seconds; the first fixture correction exposed missing graphics and remains
a retained failure. Final8 actual small-media tests pass in24.89seconds wall,
with independent semantic/mechanical review. No production caption check changed.

Root lock generation for the direct linter dependency first failed offline due
to uncached optional Tailwind WASM bundle metadata. Network lock-only resolution
then completed in 0.928 seconds with lifecycle scripts disabled. Besides the
direct declaration it recorded bundled optional dependency metadata; the
compared dependency versions did not change. npm retained its existing Node23
engine warning for `eslint-visitor-keys`; scoped lint and TypeScript passed.

## Actual visual comparison so far

Seven matched old/new native outputs retained the same requests, frame counts,
geometry, codec and pixel format. Combined wall time changed from 129.650 to
109.353 seconds (15.7% lower), but host load was uncontrolled and the output
quota changed. This is an observation, not a controlled performance guarantee.

At staircase frame 54 (1.8 seconds), the new output restores the intended white
kicker, "Step #1: do this every day", above the unchanged main copy. Both root
and an independent reviewer inspected the actual old/new pixels and checked
the unchanged source intent. That is a specific content improvement, not proof
of universal quality improvement, all-frame correctness or caption clearance.
The comparison image is retained as `staircase-frame54-old-top-new-bottom.png`.

## Remaining qualification and integration

1. Inspect actual output frames and relevant composited caption/layout cases.
   Structural checks alone cannot establish visual or auditory quality.
2. Finish the full regression suite. The previously stale scene tests now pass
   against actual newly measured catalog evidence.
3. The official one-process root-lint preflight is integrated and tested. Its
   direct validator/parser files and runtime are bound, but it does not attest
   every transitive installed JavaScript dependency. Do not expand this static
   check into render or supply-chain approval.
4. The new SDK command is proposal-only; apply its returned operation through
   existing user-authorized publication/review flow. Do not call it unrestricted
   one-shot live editing or invent current project authority from caller state.

The optional custom UI's old Studio bundle hashes/patches were intentionally not
blindly refreshed. Do not claim that shell is 0.8.31-compatible without testing.

The wider goal still includes intro/body pacing, scoped revisions, audio/color
finishing and real short/long master QC. The C0679 wide-gamut case remains a
special source qualification problem; it is not a prerequisite for every
ordinary admitted Rec.709 edit. Neither a complete 10-minute master nor the
end-to-end two-hour target has been demonstrated by this upgrade.

Claude's separate audio snapshot and baseline commit were verified read-only.
The checkpoint is not yet an integrated completion package. An independent
six-stream FFmpeg test found proposed cleanup latency compensation erased the
final1200/480 samples (25/10ms); pre-chain padding preserves the original tail.
The exact review and requested boundary regressions are retained at
`/private/tmp/sniper-audio-codex-review-20260908.md`. No live audio refusal was
removed, and no human listening or quality acceptance is inferred. The relay is
file-based; updating the handoff alone does not prove Claude has reread it.
Claude's subsequent checkpoint explicitly acknowledges the reproduced issue,
reports a pre-chain padding correction and retains final-impulse/speech-like-
tail tests. Independent verification of the corrected code passed all six
one-second impulse/speech-like tail cases, with exact 48,000-sample stereo
outputs and zero array error against a separately long-padded oracle. These
12 serial FFmpeg calls took 1.35 seconds wall; code/tool/model identities stayed
unchanged. Evidence: `/private/tmp/sniper-audio-current-qc-AeiS7w`. This is
synthetic signal verification, not listening or creator-footage approval.

The later audio checkpoint reports corrections for cancellation swallowed as a
cache miss, missing independently held master selection, and standalone output
that could omit requested finishing. It also explicitly documents old master
receipt invalidation. Fresh patches were frozen at 2026-09-09T01:45:09Z against
the isolated baseline; their hashes match the report. The owner's fresh cohort
ran 455 tests over 60 files, with 57 files passing and three known baseline
failures retained. Those three are the catalog/historical-treatment and actual
picture/graphics fixture cases independently repaired in the current SDK
mirror; combined live audio integration has not yet tested that join.

Independent review also reproduced two selection races: one could select graph
A while retaining graph B's files, and a late pointer hash could replace the
original input hold (the final publication check still rejected that mismatch).
Both fixes pass six fresh regressions in the frozen package. One remaining
narrow issue is that its raw ACTIVE re-read accepts an altered schema version
or extra fields when generation hashes match, unlike the ordinary closed V1
reader. A separate corrective patch was requested; exact metadata-only evidence
is `/private/tmp/sniper-audio-frozen-review-l6qlsI`. No audio patch has been
applied to the live editor, and all original failures remain retained.

The separate ACTIVE-shape correction now reuses the ordinary store's exact
closed-pointer validator. Independent replays of both residual cases reject;
15 pure regressions pass with zero native calls and unchanged source hashes.
Evidence: `/private/tmp/sniper-audio-active-correction-review-PduapG`.

Root integrated the frozen audio package plus that correction into the current
SDK/build mirror only. Combined body/caption/audio metadata tests passed 41/41
in 2.72 seconds wall. The first run retained four failures because the mirror
had omitted local `.wav` assets; the six existing SFX were copied byte-for-byte
and hash-verified, not downloaded or rebuilt. Three actual synthetic media
suites then passed 26/26 in 91.08 seconds wall, including the corrected
preparation fixture, cleanup tails, gain, SFX, ordinary render/assemble and
failure preservation. All 29 packaged/reconciled source hashes match the
frozen and corrective manifests. This is not guided TS activation, listening,
whole-creator-video quality or a two-hour benchmark. Logs are
`audio-sdk-mirror-pure-integration.log` and
`audio-sdk-mirror-media-integration.log` in the upgrade workspace.

Guided finishing has a concrete remaining connection gap: current TS media and
presenter profile gates still refuse enhancement/gain. The unavailable-only
historical V1 preparation blocker must stay unchanged; it is not the execution
gate. Finishing needs an explicit compatible current profile path, with original
opening/body settings joined and existing caption/framing restrictions retained.
The companion audio-revision dependency patch is separate and under review;
its owner reports coherent invalidation, not measured wall-time savings.
