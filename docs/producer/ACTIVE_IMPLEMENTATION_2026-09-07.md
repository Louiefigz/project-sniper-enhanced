# Resumed implementation — 2026-09-07

## Latest operator direction: fresh native HyperFrames authoring

Aaron's further September9 correction explicitly supersedes the earlier
integration-preserving suggestion: use the out-of-the-box HyperFrames SDK and
full catalog, with fresh visual judgment from the narration. Nateherk/Jaden are
case studies only. Do not inherit the rejected visual plan or force this native
workflow through the old Sniper graphics-only/mandatory-port boundaries. Keep
source/cut decisions and verified audio; build a new presenter-led composition.
The production task received this instruction directly. Read
[the native direction and full-catalog audit](C0679_NATIVE_HYPERFRAMES_DIRECTION_2026-09-09.md)
for source-inspected candidates, numeric speech anchors and exact audit limits.

### Native edit checkpoint — September 9, 21:25 UTC

Corrected MP4 V2 uninterrupted playback passed: exact683.766417s end,
16393 frame callbacks, zero observed dropped frames, maximum callback gap83.5ms,
no disruptive events,1370 samples. Guard694.551128s; actual ended event683.8465s
after observation start. Cleanup/lease/source/runtime checks passed. The combined
browser review still fails ONLY its previously characterized strict color gate;
do not turn that overall result into an unqualified pass. No audible listening
or human approval was performed by the muted headless browser.

Current Studio's separate65s observation FAILED in74.327707s guarded time.
Opening and35–65s timing pass (maximum A/V difference43.629ms), but narration
emitted668 seeking and668 waiting events in the active window. Both CDN attempts
for MotionPathPlugin were blocked and yielded an unhandled rejection. These are
real retained preview limitations, not proof of a usable uninterrupted soundtrack.
Owned browser cleanup passed; existing Studio41058 and user apps were untouched.

Attempt A's durable8614-file package is preserved unchanged. Its corrected MP4
is objectively qualified; Studio playback and human listening remain qualified
limitations, and saved-path actual re-export is untested. External handoff state
must record these distinctions before the fresh-input Attempt B clock starts.
Do not spend the next trial on unrelated cache/re-export work or copy A decisions.
The broad goal is not complete and the original two-hour target was missed.

### Historical native edit checkpoint — September 9, 21:01 UTC

V10 file-level objective qualification remains passed and its bytes unchanged.
The same-Chrome native-image comparison has a retained strict numerical miss at
frame653:39.4668dB versus40dB, with MAE1.8797. Controlled display of native JPEGs
and canonical FFmpeg RGB images through that same Chrome is pixel-identical to
the source images. Chrome VIDEO versus canonical image differs by about one RGB
level on average (42.25–44.27dB whole-frame), isolating the residual to the video
decode/conversion path without identifying a particular implementation defect.
Independent still review at383/653 found no obvious material visual/legibility
issue. No further encode, grade or matrix change is justified; do not call the
original strict browser color gate passed or all visual tests passed.

Full playback V1 reached the exact end with16393 frame callbacks, no disruptive
events and83.8ms maximum callback gap, but its recorder lost non-own WebIDL
quality fields during JSON serialization. Its695.017459s guarded run remains
failed/incomplete, not evidence of zero dropped frames. V2 is now running with
only explicit scalar-counter serialization changed; the video, limits and
original recorder/evidence are unchanged. A prototype-getter regression passes.
The80 focused Python tests and10 prior JS playback contract cases also pass.

Durable package integration and verified warm-cache repeated-export admission
are in progress. The current package's conservative soundtrack qualification
allows CSS/asset-only revisions; HTML/JS/JSON changes require requalification.
Do not claim arbitrary Studio edits automatically preserve audio authority. A
targeted human listening question about35–65seconds/47.7seconds is pending;
other implementation continues independently. The full goal is not complete.

### Historical native edit checkpoint — September 9, 20:31 UTC

The new `full-native-high-v10-lossless-av-color` derivative passed complete
objective QC and independent supplemental provenance/packet checks. Its SHA256
is `eef61c5a2a410d869c79c13c6fb9ba69f52690377d0a99c3fbd53db3ffa50181`.
All16394 decoded YUV frames and coded video payloads are unchanged; all32053 AAC
payloads/configuration/priming/clocks match the already-qualified donor. Audio is
exactly32820788 presented samples with460 decoded padding, −14.61LUFS/−1.86dBTP.
Only the video transfer description changed: SPS1→13 and matching MP4 nclx.
Actual-declared color checkpoints reproduce PSNR42.1–44.3dB vs native captures.

Assembly took1.434203s; all-frame preservation proof143.829307s; whole-output
QC36.454335s. Combined guarded operation194.420299s; supplemental guarded proof
18.886833s. Sixteen offline contract tests passed. Independent code/evidence
review found no remaining concrete blocker after two verification gaps were
closed without modifying the original V10 files. Original V8 failed QC stays
failed. Corrected-output browser playback and perceptual review, durable package
and an actual scoped re-export still remain; the whole goal is not complete.

### Historical native edit checkpoint — September 9, 20:21 UTC

The full native render passed; its actual output QC did not. Native AAC was
20 samples short and exceeded the true-peak ceiling. Color-managed comparison
also isolated a transfer-tag mismatch: three exact MP4 frames match the native
sRGB captures at PSNR42.1–44.3dB when interpreted as sRGB with the unchanged
BT709 matrix. The original BT709 interpretation scores28.5–33.7dB. Source PNG
to native JPEG conversion itself is correct after respecting their different
transfer metadata; raw RGB comparisons were not valid evidence of exposure loss.

A guarded new derivative is prepared: copy native coded picture and the existing
qualified AAC without re-encoding, change only video transfer metadata to sRGB,
and use exact MP4 timescales. Full packet/YUV/SPS/container/audio/color checks are
mandatory. Its first attempt was refused before child launch because compressor
growth exceeded the existing16MiB admission limit (6.752882s); nothing was rendered
or repaired. Eight offline contract tests passed. Original failed QC stays failed;
no final output, perceptual approval, reusable-package completion or two-hour
end-to-end claim is made. No user apps were closed and no cap was relaxed.

### Historical native edit checkpoint — September 9, 18:49 UTC

The complete `full-native-high-v8-url` render and guarded cleanup PASSED at
18:49:13.838547UTC:16394frames,870320056-byte output,1541.35745075s guarded elapsed.
All16394native source URL requests completed with zero errors/fallbacks. Peak
owned footprint2.21447849GiB across221actual samples; normal kernel pressure
throughout, no observed swap growth. Recorded process cleanup, independent heavy
lease release and all source/SDK/dependency pins passed. Whole-file audiovisual
QC is now the sole running media job. No final output-quality, human listening,
editable-package or two-hour edit completion claim is made. Full evidence is in
`evidence/C0679_NATIVE_PRESENTER_QC_2026-09-09.md`.

### Historical native edit checkpoint — September 9, 18:27 UTC

The complete native export is ACTUALLY RUNNING as `full-native-high-v8-url`,
PID83161, native launch18:23:39.131UTC. Source preparation reused all16394
retained full-cache frames in58ms. Owned footprint is holding around2GiB and
kernel pressure is normal; no final success or completed-output QC is claimed.
Strict native rendering now explicitly selects `--composition index.html`,
preserving test HTML/evidence without allowing duplicate root discovery.

The new full-only cache-aware admission was independently reviewed and tested.
Literal unused RAM is advisory, not an arbitrary progressively lowered launch
threshold. Three fresh distinct observations must satisfy normal kernel pressure,
>=50% coarse headroom, >=8GiB single file-backed VM category (not a guaranteed
reclaimable-byte claim and never summed with overlapping counters), compressor
<=10%physical with both consecutive/whole-window16MiB growth limits, non-growing
swap,10GiBdisk and fresh timing. The final actual snapshot is the growth baseline.
The heavy lease remains held throughout and runtime6GiBtree/5GiBprocess/1GiBnew-swap
stops plus pressure/disk/deadline/cleanup checks are unchanged. Default admission
outside this explicit qualified path is unchanged. No apps were closed.

V5/V6 were no-child admission refusals (1.543474s/1.533419s). V7 passed actual
cache-aware admission but strict lint refused retained duplicate test roots
(8.885877s; no frames; cleanup/lease/pins passed). None were relabelled successful.
Large source hashes now use documented descriptor-scoped F_NOCACHE reads with
the same SHA256, no global purge/source mutation or claimed cache reclamation.
Whole-output guarded QC is prepared; durable re-export packaging is prepared
and still unreleased. The original end-to-end two-hour target remains missed.

### Historical native edit checkpoint — September 9, 17:57 UTC

The uninterrupted4320-frame URL-transport canary PASSED in428.996674s. All4320
unique source registrations/requests completed with no errors or fallback;
cleanup, lease and file pins verified. Final covered1000-frame window medians
were2086.485/2084.485MiB (−2MiB), with13aligned observations per window and
−0.01560MiB/frame fitted slope. All55paired JPEGs and decoded pixels remain exact.
Selected agent visual checks passed, explicitly not human or whole-video approval.
The reviewed full-native export command is prepared with identical quality and
the same6GiB tree limit. Full launch is deferred by fresh host admission:
~2.4GiB unused physical RAM is below the6GiB reserve despite normal pressure.
The user has been asked to close memory-heavy apps; no user apps are closed
automatically. Durable guarded re-export packaging continues offline. No final
full output exists yet and the two-hour end-to-end target remains missed.

### Historical native edit checkpoint — September 9, 17:48 UTC

The 55-frame stock-native sparse JPEG95 baseline completed in60.362954s and
its matching URL-transport candidate in59.994337s, both with verified cleanup and
lease release. Paired comparison completed in3.653518s: ALL55native JPEG files
and decoded RGB pixels are exactly identical. This is dense-opening parity,
not continuous memory or whole-output qualification.
The sampler now reconciles identities across ps/top/ps, preserves newly observed
orphans for cleanup, and records exact missing-identity evidence. Two rapid-batch
reference failures remain failed (11.451283s and12.977903s); explicit nonce-bound
recovery verified all recorded identities absent. Sparse reference batches now
wait for a valid stable-phase host measurement before capture and before browser
turnover; this handshake is not used in continuous render memory qualification.
The 4320-frame uninterrupted canary remains gated on paired visual/pixel parity.
Its fixed numerical, coverage and teardown-exclusion rules are recorded before
launch; no cap or image-quality relaxation. Full output is still unfinished.

### Historical native edit checkpoint — September 9, 17:09 UTC

Stock800-frame baseline safely hit the unchanged6GiB guard at648frames; tracked
cleanup and lease release passed. No cap relaxation or identical retry. Tiny
candidate parity remains proven; dense reference sampling needs bounded fresh
native capture batches. The parallel task now has the sole short playback-probe
slot; no render/QC overlap is authorized. Full export/memory/audio qualification
remain open.

### Historical native edit checkpoint — September 9, 17:07 UTC

The isolated frame-URL candidate passed three actual native decode-failure
canaries and exact decoded RGB comparison against the unchanged SDK for all48
frames of a two-second sample. A process-exit supervision race was separately
fixed/tested; its failed original receipt remains failed with archived explicit
cleanup recovery. The800-frame opening baseline is running; dense-opening parity
and4,320-unique-frame memory qualification are next. Installed SDK/full edit are
unchanged. Shared heavy-job lease and preview lifecycle now prevent silent job
overlap and retain cleanup obligations;14 obsolete preview servers were cleaned
with recorded descendant verification and active preview preservation. No full
retry or delivery is qualified. See
[native presenter evidence](evidence/C0679_NATIVE_PRESENTER_QC_2026-09-09.md).

### Historical native edit checkpoint — September 9, 16:25 UTC

V4 failed after 738.891335s at ~3,949 frames; its matching Chromium renderer
crashed with SIGTRAP before the 60s native stall timeout. Owned footprint reached
15.324 GiB despite the lower compositor allowance. No watchdog abort or proven
heap diagnosis is claimed. All recorded render identities are absent; no full
output exists. No further full retry until a materially different candidate is
reviewed and passes bounded memory/pixel-parity testing. Root owns the isolated
frame-URL transport candidate and bounded supervisor; the parallel audit task
owns shared heavy-job lease and preview lifecycle enforcement. Installed SDK and
full native edit remain unchanged. Audio, full-output QC, reproducible handoff
and the already-missed two-hour target remain open. See
[native presenter evidence](evidence/C0679_NATIVE_PRESENTER_QC_2026-09-09.md).

### Historical native edit checkpoint — September 9, 16:05 UTC

V3 safely stopped on an overbroad literal-unused RAM floor; no second OOM is
claimed. V4 now runs with a tested cache-aware kernel-pressure guard and an
explicit exec-only adapter reducing the same Chrome's compositor cache allowance
from 16384 to 1024 MiB. Native source/SDK, high quality and rational fps remain
unchanged. Fifty-nine distinct focused safety cases have passed; actual whole
output, quality parity, preview/audio and full-run memory still need qualification.
See [native presenter evidence](evidence/C0679_NATIVE_PRESENTER_QC_2026-09-09.md)
and [the full optimization audit](C0679_END_TO_END_OPTIMIZATION_AUDIT_2026-09-09.md).

### Historical native edit checkpoint — September 9, 15:48 UTC

The unchanged complete native edit is now running as `full-native-high-v3-bounded`
with one stock low-memory/software worker, the same high quality/frame rate, and
a host-side footprint/headroom watchdog. Thirty-five focused safety tests pass,
including actual detached-process cancellation; this is not full-video or memory
qualification. Failed V2 and the user's manual app-closure intervention remain
recorded. Full output and continuous preview/audio QC are still open. See
[native presenter evidence](evidence/C0679_NATIVE_PRESENTER_QC_2026-09-09.md).

### Historical native edit checkpoint — September 9, 15:30 UTC

The full native stream was stopped after the user's out-of-application-memory
report: four owned Chromium renderers each held ~14 GiB footprint/compressed
memory. The failed attempt lasted 1493.032 seconds and did not produce a final
delivery. All verified owned heavy children have exited; the source and partial
work are preserved. A stock single-worker low-memory candidate and host-side
resource watchdog are being prepared before retrying. Full output, audio/preview
QC and the already-missed two-hour end-to-end target remain unresolved. See
[native presenter evidence](evidence/C0679_NATIVE_PRESENTER_QC_2026-09-09.md).

### Historical native edit checkpoint — September 9, 15:00 UTC

The complete 683.7664166666667s native edit is integrated. Independent full-frame
tail review and 24 native forward/reverse checks passed after localized crop and
ending corrections. The first full render failed the SDK's disk-capture storage
gate after completing source preparation; the supported native parallel-streaming
retry is running, reusing all 16,394 PNG source frames at unchanged high quality.
Actual final output QC and preview audio continuity remain open. The user's
audio-consistency complaint is not dismissed as lip-sync drift. See the current
[native presenter evidence](evidence/C0679_NATIVE_PRESENTER_QC_2026-09-09.md).
No completion, listening approval, or two-hour timing success is implied.

### Historical native edit checkpoint — September 9, 14:18 UTC

The deliverable remains the whole11m24s video. Aaron accepted the new native
presenter-led opening direction and requested smaller padded, rounded rectangular
presenter windows. That scoped revision is implemented and its actual exported
pixels have been inspected. The current native project on port41058 now extends
to75.408667seconds;32–75s visual/seek QC is running, and75–194s is being authored
in independent extension files. This is not full-video completion or a new
per-section approval gate. See
[native presenter evidence](evidence/C0679_NATIVE_PRESENTER_QC_2026-09-09.md).
The second32s high-quality export took98.082seconds with a5ms source-frame cache
hit. Its full decode passed and AAC measured−14.46LUFS/−2.02dBTP. Native preview
playback still fails the unchanged150ms A/V threshold. Do not confuse successful
rendering or static review with continuous playback qualification. Installed SDK
remains0.8.31. Old V5 full-playback testing has now failed, not remained in progress.

## September 9 operator rejection of the current visual direction

Aaron reviewed Studio V3 and explicitly rejected its amateurish, all-or-nothing
card treatment and return to the failed Nateherk style. He expects use of the
HyperFrames catalog, zooms/pull-outs, transitions, and graphics alongside his
visible face, including roughly 2/3 or 3/4 teaching content on the left with the
presenter in the remaining right portion. These are existing-work corrections,
not a request for another requirements or permission round.

Read-only inspection of the actual `edit_plan.v3.json` in
`/private/tmp/sniper-c0679-produced-review-20260909.FCYcRv` confirms all57 graphic
entries use `anchor: own-screen`;12 are `nateherk-pipeline` and2 are
`nateherk-ledger-dark`. The plan has no camera-motion or scene-transition track.
Its explicit `cutaway-only` rationale substitutes full-screen diagrams because
the Studio view lacks a verified presenter compositor. Fourteen template specs
explicitly disable `presenterFrame`. Graphic windows occupy48.45s of the first
minute; the credibility card uses an empty avatar source and AF initials.
Actual retained credibility/pipeline frames were inspected for this status audit.

This is rejected creative direction, irrespective of card visibility, playback,
audio or timing test results. The user's feedback must govern later revisions,
including drafts that retain the same takeover layout. Demonstrate presenter
plus useful catalog graphics and intentional camera/layout transitions in a
short actual opening revision before extending that direction across the video.
Do not equate a named catalog item or a passing technical test with good editing.
No existing draft, source, cut approval or media was modified by this audit.

Latest September9 full-real-footage checkpoint:
[C0679 full editable draft and actual QC failures](evidence/C0679_FULL_DRAFT_QC_2026-09-09.md).
The 11m24s base has qualified mastered audio (−14.61LUFS/−1.86dBTP), unchanged
picture packets and57editable graphics. Fresh V5 native graphics/114boundaries
pass with scoped per-card color and explicit legacy-safe mount contracts.
Tightened paired-preview controls pass in both DOM realms; the uninterrupted
full-length V5 run is in progress. The cold seek-reset was traced to test scripts
bypassing Studio's existing disabled-controls guard, not a proven UI defect.
The development run exceeded the two-hour target; it is not a timing success.
No final burned edit, human listening, delivery approval or overall completion
is implied. Earlier checkpoints
below remain historical evidence; no approval or human listening is inferred.

Latest September8 upgrade checkpoint:
[HyperFrames 0.8.31 implementation and evidence](HYPERFRAMES_0_8_31_UPGRADE_2026-09-08.md).
The scheduled usage-reset task is paused. SDK/CLI 0.8.31 is installed; all53
default native catalog probes are published, and a real cached SDK card revision
passed in47.973 seconds without changing quality settings. All445 original TS
file paths were reached across retained split runs; five cases remain skipped.
Reviewed route/cleanup fixes and the corrected shared audio-finishing package
are now integrated live, with exact tested hashes,32 live cleanup checks,
43 live Python metadata checks, TypeScript and default lint passing. The separate
audio dependency follow-up is also integrated:12 actual graph/revision tests
passed in135.37seconds wall, followed by20 live graph/registry tests in9.64seconds.
Audio-only revisions preserve exact picture packets and skip graphics work;
this is not a measured whole-video speedup. Guided finishing activation and
real-video QC remain unfinished. The seven final live ordinary audio render/assembly tests also
passed in37.75seconds wall; these four-second synthetic cases are not guided
activation, creator listening approval or a full-video timing result. The app goal now
reports `usageLimited`, not complete; no reset was consumed. This checkpoint
supersedes older current-version/readiness claims below, not historical evidence.

The four-file source-color staging overhead repair is now integrated too:
36independent root batch tests PASS44.00seconds,17existing live staging tests
PASS17.96seconds and final TypeScript PASS7.71seconds. The agent's158-case union
overlaps these checks. Prospective metadata checks are linear (8Ndirect/12Nwith
owner hooks), with all original controls retained; failed-batch cancellation
remains fail-closed, not a claimed complete cleanup. Details/limits are in the
upgrade record. This does not activate native source-color work.

September9 follow-up: the guided inherited-audio candidate and separate current
screened-body/parser + shared-source-pin corrections are in the integration
mirror only (86 distinct TS/47Python checks and type-check passing). The native
entry failed in 23.63 seconds: previsual correctly rejects downstream audio
settings. No opening/master/body render started. Post-cut audio authoring needs
the actual request-derived treatment path (V10; V9 is reserved for captions),
not a weakened cut gate or ordinary saved-plan substitution.
Actual upgraded Studio lifecycle is live across four files (41 final live tests
PASS3.39seconds). SDK-based ID normalization is now live across nine more files:
67 focused live tests PASS7.67seconds, and a 25.067-second actual Studio probe
kept all12 tracked hashes unchanged with clean dry sync. Off-window icon
visibility still fails and remains unqualified; the exact upstream visibility
predicate skips same-document hosts with a foreign HTMLElement realm.
Catalog discovery and adversarial ranking/shape corrections are live across
seven code/test files plus three command/skill docs (29 tests PASS4.07seconds).
Free font fetching in upstream preview was observed;
subsequent native tests use a verified test-only localhost sandbox. No paid/model
generation or old-view overwrite was authorized. Detailed evidence is linked in
the upgrade record. Do not infer readiness from process exit0 or parser success.

Ordinary caption-shard cache staging is also live in four files, independently
reviewed and identical to the tested mirror. The final live 33 focused checks
passed in 6.28 seconds wall; nine native caption/held-preparation checks passed
in 87.69 seconds. One unchanged shard reused, one changed shard rendered; cached
and uncached video/audio packets are identical and both full-output audits pass.
The four-second live cached revision was 0.030 seconds slower than its control:
this proves removed redundant rendering, not a demonstrated total speedup.
The two-file Studio guard now rejects inherited nonempty runtime URL overrides.
The seven-file Studio compatibility/advisory follow-up is now live; all121
tests pass in13.70seconds wall, including local process/socket/tiny-FFmpeg cases
previously omitted from the agent's constrained90-check run. V1 history and
unmatched-default/pending-file refusals remain intact. The separate SDK runtime
visibility candidate is still not deployed. V9 selected-caption integration remains mirror-only
and its service/current-history/readback join is now tested in a private candidate.
Root visual QC found future-word rather than current-word karaoke highlighting
despite green selection tests. The five-file compiled-word correction is now
live: all41 focused tests passed independently (0.81seconds wall live), and the
separate corrected landscape/portrait selection2 passed43.91seconds. Root viewed
current-word/gap/next-word rendered frames. Line/phrase bytes stay unchanged;
legacy and phrase semantics are not newly qualified. Old-code cue caches invalidate.
The two V9 selection-test updates are mirror-only; root independently passed
both actual renders/revisions in43.90seconds. Public four-field V9 readiness/input
now passes25.48seconds in the frozen private candidate after three retained
failed attempts (runtime configuration, real geometry dispatch, new TEST oracle).
This proves the input handoff, not a caption/opening/body render. Geometry and
TEST corrections are in the mirror. Internal V10 post-cut audio authoring is
also in the mirror. The additive TS readiness16-file/Python readback7-file
integration passes136 root TS checks with1 deliberate native skip (1.74seconds),
whole TypeScript7.43seconds, and62 Python checks0.79seconds. Six actual-file
14-document cases include exact audio presence/clear/gain/raw-text negatives,
but use inert TEST media/probe leaves: the original-cut/critic/readiness native
workflow first failed15.60seconds: the original produced intent requires motion,
transitions and B-roll, correctly refusing an audio-only candidate. No readiness
critics or14-document input ran. Evidence is retained; separately qualifying an
upfront trim-intent fixture will not qualify full produced work. That separate
trim attempt also failed (15.765seconds): the actual longform duration gate
rejects the three-second fixture below its unchanged300-second minimum. Trim
intent passes, but neither critics nor14-document input were reached. Both
failures remain retained; no gates were waived. A corrected positive fixture
must declare valid duration/partial-render semantics and suitable continuous
synthetic audio before source/cut capture, not mutate either failed source.
No execution/DSP/default/public activation
or original-intent rewrite is inferred from these metadata checks.
An actual TEST generator/sync/regenerate repro also found structural Studio
edits could be rebaselined and then discarded, including mixed timing/index
edits. The independently reviewed11-file preservation fix is now live and
byte-identical to the tested mirror. Full expanded Studio160 passes with zero
skips in27.00seconds mirror/26.85live, including local process/socket/tiny-media
cases. Unsupported residual edits, changed tracked files and late saves block
apply; original operator files at both former temp paths survive. This is not a
global atomic transaction or SDK browser-visibility qualification. The separate
SDK provenance TEST migration is live (root5/5 mirror10.93seconds). A broader
original live25-case cohort passed all5 provenance cases but failed the old
host-ID TEST probe and read-only bridge's missing V2 normalization. Both are now
fixed, including the independently reproduced retained-head import case. The
four-file follow-up is live with exact mirror hashes; all191 cases in every
non-browser Studio TEST module pass40.66seconds mirror/40.30live, zero skips.
The actual browser module remains explicitly excluded for its separately
documented visibility/offline-control issue, not a passing skip. No creator
output or two-hour master qualification is inferred.
No selected-caption default activation or full opening/body qualification is claimed.

Latest September9 audio continuation: the new initial16s longform-trim/excerpt
TEST source now passes the actual four-field cut→V10 readiness→14-document
TS/Python input path in30.067seconds wall (28.937test). No production gate was
changed; original600s caller/60s source caps remain. All10 recorded containers,
the source-worker group and both mutation locks were absent after exit. This is
mirror-only, non-speech calibration and input mechanics—not audio DSP, creator
quality, opening/body rendering or full-longform qualification.
An independent review first caught detached FFmpeg and a renewed60s leaf clock.
The named TEST route now reuses the existing verified shared-group leaf helper;
the original2s regression passes, plus27Python and40TS checks/1explicit media
skip. Two actual outer-timeout/caller-cancel tests verify that TERM-resistant
children and descendants disappear (final7.48s). Exact source-admission daemon
ownership is recorded before launch for supervision; automatic outer-death
container cleanup is still not qualified. Original failed fixtures remain intact.
Evidence: `evidence/V10_EXCERPT_INPUT_2026-09-09.json`. A new actual shared-master
and opening TEST is being prepared; no successful DSP result is inferred yet.

September9 09:38 UTC: the actual16s V10 audio master/opening now PASSES in
76.753seconds wall:23.644s setup and51.981s render/readback. Strong local cleanup,
+3dB premaster gain and the shared float master really ran; measured−14.00LUFS,
−5.68dBTP, exact480frames/768000samples. Status is ready-for-review, not approved.
The first attempt failed29.370s because the historical snapshot reader stopped
atV9; actualV10 now requires both inherited caption and additive audio pins.
The next attempt failed27.787s on a symlinked TEST venv root; the existing explicit
canonical-venv selector fixes setup without relaxing its guard. Both failures
remain intact. Regression union62PASS/1nativeSKIP27.41s; final type-check/lint pass.
One separate live-PID regression was sandbox-blocked, then passed with process
read permission. All ten owned containers and recorded workers are absent after
the successful run. Evidence: `evidence/V10_AUDIO_OPENING_2026-09-09.json`.
This remains mirror-only non-speech mechanics: no caption/audio combination,
nonempty body, creator listening, public V10 activation or ten-minute timing claim.
The native slot was released for the real-footage pilot; optional cache work is
not a prerequisite for that pilot. Automatic source-admission cleanup on outer
worker death remains unqualified.

September9 10:05 UTC: the frozen12-file V11 pure motion package is now in the
integration mirror, with49TS/12Python tests and whole type-check/lint passing.
The explicit integer-frame ordinary motion adapter also passes63 pure checks
and an actual9.96s native batch:24/30/29.97fps, nine outputs, exact picture/audio
clocks and36 eased-motion marker samples. The first8.36s native attempt failed
all three center checks: this FFmpeg scale filter counts from1 while crop and
overlay count from0. The corrected adapter normalizes only that filter counter;
same visual tolerances and encoder settings pass. Existing default behavior is
unchanged. These are mirror-only primitives, not a guided V11 lifecycle or speed
claim. Actual source-occurrence binding, profiles/pins, cold read and opening/body
consumption remain to connect. Evidence: `evidence/V11_FRAME_MOTION_2026-09-09.json`.
The now-authorized three C0679 editorial choices take priority for a real edited
audition; editorial agreement is not invented exact-word timing or final approval.

September9 10:40 UTC: real C0679 edited draft and user-requested revision now
exist. The three agreed keeps produced an actual67.15s draft; its81.35s attempt
failed only final summary serialization, and independent11.82s readback verified
the retained output without rerender. Aaron reviewed it and requested tighter
leading silence and visual cover over the natural-sounding maybe join. The
actual65.40s revision removes42frames/1.75175s and adds the supported statement
card, One-person shop, across that join. It passed in17.606s wall, including
8.826s native SDK0.8.31 card/placement,2.456s picture composite,1.674s audio mux
and3.484s full decode/clock QC. An initial0.724s missing-proof-tool-pin invocation
failed before container work; both attempts are retained. All source-float audio
after the new15ms start fade is byte-identical; zero audio/picture sample drift,
original transfer tags and clean owned-container exit are verified. Root viewed
actual sampled first/card/seam/exit frames. This is a960x540 ordinary review
draft, not guided activation, exact acoustic timing approval, a graded final
master, full-length speed proof or creator acceptance. The ASR opening timing
conflict remains explicit. Evidence: `evidence/C0679_ACTUAL_DRAFT_REVISION_2026-09-09.json`.
Aaron's final-acceptance-gated disk-retention requirement is separately captured
in `FINAL_PROJECT_STORAGE_2026-09-09.md`; no current files were deleted.

The next produced-workflow integration is ordinary motion, then admitted video
B-roll, then transitions with correct graphics ordering/shared-master audio.
An independent source audit confirms those rendering stages already exist;
V5–V10 guided operations and both TS/Python opening profiles do not connect them.
Presenter layouts do not currently satisfy motion/B-roll ownership, and a
clean-hook decision does not waive requested transition coverage. The SDK's
composition-edit APIs do not provide those Sniper-specific joins. Do not remove
the existing gates or claim the graphics-only longform fixture qualifies them.

The first concrete motion repair is now live: ordinary static/animated zooms
use half-open endpoint predicates. A retained actual render proved frame60
remained zoomed one frame late despite exact90frames/audio. Repaired24/30/NTSC
native boundary checks pass with unchanged encoder settings; independent root
63-case cohort PASS7.45seconds, final live62 pure PASS3.01seconds. All nine
control/effect outputs preserve exact source PTS/audio records. This is a narrow
ordinary endpoint repair, not arbitrary fractional-frame precision or guided
motion activation. Detailed finding in the parent monorepo:
[Frame counts do not prove effect timing](../../../youtube-automation/docs/findings/FRAME_COUNTS_DO_NOT_PROVE_EFFECT_TIMING.md).

User authorization: continue implementing the agreed video-editor enhancement goal,
including tests, adversarial review, actual HyperFrames UI operation, audiovisual
quality checks, and measured timing. This is an execution checkpoint, not a new
architecture or a completion claim.

## Latest operator priority — Codex / skills / HyperFrames Studio

The operator clarified after this session's UI render: their primary interface is
Codex, using skills to drive HyperFrames Studio. The custom Sniper web UI had too
many issues and is NOT the main delivery path. Stop expanding/polishing that UI.
Retain already-useful underlying fixes and evidence, but prioritize the direct
skill/command workflow, Studio preview/sync, intro review, held base/master reuse,
scoped edits, audio/color/temporal QC, subscription-only execution and measured
request-to-master time. Do not make custom-web-UI completion a prerequisite for
qualifying the user's actual working path. This clarification supersedes the
older custom-UI-first sequence below and in the broad historical plan.

The existing Codex Producer adapter delegates actual edit work to the canonical
`.claude/skills/producer/SKILL.md`; read it completely plus required references
before creative authoring. It already names `studio/studio_review.py open` and
`sync --apply`; inspect and improve that existing path rather than create another
renderer or a competing skill. Studio preview is not delivery authority.

## Active completion goal — 15:07:52 UTC

The user explicitly requested creation again and instructed continued work until
finished. This time `create_goal` **succeeded**: the new goal is **active**,
created2026-09-07T15:07:52Z. Earlier blocked-goal observations below are
historical and no longer describe the current goal. No token budget was set.

The objective is all remaining Codex/Producer/HyperFrames Studio development,
not custom UI polish: guided and one-request editing, accurate assisted timing
correction, fast intro/body, finishing lanes, recovery/scoped revisions,
render/review efficiency, selective ZIP/catalog integration, adversarial tests,
real short/long audiovisual qualification and measured first-master timing.
Local compute and verified subscriptions only; no paid APIs/purchases/downloads
or billing fallback. Waiting on one video's actual human decision pauses only
the dependent operation, not unrelated implementation. No simulated listening
or approval, no quality waiver to meet a deadline and no completion based only
on unit tests or a synthetic render.

Current work: source-word timing and source-faithful one-to-one text correction,
explicit new-manifest publication and the real cut-gate consumer are implemented.
The explicit manual9:16 profile and duplicate-picture-decode removal have actual
tiny-media tests and independent review. Caption integration now reuses the
ordinary renderer's actual prepared pages in the shared opening/body graph;
the two native tiny-media cases passed and V5 is now the default for new
proposals. The complete captioned command/multi-page run finished mechanically,
but actual retained frames exposed caption/graphic occlusion. This is continuing
development, not completion of short/long real-video qualification.
All agents preserve this shared dirty worktree.

### Latest continuation checkpoint — September8, goal unfinished; development resumed

This section supersedes stale "pending" wording in the historical entries
below, but does not supersede their retained failed attempts.

Newest continuation (September8):

- **Supplied native AVFrame kernel implemented, not production color finishing:**
  new `color/native/source_gamut_avframe.c/.h` reduces active G/B/R float32 pixels
  in place, without frame copies or Python transport. It checks all referenced
  buffer extents before aggregate mutation, supports padded/unaligned/negative
  strides and uses integer IEEE754 classification without clipping, tolerances
  or fast-math. Counts/extrema match the actual Python framed reference under
  both UBSan and optimized builds. One synthetic hot-frame measurement consumed
  384 MiB in 0.155899 seconds (2.58 GB/s); this excludes hashes, framing, decoding,
  zscale, source IO and the full workflow, so it is not comparable as a full-path
  speedup or a two-hour result. All three original attempt outputs are retained
  in `evidence/SOURCE_GAMUT_NATIVE_KERNEL_2026-09-08.json`: missing explicit SDK
  failed at 0.50 seconds wall; installed ASan initialization aborted at 1.16;
  UBSan/optimized correctness plus the one measurement passed at 1.44. ASan is
  unresolved, not passed. Installed tool/library paths were used without downloads.
  Independent review verified the exact current source/header/test and UBSan
  binary hashes before and after a correctness-only run; no diagnostics, exact
  framed-reference parity (0.01564 seconds child, 0.21587 seconds full check).
  Compiler syntax and scoped coding checks pass. The owned decode/filter-sink driver, original
  PTS/duration and complete EOF/hash joins, runtime closure and live activation
  remain actual implementation work. No creator source or APPLY gate was touched.
- **Actual Codex Studio sync repair:** default-only and conflicting host/panel
  edits previously returned `applied` and rebaselined a value absent from the
  resulting plan (two genuine failures, 3.44 seconds wall). Unmatched defaults
  now block before any write; exact matching host edits still use the existing
  contract/lint/history route. A strict quote/entity-aware declaration parser
  preserves types, metadata and unique IDs/keys; the reconstructed original must
  match its manifest hash. The original/current instance is read and rebuilt once,
  not separately for the note and gate. New 11 plus existing 35 tests: 46 PASS in
  7.10 seconds wall. The existing 17-case sync cohort uses a temporary 12-second
  synthetic video; the new cases explicitly substitute only the inert-base probe.
  No Studio panel, Docker/catalog publication or creator media was exercised.
  Independent no-native review passes all 11 new tests in 0.440 seconds suite
  time and the scoped coding checks; the one docstring nit is corrected. See
  `docs/findings/STUDIO_SAVED_DEFAULTS_ARE_NOT_SYNCED_PLAN_VALUES.md`.
- **Full-float reference milestone completed, not native qualification:** new
  source_gamut_reducer/samples/result joins actual supplied V2 source records,
  explicit same-clock measurement headers and bounded1MiB planarGBR payloads;
  both complete EOFs and the original record digest are mandatory. Unknown
  history stays observable; all native/gamut/APPLY/approval flags remain false.
  Original guard/context/frame/terminal/result holds are retained without a new
  clock; failures/reentry are sticky. Root67pure regressions PASS0.74s, independent
  gamut33PASS0.301s; all eight files pass300/50/4/2 including try plus docs/types.
  Real signed-zero chunk-partition inconsistency RED0.126s was repaired without
  changing payload bits/hashes. Extreme finitefloat32 serialization was fixed
  separately from exact integer-clock canonicalization; a rounded integer cannot
  pass as a measured float32. Initial TEST duplicate-key/loader failures remain
  recorded, not counted as production regressions.
- The unchanged synthetic384MiB/case benchmark ran once before and once after
  a proven-all-finite shortcut. Hot1MiB980.00→1261.79MB/s; rotating64MiB
  989.27→1248.47MB/s; all original/joined digests match. Whole commands2.33→1.99s,
  process peakRSS121.52→117.16MB. Both FULL original stdout JSON records are
  retained in `evidence/SOURCE_GAMUT_SYNTHETIC_BENCHMARK_2026-09-08.json`.
  Best C0679 arithmetic projection remains26.30min for reduction alone, beyond
  the original20min observation phase before decode/transform/pipe. This reference
  MUST NOT be treated as the production time solution. Native in-process reduction,
  tool/source/EOF/clock/settlement binding and real AV qualification remain open;
  see `docs/findings/FULL_FLOAT_REDUCTION_IS_NOT_NATIVE_GAMUT_QUALIFICATION.md`.
- **Disposable SDK declared-text proposal milestone completed:** the adapter uses
  only the existing temporary0.8.31 SDK, without changing installed0.7.33 or
  importing it into the live Studio route. Root traced an existing pure treatment-handler
  defect while preparing this join: a one-element sharedseed edit diverged the
  fire/sparkles values, and a stale element value could overwrite a different
  composition value. Both exact tests RED0.10s before repair; current11handler
  tests PASS0.10s. Scoped edits now require a unique owner and exact current
  element/composition agreement. Independent11/11 review passed (0.005s suite),
  including extra exact-type negatives and a unique numeric-variable positive;
  delta mechanics pass. The adapter accepts bounded plain supplied data, checks
  exact HTML hash/scene/unit/variable ownership and rational timing, and returns
  only an existing `title.setText` operation. Two disposable non-executing SDK
  sessions verify the exact patch/inverse and unchanged baseline; no HTML is
  published. Reopening silently dropping unexpected trailing code genuinely
  failed the first suite (11P/1F0.966s); exact candidate-byte readback repairs it.
  A final-disposal getter mutation genuinely failed its regression (0.242s);
  repeating the existing getter-rejecting snapshot repairs it without invoking
  the getter. Root actual opted-in SDK13/13PASS1.02s wall (0.993s TAP), independent
  13/13PASS1.036s TAP, zero skips. Default execution without the explicitly
  selected temporary SDK is1PASS/12SKIP, not thirteen-test compatibility proof.
  The actual Python bundle/scene validator and treatment handler participate in
  the positive test: only the selected right-card text/version and its invalidation
  change. Current combined Python78/78PASS0.74s wall; whole TSC7.22s and scoped
  ESLint1.78s PASS. The handler has no non-test live caller; static-DOM/numeric
  SDK edits, installed0.7.33 runtime parity and rendered repair remain unqualified.
- The existing goal remains app-reported`usageLimited`, not complete. The earlier
  Docker53/catalog-publication action was NOT retried. A fresh explicit approval
  question is pending for a new local45-minute qualification/publication run;
  no permission is inferred from generic continuation and no paid API/download
  or billing fallback has occurred.

- **Latest completed bounded milestone:** current source2 controller checks now
  span real body-input publication, one-use activation handoff, native-free
  readback qualification and final candidate CAS. All known concrete faults in
  this delta are repaired and independently retested; the five changed workflow
  files pass semantic review and300/50/4/2 mechanics (including try nesting).
  The final verification-publication defect was genuinely reproduced
  (RED95.81s): an actual original budget callback replaced the exact attempt's
  `verified.json` with the same bytes and a new inode after the actual candidate
  CAS. A private four-role holder now captures start/output/verified immediately
  on creation and the worker ledger before its actual parser, retains original
  returned values and ALL ancestors, and checks metadata after the last parent
  callback. No raw payload rehash, source replay or new allowance is added.
  Exact independent regressionPASS105.74s. Latest genuine candidate positive
  plus original expiry regression2/2PASS188.59s (104.541s/83.195s tests).
  Whole TSC7.21s/scoped ESLint1.97s pass; fresh same85-case Python union
  PASS36.13s wall. This milestone is metadata/protocol, not decoded video,
  subjective listening, applied grading, loaded-runtime compatibility or a
  two-hour creator-video result. Public source2 native launch is still refused.
- Current-source controller integration is now wired through the actual source2
  body writer and activation CAS. The writer requires the original remaining
  budget, checks all303 current/snapshot TS pins before publication, and transfers
  code metadata only through its private original return. Re-read historical
  activation/status cannot mint that live handoff; historical cleanup is not
  forced to match subsequently edited current code. Native source2 launch stays
  fenced. Root whole TSC8.19s and scoped lint2.06s pass; actual writer/handoff10/10
  PASS194.13s wall (two files at concurrency2, concurrent peer load). Independent
  adversarial3/3PASS118.29s wall. Foundation cohort14P/1F420.178s rejected reentry
  correctly but masked its original error; preserving that primary error now
  passes the exact unchanged case plus original Error-identity check in30.333s.
  This is not a fresh full15 green run.
  Full-code test setup publishes new-only copies, then the final lock before the
  original journal seal, after existing inert Python tool rows are finalized.
  Default shared authority6/6PASS10.93s. Latest static closure300/fresh snapshot329
  and all303 pins PASS1.040s, with45.027ms current-pin comparison (metadata only).
- Independent new controller review reproduced an exact same-byte snapshot inode
  swap during canonical-path resolution after the old stat capture (RED1/1,
  24.56s). Canonical resolution now precedes the final identity stat for files
  and directories; the exact repaired runtime check passed in that three-case
  independent cohort. These fixture timings are not renderer benchmarks.
  The separate post-writer budget concern was reproduced using the actual
  original wall budget: after activation CAS it narrowed to100ms, then the final
  metadata read consumed101ms, yet activation returned success (RED32.64s).
  The private handoff now samples that SAME parent into its retained source
  deadline before the last callback-free metadata sweep, then rechecks the
  original lease/new journal. Exact independent regressionPASS38.63s.
- The post-candidate-CAS concern now has a new prepublication complete TEST
  record/ledger fixture. Real process/cleanup/candidate CAS and result readers
  remain in place; only tool selection, runtime environment, native runner and
  readiness are code-only TEST leaves with their real defaults retained.
  Its first positive actual protocol route passed1/1 in139.70s wall, including
  the real final candidate history reader and exactly one TEST native-read leaf.
  The exact post-CAS deadline-expiry fault then reproduced a success return
  after the original wall advanced60minutes (RED89.28s). The readback repair
  now retains original outer caller/phase/result values and current-controller
  evidence against the already-spent original body remainder. Its final order
  is saved source check → original lease/new candidate journal → selected
  metadata → callback-free finite deadline tail. Positive+exact-fault rerun is
  complete:2/2PASS185.13s wall (positive101.895s; exact expired-return82.369s).
  A separate final readback-publication identity concern is under
  independent fault testing and remains unproved until the test completes.
  Latest root TSC8.39s passes; actual legacy/contract/pipeline-pin44/44PASS13.98s.
  That command included one nonexistent test filename that Node ignored; only
  the44 actually reported cases count. The correctly named cleanup-process-pins
  suite passed separately14/14 in26.34s, not silently included in the44.
- Goal query on this continuation still reports the existing goal as
  `usageLimited` (app-controlled), not complete. Development can run here;
  no duplicate goal, usage reset, paid fallback or native launch was performed.
- Second independent TS result review reproduced three additional concrete
  continuity gaps: first-input-IO original phase Buffer mutation and output
  parent replacement after the only ancestry sweep (RED2/2 in37.78s), then a
  parsed raw receipt mutated before its later private retention (third RED in
  a2P1F65.91s cohort). The result reader now snapshots its original phase before
  any version/raw IO, captures its actual source holder before input observation,
  retains parsed completion/raw result immediately on return, and repeats original
  ancestors after file/failure checks. Root first two fixes plus existing protocol
  union37/37PASS90.29s; final exact three faults3/3PASS62.86s. Independent final
  third faultPASS25.00s. Latest whole TSC8.44s/scoped ESLint2.72s passed.
  This is actual local protocol/metadata evidence with explicit native/AV leaves.
- Current TS controller-source prerequisite is being implemented as a separate
  foundation. It must use the full301-pin inventory from a data-only module,
  compare original snapshot/current bytes once, retain original identities/ALL
  ancestors and the original55-minute body remainder through legitimate CAS.
  No writer/activation/native permission is inferred from that foundation alone.
  Root added optional BEFORE-initial-journal-publication TEST fixture setup hooks
  for a complete newly constructed pin inventory; default authority6/6PASS11.17s.
  Original Python entries/tool paths and all sealed source/media records remain
  untouched. Source2 process fence remains with an accurate controller/runtime
  and native-qualification explanation.
- Source inspection also identified an inherited post-candidate-CAS readback
  deadline/result-metadata tail concern. Existing result fixture intentionally
  lacks complete native process/cleanup/ledger and candidate media records, so
  it cannot honestly reproduce that full route. No false CAS or cleanup proof
  is substituted. A prepublication complete TEST record fixture and explicit
  native/readiness/tool leaves are the prerequisite; the concern remains
  source-traced, not reproduced or declared fixed.
- Latest source2 Python body bridge/result/readback checkpoint is independently
  85/85PASS33.48s wall across ten explicit metadata/TEST-native-leaf modules.
  The actual original raw source-observation and base-consumption readers run
  as two sibling phases on the SAME bound body clock. Their hashes survive
  worker receipt, completion and ten-stage V2 readback; flags explicitly retain
  gamutMeasured/gradeApplied/colorQualified/bodyApproved/deliveryApproved=false.
  Whole-base/master reuse remains the existing engine, not a replacement render.
  Original input syntax/raw SHA is checked before source dispatch and direct
  BodyWork construction; repeated guards do not reread source payloads.
  Six initial result tests7.15s and45 legacy/control tests1.02s were earlier
  checkpoints, not additive distinct counts to the85-case union.
- Independent faults reproduced and repaired in that Python slice: copied
  work/control erasing all source tags RED1.05s; changed original source reference
  reaching admission RED0.51s and erased-tag pre-source variant RED0.50s; final
  semantic result comparison exceeding original time RED1.25s; current body raw
  receipt authority rebaseline and same-byte worker-entry replacement RED2/2
  5.64s; failure marker after final raw result read RED2.83s. Original current
  body result/entry files, ALL parent identities and held invocation now persist
  from before the first phase callback through final output construction.
  Original clock capture precedes the first remaining callback; first-callback
  clock extension and initial same-byte output substitution were real REDs and
  now reject. A first final-elapsed TEST hook failed to arm and is NOT valid RED
  evidence; the corrected armed post-fix regression passes. Independent entry9
  plus result6 passed17.38s wall. No human/native/AV qualification is inferred.
- TypeScript explicit V2 raw result/completion/readback and actual private result
  binding are frozen:26/26PASS73.93s wall, final returned-metadata ordering case
  PASS19.13s, TSC2.58s/lint2.26s. Genuine approval→body admission→activation/input
  →process-phase CAS is exercised with an explicit stopped native/OS TEST leaf.
  Exact original opening evidence reference is joined; Python owns actual source
  section replay. V1 parsers and the public source2 native process fence remain.
- Fresh static source coverage now includes the actual guided-body.ts entrypoint
  and18 additional literal modules:298 current TS imports/301 V2 controller pins.
  Combined fresh-pin and retained-tool process tests27/27PASS27.33s wall. New
  body worker/read/cleanup Python union421 imports is captured in a fresh748-file
  TEST snapshot; removing one required reader from a separately new test lock
  rejects. Original snapshots are never patched. Latest whole TSC8.11s/scoped
  pin ESLint2.31s passed. This proves packaging coverage, NOT body-side admission
  of current TS controller bytes; that prerequisite is now under review.
- Retained test-only failures during integration: initial current-result union
  7P1loader-error9.64s used a mistyped test method; legacy fake-control wrappers
  needed explicit new precheck/lifetime TEST leaves (17P2mock failures7.54s),
  then the fake clock needed a numeric end (27P1mock error17.57s). Corrected
  source-clock wiring3/3PASS0.28s and final85-case fresh-import union passed.
  These are not native failures. Python/source/pin tests never qualify the
  requested real ten-minute/two-hour output class.
- Newest source2 handoff adversarial cohort23/23PASS66.94s wall includes five
  fault cases, six document/genuine-holder cases and twelve deadline cases.
  Four reproduced defects are fixed: initial final-retain expiry RED14.17s,
  in-place first-callback submission rebaseline RED10.59s, and later guard expiry
  plus same-byte current-journal inode replacement RED2/2 in24.88s. Original raw
  current-buffer/job and historical-snapshot inode cases are post-review-fix
  regressions, not falsely labeled pre-fix reproductions. Exact original file
  identity/parents and a callback-free original deadline tail remain retained.
  TSC3.32s/lint2.45s and scoped coding mechanics passed; latest legacy authority
  and claim29/29PASS4.23s. Earlier root deadline/authority/claim/source2 union
  47/47PASS13.01s predates the last two fixes and is not their verification.
- Body source2 worker-input TypeScript mirror now has42 distinct passing tests:
  five actual admission→writer tests70.91s wall and37 parser/replay/observer/legacy
  cases4.21s. A copied source2 input with both mutable tags downgraded really
  published V1 in RED15.93s; the writer now checks original raw held-input bytes
  and closed row/input/opening joins before its first callback. TSC3.11s/lint2.90s
  passed after an in-flight fixture arity error. Python control mirror58/58PASS
  0.83s wall includes six early same-byte file substitutions; the original input,
  activation and seven references are held before dependent reads, and the three
  added original metadata files share the same64MiB budget. No native ran.
- Actual Python source observation/base-consumption body bridge and explicit
  V2 result/readback are now being integrated, not yet declared qualified. The
  root orchestration forwards one bound body clock through pre-source capture,
  current pipeline and two sibling replay phases before old whole-base/master
  reuse. Initial45 legacy lifecycle/source capture/control/contract tests passed
  1.02s wall; six source2 actual replay→receipt/readback tests passed7.15s wall.
  These use real bounded original metadata readers/new-only result files with
  explicitly inert initial admission/pipeline/native packet and whole-AV leaves.
  No rendering, whole-body decode, listening, grading or delivery approval is
  established by those tests. Copy/downgrade and final-tail review is ongoing.
  Public source2 body native launch remains fenced.
- Latest approval/status/body-opening union70/70PASS163.28s wall with test
  concurrency2. This includes the permanent original-body-verifier owner test:
  replacing its lease DTO before delegation previously accepted RED13.52s;
  original request identity is now captured before delegation and the targeted
  positive/negative pair passed19.99s. These remain TEST native/readiness leaves,
  not real source or audiovisual qualification.
- Same-live predispatch cancellation is implemented and independently reviewed:
  new-only intent/archive/ack, exact claim-only CAS, original300s cleanup clock,
  actual resource-before-project release. Final97/97PASS98.55s wall comprises
  cancellation32, prelaunch23, staging17, process-binding9, lifecycle12 and real
  legacy terminal4. TSC5.17s/lint2.98s and coding limits passed. Two final project
  release faults (same-byte ack substitution and elapsed original cleanup clock)
  were reproduced2/2RED2.02s, fixed selected5PASS4.12s, and independently2PASS2.22s.
  A release-tail failure retains irreversible released/uncertain state and
  cannot replay lease release. Cold recovery, uncertain publication adoption,
  and public V2 activation remain disabled; no real cancellation/native ran.
- New body source-color replay reference contract/projector13/13PASS56.57s wall,
  TSC2.42s/lint1.66s. The genuine current/historical selection and its distinct
  actual cleanup-held result are joined before projecting two original raw
  references. A JSON DTO cannot recreate the private capability. Independent
 9/9PASS27.44s; one measured TEST hold131.7ms/27 bounded metadata opens, five
  finite assertions89.6ms/5 opens, zero work callbacks/native increments. This
  is metadata-only timing, not a production speed claim. An earlier12P1F55.93s
  expected a changed-value error where an already-frozen TEST reference correctly
  threw TypeError; the test now accurately covers immutability and raw drift.
- Root connected the genuine projector through source2 held-body input and
  exact historical admission lineage; versions1/2 are explicit and closed.
  Original body admission/private+CAS objects, snapshot/start and parsed/raw
  returned values remain retained across later reader callbacks. The legacy
  authority/claim29/29PASS4.58s, TSC8.72s and four-file300/50/4/2 checks passed.
  The full actual source2 authority-to-admission-to-worker-input fixture is in
  progress; these tests alone do not qualify the source2 body workflow.
- Body-input V2 TypeScript/Python metadata mirrors are under test, preserving
  the seven old references and adding explicitly bounded original source-color
  replay metadata. Semantic opening claimHash is NOT equated to raw claimSha256.
  Body native launch remains explicitly fenced until source replay and versioned
  body result/readback are connected. No unfinished worker was launched.
- Root's narrow source-read clock adapter borrows the actual already wall-bound
  BodyExecutionClock; no new timeout or original live opening runtime is created.
  First40/40PASS2.98s includes6 new clock cases and34 existing read/entry/scope
  cases. Adversarial review then found a real intermediate watermark rollback
  RED0.33s: the first sampled wall value was not saved before filesystem work.
  The entry now saves/checks each actual sample before later IO and during setup;
  repaired root40/40PASS2.89s. Independent entry-check AND initial-capture
  rollback regressions plus subclass/event-list checks bring the union44/44
  PASS3.05s. This does not establish consumption/media QC.
- Actual source2 body authority→held document→closed parser six-case suite
  PASS12.98s, TSC3.05s/lint1.82s. Original project lease, cleanup/selection/approval,
  verifier capability and body replay projector are real TEMP protocol operations;
  initial readiness/media facts and native leaf remain explicitly TEST-only.
  The full-program fields are authored before the first media-result seal, never
  patched into old evidence. Initial4P1F10.47s was TEST teardown order and is
  retained; owned release is now registered before fixture tree cleanup.
- Running a pending-reader test standalone exposed a module initialization
  cycle before ANY test ran (RED0.39s). Exact spine: pending-read→attempt-read→
  opening-process→read-transport→opening-cleanup-store→opening-cleanup→recovery→
  pending-read. Deferred access to the same original pending dependency object
  fixes this; no fallback/validator change. Pending/read/recovery/route35/35
  PASS45.98s. The three authorized initial-fixture forwarding seams keep default
  selection and approval positives passing (6.61s and10.15s).
- Fresh dependency audit after these new imports12/12PASS6.44s: current static
  TS graph280, V2 pins283, simple capture309; base Python346/grade167/snapshot663/
  staged353; read Python355/20 explicit boundaries/snapshot666/staged359.
  These are fresh test-only captures; no old snapshot was rewritten/rebaselined.

- The user resumed after account-limited agent errors. The goal tool's latest
  stored status was `usageLimited`, not complete; that app-controlled status
  cannot be resumed using `update_goal`. Live usage was0% consumed with one
  existing free reset available, so no reset was redeemed and no purchase or
  paid API call was made. Continue in-scope development; do not duplicate or
  mark this unfinished goal complete.
- Direct cold selection identifiers and genuine project directory are now
  checked BEFORE resolving the publication path. Independent original fault
  RED8.45s opened an unrelated TEMP JSON once before refusal; permanent fixed
  case PASS6.86s verifies zero unintended opens, zero foreign publication stats
  and zero callbacks. No source/tool file was a fault target.
- Same-live prelaunch owner final project-lock check repaired: the last resource
  callback could release the original project lease unnoticed. Independent
  RED0.74s, permanent RED0.82s, fixed two-case PASS1.13s, independent PASS0.75s;
  full61/61 PASS71.08s (23 prelaunch,17 staging,9 process binding,12 lifecycle).
  TSC4.00s/lint2.45s/300-50-4-2 passed. Durable predispatch cancellation is a
  distinct in-progress slice, not proven by this result or inferred from absence.
- Strict cold schema2 approval reader:8/8 PASS77.77s, expanded six-file/callback
  and mutable-return subsets2PASS26.27s; exact historical read without active
  journal IO1PASS10.07s and original Buffer/caller/size negative1PASS11.57s.
  These are8 distinct cases, not12. TSC3.10s/lint1.90s passed. All decisions are
  explicit TEST attestations; native/readiness leaves remain stubs.
- Connected explicit schema2 approval dispatch and status with a single initial
  selected read under the original protected allowance. Earlier combined24/24
  PASS39.08s includes actual TEMP writer/status and20 existing approval/status
  cases. Adversarial public-wrapper checks then exposed three real gaps:
  receipt-version downgrade replay RED8.50s, timing-END deadline escape RED8.84s,
  and same-byte original archive replacement at timing-END RED8.74s. All three
  fixed with actual selection/fact version agreement and a private ORIGINAL
  committed-evidence handle checked after timing and legitimate lease release.
  Permanent3/3 PASS24.62s; actual public success control PASS9.50s; TSC4.01s,
  lint1.49s. No fresh read is used to rebaseline a replaced inode.
- Source2 body opening-evidence consumers now dispatch actual result/readback/
  approval versions and retain original private handles through final callbacks.
  Original body caller lease/remainder are captured before its first callback.
  Legacy body authority/claim plus two new caller-boundary cases29/29 PASS4.21s.
  Initial9P18F2.44s exposed an outdated TEST fixture's missing legacy process
  discriminator; its inert initial fixture now declares version1, not fake
  process qualification. Source2 consumer tests and broader integration are
  pending. Repeated source2 body assertions reuse original private proof handles
  rather than rebuilding cold history; no production speed improvement claimed.
- Latest root TSC8.78s and scoped lint2.33s pass. Earlier type-check failures
  7.18s (union narrowing) and7.47s (TEST inferred never-returning release) were
  corrected; these are not native failures. Root11-file mechanical audit passes.
  Fresh dependency coverage must be renewed after all new cancellation/body
  modules freeze; previous265-file graph numbers below are historical.
- Source2 body opening-evidence suite8/8 PASS80.53s, TSC2.30s/lint1.58s.
  Actual generic TEST approval→current/historical selection→body result/approval
  readers→private retention and same-owner service requalification are exercised.
  Known-source discriminator downgrade actually invoked a legacy reader once
  (1P1F18.71s), now refused by original private-handle version dispatch. This is
  not complete body program/activation/Python-worker qualification.
- Root updated approval/status/legacy-body cohort57P1F44.90s: the only failure
  was a stale-selection test expecting early refusal while the new version join
  attempted the unrelated supplied hash first. Current selected-pointer equality
  now precedes object IO; exact regression1PASS8.85s. Do not label the57P1F run
  a58-case pass. Broader body/legacy cohort49P4F1SKIP4.96s: three tests required
  a temporary Unix socket and hit sandbox EPERM; a fourth old selection fixture
  omitted its version. Explicitly inert initial fixture version fixed,4PASS0.68s.
  Authorized socket-only rerun3PASS2.77s; no Docker/media command or paid API
  invoked. The opt-in retained failed-run reference check remains unrun.
- Fresh source coverage after standalone approval/selection/prelaunch helpers:
  12/12 PASS6.66s, TS graph265/V2 pins268/legacy5 unchanged. Read snapshot650
  files,354 Python imports,20 explicit boundaries/358 staged files. The cold
  approval reader is still under implementation and will require its own fresh
  inclusion; this result does not cover future files or repair old snapshots.
- Schema2 selection write/current/historical read implementation is frozen
  pending independent review. Initial18-case union PASS52.95s; direct exported
  cold-helper invocation initially accepted an altered approval flag against
  an unchanged stored fact hash (RED7.33s). It now reuses the actual fact parser
  and exact current cleanup/claim/selection-pointer join before any callback;
  positive+regression2/2 PASS13.97s.19 distinct cases covered across runs,
  not a single19-case union. TSC2.98s/lint1.88s and mechanics pass. Earlier
  indented-index raw/semantic mismatch4.56s and capability-clone6.84s setup
  failures are retained; the latter is fixed by private cap identity, not by
  serializing capabilities or weakening their checks.
- Root updated service/history/terminal cohort45/45 PASS93.79s wall: all26
  service cases,15 retained-history cases and4 live terminal cases. This is
  metadata/TEMP lease/CAS testing under concurrent test load, not render latency.
  WholeTSC PASS7.60s after both earlier typed-projection/annotation fixes;
  root scoped lint PASS2.24s. The service now closes the actual range-inode
  gap without requiring0600 on ordinary owned media.
- Root controller/terminal/execution/transport/integration/readback combined
  cohort76/76 PASS78.24s wall after the live release and owner assertion work.
  This includes23 service cases, not the three later range-lifetime additions.
  No native video or human approval was performed by that cohort.
- Source readback now also retains original core/review media inode/stat and
  ancestry from before native invocation through final return. Its earlier
  JSON/tool-only metadata could not reject a range replacement between native
  return and later selection entry. Dedicated media identity accepts ordinary
  owned0644 files beneath the private output root while rejecting group/other
  writable, aliased or substituted files. The shared test fixture publishes
  inert range rows/files before raw result hashing; no sealed records change.
  Three new post-fix regressions PASS11.97s: afterSettled replacement, final
  remaining-callback replacement and post-return metadata rejection. Service
  tests now26cases/275lines; TSC2.93s/lint2.15s/mechanics pass. No old-source
  RED is claimed for these three. Full updated cohort is running separately.
- Standalone schema2 explicit human-decision writer is implemented but NOT
  connected to public approval/status/body yet. It requires the existing exact
  attestation contract, genuine selection and same-owner current requalification;
  retains historical selection/readback before CAS; and checks finite original
  evidence after the intentional journal advance without replaying stale caps.
  Root5/5 TEST decision cases PASS46.44s. Two independent first/late attestation
  mutation negatives PASS22.52s; no concrete decision-authority gap found.
  First run4PASS/1 failure33.95s exposed canonical semantic SHA incorrectly used
  for an indented index's raw SHA; fixed by observing actual raw bytes and also
  comparing exact semantic value. Independent first run1P/1F14.18s hit that
  same known issue. Initial TSC7.35s unknown-typed TEST projection corrected;
  later wholeTSC7.77s hit the separate in-flight prelaunch annotation, already
  corrected by its owner. No real user's watching/listening was invented.
- Selection/current/historical schema2 consumer integration and private live
  prelaunch ownership are being qualified independently. Public V2 launch/
  execution and full body activation remain fenced. Prelaunch state retention
  is NOT cancellation: no unlink, acknowledgement, cancellation CAS or release
  has been implemented in that slice. Exact cold approval history/dispatch is
  the next approval prerequisite; real AV/timing qualification remains undone.
- The existing goal was checked again and is ACTIVE; no duplicate goal or
  completion was recorded. Public source-color execution, real short/long QC
  and the prospective two-hour master benchmark remain unfinished.
- Live controller release is now implemented: the private original phase is
  set before claim entry; uncertain claim/process/cleanup retains the lease;
  only untouched preclaim or authenticated exact terminal cleanup may release.
  Cleanup's original ledger and failure-marker absence survive the last
  callback-free sweep. Later verification failure does not erase cleanup.
  Agent26/26 controller-lifecycle/cleanup/legacy-execution tests PASS25.66s,
  TSC4.16s/lint2.06s/mechanics pass. The earlier host-sensitive launch-store
  cohort retained7PASS/1 failure because exact sysctl/ps identity was unavailable;
  new tests explicitly stub only that OS identity, not the actual TEMP leases.
- Root's new four live preclaim-to-terminal tests PASS12.98s. Original claim
  bytes, complete project context, token, submission and prior pointer are
  preserved under the same real lease and actual journal CASes. Wrong cleanup
  refs and same-byte retirement-archive replacement refuse release. Original
  creative/claim admission and native cleanup are explicitly TEST leaves.
  Independent exact continuity/teardown diagnostic PASS3.70s. Retained setup
  failures: malformed TEST preclaim journal4 failures1.10s; duplicate borrowed
  lease teardown4 failures12.59s and12.68s. Adversarial review also found that
  rebuilding the TEST parent changed token/plan while still passing the release
  checks; the optional parent path now preserves the actual observed parent,
  and complete-context assertions are permanent. No sealed claim was repaired.
  Root wholeTSC7.71s/scoped lint2.50s pass. Default fixture constructor behavior
  is retained; borrowed owners do not register a second owning teardown.
- Strict source-color historical readback is implemented without consumer
  activation: actual saved-before cleanup, SAME-held result and all three
  original schema2 files/ancestors are held before the first caller callback.
  Exact raw/version/tool/archive/evidence/time joins and original callbacks are
  retained privately; later metadata checks invoke no expired proof callbacks.
  Agent15/15 PASS53.28s, two initial positives7.46s; TSC3.24s/lint2.31s and
  mechanical192/92/110line limits pass. Independent5-case boundary subset
  PASS16.04s with no concrete defect. No media/native qualification is implied.
- Readback now exports an exact same-owner assertion for selection callers;
  its new permanent case PASS4.29s (23 service cases total), TSC3.34s/lint2.26s.
  Original held read-tool projection is exported for the historical reader;
  genuine result metadata brackets it. Root transport/integration23/23
  PASS20.46s after the fixture constructor changes.
- Fresh source coverage adds the detached controller as an explicit static
  entrypoint plus the history reader:12/12 pin tests PASS6.24s. TS graph261,
  V2 pins264, legacy5 unchanged; read snapshot646 files,354 Python imports,
  20 explicit read boundaries and358 staged files. This does not qualify
  uninspected dynamic/package/native code or repair an old snapshot.
- Next active implementation is explicit schema2 selection write/current/
  historical reads, retaining original live/cold proof and budget through CAS;
  public execution/body remain fenced. Partial-prelaunch durable cancellation
  and cold recovery are separate unresolved prerequisites, not inferred from
  the new live release gate.
- Root final combined readback/transport/integration/legacy cohort62/62
  PASS68.25s wall:22 permanent service cases,23 transport/integration cases,
 17 legacy execution/result/version cases. The new initial-store callback
  substitution RED4.08s is fixed by capturing request/lease/remainder BEFORE
  dependency or cleanup-store callbacks. Agent19/19 PASS55.73s, promoted3/3
  PASS9.78s; final test228lines/maxfunction20/params1/nesting1, wholeTSC3.12s/
  lint2.16s pass. Root's earlier wholeTSC failed8.92s only on the same two
  TEST fixture-shadow type errors, subsequently repaired; no suppressed errors.
  Service164lines/maxfunction26/params3/nesting2 and shared87line failure
  helper passed independent mechanics/lint2.02s. This closes the bounded
  verification-service test slice, not public/native/selection/body activation.
- Next active code responsibility: opaque live controller lifecycle and safe
  lease-release gate. Proven untouched preclaim abort or authenticated exact
  cleanup may release; uncertain claim publication, process settlement or
  cleanup must retain ownership. Public source-color V2 remains preclaim-fenced.
  Partial-staging durable cancellation/recovery remains a separate prerequisite.
- Genuine same-execution cold transport and exact read tools now pass23/23
  focused integration/negative cases20.42s, independently23/23 in22.27s.
  Expected interpreter/runner/venv come from the original held media intent;
  only script/hash change to the exact original pinned read worker. Alternate
  interpreter with a matching self-declared hash, wrong worker and extra tool
  keys refuse before callbacks/native. Correct shared-runner path uses a
  pid-less TEST child; no installed executable is run. TSC3.30s/lint1.72s pass.
- Separate explicit schema2 readback service is implemented and UNDER TEST,
  not connected to public execution/selection/body yet. It reuses the actual
  final cleanup/result/transport, original lease/remainder, schema2 start/
  output/verified records and shared failure logging. Original read tools are
  retained by stat/ancestry through the final callback; new record identities
  are private and frozen, and an attempt failure invalidates verification.
  Independent diagnostics reproduced deadline-overrun success and mutated
  failure clock identity (three-case diagnostic9.57s; third hit a safe TEST
  target assertion before any write). Root fixed borrowed-budget tail charging
  and captured original failure lineage. Positive service1/1 PASS3.60s;
  full fault cohort is still running, so no qualification is asserted here.
- Fresh source-pin audit after adding the separate readback module:12/12
  PASS6.54s; TS graph256, V2 pins261, legacy5 unchanged, read snapshot643
  files/354 Python imports/358 staged files. This is current source coverage,
  not old snapshot repair or package/native qualification. Legacy execution/
  result/version-gate regressions17/17 PASS4.78s after shared failure extraction.
- Independent copy review then found a real retry gap beyond the original
  truncation: a fitting retry could delete/reorder/substitute selected points,
  blank a label or omit a nonempty title and still pass ordinary graphics lint.
  Nine new cases ran23 total with15PASS/8RED in0.19s wall. The helper now
  requires the same ordered valid original anchor sequence, including duplicate
  multiplicity, and a nonempty title when originally supplied. A mismatch
  returns the detached unchanged pending obligation, not a shortened candidate.
  Root42-case cohort PASS3.06s; independent full copy/sequence/illustration/
  word-land cohort54/54 PASS3.80s. Production220lines/maxfunction47/params4/
  nesting2 and test266lines mechanical checks pass. Actual broll parsing is
  identical for short/full context captions; no native/render/semantic proof.
- Root independent transport16/16 PASS3.92s. Genuine same-execution integration
  subsequently passed3/3 in9.63s through actual stopped/media records, cleanup
  writer, both CAS operations, retirement, final store, result and transport;
  original claim/input/color bytes and stat identities are preserved. Claim
  admission and native/tool cleanup remain TEST leaves. The first run failed
  only a TEST expected receipt-flag spelling3.79s; test-only inferred stopped
  types were corrected by composing actual dependencies, not modifying a sealed
  capability. Exact read-tool/interpreter binding is now being added and tested
  before this path is connected. Whole TSC PASS7.39s after the production join.
- Exact late-callback argv bypass reproduced and fixed. `invokeOpeningChild`
  now captures one frozen copy of original extraArgs before any caller callback,
  validates that copy and passes that same copy to the shared process runner.
  New16/16 transport regressions PASS3.94s; TSC2.52s/lint1.75s pass. Tests use
  actual runtime file/hash reads and shared runner, with only the exact TEMP
  socket type and pid-less child stubbed; no native execution. Retained attempts:
  namespace-mock harness error1.11s; actual RED missing rejection2.72s;
  permanent stable-argv run14PASS/2RED4.66s; test-cast TSC failure3.40s.
  Genuine final cleanup and result from DIFFERENT original held objects refuse;
  same-execution positive composition remains under development, not assumed.
- Copy-fit implementation is now complete at helper/caller-instruction level:
 33/33 PASS3.64s, independently rerun33/33 PASS4.48s. Oversized wording is
  retained in detached structured copyRepair state; failed/empty retries do not
  clear that obligation. Fitting retries retain original timing; illustration
  captions retain complete text. Semantic equivalence still requires the
  existing grounding/review step; no model or full-video qualification occurred.
  Final canonical Producer skill validation PASS0.07s using existing sibling
  Python. Skill-creator informed the surgical instruction update, not a new
  editor framework or a relaxation of review.
- Cold generated-size alignment completed: only new base/picture-master capture
  uses existing2GiB MAX_MEDIA; source, metadata, tools, encoders and global
  reader policy are unchanged. Four pre-fix boundary cases RED0.31s wall;
  new4+existing33 PASS37/37 in2.12s. Root independently reran4/4 PASS0.28s
  with payload/native callbacks forbidden. Exact2GiB vs2GiB+1 uses simulated
  size metadata over tiny actual TEMP files, not multi-gigabyte qualification.
  Root reran the actual-writer-to-cold-consumer integration after this cap
  alignment:1/1 PASS9.65s wall; native leaves remain explicitly TEST-stubbed.
- Copy-fit active route confirmed: only the Producer skill directly calls
  `fill_list_spec`; planner candidates carry needsCopy/anchors/rawSpan and
  ordinary lint already rejects unresolved copy. The implementation owner is
  adding a detached structured repair result rather than truncation or generic
  exception fallback. Root used skill-creator guidance for a surgical canonical
  Producer paragraph: preserve complete meaning/timing/obligation, retry fit
  and semantic grounding under the same budget. Official quick_validate PASS
 0.12s using the already installed sibling Python environment. The first
  project-venv attempt failed0.06s because PyYAML is absent; no installation or
  download was attempted. This validator proves skill structure, not semantic
  behavior or a finished real edit. Code/tests still in progress at this entry.

- Latest operator clarification via the parallel audit: inspect Vex's solved
  failure cases and independently implement useful behavior; no code, prompt,
  schema, fixture or stack transplant. Root read the complete new Vex lessons
  audit and copy-fitting finding. Confirmed helper defect: `graphics_copy._cap`
  can truncate a complete nine-word instruction to an unfinished six-word
  clause. Active-caller/repair-path trace is underway before changing behavior;
  no semantic review, requested treatment or timing will be silently dropped.
- Generated-size audit found8GiB cold capture versus an effective2GiB hash/
  publication/read limit. It did NOT demonstrate a ten-minute final failure:
  actual12/18Mb/s H.264 plus256kb/s AAC estimates are0.856/1.275GiB for10min;
  source-float refers to audio, about219.73MiB for10min. These are size estimates,
  not output measurements or bitrate guarantees. Longer declared frame classes
  can exceed the output cap (about23.36min at30fps /15.68min at60fps under those
  settings); that support gap remains. Aligning the new cold capture to existing
  MAX_MEDIA is being tested to fail early, not widen policy or reduce quality.
  Input source8/16GiB and generic sealer limits are separate authorities.
- Final whole type-check7.82s and root scoped lint2.28s passed after invocation
  null/false, frozen-role and raw-source-color-flag hardening. Positive genuine
  final-cleanup/result invocation composition remains unqualified and gated.

- Final authenticated discovery/read cohort147/147 PASS11.63s wall,
  fresh pipeline-pin cohort12/12 PASS6.31s. Independent tests exposed two
  additional real holes: shortened AST imports restored before later hashing
  (RED0.034s), and an original local module hidden during directory enumeration
  then restored changed (RED0.24s wall). Discovery now parses the same bounded
  SHA-authenticated bytes and includes original locked local module paths;
  neither bug can be covered by later hashing only the discovered subset.
  Six-file300/50/4/2 mechanical review passes. One mechanical diagnostic had
  a TEST-script empty-args TypeError and was corrected/rerun, not suppressed.
  The earlier fresh-pin run11PASS/1FAIL6.34s omitted four newly reachable TS
  modules; those four explicit pins are now included. Current static TS graph
 255, V2 pins260, unchanged legacy5; fresh read snapshot642 files with354
  read imports/358 staged files. These are source/package checks, not execution.
- Historical cleanup store now privately retains the genuine final reader
  under its frozen historical wrapper. `assertOpeningCleanupMetadata` provides
  metadata-only checks without replaying an expired proof-read callback or
  creating a new clock. New+route18/18 PASS28.03s, separate legacy8/8 PASS0.74s;
  root independent original-file-replacement and expired-proof tests2/2
  PASS6.16s. Baseline capability-loss RED3.60s retained (preceded by a0.11s
  inline import harness error); initial test-only type cast failed TSC3.10s,
  corrected final TSC2.29s/lint1.58s. Downstream consumers are not activated.
- Separate schema2 TS actual-stopped/result binder33 cases plus11 contract and
 8 legacy/version-gate cases PASS3.90s wall; root independent binder+legacy
 41/41 PASS4.44s. Original claim/input/result/media-protocol file identities,
  exact completion/record/readback evidence references, original held identity
  and final callback checks are retained. Native/media qualification is not
  inferred from these actual TEMP protocol tests. Public legacy V3 refusal
  remains covered.
- In-progress, NOT qualified: explicit cold read invocation binds actual final
  cleanup and actual selected result; four flags come only from that chain.
  Its process branch strips retired Docker controls, uses original recorded
  tool directories and the existing owned runner/deadline. Raw color flags,
  DTO/null/false handles and role/argument overrides cannot choose the branch.
  Adversarial transport/integration tests are still in progress; no native
  invocation or public controller/selection/body activation has occurred.
  Initial whole TSC6.99s and scoped lint1.71s passed before the last small
  argument/role hardening; final rerun remains required.

- Continued goal verified ACTIVE; not a completion claim. Current read-only
  Python dependencies are now separately bound to the original pipeline lock,
  captured before hashing and retained through later readback. The original
  media execution inventory is unchanged; shared media imports are not hashed
  again by this additive check. Existing144-test regression union PASS11.18s
  wall (10.710s suite). New adversarial coverage is being authored separately.
- Cold base-consumption reader: independent33/33 PASS1.99s wall. Retained
  serialization failures2RED0.31s and holder-lifetime failures4RED0.42s were
  repaired without another decoder or renewed clock. Actual producer writer
  through cold reader integration1/1 PASS9.63s wall; its first TEST-only
  str/Path hash-adapter error8.57s remains a failed attempt, not native failure.
  Native packet operations remain explicit TEST stubs using actual parsers.
- Fresh media/read pipeline capture tests12/12 PASS6.23s wall, type-check2.72s,
  lint1.96s. Read-seeded closure354 vs media346 had15 read-only additions;
  these were included in fresh snapshots but missing from current read-code
  verification. The additive check above addresses that omission; old locked
  snapshots and recorded media inventories are not retroactively repinned.
- Parallel operator steering: use HyperFrames as the engine, selectively take
  only missing Vex ideas. Prefer structured SDK mutations/history/checks over
  broad generated HTML rewrites. Production runtime0.7.33 is unchanged; audited
  SDK0.8.31 needs a disposable compatibility adapter and actual rendered
  qualification before migration. SDK setTiming must be read back through
  getElementTimings(), not getElement().duration. Vex's audited noncommercial
  license blocks direct business-use code copying without a separately
  applicable license. Implement general ideas independently; no purchases or
  license-contact actions are authorized. See the September8 open-source audit
  and SDK timing-readback finding for evidence and exact frozen source refs.

- The separate Python schema2 read path is now connected through an explicit
  all-or-none stopped-sidecar/final-reservation-archive quartet. No flags retain
  the original three-argument schema1 reader. Mixed legacy results reject
  before source admission. All eleven original/new read stages, final actual
  event history, complete-stage status and rounded elapsed totals are checked.
  The original entry clock and six original control-file identities are held
  before the first metadata read. Metadata-only claim reads do not resurrect
  the old Docker socket; the legacy helper still performs one metadata pass
  and exactly one unchanged runtime admission call.
- Retained cold observation handles now survive later base/AV stages without
  another frame replay. Independent combined133 metadata/orchestration cases
  PASS10.581s suite/11.06s wall; mechanics pass. Native/source-admission/current
  pipeline discovery/full-media leaves remain explicit TEST stubs where used.
  Earlier source/claim baseline, method-shadow, authority-before-source-read,
  event-prefix and original-clock-extension REDs are retained as regressions:
  0.32s/0.54s/0.40s/0.45s and2RED0.42s wall respectively. Intermediate
  20PASS1.59s,28PASS2.05s and131PASS11.02s preceded the final fixes.
- Real worker-to-writer schema2 and an explicit unchanged legacy counterpart
  both PASS16.530s suite/16.94s wall. Legacy emits schema1 with no new evidence
  ref/file and does not call the source-color writer or verifier. These use
  inert media/native/AV/selection leaves, not rendered video.
- Cold base-consumption reader is implemented with original generated-file,
  tool/PATH, packet-observation/copy-return and original publication holds.
  Author24 cases PASS1.15s wall. Independent review caught the actual renderer's
  raw float-valued timeline/manifestation spelling versus the recorder's
  canonical JSON domain (2RED0.31s wall). The corrected reader uses the actual
  raw compiler domain and canonicalizes only expected recorder projections;
  author26PASS1.12s wall. Independent recheck and actual-writer integration
  are pending. No source decoder/hash, approval or public activation added.
- The TS result/readback/public-selection fences remain intact. Remaining
  integration includes independently authenticated FINAL cleanup transport,
  a versioned read invocation without the old socket admission, exact result/
  readback joins, and explicit selection/body version handling. The historical
  cleanup wrapper also loses its original private reader identity when spreading
  the result; this remains a documented integration issue, not a fixed claim.
- Goal reverified ACTIVE; no replacement goal, completion, billing/reset,
  catalog retry or native media run. Codex/Producer remains the primary route.
- Corrected observation-projection fixture rerun passes7/7,41.045s suite/
  41.33s wall. Its earlier5PASS/2fixture-method-collision errors remain recorded.
- Pure observation-section contract is now explicit and closed:24new plus
  34related tests pass58/58 in0.201s suite/0.30s wall. Original raw SHA,
  project history, admission decoded-frame facts and actual frame replay are
  NOT granted by this pure validator. The separately authenticated cold reader
  is being implemented and cannot reconstruct execution owners from JSON.
- Actual private picture-consumption transport now binds ordered cut calls,
  exact concat→master, original packet-copy proof, original sealed final
  source-float-master receipt and final master return. JSON-boundary tuple
  serialization,8MiB aggregate metadata growth, reentry, original callback
  substitution and late publication are tested. Source-only channel routing
  uses the existing original-source float-bus channel correction instead of
  another unused legacy AAC channel repair; legacy routing is unchanged.
  Unsupported rail/recomposition inputs reject before cuts.
- Recorder/context regression62/62 PASS108.336s suite/108.72s wall; estimated
  60–90s was exceeded and is not reported as the actual result. Earlier23-case
  attempt was20PASS/3TEST-bus.samples setup errors in22.81s; repaired fixtures
  retain actual typed TEST bus metadata. Quick bounds/legacy23 PASS0.35s.
  All native, decoded-output and audio-admission leaves remain TEST-stubbed.
- Separate source-color schema2 completion/readback transport is implemented
  but remains unwired in the public result reader. A shared bounded JSON
  scanner preserves treatment request diagnostics and rejects duplicate keys.
  Adversarial tests exposed impossible timing acceptance:11rounded2ms stages
  cannot fit a rounded11ms total. The former11ms tolerance is replaced by a
  conservative6ms maximum independent-rounding allowance. Retained RED:
  2PASS/2FAIL0.24s; final independent43/43 PASS1.49s, whole TSC2.80s, lint1.38s.
  Earlier root39PASS1.47s/TSC6.80s preceded this timing repair;18focused then
  pass0.24s. This is protocol validation, not native throughput or approval.
- Python worker now accepts exactly all four source-color flags or none;
  invalid/partial transport refuses before output-directory work. The explicit
  opt-in branch constructs one OpeningSourceLifetime, captures one bound guard,
  observes sources outside a nested phase alarm, and prepares the actual
  source-only base under the SAME opening deadline. No-flags call arities,
  source capture and schema1 route are preserved. New adapter9 plus existing
  source-capture5 pass14/14, latest0.44s wall; two prior single-case mock-fixture
  setup failures (0.41s/0.32s wall) were repaired without production waivers.
- Separate source-color-evidence.json writer joins original live
  observations, actual base-publication return, original cut/timeline refs and
  generated base bytes, always ungraded/unapproved. Its6 actual-holder/inert
  output tests PASS45.217s suite/45.51s wall. Actual worker-to-writer schema2
  handoff passes again after final-tail repair:1test8.847s suite/9.29s wall.
  Native audiovisual and selection leaves remain explicit TEST stubs.
- Adversarial review exposed four original transport/publication/deadline
  failures (4RED0.34s wall), then a separate final source-guard callback could
  change already-hashed completion bytes (1RED0.45s). Original argument and
  generated-artifact holds, the sole final-hash deadline check and callback-free
  final file/ancestry checks now reject these faults. Independent final union
  19/19 PASS0.111s suite/0.51s wall; mechanics/whitespace checks pass.
- Cold source-observation replay now verifies independently supplied original
  sidecar/archive refs, saved source history/admission, original pinned worker
  and full frame records. It never opens retired active.json or source bytes,
  constructs an execution owner or grants grade/quality approval. Author76
  PASS7.046s suite; independent76 PASS6.411s suite/6.68s wall. Independent
  historical-expired-phase/current-valid-read-deadline case PASS0.53s wall.
  Outer actual stopped-process/final-cleanup authentication and complete
  schema2 media readback remain required; this is not public activation.
- Still required before normal activation: complete producer-to-cold-reader
  integration and real source-color base/native readback, exact schema2
  TS/result selection, partial-prelaunch recovery and uncertain-owner controller
  settlement. Real short/long AV quality and the ten-minute/two-hour target
  remain unqualified. No existing V3 public/result fence has been removed.

- Reverified the actual active goal on the latest "create a goal/don't stop"
  request. No duplicate, completion, reset, paid call, render or catalog retry.
- Final finite scheduling checkpoint and sticky monotonic rollback fixes are
  independently reviewed:67 clock cases PASS36.61s wall;2,415 durable checks
  take28.244s. Actual inventory1150files/2415guards/one watermark took35.06s
  wall with the earlier wrapper; see the clock finding for that limitation.
- Full internal V2 opening submission survives intent, media-input and claim
  lineage. Original claim/readback now returns the validated full submission.
  Public launcher/controller and actual V2 execution remain fenced. These
  fences prevent a V2 request silently taking the legacy uncolored path.
- New live TS process binding authenticates the existing sidecar/reservation
  against original staging and complete request.9binder+17staging cases
  PASS5.79s wall; independent binder review found no additional defect.
  Current staging inventory has23 additive TS pins at this checkpoint; new
  cleanup consumers still need inclusion before activation.
- Cold Python reservation parsing reads only original reservation/claim/input,
  even before sidecar/job publication:68 tests independently PASS1.12s wall.
  Internal Python opening observation adapter17 cases independently PASS
  43.26s wall; actual metadata/preparation/owned-publication/sealing executes,
  but source admission/native observations remain explicitly TEST-stubbed.
- Python V2 cleanup CLI/driver and adapter use one original protected deadline,
  existing original runtime controls and the complete reserved name set. Initial
  122-case union PASS8.09s wall and actual Python output parsed through the new
  TS V2 contract. Daemon/time leaves are virtual except bounded setup-timer
  tests. Independent review then reproduced late claim.path/sha256 mutation
  after final runtime callback; repair and permanent negatives are in progress.
- TS V3 stopped-process records are strictly paired with V2 activation; full
  submitted source clauses and the original worker ledger remain mandatory.
 14new/binder cases independently PASS3.98s wall. The real legacy PID test
  initially failed under sandbox `/bin/ps` EPERM (unknown remained unresolved).
  A narrowly approved own-child-only rerun PASS1/1 in0.70s wall. No Docker,
  source or catalog permission was broadened; no liveness rule was relaxed.
- New cold TS reservation hold captures the original three file/parent
  identities before callbacks and joins all ordered reserved names. Review
  reproduced late result mutation and missing top-level claim joins. Both are
  repaired with entry-held actual result plus final comparison and exact full
  claim/graphic-order joins. Current cold+V3 cohort26/26 PASS6.21s wall;
  independent retest pending. Initial TS failures were one ES target bigint
  literal and TEST readonly-field mutation, both corrected rather than waived.
- Known-BT709 identity eligibility is the next bounded color slice, with actual
  held probe/frame SAR/rotation evidence. xvYCC conversion remains separate:
  current compiler requires FFmpeg8.0/zimg3.0.6, whereas the approved image
  retains FFmpeg4.4.2. This mismatch is limited to the isolated-image route:
  the ordinary cut path uses hostFFmpeg8, and retained tiny host math evidence
  already exists. Neither route has full original-pixel gamut/owned-transform
  qualification. A bounded image-tool inspection was prepared but NOT run;
  no tool replacement, new policy or gamut measurement is assumed. See
  `docs/findings/SOURCE_COLOR_POLICY_MUST_MATCH_RENDER_RUNTIME.md`.
- Follow-up review closes the Python late-claim substitution: original exact
  type/path/SHA/value is retained across final callbacks.124/124 simulated
  tests PASS9.59s wall; independent exact two negatives PASS1.15s wall.
  TS cold hold18/18 independently PASS6.46s; both original repros reject.
- BT709 held metadata reader preserves original V1 records while supplementing
  actual all-frame SAR/no-rotation-signaling/chroma evidence. Independent
  review reproduced equal-valued int→float replay acceptance; exact typed
  comparison now rejects it.124 metadata cases independently PASS2.34s wall.
- Source-picture guard transport now reaches the actual first lossy FFmpeg
  call for every cut part and its return, plus final return/publication checks.
 6 native-leaf-stub tests PASS0.12s. The actual existing12part NTSC native
  suite first passed6/6 in7.93s wall; with a new guarded-versus-legacy decoded
  equivalence test it passes7/7 in10.48s wall. All291RGBframes at320x180 and
  decoded audio bytes are identical. Picture SHA
  `89fb4c89f5b742439562f061a76a66eb7b584e872241ee64392ad103947d381c`;
  26 guard checks, guarded generation2.625595s, generation+comparison2.948550s.
  The guard in this native test is a TEST timing-only hook, not genuine
  source-color/presenter authority. Temporary generated source/output files
  are removed by the existing fixture; this is not longform quality evidence.
- Actual internal known-BT709 base holder now joins the SAME completed batch,
  original source bindings, presenter/input owner and OpeningExecutionClock.
  Absent/null baselineLook is retained; every non-null look refuses. No public
  registry/intake gate was removed.38 new/legacy/transport metadata cases pass
  independently58.72s wall.26 real held two-source guards take0.485817s total
  (18.685ms mean); setup5.739s includes two supplemental metadata replays,
  repeated guards replay none. The fixture clock is virtual; this is measured
  guard overhead, not production source/media throughput.
- Cut transport mechanical extraction preserves probe order and FFmpeg args.
 28 focused nonnative tests PASS3.18s wall. Repeated actual native7/7 suite
  PASS10.75s wall after extraction: same291frame RGB SHA and decoded audio,
  guarded generation2.645685s, generation+comparison2.963005s. The source is
  synthetic320x180; the 10-minute quality/timing target is still unproved.
- Separate cold source-resource recovery acquires only the existing global
  lease after actual resolved V3 readback and original reservation hold.
  Fresh acquisition remains unchanged. Initial63 tests PASS11.29s wall.
  Independent review found a final remaining callback could release the lease
  while assertCurrent/cold-hold/copy still accepted. The actual resource guard
  now follows that callback, then original metadata is checked; all three
  permanent negatives reject.53 cleanup-hold/recovery cases PASS12.333s,
  TSC/lint pass. No lease auto-retirement or native cleanup is inferred.
- Literal reservation archive writer and historical reader are implemented:
  new-only exact cleanup-attempts/UUID/reservation.json bytes keep the original
  active reference/hash/size. Historical reading never opens current active,
  source, sidecar or job files.50 archive/core tests PASS11.72s wall; TSC/lint
  and mechanics pass. Independent review is running; archive possession is
  not actual process, cleanup, commit or retirement authority.
- Actual source-color cleanup process helper uses the original held reservation,
  resolved V3 media evidence and existing pinned child invoker, now optionally
  wrapped with an attempt-scoped cleanup lifecycle ledger. Full V2 output and
  all names must match; forced/unknown/incomplete child ownership refuses.
 10 boundary-stub tests PASS4.27s wall; TSC6.00s/lint0.99s pass. Independent
  review is running. Additive actual Python wrapper smoke cases pass, including
  zero-helper complete return and abrupt-zero-exit refusal. The combined
  14-case ledger run had13P/1sandbox ps-observation failure in1.11s; the exact
  own-child-only ps regression passed after narrow approval in0.18s wall.
  No Docker, source, catalog or paid API calls were made by these tests.
- Requested explicit user approval asynchronously for the separately denied
  local53-preview catalog evidence refresh. It has NOT been retried; work on
  independent code continues while the response is pending.
- Independent archive review passes30/30 in10.938s plus a final-callback
  original-inode substitution negative. No current active/sidecar/job/source
  bytes are read by the historical path. Core reference parsing is explicitly
  performed before the pure name helper; mutually redirected paths refuse.
- Independent cleanup-process review reproduced final-guard changes to the
  original reservation and the already-settled cleanup ledger. A finite final
  callback-free original metadata/rawSHA/stat sweep fixes both; same remaining
  budget pays its IO.33 cold-hold/process tests PASS8.39s wall, independent
  exact two negatives PASS1.78s and12 process cases PASS4.92s. Core metadata
  helper21/21 independently PASS7.005s and calls neither owner nor clock.
  See the new cleanup-callback finding; no repeated general callback loop.
- Cleanup invocation provenance is now published new-only before native work
  as invocation.json, from the actually captured frozen tools. That original
  record/tools are returned and held through success; current tools are not
  resampled later to invent historical provenance.14 helper cases PASS6.07s,
  TSC6.65s. Two additional sibling-attempt refusal cases are running: actual
  project/global lock acquisition alone does not settle an earlier cleanup
  worker. Until an all-prior-attempt reader exists, any sibling directory entry
  explicitly refuses. Public/controller V2 execution remains fenced.
- V2 commit design uses the EXISTING openingCleanupHash in two CAS phases:
  awaiting-retirement retains claim/outcome; only exact unlink or honestly
  observed post-intent absence, a new-only acknowledgement and final CAS may
  clear them. Cold post-unlink recovery needs a separate retirement-only path.
  Partial staging still needs a journal-held preparation fact before publishing
  active.json. Closed fact contracts are being implemented, not activated.
- Latest frozen helper union passes145/145 in18.67s wall: legacy cleanup,
  V3 media readback, binding, reservation hold, actual TEMP pin faults, owned
  first-attempt cleanup helper, archive, recovery and actual own-child Python
  lifecycle smoke. No Docker/media/catalog run was part of this union.
  Independent extended helper16/16 PASS6.79s;14 additional TEMP pin/invocation
  checks plus those16 PASS8.488s, with TSC/lint passing. All tested mutations
  were exact fixture-owned files, not source code or installed interpreters.
- Closed V2 prepared/final/retirement-ack fact contracts are now frozen and
  independently reviewed:15 tests PASS0.24s wall, mechanics pass. They retain
  exact phase/claim flags, original archive raw bytes, structural UUID paths
  and canonical timestamps; parsing alone grants no cleanup or retirement.
- Actual attempt recording/readback now joins the archive/start, actual helper
  return, stdout/ledger/invocation and prepared fact under the original clock.
  The historical reader holds all five original media-proof files as well as
  cleanup records. Its expanded union passes169/169 in23.495s, including the
  writer's17 cases, with whole TSC/scoped lint/mechanics passing. Native/tools/
  original claim admission leaves remain explicitly TEST-stubbed; this is not
  real cleanup or media qualification. The exact default media-proof provider
  also exercises the existing activation parser on TEST records.
- Legacy cleanup now explicitly refuses a V2 request before native work, so
  source-color reservations cannot silently take the graphics-only clear path.
  Its eight cases pass0.47s wall. All public/controller V2 fences remain.
- Further actual-record integration found a fixture coverage defect: raw job
  observations contain Node Buffer bytes, while structuredClone changes those
  to Uint8Array. Deep-strict comparisons then rejected unchanged real held jobs;
  reduced fixtures omitted the bytes. A real TEMP raw-journal positive failed
  before native work in1.01s wall. Buffer-preserving metadata snapshots and
  actual-byte regressions are in progress; no fields or identity checks removed.
- First prepared-fact CAS and exact pending-journal reader are in progress.
  The writer publishes result/fact/original snapshot, retains original clock
  history and keeps claim/outcome. A separate final retirement edge is still
  required. Initial current TSC6.32s and scoped lint1.81s pass; actual first-CAS
  integration/fault tests and independent review are not yet complete.
- The real Buffer-bearing journal correction is now frozen:145/145 PASS20.97s
  wall and9 dedicated cases PASS2.89s. The new real-journal finding explains
  why reduced fixtures missed it; no persisted hash or JSON format changed.
- Actual first-CAS integration with real TEMP project and resource leases now
  passes36/36 in17.61s wall. Both guards reject stale records, expired clocks
  and lost leases; post-CAS failures retain pending evidence without creating
  failure.json or retrying native work. Pure final-job transition8/8 PASS4.04s.
- Exact current pending read16 cases and explicit retained-pending history11
  cases are implemented. The80-case current/history/first-CAS/retirement union
  passes26.961s suite, with TSC/lint/mechanics passing. Only the explicit history
  entry ignores an unrelated current journal; current checks are unchanged.
- Actual TEMP marker unlink, acknowledgement and crash recovery initially
  passed16/16 in12.52s wall. Independent review found a read-bound mismatch:
  an existing valid ack padded to128KiB+1 was accepted although final facts
  allow at most128KiB. The permanent regression was RED1.45s; bounded read fix
  gives17/17 PASS13.54s and independent exact1/1 PASS1.50s. The marker-only
  retirement operation took77.490ms in the positive TEST fixture; native work
  and enclosing elapsed remain TEST leaves, not production cleanup timing.
- Actual second-CAS writer is implemented but its integration tests are still
  running. It requires the actual retirement capability, preserves exact pending
  raw bytes, authenticates retained history and uses the same original remainder
  through final write/read/lease checks. Independent source review found no
  additional blocker. Initial whole TSC6.59s passes. It does not release leases
  or select/approve media. Final cleanup-store consumer wiring remains pending.
- Expanded retirement25/25 PASS19.64s wall covers authenticated-history refusal
  for unlink, private live parent/remainder/resource helpers, regular and dangling
  marker reappearance, existing-ack replay with a new active entry and actual
  lease loss after unlink. Markers and all faults are exact TEMP fixture targets.
  Original current pending metadata is never waived after intentional retirement;
  final CAS instead holds a separate explicit retained pending snapshot.
- The attempt reader now returns its already-held immutable actual stopped
  observation for existing status consumers, with no extra process read.31
  attempt/history cases PASS10.916s; final receipt/store wiring is in progress.
  A TEST fixture property collision (workspace dependencies shadowing inherited
  process dependencies) was corrected by a distinct retirementDependencies name.
- Actual final CAS now passes39/39 in27.50s wall. It keeps explicit pending
  history through both CAS guards and the finite post-write proof. Native/tool/
  original-claim provenance remains TEST-only; actual TEMP journals, records,
  acknowledgement, unlink and both leases are exercised. No media approval.
- Full internal live cleanup workflow passes12/12 in9.81s wall. The positive
  actual workflow takes680.308ms after fixture setup, with TEST native/enclosing
  elapsed leaves. Two permanent late-clock journal/acknowledgement inode faults
  first reproduced false success (2.90s wall). The workflow now awaits timing
  bookkeeping, samples its original clock, then checks an authenticated final
  commit metadata hold and charges that finite tail before returning.
- Actual final cleanup readback now dispatches current/historical schema2
  final receipts without relabeling prepared facts or granting approval. Three
  first-callback media/output/archive replacements initially bypassed child
  baseline capture. Explicit deferred pending admission now captures the actual
  attempt/media before the first caller guard; six admission/reentry regressions
  preserve the same original callbacks and clock. The14-module union passes
  256/256 in56.660s suite; follow-up15 cases pass9.086s. Native leaves are still
  TEST-only. Independent final-reader/workflow review is running.
- Pending-only global resource reacquisition passes78/78 new/legacy/fresh
  acquisition cases in21.12s wall; TSC2.56s and lint2.16s pass. It authenticates
  current pending evidence and the exact original resource namespace, but never
  reads or deletes an active marker. Fresh and original recovery remain unchanged.
  The shared lease protocol proves a live same-process on-disk owner, not a
  private JavaScript handle identity; this existing limitation is retained and
  documented, not misreported as a foreign-process or no-lock bypass.
- Pending-only recovery service implementation and actual checkpoint tests are
  now in progress. It must complete retirement/final CAS/readback without another
  native cleanup, release only after verified final evidence, retain uncertain
  post-handoff ownership, and count parsing/acquisition/proof/release against one
  enclosing protected clock. Public route activation and closure pins have NOT
  changed yet. Earlier in-flight TSC fixture-only errors remain failed attempts,
  not production/native qualification.
- Pending-only service and existing recovery dispatcher now cover46 distinct
  actual TEMP checkpoint/retirement/CAS/final-read/lease cases. The expanded
  service37-case run passes55.75s concurrent wall (not a performance benchmark);
  dispatcher8 plus timing7 pass20.20s. Exact pre-directory-parse expiry proves
  that the same protected clock crosses dispatch. Unknown release, post-commit
  evidence faults and final timing-append faults stay failures, with no new
  native cleanup or attempt. Independent verification is running.
- Added explicit direct Codex `cleanup-status` and `recover-cleanup` commands,
  a closed checkpoint request parser and original pre-parse clock transport.
  The command invokes only pending retirement, not legacy native cleanup.
  Six new transport cases plus14 legacy command cases PASS1.62s wall. An
  initial TEST callback-parameter type mismatch was corrected; whole TSC then
  passes6.62s. Actual status→request→recovery integration is still running.
- Fresh snapshot capture was missing four actual required paths:
  studio/import/files.ts, evidence.ts, model.ts and lib/debug.ts. A permanent
  fresh-capture negative first failed0.43s wall. The finite capture additions
  and literal current TS import closure now cover247 project TS files from
  five actual cleanup/CLI roots; V2 controller union252, legacy V1 remains5.
  Final fresh-capture/intent19 cases PASS1.47s wall; prior broad44 PASS28.98s.
  Actual bounded TEST capture278files takes40.708ms, current-pin check35.038ms
  and AST audit161.700ms in that test context. This is not native/package or
  source-media qualification, and no prior production capsule was rebaselined.
- Actual CLI status→new-only literal request→recovery→final readback is frozen:
  11 new integration cases plus6 transport and14 old command cases PASS31/31
  in26.20s concurrent wall; TSC/lint/mechanics pass. No native calls were made;
  actual journal/CAS/leases/readers use explicit TEST claim/media provenance
  leaves. The initial request-open expiry fault missed its intended TEST hook
  (10/11 passed), then was corrected to the actual descriptor open and rejected.
- Normal status now keeps current-claim precedence but can strongly read exact
  prepared cleanup after that distinct claim reader rejects the extra pointer.
  Same original raw-hash/claim scalars, private pending proof and one30s read
  allowance survive the last status callback. New16+legacy7+selection4 cases
  PASS27/27 in38.09s wall; independent two identity negatives PASS6.617s.
  Existing status schema/UI are unchanged, with no media or approval projection.
- Independent final/read/workflow review passes22 scoped cases, including the
  five original callback regressions and release-independent historical proof.
  Independent recovery timing/release9 cases PASS25.01s wall: fixed30s proof
  within original300s, actual release tails and final awaited timing bookkeeping.
  No new functional blocker found. Errors retain still-unreleased leases; a
  separately verified completed release is not falsely described as retained.

- Added new-only attempt-local `prepared.json` after the actual completed
  output passes the independent attempt reader, before any journal CAS. The
  actual live completion now holds that publication's raw SHA, size and inode.
  Existing historical pending/final readers remain compatible with older
  records; an already-present preparation entry refuses native replay.
  Preparation9 plus writer17 PASS26/26 in32.42s wall; independent preparation9
  PASS16.77s. Publication-after-expiry retains data without a live capability.
- The new all-prior completed-history reader captures all six files in every
  attempt and five shared original media records before caller callbacks.
  It requires1..32 complete attempts, at most64MiB aggregate actual raw bytes,
  balanced cleanup intent/spawn/reap and the same original claim/reference/name
  coverage. Missing/partial/failed/retired siblings remain unresolved. Actual
  first fixture preparation comes from the writer; added attempts are explicit
  TEST metadata.27 cases PASS50.69s and five additional bounds PASS11.43s;
  independent15 PASS28.76s. A try/loop/if depth3 was extracted; relevant8 cases
  PASS17.00s, lint/TSC/mechanics pass. No native launch fence changed.
- Added a DISTINCT private historical-completion adoption capability and an
  authenticated union at the existing first CAS. It requires the exact current
  precleanup journal, actual project/global leases, full actual current claim,
  all prior completed attempts and the original full cold reservation. It never
  casts history into a live recording, starts another native cleanup or releases
  either lease. An expired old native clock is not reused; the enclosing recovery
  request supplies one protected cleanup clock, never a new render allowance.
  Initial21 cases PASS43.86s; eight callback/lease cases PASS27.97s (one overlaps
  the initial workspace case,28 distinct total). Independent six critical
  cases PASS20.46s. Full-chain adoption/retirement/final-read integration is
  running; no public adoption command/controller activation is claimed yet.
- Root preparation/first-CAS/live-workflow/final-CAS regression96 cases PASS
  92.48s concurrent wall. Actual TEST live workflow measured980.390ms; native
  and enclosing elapsed values remain TEST leaves. TSC PASS6.74s and scoped
  lint PASS1.87s before the final additive test-only cases. These are reliability
  checks, not a first-master timing or audiovisual-quality benchmark.
- The exact five-root current TS closure is now251 files: four additional
  preparation/history/adoption modules, shared source-color254, V2 controller
  union256, V1 still5. Fresh capture/closure/intent19 PASS1.64s. TEST capture
  282files39.254ms,256-pin check38.222ms, AST251files168.910ms. Internal V3
  media-process/media-record modules are not yet reachable from those roots;
  their pins are deliberately not represented as transport-qualified.
- Internal V3 media ownership writer now emits actual V3 intent/outcome and
  V2 activation CAS, preserving truthful failed/unforced settled timeouts and
  refusing missing/unfinished ledgers. Actual completed staging results have a
  private metadata capability, not just a constructed reader.50 metadata cases
  PASS36.969s and eight selected legacy cases PASS0.702s; TSC/lint/mechanics pass.
  These use TEST native/readiness/claim leaves and real TEMP leases/records.
  Independent review found a concrete first-ledger rebaseline: a post-return
  callback could remove an original intent before the first ledger capture,
  incorrectly allowing complete/resolved output. Exact TEST-only RED reproduced
  in3.24s; a separate ancestor-deadline callback exists inside the shared runner.
  An opt-in earliest-settlement capture and final pre-spawn guard are being
  repaired/tested without skipping ancestor deadline enforcement. The original
  50 green tests did not cover this gap. Existing public V2 fences remain; Python does
  not yet consume the source-color flags, so this is not real media transport.
- Actual adopted completion→both CAS edges→exact retirement→final read and
  lost-response pending-service recovery8 cases PASS23.221s. Old native allowance
  is explicitly expired; the same recovery clock survives, native TEST calls
  remain exactly1, and recovered owners are actually released. Initial TEST
  assertion read the wrong result field and was corrected; no production waiver.
- Explicit cold completed-recovery service now shares final proof/release with
  the existing pending service. It is under test, not a public Codex command yet.
  First new10+existing integration8 run:11P7F in37.81s, all seven at a TEST media
  provider that demanded the old live JS object rather than the actual cold claim
  with identical full bytes/metadata. Corrected only that TEST leaf. Next10 run:
  9P1F in24.47s; actual full service1190.463ms passed, while its stale-replay TEST
  expected a later message than the earlier exact publication-identity refusal.
  That assertion is corrected and a full new-suite rerun is pending. No native
  or quality-throughput conclusion follows from these timings.
- Review also reproduced a failed-acquisition release gap: a void/no-op release
  could leave the original actual lock present while returning only the primary
  error. Permanent TEST RED0/1 in2.86s. Original lock/release identity capture
  plus verified release-or-AggregateError is being fixed; successful acquisition
  behavior is not being broadened.
- Subsequent completed-recovery service10/10 PASS25.73s; actual TEST service
  1167.813ms, no native replay. Existing pending dispatch/CLI25/25 PASS26.23s.
  Added direct `completed-cleanup-status` and `recover-completed-cleanup`, a
  closed existing-attempt/raw-preparation request and actual bounded store proof.
  Nine new command/store/contract cases plus six pending transport cases PASS
  15/15 in22.51s; whole TSC6.73s and scoped lint1.88s pass. The first service
  claim provider and replay-message TEST failures above remain retained.
  On-demand status scans all prior completed history; ordinary status does not.
  New store/command independent review and extra service faults are in progress.
- Failed-acquisition release repair is frozen: original acquired lock inode and
  release function are captured before admission callbacks; no-op/unknown
  release yields AggregateError and retains uncertain ownership. No extra unlink.
  New/relevant cold7/7 PASS12.70s; pending parity6/6 PASS11.97s, TSC7.00s,
  lint1.68s and mechanics pass. Success ownership protocol remains unchanged.
- Combined V3 shared-runner correction is frozen. Optional pre-spawn checks run
  after ancestor deadline callbacks and charge their elapsed time to the same
  captured shortest timeout. Optional settlement capture holds the original
  ledger before subsequent ancestor callbacks; hook failures preserve real stop
  facts, and no missing ledger is upgraded to success. Focused69 PASS110.128s
  under concurrent test load; safe legacy8 PASS0.618s. Independent original
  ledger mutation now rejects1/1 in4.44s; additional18 PASS22.64s. Whole TSC,
  lint and13-file mechanics pass. No native or public/Python activation occurred.

- Further independent completed-store/command review: six selected cases PASS
  10.52s. Extra actual completed-service faults11/11 PASS27.856s, including
  no-op global release, stale selection across project acquisition, claim/job
  substitution, and final clock/timing-append mutation after actual releases.
  Initial9P2F were TEST media-provider equality assertions masking the intended
  production claim guard; narrowed only those explicit TEST leaves. Whole TSC,
  scoped lint and mechanics pass. Root pending recovery regression38/38 PASS
  53.69s after the shared service and failed-release changes.
- Added an internal source-only longform base context, retaining the actual
  completed V1 BT709 holder and SAME original opening clock through cut and
  master picture encodes. No presenter owner is fabricated; short/reframe,
  unknown-history, registry, public execution and readback gates remain closed.
  Python metadata/transport cohorts cover87 distinct methods across overlapping
  runs, not one87-test run:31/31 PASS57.12s,61/61 PASS51.37s, final shared-prepare
  parity1/1 PASS6.09s. Initial fixture failures (import path, a method/field name
  collision, and missing TEST plan hash) are not production exceptions waived.
  Independent review reproduced one late-hash receipt-publication gap in0.10s:
  output hashing could invalidate the original guard after the prepublication
  check but before sealing. No successful result escaped, but publication
  happened too early. Repair and its permanent regression are in progress.
- Fresh-capture audit found NO missing production Python pin: existing recursive
  capture already includes all338 local execution imports. New permanent TEMP
  regression verifies341 execution closure rows including schemas,151 grade
  implementation hashes, explicit worker/input pins and345 staged rows. Missing
  context pin is explicitly rejected. Combined28/28 PASS4.61s; Python discovery
  842.049ms, full metadata test3133.222ms, TS251 AST178.797ms. TSC1.97s/lint1.38s
  pass. No redundant production pins, old-capsule rebaseline or native run.

- Both late-hash publication defects are repaired. Master payload/hash now
  finishes before the same original pre-seal guard. The base source-bus pointer
  also checks after payload/hash construction, followed by guards before each
  cache write. Permanent REDs retained: master1 and cache3 (one baseline cache
  case passed). Final focused39/39 PASS6.88s; independent master/holder14/14
  PASS31.78s and exact cache4/4 PASS0.32s. No successful stale return had escaped,
  but stale intermediate publication was real. No native run.
- New internal opening source lifetime holds original admitted snapshots and
  bounded code/control/tool dependencies for repeated cheap checks. It remains
  unwired. Initial tests hit macOS Unix-socket path length and sandbox bind
  restrictions before behavior; canonical short TEMP path plus authorized
  local-only socket test run passed8/8 in0.36s. Tiny100 checks34.928ms, no repeated
  file hashes. Adversarial tests then exposed setup alias substitution and a
  replaceable check method; both were repaired with private pre-hash holds and
  class-method dispatch. Next union13P1F in1.28s rejected both actual attacks;
  remaining TEST failure expected a later diagnostic than the earlier refusal.
  Independent final rerun is pending. Synthetic524-file/32-source fixture took
  282.314ms to hold and278.382ms for26 checks with no subsequent hashes; these
  are not real-video timing or quality evidence.
- Mixed-version hole reproduced: actual V3 process plus exact legacy schema1
  result could pass the old result parser (permanent RED0.553s). V3/sourceColor
  is now explicitly refused before legacy parsing, readback attempt creation,
  readiness/tools or native invocation. New/legacy18/18 PASS3.647s; TSC/lint/
  mechanics pass. Schema2 support is still missing, not implemented by a stub.
- CLI help now explains both pending and completed recovery. First20-case run
  was19P1F because help changed the tested phrase "never retries"; clarified
  wording without changing behavior. Rerun20/20 PASS1.71s; help lint1.67s.

- Final independent lifetime union14/14 PASS0.98s; root after test-only
  nesting/docstring cleanup14/14 PASS1.03s. Both setup-alias and overridden-check
  attacks reject. Original weak-key caller/hold bindings precede dependency
  callbacks; original source capture cannot be replaced. Final synthetic hold
  252.448ms;26 checks273.006ms (10.500ms mean), zero subsequent hashes. The
  helper is still internal/unwired. Findings updated without calling this a
  real-project optimization benchmark.

- Actual completed batches now retain their SAME original preflight, not just
  the prepared source metadata and final observations. Two permanent pre-fix
  REDs accepted same-byte rewrites of original implementation/preclaim records
  after completion (6.170s/6.208s); both now reject. New9 plus existing19 cases
  PASS28/28 in140.907s suite under concurrent test load. No raw rehash, phase
  clock renewal or retired launch/resource callback is introduced. Retained
  original files will supply separate source-color receipt projections.
- Added an unwired live all-source observation projection: exact original
  sidecar/reservation reference, ordered original jobs/artifacts, supplied frame
  records, supplemental BT709 metadata and timings. Every base/color/approval
  grant remains false. Initial7 tests:5PASS2 TEST errors44.08s; SourceOnlyBaseFixture
  used a `change` data property that shadowed its inherited safe mutation method.
  Those tests now call the exact base-class allowlisted mutation helper; fixture
  field rename and source-stable rerun are pending. This data section is not
  the missing durable schema2 base/media receipt or cold read.

Still unfinished: actual V2 controller/process/media/base wiring, shared
reservation retirement/release and partial-prelaunch recovery, source-aware
base/readback, real supported short/long rendering and audiovisual QC, scoped
revision and two-hour first-master timing. C0679 creator history remains
unanswered; catalog render/publish remains denied pending explicit approval. Continue independent work
without manufacturing either approval. Tests above are not output qualification.

### All-source preparation lifetime — September8, in progress

The active goal was verified again on the operator's repeated request. It has
not been marked complete. The new same-process `HeldSourceColorPreparation`
retains the actual successful all-source preparation read, original opening
input/context and SAME verified source objects. Later checks use the original
guard, small typed metadata and source/parent stats; no media/receipt byte replay,
new timer, decoder, job directory or render/grade approval is introduced.
The existing data-only preparation API remains compatible.

- Initial18 new lifetime cases plus32 preparation cases pass0.243s suite/
  0.43s wall. Native/admission work remains TEST-stubbed over inert tiny sources.
- Independent review reproduced a clock-callback replacement on the same
  private runtime object and a resulting final-check input mutation. Both
  were missed by that initial50-test suite. The original runtime/remaining
  callback identities are now bound before caller callbacks, and one final
  pure argument comparison follows every filesystem/clock helper. Three added
  permanent regressions include a final stat-helper mutation;53 tests pass
  0.266s suite/0.56s wall. Independent rerun is pending.
- The launch metadata reader now permits an absent execution directory, while
  the live holder still requires the actual empty private directory and source
  identity. Its data-only return does not invoke an owner callback or launch.
  Independent review found TWO late rebaseline intervals (last parent check,
  then live directory/source setup) accepting request frameCount25 against
  claim24. Both are fixed with the same entry-held arguments;34 tests pass
  0.527s suite/0.70s wall, with the exact failures retained as regressions.

All-used-source job preflight and sequential observation orchestration are now
the active implementation step. Shared TS resource claim/cleanup wiring,
source-aware base activation and real audiovisual qualification are still
unfinished. No native render, catalog refresh, paid API or reset was attempted
in this continuation. The previous catalog-render denial is not retried.

Subsequent all-source implementation and independent checks:

- Final preparation lifetime adds a last SAME-deadline check after metadata
  serialization. The exact final-serialization expiry repro is now rejected.
  54/54 cases pass0.262s suite/0.52s wall; independent54/54 pass0.46s wall.
- All-used-source job preflight validates exact first-kept source order,
  complete declaration/profile coverage, actual original controller parent PID,
  code inventory, exact grade/opening preclaims and the SAME source capture.
  No execution directory, decoder, timer or lease is created. It retains raw
  metadata and code stats for later checks, not a cross-call hash cache.
- Independent/self-adversarial review exposed a fresh parsed-input interval:
  changing the returned `ownerPid` to0 during implementation validation was
  adopted by a later snapshot. The job is now retained immediately upon the
  shared reader's return, before subsequent helpers. The original failed
  regression remains.28/28 batch tests pass27.395s suite/27.68s wall;
  independent28/28 pass27.32s wall, including mixed explicit V1/V2 cases.
- The sequential batch runner now calls the existing owned project observer
  in the SAME Python process, immediately seals each actual returned result,
  and reports each source's queued/work/failure time. No new outer timer wraps
  protected cleanup. A virtual90s+90s case keeps the first result after its
  original1120 phase expires while the second remains within1210; overall
  deadline1300 is never renewed. Completed data uses the persistent original
  preparation guard, never a retired transient launch/resource callback.
- First13 runner tests had11 passes and two TEST constructor-arity errors,
  not a decoder failure. The corrected failures pass6.50s wall; the full13
  pass46.792s suite/47.12s wall and independently47.86s wall. Two additional
  final earlier-source file-mutation/replay tests pass11.60s wall.
- Review caught missing final validation time in `elapsed_ms`. The final
  timestamp now updates ONLY its scalar slot in the OLD held metadata binding;
  it cannot rebaseline changed observations, contexts or approval flags.
  Four dedicated final-timing/expiry/mutation/origin tests pass21.09s wall,
  independently20.87s wall. Preparation time remains a separate caller stage,
  and outer request-to-master wall time must still include all stages.
- Current unique26-module Python regression union: **359/359 PASS**,
  108.198s suite/108.62s wall. This includes19 batch-execution cases,28 batch
  preflight cases, preparation, grade/launch/project/ingest contracts and
  existing legacy cases. Actual project/publication/sealing and current-code
  pins run, but admission/decoder/native leaves remain explicit TEST stubs.
  This is NOT a natural-video or two-hour quality/performance result.
- Shared TS color-resource acquisition now reuses the existing workspace
  namespace/lease protocol without acquiring a project lease or a new clock.
  Legacy source observation reuses it; six core legacy service functions are
  independently AST-equivalent, including existing120s work/5s cleanup checks.
  Source inventory includes the new helper. Review found inherited `existsSync`
  false absence for a dangling active marker; no-follow `lstat` now treats
  only ENOENT as absence. Markers are retained and only the newly acquired
  lease is released on failure.41/41 TS tests pass1.87s wall; whole type-check
  and scoped lint pass. This does not activate opening resource staging.

Subsequent transport/staging work (still not a released execution route):

- Additive explicit source-color/V2 request parser preserves the old V1 parser
  and requires exact used-source coverage, explicit profile/history/declaration
  and contiguous bounded lighting groups. New9 plus legacy4 TS cases pass;
 61 pure actual Python declaration/profile parity cases run in one process.
  A sparse caller source-ID array hole was reproduced and fixed. Whole TSC/lint
  pass. The parser does not prove that a declared endpoint covers actual media.
- The shared code inventory now offers an original-callback wrapper without
  converting/restarting a monotonic clock. Legacy walker/hash/limit/exclusion
  behavior is unchanged;49 inventory/resource/service/process/finalization
  cases pass1.72s wall, independently1.76s. This is not a faster code-walk claim;
  inherited depth3 walker/declaration structures remain unchanged.
- TS source expectations retain the exact original input,14 raw document refs,
  saved parents, source-set receipt and individual admission receipt metadata.
  They preserve first-kept source order and declared frame-count expectations;
  no source bytes, native calls or jobs occur. Review caught unjoined original
  document SHA/size fields and missing exact FPS/six-hour bounds. Those cases
  now reject;50 tests pass2.51s wall, with whole TSC/scoped lint passing.
- `assertCurrent()` is a metadata lifetime check, NOT a hard-deadline or launch
  capability. Its final stat sweep can consume the caller's last remaining time.
  This limitation is OPEN and documented: actual controller integration must
  recheck the SAME original remaining budget before work and retain Python's
  original hard-wall timer. Appending another arbitrary callback after the
  sweep would weaken final-callback file checks and was deliberately not done.
- New private TS staging writes a complete exact-name reservation BEFORE any
  grade-job directory/input/preclaim, copies one executed-snapshot inventory
  into distinct immutable job files, retains explicit V1/V2 policy/declaration,
  and publishes a separately hash-bound sidecar. Prospective execution dirs
  remain absent. No worker, route, source decode, resource release or approval
  is activated. Partial failure retains the complete reservation and metadata.
- First staging11-test run:10 passes,1 TEST error expecting16 preclaim fields
  instead of the actual existing15 (2.33s wall). Independent review also caught
  a genuine cross-language V2 policy mismatch; corrected version/policy/profile
  now passes the actual Python input reader with raw files and actual parent PID.
  The resulting12 cases pass2.59s wall. A final expectation callback releasing
  the resource lease is now followed by the original resource assertion; its
  regression is added. Current staging13+expectations50+inventory8 union is
  **71/71 PASS,3.141s suite/3.17s wall**. Sources are absent, admission/runtime/
  original clock authority are explicitly inert TEST metadata, and only the
  exact temporary fixture resource uses an actual lease. Independent review
  of the current staging changes is pending.
- A shared-window Python cleanup-only coordinator has14 virtual-daemon tests
  plus10 existing cleanup regressions (24 pass5.53s wall; independently5.45s).
 128 exact names share one3s VIRTUAL stability window, not128 separate windows.
  This is not measured Docker throughput. Review found a replaceable context
  binding method masking runtime changes and constructor filesystem work
  outside the wall timer. Both repairs/regressions are IN PROGRESS; coordinator
  is not qualified or wired into any cleanup/release path yet.

Later current results:

- Cleanup repairs reproduced both original failures before fixing them.
  Non-dispatchable original bindings reject substituted runtime controls before
  any daemon leaf; one original wall timer now includes filesystem setup.
  The real30ms/80ms TEST delayed setup now interrupts39.093ms including unwind
  (previous90.49ms), returns false cleanup diagnostics, and launches nothing.
 27-case union passes2.491s suite; independent27 passes2.470s suite/5.58s wall.
- TS staging now has a separate `assertUnstarted()` for prospective execution
  paths. A later job's callback creating an earlier job's dangling execution
  entry rejects before staging returns. `assertCurrent()` remains metadata
  lifetime only, including after actual execution legitimately creates dirs.
 15 tests pass3.28s wall, independently3.38s, before additional pin tests.
-18 explicit new staging/contract/helper TS pins are now included from the
  executed snapshot and held against later metadata changes. Legacy standalone
  inventory behavior is unchanged. Old snapshots missing a required new helper
  reject before reservation;17 staging plus8 inventory cases pass4.77s wall.
  Current controller-versus-original-lock comparison remains an integration
  requirement, not supplied by these data-only inventory pins.
- Pure Python sidecar/reservation contract18 cases pass0.11s wall, independently
 0.16s. Actual TS-produced sidecar/reservation plus V1/V2 job inputs pass the
  actual Python contract/input reader in0.82s wall (independently0.79s).
  The raw-reference reader is being implemented; no worker route is activated.
- NEW integration blocker: actual source inventory has1150 files and2415
  guard calls,241.596ms with an inert guard. Existing durable clock history
  permits only512 distinct observations. A real-file513-stamp TEST accepts512
  then fails, consuming17.29s wall. The quadratic scan/capacity issue is being
  repaired before wiring high-frequency checks to `budget.remainingMs`.
  No guard skipping, original-clock renewal or deletion of history is authorized.
  See `docs/findings/CLOCK_CHECKS_MUST_NOT_EXHAUST_HISTORY.md`.

Latest frozen components and integration regression results:

- The authenticated live Python staging reader now holds exact sidecar,
  reservation, original claim/input raw bytes and original caller context.
  It joins actual parent PID, runtime and first-kept source order without
  reading job/source bytes or launching work.23 reader+18 contract cases
  pass0.83s wall, independently0.80s; final-callback same-byte reservation
  replacement rejects. This live-parent reader is NOT cold cleanup authority.
- Current unique32-module Python union:417/417 PASS,121.984s suite/122.62s
  wall. This includes staging contract/reader and repaired batch cleanup.
  Native/admission/daemon leaves remain explicit TEST stubs, not video QC.
- New schema2 clock scheduling state retains one monotone watermark per
  execution while preserving every legacy immutable observation. Every call
  still validates history/lease; file and directory fsync remain. The wrapper
  charges persistence to the SAME original remaining clock before returning.
 82 clock/claim/deadline/readback cases PASS38.76s wall. The2,415-check case
  completed29,580.663ms with one record, versus the old failure at513.
  This remains material metadata overhead, not a video throughput result.
  The512 total-entry bound remains; saturated old history is not reset.
- Staging now has22 additive TS pins, including four clock dependencies.
  Final staging/inventory rerun and actual original-budget/inventory
  composition are in progress. Actual controller current/pinned comparison
  and Python closure activation are still required.

Next: carry explicit V2 intent/process bindings through the existing controller
and actual Python owner, and connect versioned color cleanup to actual
outer/nested worker settlement. Component readers/cleanup are implemented;
the public workflow is not activated by those components alone.
The existing 14-document opening input and claim V1 are not silently expanded.
No color application, presenter-base fence or delivery approval is enabled by
any of the observation-only work above.

### Preclaimed color launch integration — September8, in progress

The private isolated grade policy now has an optional owned entry point using
the exact externally preclaimed job/container name. It keeps the original
decoder request, resource limits, before/after source hashes, worker checks and
90-second protected cleanup implementation. Legacy callers retain their
original random-name interface and evidence. No product route is activated.

- The helper binds raw grade/opening input and claim bytes, original runtime
  controls and source/count/profile, and compares the actual argv against the
  existing builder. It publishes new-only launch intent/response files, fsyncing
  the file AND directory; failures retain partial evidence.22/22 metadata tests
  pass0.328s suite/0.50s wall. The first17-case fixture setup failed because a
  temporary Unix socket could not be created under the sandbox; no native
  launch occurred. The replacement fixture explicitly stubs only socket type
  over a TEST sentinel; it does not claim a real daemon.
- Root policy+legacy grade91-case union passes1.090s suite/1.35s wall. The
  current11-case policy suite passes0.689s suite/0.95s wall, including real
  preclaim files, current source/code hashes and actual intent publication
  with all native leaves explicitly stubbed. No source admission/AV proof.
- Independent source review caught a phase-clock gap at result publication.
  Owned success publication now remains inside the exact effective phase
  cutoff, not merely a longer enclosing deadline. A virtual121s publication
  delay rejects under the V1 120s cap even with300s overall remaining. A
  separate20s preclaim-acquisition case proves preparation consumes the same
  original request cap; it does not restart afterward.
- Lost launch response and post-launch publication failures both enter the
  unchanged exact-name cleanup path. A pre-runtime owned failure conservatively
  retains unknown cleanup state. Neither this helper nor its JSON authorizes
  project/resource lease release or crash recovery by itself.

Subsequent independent review and wiring:

- The launch helper/policy33-case independent suite passes0.959s suite/1.13s
  wall. The unique wider low-level116-case suite passes2.047s/2.31s; the earlier
  129-case2.36s run accidentally repeated the13-case policy module and is NOT
  129 unique tests. All native leaves remain explicit TEST stubs.
- A second timing counterexample exposed the boundary between the project and
  low-level runner: a scheduling pause AFTER calculating relative remaining
  time could extend the actual project phase. The independently reproduced
  50s pause moved1120→1170. `OwnedGradePhase` now carries the SAME launch owner
  and original absolute phase cutoff, and the caller transmits that exact
  cutoff. The permanent10s-delay regression first produced1130 instead of1120
  and now passes. It does not rebuild the owner or derive a fresh clock.
- Another fault test showed untimed owned directory setup:30ms remaining plus
  an80ms setup stub failed only after87ms. Directory/source setup and result
  directory creation now run inside the same effective phase timer. Independent
  rerun observes29.237ms on the active timer, interrupts the setup, returns
  after39.760ms including fixture unwind, and launches nothing. Cleanup's
  unchanged protected timer remains separate; no outer work timer wraps it.
- Minimal optional fourth-launch plumbing in `color/grade_project.py` now
  reaches the owned isolated runner. Original three-argument behavior, original
  typed context/source, actual reader/file evidence and completion lifetime
  stay unchanged. Final138-case project/input/launch union passes2.305s suite/
  2.51s wall. Independent original50s scheduling repro now retains1120
  instead of1170, with the same owner;86 focused cases pass1.243s/1.43s wall.
- The worker's job-input and implementation reader is now shared with the
  future same-process owner; actual parent PID, raw input SHA, profile, paths
  and expected parents are preserved. Both worker and new shared-reader code
  pins are required. Review found an inherited null `implementationSha256`
  bypass of `bound_json`'s optional hash argument. It now rejects at BOTH input
  and direct verification boundaries before inventory IO/discovery. Independent
  original repro rejects;33-case input/project/V2host suite passes0.126s/0.30s.
- Required implementation discovery expanded from24files/174959bytes to
  147files/1337679bytes in the first stable helper comparison: three calls have
  medians48.886→304.627ms. This provisional pre-final-wiring measure is code
  verification overhead, not media throughput. Removing only the direct input
  constants import changes none of the147 files; no used-code exemption or
  ineffective constants extraction was made. Final stable three-call measure
  after phase wiring is148files/1341050bytes, median307.377ms, against a fresh
  retained-capsule median51.339ms for24files/174959bytes. All path/SHA rows
  matched within each cohort. This adds256.038ms per inventory call; actual
  used dependencies remain pinned. It is not a decode/render benchmark.
- Final18 phase tests cover original owner/cutoff types, scheduling, original
  metadata/callback drift, active setup/mkdir timers, nested timer rejection and
  cleanup after expiry. Phase+policy+helper51-case independent run passes
  1.109s/1.36s wall. Root's final unique21-module regression union passes
  **278/278,3.629s suite/4.02s wall**. Restored scanner source still matches
  SHA04a66fa1a1d492a24bd6e0cbd48d24e1c3c9cf98c9603514e3e5956d35e812f0.
  New source/test functions satisfy the mechanical limits. The policy's
  pre-existing three-level cleanup nesting is AST-identical and deliberately
  unchanged; the newly added execution nesting was reduced to two.

All-used-source staging, shared TS color claim/reconciliation, source-aware base
work, native activation and actual short/long qualification remain outstanding.

### Cold body read closure and matched guard optimization — September8

- Root cold full-body presenter/caption readback now joins the unchanged
  opening report to the ORIGINAL opening observations, and the full-body report
  to the CURRENT body observations. Independent review exposed a false refusal
  when those valid attestations differed; the regression now passes. The body
  oracle also rejects extra fields against the actual producer's20-field
  schema2 envelope, including a forged `qcPassed` field.25/25 body/direct-route
  tests pass16.691s suite/19.78s wall. Independent original repros pass their
  expected outcomes1.596s work/4.70s wall; seven reviewed files meet300/50/4/2.
  Repro evidence: `/private/tmp/sniper-presenter-caption-body-adversarial-fixed-20260908.log`.
- The same profiled cold read, with identical2cues/2pages,15local+45external
  files and all605 outer checks/1211 held fences/117369 lstat calls retained,
  improves4.8628→1.2364s read time and5.31→1.72s command wall. The private
  comparison now holds a detached exact-type metadata tree instead of repeatedly
  rebuilding canonical receipt serialization. Public hashes, byte observations,
  callback checks and source authority are unchanged. These profiled metadata
  timings are NOT video throughput.11 focused tests pass; root23-case route
  regression passes5.428s suite/5.78s wall after correcting one stale expected
  error message in the earlier84-test cohort. Finding:
  `docs/findings/PRIVATE_METADATA_GUARDS_ARE_NOT_RECEIPT_SERIALIZERS.md`.
- All-used-source color preparation validates the explicit complete source map,
  original source capture, saved parents, admission receipts, declarations and
  profile limits before returning DATA ONLY.32 focused tests and192-test union
  pass0.38s and2.56s command wall respectively. Independent32-case review passes
  0.139s suite/0.31s wall, with no concrete scoped identity/deadline/profile
  blocker and all four files meeting the mechanical limits.
  No jobs, native calls, resource claims, profile widening or grade authority.
- Live full-body caption/presenter binding passed46 control-flow tests, but
  independent review then reproduced a last-caption-callback substitution of
  the presenter runtime deadline. That cohort does not close this newly found
  defect. A construction-time owner/runtime/source/tool fence now rejects the
  runtime substitution, observed graph-width mutation and expiry of the SAME
  original clock after the last caption callback. Corrected50-case cohort
  passes21.749s suite/24.94s wall; independent original three repros now all
  reject6.76s command wall. No native result or approval is claimed.
- Current full TypeScript type-check passes6.64s command wall with incremental
  caching disabled. This is static checking, not UI or full workflow proof.

Next integration dependency remains exact prelaunch color-resource ownership,
same original generation deadline and independently proven cleanup. The existing
presenter execution/color fences remain in place; the broad goal is active.

### Opening/grade independent closure and remaining cost — September8

- Fresh repaired live caption binder77/77 passed17.776s suite/20.87s wall,
  including17 focused cases; source recovery SHA remains exact. Both fresh
  import orders and owned-file mechanical checks pass. Evidence note:
  `/private/tmp/sniper-presenter-caption-live-binding-20260908.md`.
- Cold caption reader10/10 passed31.934s suite/32.20s wall after canonical
  parent walks were reduced to one initial sweep plus original node identities.
  The earlier7-test62.78s baseline is a different cohort, not an identical A/B.
  A separate profiled single read had2cues/2pages,15local+45external files;
  setup0.179s/read4.863s/setup-through-cleanup5.045s/command5.31s.144 streamed
  file observations create605 caller guards and1211 metadata fences. Nested
  canonical serialization accounts for3.772s cumulative, overlapping with
  guard time—not additive. Byte reads/callbacks were not removed. A narrower
  private metadata-fingerprint optimization is in progress, unqualified yet.
  Diagnostic: `/private/tmp/sniper-presenter-caption-cold-profile-20260907.log`.
- Root opening/caption route27-case run had26pass/one stale legacy test patch
  (`read_inputs` no longer imported); corrected to actual `read_current_inputs`
  and added explicit A/V-not-called assertion.13-case legacy contract passes
  0.010s suite/0.43s command. No source or media refusal was weakened.
- Independent audit found a genuine local before/after rebaseline gap: a fresh
  guard at each verification could accept A/V-callback changes to media/ranges.
  The outer raw-result readback was a separate defense, not an excuse for the
  local gap. Added a single retained record/audio/authority/tool/lane snapshot
  across both checks. A further int0→float0.0 sample mutation exposed canonical
  hash normalization; a private Python-type spelling fence now rejects it
  without changing receipt hash domains. All three independent repros reject.
- Root50-case combined opening/caption/body readback passes13.565s suite/
  16.85s wall. Prior50-case attempt had one TEST expected-error mismatch for
  a tool mutation caught by the earlier original acquisition guard; expectation
  and TEST cleanup were corrected. One TEST nesting issue was then extracted
  into a single-case helper; no production behavior changed in that extraction.
  These are actual tiny held-file checks with explicit A/V stubs, not native
  decoded video, creative acceptance or full current renderer activation.
- Completed grade evidence now retains the SAME actual observation, source,
  files, guard and original overall deadline. Sealing requires a still-live
  original phase; only later data reads may outlive that phase within the same
  overall deadline. Original phase authority is never renewed. Independent
  construction binding also rejects a replaced longer-lived context before
  sealing.153/153 passed2.176s suite/2.41s wall; independent62-case audit passed
  1.045s suite/1.23s wall, no further concrete defect. No native worker, applied
  grade, source declaration, resource claim or cancellation policy changed.

Next bounded work is full-body caption/presenter evidence, the identified
private guard cost, and explicit all-used-source color preparation. Original
source declarations, durable shared-color ownership/cleanup and source-aware
base integration still precede any presenter activation. Broad goal stays active.

### Opening read integration and recovered TEST fault — September8, 04:24 UTC

Goal remains active; the broad workflow and real-output qualification are not
finished. Current new presenter execution remains fenced before media work.

- The current opening reader now holds original source/tool/base identities
  around its existing A/V phase and requires a typed read-only lane for the
  exact new presenter profiles. Missing/JSON lanes reject before A/V work.
  New caption/full-program/manual-short field selectors preserve the four
  explicit new tokens without upgrading legacy profile parsers. Root58 pure/
  stub integration tests passed1.016s suite/1.32s command. An earlier56-case
  run had55pass and the existing stale-motion-catalog failure; a separate
 57-case run had one TEST legacy-fixture setup error, corrected explicitly.
  The stale catalog was not waived, refreshed or removed from its own test.
- Independent grade-handoff audit repairs now reject the two original
  late-publication source/observation mutations.128 tests passed1.674s suite/
  1.91s command; independent37-case audit passed0.606s suite/0.677s command.
  This qualifies only typed data handoff, not color application or native
  source observation. Completed-observation lifetime handoff remains in work.
- A new live caption fault test incorrectly selected `held.external[0]` as
  if it were owned TEST data and overwrote untracked production source
  `scripts/producer/audit/audit_glitch_scan.py`. All agents stopped writes
  and tests for recovery. The prior13-case hot-import live cohort is invalid
  as final evidence, even if individual assertions passed. This was a test
  ownership bug, not an intended source edit.
- Restored exact original bytes with apply_patch from the retained capsule
  `/private/tmp/sniper-capsule-actual-20260907.rEbRi7/owner/capsule/repo/scripts/producer/audit/audit_glitch_scan.py`.
  Both independent durable BEFORE03:54:16.935Z and AFTER03:54:59.297Z inventories
  record size13013 and SHA256
  `04a66fa1a1d492a24bd6e0cbd48d24e1c3c9cf98c9603514e3e5956d35e812f0`.
  Root independently checked both inventory rows, restored SHA/size and exact
  `cmp` against the capsule. No HEAD/reset or inferred reconstruction was used.
  Bytes are recovered; mtime/ctime necessarily changed, so preincident live
  stat holds must still reject and must not be refreshed into old authority.
- TEST mutation now explicitly selects `caption_pages.json`, verifies exact
  canonical TEST-root containment plus held-file membership, and rejects
  aliases/nonregular/multiple-link targets. Fresh no-write negative guard
  tests passed3/3. Live/cold cohorts are being rerun in fresh processes; their
  final qualification is pending. Shared dependency lists describe inputs,
  never permission to modify them. See the incident finding for the rule.
- Root opening-caption route now delegates graph validation to the cold
  caption reader exactly once per boundary rather than duplicate it in both
  wrappers. New actual-held-file tests bracket explicit A/V stubs and check
  missing proofs, late mutations and expiry. These tests are authored but
  not yet executed at this checkpoint. The cold-reader7-test62.78s baseline
  exposed excessive repeated parent walks; optimized guard timing remains
  pending, and no speedup is claimed before the fresh run.

The denied catalog/Docker launch remains untouched. No real ten-minute master,
human listening, creator approval, automatic grade or two-hour claim follows.

### Cold body evidence, durable V8 input and original clocks — September8, 03:56 UTC

Goal verified active again. These are completed bounded integration foundations,
not completion of the broad editor goal or permission to enable presenter renders.

- Python CURRENT intake now recognizes the four exact new opening/body pairs,
  keeps actualV8/V2 and whole-candidate conflict/workload checks, and preserves
  old selectors. All four actual current intake routes still refuse the real
  `audio_policy_reason` registry result: `presenterLayouts` is unreleased. No
  field stripping or relaxed audio policy.132 pure/stub tests passed3.764s
  suite/6.99s wall, including16 new cases. Root added explicit opening/body
  execution-entry presence fences before frame/template/base/media work, so
  a future metadata/registry change cannot activate legacy presenter omission.
- Data-only opening/body read evidence now shares the live schema2 graph-hash
  constructor without making a live owner from JSON. Body readback reconstructs
  full and opening/future windows, exact caption tails, actual original asset/
  tool inventory and complete original prefix ranges; ordinary retained-output
  and full media/QC checks remain required. Source identities reuse the initial
  hash-pass capture. The cold reader pins the actual ffprobe and performs one
  extra original-budget base hash because its supplied base reference lacks a
  same-pass stat; it never decodes selected presentation media again.
- Independent audit caught a final-callback future-plan mutation after the cold
  reader's previous last check; fixed using the shared whole-input/capture guard.
  It also found integer/float tool-size normalization, now exact-type checked.
  Root then asked for a same-inode parent-symlink test: the leaf's9-field stat
  remained identical and the old context incorrectly returned. All original
  captured source parents are now checked around the stat sweep, including
  unselected talking sources. Normal context exit rejects the swap. Both exact
  remaining repros were independently rerun and reject,3.31s wall.
- Root75-case pure/stub regression passed5.181s suite/8.41s wall before the
  ancestry addition; four cold-route wiring tests passed0.541s/3.77s with actual
  tiny held files but explicit stub prior graphic/approval metadata. Final44
  acquisition/cold-body cases after ancestry repair passed3.937s/7.12s. The
  earlier65-case8.95s cohort also included two existing actual tiny owned-
  composition media tests; it was NOT an aggregate bounded performance cohort.
  Mechanical and legacy/import review passed; all new root files<300lines.
- Optional caller deadline propagation is implemented for the owned planning
  process path and compatibility projection. Absent-scope behavior remains
  unchanged. Initial durable attempt failed before media: Node/Python absolute
  monotonic origins were not interchangeable here.109.9ms callback/0.91s command,
  one owned group stopped without forced cleanup; failure log/workspace kept.
  TEST handoff now passes a capped decreasing duration while the original Node
  parent/group deadline remains authoritative.10TS+2Python controls pass; TSC
  2.22s/lint1.64s pass. No production env flag, new clock, purchase or provider.
- Corrected durable attempt PASSED: real generated testsrc2/tone bytes, previsual
  cut gate, private cut preview, raw intake, actual advisors/store/readiness
  storage and warm/cold14 full documents. Actual proposal8/evidence8/bindings2
  and presenter-caption profile survive cold reload. Source admission decode/
  image facts, writer, two cut critics, two readiness critics, full-plan gate
  bundle and cut acceptance are explicit TEST stubs. No real human acceptance,
  presenter/opening/body render, finishing eligibility or delivery is inferred.
  Setup9.685517s, readiness3.653689s, total work13.973432s, Node subtest13.975099s,
  complete command14.60s. Original300s callback work/60s owned-group ceilings
  stayed unchanged. All15 owned groups were ESRCH-observed and independently
  confirmed absent. Legacy native leaves rely on inherited owned-group backstop;
  their requested per-leaf timers are NOT all qualified as<=60s.
  Separate1,924-file precheck0.407s/postcheck+comparison0.290s matched digest
  `2a43af34d7dea8db3effca89c37ddae03683bba09c3f991b45e0c60900cb482c`.
  These are outside the test callback/command, with orchestration gaps not
  mislabeled as processing time. The exact successful fixture project was
  removed; the parent evidence directory and failed first attempt remain.
  Evidence: `/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-v8-presenter-durable-QjkfTY/TEST-EVIDENCE.json`.
  Logs: `/private/tmp/sniper-v8-presenter-durable-input-20260907-first.log`,
  `/private/tmp/sniper-v8-presenter-durable-input-20260907-second.log`,
  `/private/tmp/sniper-v8-presenter-durable-input-20260907-second-groups.json`.
- Typed actual grade-observation handoff was implemented with118 initial
  V1/V2/worker/publication tests passing1.598s/1.84s, no native worker. Independent
  audit then found TWO missed late-publication bugs: a final guard could mutate
  the source context SHA before return, or change the actual nested observation
  flags/dimensions while the immutable report stayed original. Singer is fixing
  these now; the initial green suite is not release qualification. The object
  still expires at the observation-phase cap; later render integration needs a
  separately designed held-data handoff under the original aggregate owner,
  not a renewed clock. Original-source declaration/history, color resource
  ownership, nonnested ALRM and color application are still unresolved.

No denied catalog/Docker refresh was retried. No real10-minute/two-hour or
real-person audiovisual quality claim was made. Next independent work remains
caption-clearance-to-picture binding and actual source-color orchestration.

### Adversarial range fix, body graph join and current TS intake — September8, 03:20 UTC

Goal remains active; public end-to-end presenter execution is not yet released.

- Independent review reproduced an actual graph-record mismatch: mutating the
  outer combined clip list at the compositor call could add an encoded clip
  without changing the tuple-held saved graph. Root now detaches the new lane's
  command list and checks both original and actual-command outer inventories
  before/after each encode and after context exit. Add/remove/replace/reorder
  tests pass and stop before a second encode. Independent reviewer reran18
  root opening/profile tests successfully,0.332s suite/3.39s wall. Legacy tuple
  behavior is unchanged. Retained raw observation JSON is projected/hashed once
  per asset rather than on every repeated guard; call-count regression added.
- Fresh actual tiny opening still/video cohort after these changes passed2/2,
  4.363s suite/8.02s wall:
  `/private/tmp/sniper-presenter-observer-media-g7mvhx7g/TEST-opening-presenter-evidence.json`.
  All36 encoded RGB frames still match the same-range full original graphs.
  Original setup-inclusive180s clock retained; no1080/natural-source/audio or
  creative qualification is inferred. Earlier successful cohort remains kept.
- Root108-case combined Python regression passed10.652s suite/14.72s wall,
  including acquisition/frames/clearance/legacy/profile/registry controls.
- TS current opening/body intake now accepts the four exact new profile pairs
  using full original context and actualV8/V2. Historical selectors/parsers,
  human approvals, default proposal version and stdout stages are unchanged.
  Fresh and cold14-document metadata paths, all-body disjointness/workload and
  conditional captured source requirements have65PASS/1opt-inSKIP,2.40s wall;
  full TSC3.42s and scoped lint2.49s pass. These are synthetic metadata fixtures,
  not admitted media, real readiness eligibility or completed execution.
- New internal body prefix join validates the held opening as READ evidence
  and uses only its separately acquired live body owner for the new graph.
  Original full/future windows, options and same held pages remain required.
  BodyGraphicsOwner forwards the optional actual presenter through existing
  hooks; production orchestration does not yet supply it.25 body-prefix/legacy
  tests passed0.799s suite/4.09s wall, including6 new actual control-flow tests
  with explicitly stubbed decoded/media facts. Earlier43-case legacy/base/
  registry cohort passed7.113s suite/8.61s wall.
- Base/unowned cache fences are implemented: exact presenter context at render
  entry, prepublication and base-sidecar write; CLI refusal before source/auto-
  base work; presenter picture reuse refused.80 tests12.130s suite/13.48s wall,
  final16 new controls0.104s/.50s. No registry/cache-domain activation or audio
  changes. The live base branch still rejects unresolved original source color.

Next work is active: data-only cold presenter receipt verification; typed actual
grade-observation handoff without invented camera/history or a new clock; and
optional caller deadline propagation through planning/projection processes.
Existing grade V1 requires explicit known history and has pixel/source/time
limits; V2 is xvYCC-only, not a larger BT709 profile. Shared color ownership and
timer orchestration are not solved by passing a Python object. Compatibility
projection currently has an unbounded legacy process path; deadline-scope work
is being tested before a bounded durable metadata/store scenario. No denied
catalog refresh, paid API, provider or new Docker launch was attempted here.

### Live opening range integration and native frame comparison — September8, 02:54 UTC

The completion goal was verified active again; no new goal or completion claim.
The already retained1080p optimization comparison below remains unchanged.

- Root added four explicit Python presenter metadata classes with native target,
  exact guided clock, explicit caption choice, one-source manual-short geometry,
  full-program graphics/presenter disjointness and aggregate native-input cost.
  Existing public profile selectors still reject any presenterLayouts property.
  Numeric0 cannot stand in for caption burn:false; booleans cannot be cut speed
  or audio leads. The TS counterpart has the same intended new-class limits.
- Actual acquisition now consumes same-initial-hash source identities, keeps
  original tool/file lifetimes, validates original actualV8/V2 frames, all graphics
  and combined workload before selected decoding, and requires actual observed
  asset dimensions/cost to match before yielding. Repeated assets share one
  observation while every occurrence counts. Body entry and readback pass their
  existing clocks into the initial source read. Hubble's160 pure/stub union
  passed1.686s suite/2.19s wall;31 new acquisition/frame/body cases. This is not
  real isolated source admission or native1080 rendering.
- New presenter-caption clearance uses the actual retained cue alpha bounds and
  the swept manually protected subject envelope across entry/hold/exit, with
  explicit16px gutter and bounded work. All21 new tests plus adjacent75-case
  union passed3.341s suite/3.56s wall. Retained TEST caption files are real;
  decoded presenter facts are stubs. This is precomposition geometry, not face
  tracking, slide-text legibility, picture proof or creative approval.
- The existing opening range compositor now has an internal live-presenter
  context. It retains original full-window geometry, selects only windows that
  intersect opening review, preserves future-window identity, binds actual
  range output hashes to its graph record, checks original ownership/base
  identity before/after each encode, and retains unchanged legacy tuple fields.
  It does not activate a public profile or make a body-prefix/clearance claim.
- Root30 pure/metadata tests passed0.217s suite/3.37s wall. The initial30-case
  run failed3.23s wall because new internal tuple metadata was passed to the
  strict JSON canonicalizer; corrected to explicit JSON arrays. An earlier
  28-case run failed0.50s wall: one test incorrectly rejected primitive reduced
  integer rate spelling, and inherited caption-fixture lint found the already
  stale catalog. New profile clock spelling was subsequently made explicit;
  the catalog failure is neither fixed nor waived.
- Actual low-level opening media tests passed2/2,3.641s suite/6.64s wall:
  `/private/tmp/sniper-presenter-observer-media-_7yt1gk0/TEST-opening-presenter-evidence.json`.
  Generated64×36 PNG and NTSC video use returned real observations without a
  second selected decode. Core6 + review12 encoded RGB frames for each case
  (36 total) match identical-range encodes using the full original graph,
  including crossing and future windows. Production complete-decode and exact
  packet-clock checks also ran. One original180s setup-inclusive fixture clock
  and per-child60s cap; actual subprocess/owned command timings are retained.
  TEST admission metadata, no captions, no audio comparison, no native1080 or
  full-workflow performance/quality qualification.
- Independent cache review found that base-fingerprint exclusion alone would
  allow stale presenter picture reuse. Generic/unowned/CLI-auto-base refusal
  and base-only live ownership are being implemented before any cache-domain
  activation. Original talking-source color is still not qualified by source
  admission's retained fields; selected slide observations or late base tags
  cannot substitute for source-aware color handling before lossy encoding.

In progress: TS actual-context admission/current-token integration, original
source-color/base ownership, opening/body receipt/readback/prefix/clearance
integration, then complete real-output tests. The53-template current-catalog
refresh remains permission-blocked; no retry or alternate launch was made.
Ten-minute/two-hour and long/short audiovisual acceptance remain incomplete.

### Full-resolution bottleneck, exact-pixel optimization and source capture — September8, 02:24 UTC

The goal is still active. Production presenter-layout profiles are not released.
The first real1920×1080 textured30-frame sample exposed unacceptable mask cost:
still47.219787s/video50.108235s for1.001s of picture; the still prefix check cost
151.082885s. The original300s cohort correctly failed in the video prefix and
reaped all28 exact process groups. Nothing renewed that attempt or lowered quality.

The mask expression now computes repeated scalars once per pixel using four
locally initialized FFmpeg expression registers. There is no previous-pixel,
previous-frame or cross-thread state dependency. Mask arithmetic, ramp/frame
origins, native resolution, color conversion, cubic resampling and libx264
CRF12/veryfast settings are unchanged.

The distinct optimized cohort passed in54.711089s (57.85s shell), with all30
process groups observed absent. Still encoding fell to5.873505s; video6.414887s;
still prefix18.987182s/video19.384358s. Exact comparison passed for **every one
of the30 decoded native-yuv420p frames in all three outputs**, and all six
completed old baseline/still pre-encode streams. Actual generated inputs,
tool/encoder and unchanged TEST-helper bytes match. Full code-closure differences
are recorded rather than hidden. See `docs/findings/PRESENTER_1080P_MASK_COST.md`
and `/private/tmp/sniper-presenter-1080p-x_ui1q_9/TEST-pixel-comparison.json`.
This is about8× faster for this effect, not a ten-minute/two-hour guarantee or
natural-person visual/creative/audio approval. The old failed root remains
`/private/tmp/sniper-presenter-1080p-2r3kbvg4`.

The original source verification now optionally retains its same-hash-pass
nine-field FD/path identity under the original clock. The production opening
worker captures this ephemeral result during its existing initial read; no
extra source hash, receipt-field change or weaker final revalidation is added.
It supplies the missing input for later live presenter acquisition, not authority
from an arbitrary constructed dataclass. Focused agent70 tests passed8.86s wall.

Actual V8 proposals with **no presenter operation/property** now keep their true
V8/evidence8/bindings2 through the existing profiles. Nonempty or even null/empty
candidate layout properties still refuse legacy execution. Python independently
rechecks original V8 policy/packet, not just schema flags. Exact helper closure
requirements and original body metadata context are retained in TS.

Root checks so far:33 geometry/native tests PASS5.94s wall (including all-byte
old/new mask parity for inset/bubble/split with still/video);103 pure/metadata/
registry tests PASS10.79s wall. Independent root review found no bounded blocker.
The expanded import closure exposed27 registry discovery gaps: actual new layout
reads are now explicitly **unreleased-rejected**, and caption-page manifest
reads have exact file/key exceptions without permitting those keys in the actual
asset manifest. The initial8-test registry run failed7.27s wall; corrected8/8
passed7.25s. Initial37-test V8 fixture run failed0.60s on an unrelated TEST-only
sentinel; corrected render-shaped fixture passed37/37 in0.61s. These attempts
are retained, not counted as performance qualification.

Next: connect actual live acquisition and distinct opening/body profiles, retain
and independently read new presenter evidence, prove caption/subject clearance,
and test the real creator-footage workflow plus scoped revision. No denied
53-template Docker/catalog refresh has been retried or indirectly substituted.

### Actual presenter observation, live composition and prefix proof — September8, 01:48 UTC

The completion goal remains **active**. This slice connects actual selected
picture observations to an internal live compositor owner; it does **not** yet
release V8 through the production opening/body profile or qualify a real edit.

Python now independently validates actual V8 request/policy/frames/candidate.
Only the existing crop/music validators receive a local V7 validation view;
all stored/readiness/authority documents remain actual V8. The executed closure
requires both exact V8 and V7 schemas. Existing profile fences remain intact.

The new picture observer borrows the caller's already hash-linked source/tool
identity and original deadline. It reads actual header and complete decoded
frame evidence: geometry, SAR, progressive picture, exact cadence/PTS/durations
and explicit supported color. Initial video is8-bit BT709-limited MP4/Matroska;
initial still is opaque explicitly tagged sRGB PNG. PNG chunk checks reject
hidden ICC/HDR/orientation/animation/alpha metadata rather than guess a transform.
These observations do not establish admission, rights or natural-person framing.

The live owner joins every original selected occurrence to those observations,
checks later-window offsets/coverage separately, and reuses one observation for
repeated assets. Its exact requested plan, source/tool references, original
clock, typed snapshots and isolated graph copies are bound at assembly. The
ordinary assembler now refuses a presenter plan without that live owner.

The existing compositor/prefix oracle accepts a distinct internal presenter
graph pair. Crossing windows retain full original geometry/ramps/asset origins;
all-future windows yield an empty opening selection, not an omitted body graph.
One shared hash covers the actual full graph, base, original clock, selected
input bytes and caption tail. A new schema2 proof/layer policy cannot be replaced
by a legacy clip-only proof. Base + each graphic/caption occurrence + each
presenter-window input share the existing64-MiPixel work bound. The visual order
is presenter, ordinary graphics, then caption pages, followed by range trim.
No selected asset is decoded again merely to enter the prefix consumer.

Actual tests caught two receipt transport issues: raw PNG metadata bytes were
not JSON serializable, and integer stat nanoseconds exceed JS-safe precision.
The new proof uses explicit lossless ordered PNG hex payloads and decimal-string
stat identities; live file/stat checks retain exact integers. Both JSON and the
cross-runtime canonical writer now have regression coverage. Adversarial review
also fixed within-frame deadline checks and Python's `0 == False` snapshot trap.

Measured formatting optimization: on identical actual retained probe objects,
compact JSON preserves every parsed field and cuts video output15,826→10,274
bytes (35.1%); still2,572→1,704. No frames/fields/caps were removed. This is not
a demonstrated production-time saving. The final video fixture contains genuine
AAC audio, so output audio exclusion is not tested only on silent inputs.

Final root checks on frozen code:

- **286/286 Python tests PASS**,65.494s runner/**69.45s wall**. This includes
  actual observer, new live owner/prefix/encode, full output decode, all-frame
  reference comparison, and existing body/caption/graphics regressions.
- **46/46 TS tests PASS**,0.802250s runner/**0.83s wall**; whole TypeScript
  **PASS6.83s wall**. Independent mechanical/adversarial passes found no remaining
  issue within this bounded internal-owner slice; new logic meets300/50/4/2.
- 48-frame actual owner cohort:
  `/private/tmp/sniper-presenter-observer-media-5qqvga9e/TEST-owned-composition.json`.
  Still owner+full-decode QC1.899149s; video0.842782s. A genuine equal-opening proof
  for changed future geometry is rejected by the live caller.
- 24-frame presenter+TEST caption-role page cohort:
  `/private/tmp/sniper-presenter-observer-media-m58sw4c2/TEST-prefix-media-evidence.json`.
  Still2.586108s/video1.046954s including same-graph ordinary-reference encode and
  complete RGB comparison; every final frame matches that reference.
- Final observer cohort `/private/tmp/sniper-presenter-observer-media-4x5imcjt`;
  actual final primitive pixels `/private/tmp/sniper-presenter-pixels-ayrphsjg`.

All native inputs above are synthetic64×36. Actual tools/output are unstubbed;
**source admission references remain explicitly TEST metadata**, not genuine
isolated external-media admission. Caption-role test pages are not actual caption
authority or legibility qualification. No real-person framing, listening,
source-aware grading, full-master or ten-minute/two-hour quality claim follows.

Failures remain counted: initial root owner25-test run24PASS/1message-regex FAIL
**7.19s**, corrected25PASS**7.04s**. First simultaneous native prefix cohort
**6.31s** and owner cohort **8.16s** both failed on PNG proof serialization after
their rendering assertions; output media/probe logs survive in `dgvjyny_` and
`47mev4or` fixture trees, but the failing combined receipt could not be written.
Those overlapping attempts are not serial performance benchmarks. A subsequent
pure canonical transport test caught the stat-nanosecond problem before rerender.

Next: measure representative1080p work before assuming two-hour feasibility;
connect the distinct actual opening/body profile and held-source observation
acquisition, cold readback, layout-aware graphic residual reference and caption
clearance; then real creator-footage/full audiovisual QC. The denied53-template
Docker/catalog publication still has no new approval and remains untouched.

### Presenter authoring, retained source selection and profile fences — September8, 00:48 UTC

The goal remains **active**. Actual V8 authoring now connects to versioned evidence,
the configured subscription-provider schema, candidate construction, immutable
storage/replay and historical reading. Default compilation remainsV5. This is an
explicit internal qualification path, **not a released presenter render workflow**.

The new policy projects all bounded admitted project b-roll metadata, without raw
paths or rights claims. Both motion and b-roll must be system-owned under the exact
accepted target/context scope and lane overrides. Controller anchors and retained
cut occurrences determine each window's ordered unique source IDs; frames, ramps,
geometry, disjointness and original operation indices are independently rederived.
The candidate adds only `presenterLayouts`; V2 frame bindings keep these separate
from ordinary graphics. Existing cuts, captions and audio are not reauthored by
this projection. Mixed music/captions/layout/catalog tests preserve actual indices.

The pure Python selected-asset bridge joins these windows to the owner's already
verified source-set entries, including exact byte size (not just path/hash),
source-or-broll lane, media kind and receipt. It does no new file scan, source hash,
decoding or rights approval. Real three-second synthesized bytes now pass V8
compile/store/cold-read/history and the source-set→selected-asset hash/receipt join.
**The inherited admission runner stubs decoded-media facts and probe-image identity;
this is not qualification of real isolated external-media admission.** A separate
no-op case passes replay without another provider call. Creative/compiler/acceptance
responses are also explicitly TEST ONLY. These tests do not execute the requested
presenter layout or qualify creator footage.

Adversarial review caught the existing selectors ignoring a new plan field. All
six old fresh/held media profiles now reject any present `presenterLayouts`, even
null/empty/false, including the direct Python held-profile entry. Actual V8 is
refused before readiness critics, gates, source work or legacy opening execution.
This fence remains until a distinct owned graph/prefix/QC consumer is connected;
an old renderer must not silently deliver a video without the requested layout.
Review also corrected Python/JS identifier whitespace parity and allowed only the
exact proposal-authored pair of graphics-style target fields in the new assertion.

Root verification on final code:

- **156/156 TS regression tests PASS**,1.250555s runner/**1.29s wall**.
- **8/8 candidate+source-store tests PASS**,21.803129458s runner/**21.83s wall**;
  six repeat the regression cohort and two are actual synthetic source/store
  roundtrips with TEST admission facts (10.359589s and10.705808s). Do not count
  those six twice. This is the final honest fixture-label/brief-corrected run;
  the earlier passing cohort took24.56s and remains recorded separately.
- **94/94 Python tests PASS**,11.214s runner/**14.37s wall**, including six
  tiny actual presenter-media tests3.904621s. Retained observations:
  `/private/tmp/sniper-presenter-pixels-836iydcg/observations.json`.
  Actual24-frame picture-only encode0.223567s; zero audio streams. Cached and
  uncached still references remain raw-byte identical; no production speed claim.
- Whole TypeScript **PASS6.69s wall** on final fixture labels/brief (previous6.92s);
  root18-file lint **PASS2.02s**,11-file focused lint **PASS2.08s**, final4-file
  fixture/candidate lint **PASS1.66s**. Independent reviews and strict profile/
  identifier parity tests pass. Scoped tracked diff whitespace check passes.
  Final review identified that inherited admission was stubbed, and that the
  presenter test reused a contradictory no-asset brief. Both evidence labels and
  the test's exact separate presenter request/clause/rationale were corrected;
  no production gate or admission fact was weakened.

Failures remain part of the accounting. First catalog-dependent storage setup
failed9.67s wall with `matrix is stale: motion source digest changed`. It was not
refreshed or waived: the passing cases author an explicit graphics-off intent
before any pin/acceptance and make no measured-catalog claim. A later two-case
test run was1PASS/1FAIL21.48s wall due to a nonexistent test-only Python import;
the discovered existing `bound_json(Path(...))` reader fixed it. Earlier profile
and identifier RED cases are retained in the turn verification note. The previously
denied53-render Docker/catalog-publish workflow still has no new user approval.

Remaining presenter work: actual bounded selected-asset picture/color/CFR/SAR
observation, Python actual-V8 request/evidence rederivation, a separate opening/body
profile and render-root mapping, held input/command/graph/prefix proof, layout-only
reference for graphic residual checks, unchanged-caption clearance and real
presenter framing/audio/full-output QC with timing. No ten-minute/two-hour proof
or natural-person quality claim was added. Details and exact commands:
`/private/tmp/sniper-presenter-authoring-20260908-verification.md`.

### Presenter primitive and V7 history correction — September8, 00:07 UTC

The completion goal is still **active**. This is connected low-level compositor
work, NOT a released guided presenter owner or a completed video editor.

Fixed an actual historical compiler allowlist omission: current V7 music
proposals were rejected by `guided-proposal-history-snapshot` although V7 schema
bytes were correctly pinned. V2–V7 now validate; unknown/string/V8 versions still
reject. Exact schema SHA, captured TS closure, bytes and runtime checks remain.
Initial targeted RED was6PASS/1FAIL,0.26s wall; the corrected focused cohort was
26PASS0.64s. Independent review found no broadened old-code execution authority.

New opt-in V8 authoring/schema describes timed presenter-layout-window operations
without losing real clause/beat/anchor indices. V1 geometry declares ordered
sourceIds, held-base-display crop, a manual protected envelope, real inset/bubble/
split masks, an admitted-asset name and rational start, full enter/exit frame ramps
and discarded asset audio. It is not continuous tracking or an observed subject.
The existing default/current dispatch remains unchanged. Independent review
caught schema boolean/integer const coercions; explicit types now reject both.

New pure Python geometry retains exact normalized tuples, validates final and
continuous affine framing, and compiles smoothstep expressions on global frames.
It preserves subject aspect and the original/full final written frames. A
normalized→pixel→normalized floating-point roundtrip and split-tile false rejection
were fixed without clamping or widening accepted geometry. Pixel-index/edge-space
half-pixel mapping is explicit. These are declared-envelope checks, not face QC.

`CompositeOptions.presenter` now connects an explicit low-level graph to the
existing single picture encode: immutable input0 branches → presentation and
transformed presenter → ordinary graphics → unchanged caption tail → outer trim.
Legacy layer helpers were moved unchanged to `graphics/composite_layers.py` with
public reexports. `compose_owned` explicitly REJECTS presenter options until new
asset/provenance/prefix/QC owners are integrated; old graph proofs cannot cover
the new layer. No plan root/profile/default was silently enabled.

The bounded primitive requires no upscale,8-bit yuv420p held base, square pixels,
exact even presentation cells, disjoint windows, canonical representable rational
FPS and sufficient same-cadence video coverage. Opaque RGB stills use explicit
display-referred sRGB→BT.1886/BT709 limited-range relative-colorimetric conversion,
not nominal camera BT709 OETF. New graph metadata is caller-supplied; actual byte,
clock, source lineage, ICC/mastering/color policy and admission checks remain
required. Base `setparams` records the declared policy, NOT a source grade.

Actual64×36 local probes exposed two different classes of failure. Rounded
Matroska millisecond timestamps lagged the exact image clock by one presenter
frame; a common exact frame-index lattice fixed masks and all interior opening
trims without changing frame counts. A nominal camera-OETF assertion did not
match installed libswscale's display-referred mapping: RGB128 producedY131,
consistent with ideal BT1886 Y131.616, not nominal OETF Y115.018. The policy and
independent equation are now explicit, with1LSB quantization allowance. Separately,
the synthetic video's encoder auto-converted untagged YUV before it reached the
graph; the fixture now declares its generated color before encoding and compares
against independently decoded source frames. Failed attempts remain retained:
`/private/tmp/sniper-presenter-pixels-{uqtgk385,fmpvzf_3,ibpow3j0,kc978zxs}`.

Pre-cache root regression: **59/59 Python PASS**,9.988s runner/**13.16s wall**.
Includes17 geometry,7 graph,5 new actual-media cases and existing compositor,
prefix-contract, owned-graphics and caption regressions. Final new media cohort
3.290s; actual24-frame MP4 encode/probe/decode0.217s, one video/no audio streams
despite real presentation-asset audio. Exact outside-window pixels, all three
masks, NTSC entry/hold/exit trim parity, source-frame offset and midgray transfer
passed. Evidence: `/private/tmp/sniper-presenter-pixels-sao113lq`; its JSON retains
observations, not unittest pass authority. Actual encoder/pixels here are tiny
synthetic evidence, not throughput, natural-detail resampling, full color/alpha,
real framing, full caption/graphic occlusion, final audio-master or user approval.

Root TS/history/revision cohort: **91PASS/1 opt-in integration SKIP**,22.914s
runner/**22.94s wall**. Whole TSC PASS6.89s. Counts include overlapping earlier
cohorts, not separate qualified videos. Adversarial review additionally matched
10 historical ordinary graph strings and20 command lists; that is ordinary-helper
parity, not an exact immediately-pre-change full source snapshot comparison.
Root seven-file ESLint also PASS1.90s.

Final static-still correction: decode/contain/color/pad once, then finite
graph-local repetition of that prepared frame. No intermediate encoded video.
The actual comparator restores the prior repeated decode/color work and confirms
all82,944 raw bytes identical. Two tiny observed cached/uncached pairs were
0.342/0.310s and0.282/0.326s; this noise-dominated fixture does NOT establish a
wall-time speedup. The removed repeated work is structural, not a claimed
ten-minute throughput gain. Final **60/60 Python PASS**,10.731s runner/**13.94s
wall**; six new actual-media cases3.904s,24-frame actual encode/probe/decode0.217s.
Latest evidence: `/private/tmp/sniper-presenter-pixels-5lu09_fq`; earlier cache
comparison: `/private/tmp/sniper-presenter-pixels-f03m2y3g`. Final independent
pure/code check13PASS confirms finite loop, unchanged video semantics and all
touched root logic/test300/50/4/2 limits. All exact assertions remain; no failing
quality check was removed to claim this low-level completion.

Next: connect admitted source/asset frame bindings and held still/video transport,
versioned graph inventory/hash and opening/body/cold-read owners; layout-only
graphic-presence reference, actual caption/framing clearance and natural-detail/
mask/color qualification before enabling V8. One-request/finishing/revision work
and real short/long total-time qualification remain as listed in the checklist.
The denied53-template Docker/catalog update was NOT retried; no Docker, provider,
download, paid API or original creator-media operation occurred in this slice.
Exact commands/remaining boundaries are retained in
`/private/tmp/sniper-presenter-implementation-20260908-verification.md`.

### Direct source/brief cut implementation and clarified layouts — 23:15 UTC

The goal remains active. The operator's parallel task relayed a material scope
clarification: presenter framing is full talking head ↔ inset/bubble or split
presentation, with smooth transitions, correct framing throughout the visible
interval and no blank/jumped frames or audio discontinuity. Continuous
face-following is not required. Existing PiP/static primitives are not yet a
connected/qualified workflow. Planning should search the FULL HyperFrames
catalog first, reuse compatible items, adapt selected items only as necessary,
and retain integrations. Do not require catalog-wide porting or hand-build an
available equivalent. Item-specific asset/content/scrub/render compatibility
still matters, as does saved-plan round-tripping. Current Studio base+graphics
projection is unchanged; no full native source/effect-track redesign is promised.
The intended roughly20minutes of raw, possibly several recordings, followed by
trim/collaborative treatment/execution/Studio review aims for about two hours.
Our separate preparation/cut and120-minute generation allocations do not yet
prove that total user experience. Both intervals and total must be reported.

`guided-project.ts author-cut <request.json>` now connects an already admitted
manifest, exact full validated brief and explicit supported target metadata to
the ordinary subscription-backed cut authoring/review/preview path. It neither
ingests/transcribes nor creates human review. A distinct closed authoredCut
policy retains the exact saved empty seed, original transcript digest and
preparationStartedAt. Existing candidate bootstrap remains read-only. A saved
seed version2 may be preserved/incremented; fixed target fields cannot change.
Numeric FPS1–60 is supported as target metadata; rational strings were rejected
after actual target-reader inspection exposed their incompatibility. Preview
source aspect/cadence is not proof of a final canvas/cadence conversion.

The existing initial writer runs once; deterministic previsual gates and two
independent clean cut reviews remain. Evidence-backed revisions keep the same
six-review ceiling, one fresh conflict recheck and source-timing stop rules.
Explicit editor/protocol/provider failures precede binding/normalization; a
genuine changed plain-timeout candidate retains ordinary recovery only within
all guards. No empty seed, reused draft or fake writer completion is adopted.
Actual source verification, parent-PID handoff, observed subprocess scope,
unknown-cleanup retention, preview PAUSE and strong acceptance guards are shared.
One original120-minute PREPARATION engineering allocation includes request
reading, pinning, queue/lease, writer/review/preview and settlement. It is not
the later generation clock, a resettable retry, an OS-independent watchdog or
proof that interrupted descendants disappeared. Timely PAUSE remains readable
while a human reviews it. Repeated UUIDs only read retained state.

Independent adversarial review found two concrete gaps and both were corrected:
(1) the lower job store could persist complete/rendered despite the worker's
preview-only refusal; parser AND common write boundary now prohibit completion,
downstream checkpoints and delivery artifacts for both bootstrap policies;
(2) a late replacement after a successful plan check could pass the final
"not seed" comparison. Current guards now hold the validated plan hash and
reobserve plan/journal/project/manifest/seed/transcripts after code checks;
seed/draft predicates use that checked identity. A TEST callback reproduces
speed1→2, transcript and journal changes during the authorization interval.
Neither reproduced issue was presented as an actual media-approval bypass.

Root integration before that final race fix:128/128tests PASS4.76s wall.
Final race/review/metadata subset16/16PASS2.34s; independent loop extraction
matched full traces across8scenarios in0.157s. Separate existing guided PAUSE/
preview and prompt cohort21/21PASS7.71s. These counts overlap and are not summed.
Source/brain/preview boundaries are TEST stubs; one test captures/restores the
actual repository code closure. Initial root test failure was an overly narrow
expected error regex (`unsupported fields` was the correct rejection), fixed
without changing production admission. Agent fixture typing errors were fixed
to the actual workflowV2 and numeric seed factory, not by relaxing the contract.
No real provider, media workflow, creator decision or original source was run or
changed in this implementation slice. Current full audiovisual/throughput
qualification and the denied53-render/catalog-publication permission remain
outstanding. This is a finished implementation slice, not a finished goal.

Final root post-fix integration: **145/145PASS**,8.810978s runner / **8.84s whole
wall**,28test files covering source/brief intake, stage/writer, actual critic-loop
packets with stub providers, recovery, ordinary jobs, quiescence, clocks and
guided preview/PAUSE. Whole TSC **PASS7.49s**,root11file ESLint **PASS3.74s**
(concurrent with the regression run). These final counts include the earlier
cohorts, not additional independent145 outputs. Independent final exact-race
repro now rejects;34overlapping tests PASS2.687s and applied loop extraction
matches the original branch statement AST. New guard/loop/test logic satisfies
300/50/4/2 limits; inherited unrelated limits are not claimed remediated.

#### Next bounded owner integration: presenter plus presentation

Independent read-only mapping found actual ordinary static-hole execution in
`graphics_stage._clip_record` → `pip_hole.hole_clip_fields` →
`composite_core._pip_hole`. It is fixed rectangular placement plus template
rounded mask, not animated full-frame ↔ inset. `motion/reframe_split.py`
`apply_split` makes two crops of the SAME baked-in source for9:16; it does not
combine a presenter with a separate presentation asset. `pip_takeover.build_graph`
has useful easing/crop/mask mathematics but no normal renderer/assembler caller,
hardcoded1920×1080, separate encodes/AAC and unbounded subprocess execution.
Do not invoke that demo as the new guided owner or call its fixed4:5 starting
crop/dissolve a proven geometrically continuous full-frame transition.

Actual guided blockers are TS`guided-opening-media-input.assertMediaMetadata`,
Python`guided_opening_frames._presentation` rejecting active holes/placement,
the closed `guided_body_prefix._actual_clip` result fields, and
`guided-proposal-candidate.graphicCandidate` rejecting unresolved asset-bearing
templates. Manual-short input is fixed fill/crop/track:false, not split. The old
`presenter.animatedPip` release matrix combines tracking with PiP and must not
be enabled as a shortcut for the clarified narrower requirement.

Smallest proposed next execution class: one versioned post-cut presenter-layout
operation with one held presenter source and one admitted manifest.broll asset,
exact frame/occurrence window, manual crop, destination geometry and bounded
enter/exit ramps. Start with an explicitly named rounded-inset class, then
separately implement/qualify split and circular variants; do not call one class
all requested layouts. Reuse graph math inside the existing shared picture
compositor BEFORE captions. Read presenter pixels from the held original base,
not an already composited branch containing earlier graphics. Keep the full
program frame origin and compose before opening trim. Still and video
presentation assets need separately explicit executable timing/rate semantics.

Required connected boundaries include GraphicsComposition/OwnedGraphicsExecution,
`guided_opening_graphics.py`, `guided_body_prefix.py`, their result/readback
adapters, exact asset/layout provenance and versioned proposal bindings. Retain
the current prefix oracle, workload/deadlines/cleanup, shared master, combined
picture, packet-copy checks and full AuditB. The fourteen-document manifest
already carries source-set admission; no caller path may stand in for it.
Test actual intermediate geometry and return endpoints, opening-boundary
crossings, captions, blank/duplicate frames, unchanged presentation geometry,
audio/sample continuity, changed assets/geometry, cold readback and failed
cleanup. Existing PiP math/static-hole/same-source-split tests are NOT this
guided qualification. No implementation or media run for this next class has
occurred yet. Full-catalog-first discovery must remain distinct from measured
compatibility of the selected item.

### Current continuation — revision recovery, music authoring, catalog permission

The goal remains **active and unfinished**. The attempted current53 catalog
workflow was denied by the escalation reviewer **before process creation**.
No Docker request, render or catalog publication ran. Retained preparation and
denial: `/private/tmp/sniper-capabilities-current-okEim2/LAUNCH-BLOCKED.md`.
Original preparation kickoff21:15:08.347608UTC is retained, not a completed
benchmark. The user has been asked explicitly to approve the resource-intensive
local53-render/catalog-publication workflow. Do not retry indirectly, remove
publication as a workaround, or relabel old measurements. Source/media freeze
was released because no process owns resources. No paid APIs were used.

Post-cut full-brief replacement is implemented through `guided-treatment.ts`
`revise` and explicit `reconcile-revision` commands. It preserves the accepted
cut, target, context, token, picture-lock head and original generation clock.
New immutable parent-linked admissions invalidate only unapproved proposal/
readiness pointers; complete prior briefs and proposals remain historical.
Pending reservations block old-brief compilation/readiness/opening and status
rechecks the reservation independently of the journal. Recovery requires the
same exact retained request under the existing lease; no new key or clock.
Any opening/body pointer or retained launch ownership blocks this first revision
class. It does NOT implement changes to accepted lane/finish intent or scoped
replacement of an already selected/approved opening/body/master.

Root independently read the production/history/request code and protocol tests.
Independent rerun: **45PASS/1explicit actual-media integration SKIP**,
22.925s suite / **22.95s whole wall**. The32-admission boundary alone took17.096s;
this is a protocol stress test, not ordinary creator latency. Entire project
type-check PASS6.42s wall; scoped lint/standards PASS. The actual fresh cut→new
brief→new compiler/readiness integration still needs its real media gate and
must not be called qualified from these inert tests.

Explicit **nondefault V7** music authoring now connects the TS request/evidence/
candidate/14-document reader/schema route to the existing source-float-v2 music
engine. Python's independent request/source-set bridge is implemented and tested.
The default remains5. First slice selects exactly one admitted PROJECT music
asset, with explicit finite gapDb[3,40] and duck:true, across the full program.
The accepted target must already have music:true and no inherited plan.music.
**22:03UTC correction:** target.music alone was not actual intent authority.
Ordinary starter/authoring paths previously omitted this mirror. Fresh authoring
now copies explicit true/false from the submitted intent; new bootstrap strictly
checks the mirror against actual intent BEFORE project creation. V7 fresh/stored
evidence, candidate (including no-op) and opening intake independently compare
the mirror with the strongly read accepted job.ctx.intent. That context is already
bound to the original accepted decision and journal snapshot. No accepted plan is
retrofitted. A historical intent-true accepted cut without the mirror remains
unsupported by this V7 slice; supporting it needs an explicit revision/proof path.
The existing fourteen worker documents have contextHash but no context preimage,
so Python rederives the exact locked mirror and manifest rather than inventing
operator consent. Historical proposal versions2–6 retain their music-preservation
behavior; only actual7 adds strict mirrored-metadata checks. Previsual cuts may
legitimately defer the bed; the ordinary full finishing gate still requires its
eventual enabled state to match the actual stored intent.
It cannot override music-off intent, replace an old bed, use a raw path, search,
download, infer beat matching or silently choose a bundled track. Builtins are
counted as unavailable in this first authoring class. Metadata/source admission
never implies rights clearance, creator choice, listening or delivery approval.
Existing fitting loops/crossfades a short bed and applies the ending fade; the
original dialogue bus and one shared full-program master remain unchanged.
Historical packets cannot add/remove/change accepted music. An explicit V7
graphics-off four-field cut plan shares V6's exact no-graphics dependency
omission; it receives no measured catalog/render authority.

Root TS regression cohort **62/62PASS**,0.791s suite/0.82s wall, followed by an
added mixed music/crop/caption/graphic index test: V7 **14/14PASS**,0.457s suite/
0.49s wall. Four earlier broad-cohort failures were stale TEST setup (V2 proposal
paired with version1 evidence, missing schema, obsolete validation-order
expectation); fixtures were corrected and negative version/schema assertions
retained. No production gate was weakened to pass them. Independent reviewer
also passed44 V5/V6/V7 tests and found no TS authority bypass. None of these
results qualify actual V7 audio, the complete short CLI, or the ten-minute target.

Final independent root continuation checks after the intent fix: **67/67 TS**
proposal/caption/crop/music/catalog contract checks,0.879s suite/**0.91s wall**;
**22/22 TS** bootstrap/intent/command checks,1.201s suite/**1.23s wall**;
**172/172 Python** music/reframe/input/admission/body/operator-intent regressions,
0.926s suite/**4.03s wall**. Whole-project type-check **PASS5.93s wall**; scoped
lint **PASS1.76s wall**. Independent reviewer separately passed18 focused TS
checks and found no remaining concrete intent-binding defect. Real media,
models, source decoding and creative acceptance are not inferred from these
metadata/sentinel/mocked-admission tests. The default proposal version remains5.

The actual-return caption-observation race is fixed: the renderer's original
returned runtime/layout/media identities must match the independently reread
sealed output, except the explicitly verified retained input archive addition.
Agenda and dense pipeline upper layouts have explicit separate observation
classes. Caption adapter cohort167PASS; independent NPL review39PASS. Actual
combined caption+graphic opening/body tests remain unlaunched. The prepared
two-case TEST lower-owner harness has completed adversarial corrections for exact
returned-byte reads, imported source holds and ledger identity. Both cases now
retain the FIRST actual source-inventory reference; a changed non-owner Python
or TS file between cases fails before the second operation instead of creating
a new baseline. Root rerun:15PythonPASS0.173s suite/1.18s wall;7NodePASS0.203s
suite/0.23s wall. No actual media run occurred. The harness still requires
current catalog authority and separate media GO; it is not the authenticated
14-document CLI or human approval. Do not substitute native-only geometry tests
for the missing captioned-composite qualification.

### Cut brief and authoring follow-up — 22:15UTC

The ordinary cut-writing prompt omitted the saved creative brief even though
its critics received stored intent. The prompt now includes the original brief
as validated, bounded JSON DATA. No truncation, permission expansion, tool/model
changes or human-review inference. Downstream requests stay deferred; unsupported
cut requirements receive an explicit blocked marker. Existing no-brief prompt
hashes, literal commands, speed and source-timing walls remain unchanged.

An independent root mechanical check found the visual authoring prompt already
exceeded repo limits (316lines and a62-line builder). Pinned read/command inputs
were extracted to `authoring-prompt-inputs.ts`, without moving the authoring
policy or changing its text. Review caught one omitted command callback at the
allowlist call site; that failure is retained, and the call was fixed. Root final
prompt/provider-STUB/reference/doctrine/brief/intent cohort:12PASS0.663s suite/
0.69s wall. Independent reviewer additionally reproduced24 exact full-prompt/
allowlist comparisons and6/6 requested regression cases; new helper is captured
by the existing pipeline source scan. Builder50lines; main/helper238/85lines.
These are prompt/code tests, not a subscription brain's real editorial output.

The direct source+brief command remains missing, despite reusable ordinary
cut writer/review/preview stages. It needs a distinct new-only authored-cut
policy: original source/intent/target holds, one aggregate preparation budget,
no timeout-draft promotion after a blocked result, retained child cleanup and
the actual human cut PAUSE. Do not opt the supplied-candidate policy into writes
or pretend the full starter plan is a qualified previsual seed. Structured
`CUT_AUTHORING_BLOCKED` propagation was subsequently fixed in the bounded
adapter/stage slice below; this does not implement the new launch policy.

### Brief-aware cut review and source-timing revision — 22:41UTC

The existing critic packet already contained the stored intent; it was not
necessary to add another authority document. The critic now explicitly compares
cut-stage brief clauses with exact kept-word/source evidence, keeps all fixed
target fields immutable except predicted duration, and distinguishes deferred
downstream work from completed work. A real packet/adapter test with a STUB
provider preserves Unicode, quotes, newline and hostile delimiter/tool/approval
text as the exact JSON data value. A changed context brief fails before a second
stub invocation. Read-only/tools-none/no-additional-directory settings remain.

The fresh revision prompt still carried an obsolete instruction that the ASR
dead-air rule could forbid restoring an opening span. That contradicted the
current critic and source-timing wall and could cause avoidable critic/writer
conflict. It now preserves disputed spans, defers the exact affected issue code,
requires source-grounded review in the existing JSON receipt summary, and never
infers silence or invents acoustic evidence, timestamps or human listening.
Genuine evidence-backed cleanup remains required. The full brief uses the same
validated untrusted-data line as initial authoring, while retaining revision
JSON/issue accounting rather than leaking an initial-writer stop-marker protocol.

Root prompt/critic/revision/loop cohort14PASS1.739s suite/1.76s wall; earlier
combined authoring/critic cohort20PASS2.508s suite/2.53s wall. Whole TSC5.81s and
root scoped lint0.90s passed. These overlapping cohorts are not additive counts.
Independent reviewer:3-file critic cohort3reported tests PASS1.867s suite/1.89s
wall; original test fixture/gate bodies matched exactly after extraction. New
fixture53lines is shared by both relevant tests instead of copied. Changed/new
prompt/test functions meet the bounded mechanical limits. No model/provider,
media, actual cut decision or human approval ran. Terminal-result transport
fault review is recorded below; broad goal and real render QC remain open.

### Explicit authoring failures preserved — final bounded checks22:43UTC

Actual authoring adapters with injected providers/PID-less streams reproduced
the initial failure: a root `CUT_AUTHORING_BLOCKED` message could return exit0
without a structured stop, so a valid leftover plan entered normalization and
review. The controller now carries distinct editor-stop, transport-failure and
provider-failure state and rejects before session binding, draft normalization,
timeout salvage, critics or approval. Later success-looking text cannot clear
a failure. Tool output, prompts, nested messages and nontext thinking/tool blocks
are not root editor decisions. Unknown/missing stop reasons remain explicit and
bounded rather than becoming inferred success.

Adversarial review first found and reproduced invalid UTF-8 replacement,
malformed known assistant content, and the exact Codex output-budget exception
missing its early-failure classification. Three new tests failed before the
fixes. Claude now uses fatal incremental UTF-8 with EOF validation; both adapters
validate known editor shapes and cap aggregate output at16MiB. Root then found
malformed terminal result arrays/missing text and explicit `is_error:true`
success-looking results could still be ignored or accepted. Two further tests
failed first; terminal result shapes now fail closed and legitimate error
envelopes without result text remain a distinct provider failure, not a crash.
The shared Codex runner was not changed: this slice observes its parsed events
and exact existing cap exception; it does not establish raw-byte UTF-8 validation
inside that generic runner. Existing owned-process cleanup and later explicitly
requested manual-resume semantics were not redesigned.

The adjacent provider test initially emitted an incomplete agent_message with
no text and failed the stronger contract. Only that stub's emitted/expected text
fields were corrected; production validation was not relaxed. Original plain
nonblocked timeout recovery still passes. Retained reproduction/commands:
`/private/tmp/sniper-authoring-blocked-20260907-verification.md`.

Root final10-file overlapping authoring/brief/review/revision cohort:
**33PASS**,2635.505ms suite/**2.66s whole wall**; whole TSC**6.19s**, scoped
ESLint**1.86s**, diff whitespace check PASS. Independent final transport review
reran its4-file subset19PASS2.411s. Those numbers count node:test cases and
existing assertion-style file suites, not33 rendered-video or model trials.
Changed functions remain within bounded standards; untouched inherited nesting/
parameter debt is documented in the retained note, not silently declared clean.

No actual provider, Docker, media render, source correction, human listening,
cut acceptance or delivery ran in this follow-up. The denied53 catalog workflow
still needs explicit trusted-user approval. Its original2700s preparation
window has elapsed while blocked; retain that attempt and do not reset/relabel it.
All remaining goal sections and the direct authored-cut launch remain unfinished.

### Full captioned CLI result — completed 18:26 UTC

Fresh five-minute synthetic command chain completed with exit0 in
**3573818.171ms (59m33.818s)**; external wall3574.41s. Source preparation,
actual cut preview, proposal/readiness, original opening, TEST-only approval,
body, cleanup, final readback, read-only replay and conflicting-request rejection
all executed. No live model/provider calls, purchases or downloads. This is
**mechanical PASS, visual qualification FAILED**, not a creator-approved master.

Workspace:
`/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-body-live-lyq4t3`.
Producer is `test-only-cut-review-15571697/producer`; body request
`514724fa-82e2-4270-bb74-daa8decac548`, execution
`1139ce8b-d571-4440-9ad0-ca5a961985ef`.
Candidate466551822bytes,311.144s,9325frames1920x1080@30000/1001,
SHA`852be3ac91a05bcb68931c1cb7e3baa9dfbcc353a32a6fae300d57a8b009483a`.
Body candidate hash`a774fbd388755f7218e9da7f6be50b45661e230f422929dce38498ca1f99adb2`;
cleanup hash`8dec9ddf3075dcb62236dfcf376d6f248e9437981ea1bc408489ba643e1485a4`.

- Setup/preview/proposal/readiness:128723.723ms17:26:33.771–17:28:42.496UTC.
- Original generation clock:17:28:25.804UTC; never reset for the body.
- Opening attempt805174.582ms plus733.182ms cleanup; selected17:42:44.626UTC.
- TEST-only approval31723.917ms, no genuine listening/creative acceptance.
- Body CLI2564230.294ms17:43:18.985–18:26:03.192UTC. Actual owned worker
 2156653.588ms, complete18:19:53.229UTC, no timeout/forced stop, group stopped.
 Cleanup fact18:19:56.159UTC; remaining readback/qualification stayed charged.
- Body status1607.807ms; identical no-render replay1693.573ms. Different request
 rejected and exact protected state/inventory checks passed.
- Ordinary full caption stage471612.946ms for81shards/11pages; cut87748.009ms,
 master68923.781ms. These are stage spans, not isolated decode benchmarks.

Audit B retained36PASS/3WARN/0FAIL,81-cue caption-authority PASS,
**272/272 required review frames** and24/24 graphic references extracted.
Audio measured−14.0LUFS/−2.4dBTP,48k stereo, zero measured end skew. Dark-run,
freeze and low scene-change warnings remain unresolved, not waived. Three
safe-zone PASS entries are INVALID as visual evidence: old code measured an
empty y>1400 portrait band on1080-high landscape frames.

Actual retained `graphic2_mid_t024.608.jpg` shows subtitles obscuring the
pipeline footline/node label; `graphic6_mid_t067.217.jpg` shows severe overlap
with agenda rows2–3. Root inspected six existing JPGs and an independent reviewer
confirmed the pipeline defect. Caption lineage/prefix parity does not establish
readability. Partial notes: `/private/tmp/sniper-caption-live-visual-observations-20260907.md`.
No old report, candidate, selection or approval was rewritten.

One-second targeted native sample of owned worker83652 at17:54:08UTC found
405/756samples in the os_lstat branch. Code-backed inference: every1MiB caption
read repeatedly walks shared parent ancestry for every held control file.
New per-call ancestry dedup preserves every original file identity check,
checks all shared parents before AND after, retains deadline/full-byte checks,
and has no cross-call cache. Independently reviewed;13actual metadata-fault tests
PASS0.075s after application. Live speedup remains unmeasured. Sample overhead
stays in this run; no media comparison was run concurrently.

After terminal release, geometry-bound safe-zone scanner applied:19tests
PASS1.188s. It reports missing/invalid measurements as unmeasured rather than
zero; does not pretend to detect caption/graphic text collisions. Explicit
canonical installed-venv selector:7tests PASS148.874ms; no interpreter/download
in those tests. TypeScript no-emit check passed with incremental cache disabled.
V6 request/candidate/TS reader integration applied, default still5; combined
V5/V6 cohort23PASS455.614ms. Python parity/real short bootstrap qualification,
caption-compatible native layouts, page-proof A/B, and scoped revisions remain
required. Current checkout changes intentionally invalidate prior execution
source equality; historical successful run facts are not reblessed as new code.

### Post-run implementation and isolated speed evidence — 18:56 UTC

The actual V6 Python reader and opening/body schema dependency holds are now
applied, along with the short hook/operator-intent correction. Combined new and
existing Python cohort:139/139 PASS,0.170s suite/3.15s wall. V5/V6/explicit-venv
TypeScript cohort:30/30 PASS,0.53s wall. V6 remains qualification-only; the public
default is still5. No real crop/bootstrap/render claim follows from these units.

The caption-page one-decode proof is applied and genuinely compared against the
old proof on the exact retained899-frame1920x1080@30000/1001 page. Old-first:
25732.779ms old,10459.060ms new; reverse order25967.625ms old,11403.878ms new.
Entire72142-byte framemd5 output, all-frame alpha text and public proof values
(including floating255.0 transport) match. Six native corruption/geometry/rate/
empty-alpha/last-frame controls reject. Actual held-caption capture/read/stage
2/2 PASS11.651s suite/12.31s wall; broader focused53/53 PASS3.759s/3.88s wall.
The147 observed page/cohort children have matching reap/group-absence evidence.
Details: `docs/findings/CAPTION_PAGE_SINGLE_DECODE_2026-09-07.md`. This is an
isolated stage reduction, not a new measured full-program completion time.

The first caption-compatible native agenda layout is implemented as an explicit
`agenda-slide` spec.layout=`caption-safe-upper-v1`, not automatically selected.
It reflows at most three steps into a native upper split, keeps the original
132px/52px/30px typography and88px chips, permits wrapping, and never suppresses
captions or scales the whole picture. Its explicit flat treatment has no text/
box shadows. Original default geometry remains unchanged. Protected DOM roles
are declared, but full-animation observer integration/rendered visual evidence
is still being implemented; the variant name is NOT a collision-safety proof.
Initial variant/visual tests13/13 PASS2.84s wall. The other observed graphic
collision and complete-program visual qualification remain unresolved.

Adding the referenced stylesheet exposed a freshness omission: the measured
catalog digest previously hashed a fixed shared list, not all declared local
CSS/JS dependencies. It now discovers that actual recursive closure. An initial
safe implementation repeated common CSS parsing per composition:31tests took
25.25s wall. Sharing discovery only WITHIN one observation reduced the expanded
32-test cohort to3.51s wall (0.559s suite), with exact many-root/single-root byte
parity and mutation/rejection tests. No cross-call cache was introduced. One
intermediate test failed only because the strict reader's error text changed;
the test now expects that reader's actual error, with no policy relaxation.

The existing53-row measured capability artifact is now stale after template
changes. It has NOT been relabeled/rehashed as fresh. Actual graphics need fresh
measurements. For V6 only, an exact four-field previsual with explicit graphics
off may instead carry an empty catalog and a domain-separated omission hash
bound to its full original target. Historical versions, inherited/expanded
plans, other graphics ownership and Caleb's named hook exception keep the
original dependency path. Requested catalog operations have no available kind
under omission and must block. This scope-specific dependency fix is under
adversarial/integration test; the genuine short bootstrap is next, not done.

### Actual short bootstrap and visual-safety implementation — 19:35 UTC

The real new short/light V6 path now reaches source admission, bootstrap, exact
saved four-field previsual intent, private cut preview, pause/quiescence, TEST
cut acceptance, raw crop/caption clauses, real proposal readiness and both TS
and Python 14-document readers with current code closure/manual geometry checks.
It has NOT rendered an opening/body/master, used ASR, exercised a detached
bootstrap worker, or received genuine human acceptance. Public default remains5.

All four fresh attempts are retained, not repaired or excluded from timing:

- First:2.90s wall, failed before source launch because the new TEST source
  helper supplied a bare FFmpeg command instead of its required absolute path.
- Second:3.62s wall, source generation succeeded, then the sandbox denied access
  to the Docker socket. No container or bootstrap launched.
- Third:23.55s wall, reached both inputs but Python rejected the actual pipeline
  identity. Bootstrap's raw timestamp token contains colons; the existing TS
  writer replaces unsafe UTF-16 units with underscores then keeps the last96.
  Python incorrectly compared the sanitized lock to the raw authority token.
- Fourth:24.41s wall (actual integration22.070s),4/4 test PASS after the exact
  writer/reader mapping fix. Workspace ends `sniper-v6-short-kwOd1i`; third failed
  workspace ends `sniper-v6-short-WUpEVU`. Logs are
  `/private/tmp/sniper-v6-short-actual-{first,second,third,fourth}-20260907.log`.

The run-ID correction changes ONLY lock-domain comparison. Raw cut facts,
clocks, approval identities and replay tokens remain exact. Eleven new identity
tests plus existing reader/target tests:36PASS,0.053s suite/3.13s wall. Includes
astral Unicode's two-underscore JS behavior, 96-unit suffix, UUID preservation,
changed raw IDs/lock bytes/source and invoked-code failures. Independent audit
found no second mismatch; body validation delegates to the same corrected reader.

The sealed full-animation agenda geometry observer is now applied, but remains
opt-in and deployment-blocked by the old approved image. It records native DOM
Range/element envelopes in an isolated CDP world before/after every actual
screenshot and requires real encoder write acknowledgments. Default rendering
does not import or activate it. Applied tests:72Python/14Node PASS, plus37
historical runtime/container/IO/removal/seal tests PASS. No actual browser
observation exists yet. This is not pixel legibility or creative approval.

The pipeline card now also has an explicit upper full-width layout. Original
font sizes, copy, node order, timeline calls and default presenter/eight-node
class are preserved. The opt-in requires2–6 nodes, hold exit, no presenter hole;
overflow remains visible for future observation to reject. Its script was
split into focused functions. Eighty-eight old/new authoring-DOM/timeline
comparisons PASS0.09s wall;6permanentNode tests PASS0.08s; combined24Python
native-layout/source/visual tests PASS3.06s. These are NOT native geometry or
pixel qualification. The two known caption collisions remain unresolved until
actual rendered comparisons pass. No old accepted plan or output was changed.

TEST-only runtime capsule copy/verification helpers are applied to support long
qualification runs without editing their held source. Initial installed-tree
probe failed0.80s because Homebrew's bundled base-site-packages link left the
four declared roots. Explicitly holding that complete fifth tree passed16.03s:
55423files,5806directories,73links,1577882100bytes. It is distinct from Homebrew's
global sysconfig purelib; the actual isolated venv excludes that global path.
No arbitrary link following or package subset was substituted. Capsule/Python
startup pure tests25/25PASS0.49s; whole TypeScript and focused lint pass. The
owned build/prelaunch/final startup comparison and real capsule smoke are still
being wired. No runtime capsule, media or provider has been launched by this
work; complete declared trees do not claim a self-contained OS/dylib sandbox.

The measured catalog remains stale. Fresh local image/probe/catalog work,
actual short opening/body, whole-animation overlap checks, full audiovisual
review, correction/scoped revision and the ten-minute/two-hour qualification
remain required. Goal stays active; this checkpoint is not completion.

### Real runtime checks and intro timing — 20:10 UTC

The new local observer image was built offline from the exact cached prior image
and actually probed. Total29.826s: build25.283s, metadata probe0.933s, mandatory
exact-name cleanup3.248s. All13 observed closure files match; seven unchanged
runtime members and the original nine rootfs layers remain intact. Root independently
checked source holds, candidate hash, config/layers, isolation and actual returned
probe/cleanup evidence before updating the active image approval to
`sha256:880027231f9dab9dca988734991f25b69178929d9886c7489c6c02dfbe2c3985`.
No pull, download, package install, paid service or persistent environment change.
Old image retained. Evidence: `/private/tmp/sniper-layout-image-20260907.z0WOfg`.
This approves the measured runtime identity, not native layout or creative quality.

Actual fresh TEST source capsule build/control smoke passed139.229s harness elapsed,
139.64s process wall, under its original300s cap. Fresh production capture0.326s;
full installed dependency inventory13.684s; independent capsule build38.463s;
built readback17.561s; prelaunch complete verification27.391s; unchanged copied
`--check-controls` CLI0.686s; final complete verification27.214s. Python startup
was actually observed and rechecked with the same exact isolated venv paths;
full copied/dependency bytes were checked again after startup. This was control
smoke only: no Docker call, media, provider, user approval or full-runtime claim.
Evidence: `/private/tmp/sniper-capsule-actual-20260907.rEbRi7/owner/result.json`.
The outer temporary CJS/ESM observer captured zero child PIDs, so its aggregate
group-absence field is vacuous and NOT independent process evidence. The unchanged
owned runners completed their built-in normal-return/group-absence checks.

Independent review first reproduced an unknown-child-cleanup bug in the TEST
capsule owner: unconditional final verification launched another Python startup.
Fixed to retain only byte verification after forced/unknown/timeout or generic
invocation failure; no further child launch, no retry and the attempt stays failed.
53inert tests PASS1.99s; original independent failing repro now14/14 PASS1.33s.

Pipeline cards now enforce the actual last headline/node/footnote ramp plus1.25s
minimum readable dwell. Shared timing constants come from the actual authored JS
and motion tokens, with sparse modules and explicit schedules checked against
executing those scripts. A three-module card needs3.45s with default lands; a
single short footnote may fit1.65s. Neither result is a subjective reading guarantee.
Validation never retimes or removes copy. Independent review caught Python-only
numeric extensions (`1_0e-3`, Unicode exponent digits) that JS parsed differently;
complete ASCII decimal syntax now prevents this earlier-than-actual landing claim.
Focused34Python tests PASS0.301s suite/3.30s wall;6Node authoring tests PASS0.08s.
Sealed input includes the extracted native pipeline script's exact bytes.

The first two REAL native agenda observations completed18.754s total with exact
4/12role inventories,120written frames each at30000/1001, successful strong byte
readback and owned cleanup. Both returned **UNQUALIFIED: role-layout-overflow**.
Evidence: `/private/tmp/sniper-native-agenda-run-L1ERmN`. Retained native PNGs and
exact roles are being inspected; neither layout is approved or silently accepted.
The subsequent fresh comparison fixed only the explicit upper-layout title/eyebrow/
row-title line boxes to1.25, preserving132/34/52px fonts, copy, colors and timing.
Both real cases then returned **observed, zero issues** over120written frames each,
18.430s total. Maximum all-frame bottoms515px/545.25px. The sealed archives differ
only in `motion/agenda-caption-layout.css`; default agenda styling is unchanged.
Evidence: `/private/tmp/sniper-native-agenda-run-lilix0`. Retained PNG inspection
confirmed readable unchanged-size text. This proves these two native envelopes,
not caption coexistence, general copy, listening or creator approval.

The actual V2 color-observation TEST owner also completed: fresh24frame UHD source
admission0.976s/1.10s wall; owner2.750s/3.13s wall. Original120s clock shortened to
118s worker; actual EOF, exact raw probe/frame hashes, all1105 pins unchanged,
owned group and exact Docker ID/name absent, real shared resource claim released.
No grade/conversion or delivery approval. PhaseB fault/recovery has not run.
Evidence: `/private/tmp/sniper-grade-v2-live-4v4t2nmo/PHASE-A-EVIDENCE.md`.

Broader timing/intro Python regression:160PASS1.813s suite/4.83s wall. Ten Node
modules:64PASS,2FAIL,1SKIP20.17s wall. Both failures correctly reject the stale
measured catalog (`motion source digest changed`); they are not passing evidence.
All53 existing measured rows also use the previous image, so a real current-image
refresh is required. No old row or digest has been relabeled as freshly measured.
The full-program caption adapter, actual short render, remaining finishing lanes,
real audiovisual comparison and ten-minute/two-hour qualification remain open.

### Caption screening and real pipeline layouts — 21:06 UTC

New captioned proposals now select explicit screened opening/body profiles,
including the manual9:16 family. Historical saved caption profiles remain readable
only through their exact held profile; current defaults do not silently upgrade
old results or invalidate their original authority. A52case actual Python/TS
matrix caught and fixed explicitNone being mistaken for an omitted default.
Focused29tests PASS0.75s wall; whole TypeScript and focused lint pass.

The opening/body adapter now binds original full-program caption timing to actual
native graphic observations and explicit covered frame ranges. True caption gaps
remain legal; unsupported simultaneous graphics fail before graphics rendering.
Neither sampled boxes nor a layout name constitute safe-output approval.
159automated checks passed3.567s suite; independent31passed0.71s wall.
Independent review subsequently found the fresh adapter discarded the actual
renderer return and could adopt replaced layout/runtime sidecars. The actual-return
binding fix is in progress; this adapter is NOT yet real-owner-qualified.

A second offline image build/probe completed27.212s owner/27.27s wall:
22.641s build,0.699s metadata probe,3.306s mandatory cleanup. Only browser/host
observer bytes changed. Other11 closure files, config, labels and original11-layer
prefix remain exact. All11 recorded groups and three reported build intermediates,
plus exact probe name/full ID, were observed absent. Root independently checked
held source, actual probe, candidate and terminal evidence before activating
`sha256:b96bfe4a5b828eb99bebc26209cd005d1ab1a658a13f14d70cf596f3e27ca686`.
Evidence: `/private/tmp/sniper-native-layout-image-20260907.X5Y8Fa`.
No pull, provider, install or paid API. Image identity is not visual approval.

The separate sealed pipeline observer protects actual headline/eyebrow/explainer,
every node, every connector and provenance text over every written native frame.
Default full-canvas/PIP behavior stays unchanged. Upper-only connectors became
real DOM siblings inside original node-fade wrappers; independent review caught
and corrected accidental overflow and lost inherited fade before real execution.

First real two-case NPL run23.082s owner/23.30s wall: sparse3roles observed;
dense16roles **unqualified**, headline/eyebrow line-box overflow. No waiver.
Root inspected the actual PNG and retained all-frame roles. Dense headline Range
height134px exceeded its115.5px line box; eyebrow Range38px exceeded34px.
Fresh upper-only CSS sets1.25line boxes, places provenance beside the explainer
and keeps the chain below them. Fonts110/34/22/21px, every character, colors and
all original animation times remain unchanged. No scaling, clipping or omission.

Fresh second real run19.374s owner/19.56s wall: both3/16role cases observed,
zero issues over120written frames each at30000/1001. Dense all-frame bottom569px,
above the584px graphic limit for a600px caption reserve plus16px gutter. Actual
PNG inspected. Before: `/private/tmp/sniper-native-pipeline-run-NF0zx0`;
after: `/private/tmp/sniper-native-pipeline-run-avuaMd`.40Python/23Node tests pass.
This qualifies these native CSS envelopes, not actual caption coexistence,
arbitrary long copy, pre-connector pixel parity, listening or creative approval.

Current53 measured catalog remains stale, full short CLI has not run, and remaining
music/SFX/grade execution, explicit post-cut revisions, one-request operation,
representative audiovisual QA and ten-minute/two-hour qualification remain open.
The app goal is active; this is continuing implementation, not completion.

### Current-turn evidence and retained misses

- Latest caption result supersedes the initial failures below: fresh **2/2 PASS
  54.551s suite/57.56s wall**, landscape27.958s at
  `/private/tmp/sniper-shared-caption-c4bezhi5`, portrait26.592s at
  `/private/tmp/sniper-shared-caption-j82z5pi1`. Both120frames30000/1001,
  192192master/AACsamples, exact intro/body preencode prefix, captions above the
  later opaque graphic, late-body glyphs,7 support files, unchanged master and
  **AuditB31PASS1WARN0FAIL**. Warning is synthetic opaque-hold freeze; retained,
  not waived. Native extracted frame8 inspected for both orientations: readable
  text/no edge clipping; this is not speech/listening or whole-video QC.
  Pure33 PASS2.334s; independent32 PASS2.303s. No OCI graphic/real creator proof.
- V5 activated for new proposals after those combined-media results; historical
  parser entry points remain closed.32 V4/V5/history tests PASS472ms, whole
  TypeScript and lint pass. First durable cohort **13/14 PASS150.911s**: the new
  caption request reached the exact stored opening inputs, but its TEST assertion
  incorrectly expected original captions:{burn:false}; the actual original has
  no caption field. Assertion fixed without changing the original plan.
- Full captioned TEST driver keeps the original five-minute media class and all
  real gates. Its separately authored initial project/plan now agree on captions
  auto BEFORE pipeline capture/cut creation; old non-captioned fixture remains
  unchanged. An original captions-off intent cannot be silently overridden:
  V5 now blocks that conflict during proposal, including off/operator/trim.
  Explicit post-cut intent revision remains required development, not a hidden
  fixture waiver. Updated initial-intent/driver/V5 cohort28PASS726ms before final
  null-directive negatives. Corrected durable input rerun/full CLI still pending.
- Independent observer review exposed a final execution-file replacement race;
  the reader now captures exact bounded raw bytes and returns that held SHA only
  after the final identical-byte check.79 tests PASS0.912s/1.01s; independent79
  PASS1.02s and original exploit now rejects. Reproduction/failure logs retained;
  no source/color/grade approval created. Terminal-aware early-death waiting is
  being added to avoid spending the whole1200s on a known dead worker.
- Continued shared-caption qualification: initial actual native longform/portrait
  cohort **0/2,15.315s suite/18.26s wall**, retained at
  `/private/tmp/sniper-shared-caption-vop574nv` and
  `/private/tmp/sniper-shared-caption-57vih5ow`. Cold caption read exposed a real
  transport bug: cross-runtime JSON changed historical255.0→255 while preserving
  a caption-domain digest that distinguishes them. The fix reads held original
  records unchanged, not resealing old proofs. The portrait TEST recipe also
  failed existing duration/canvas lint; no gate waiver or accepted output.
- Root added an explicit `--run-captioned` mode to the existing expensive
  TEST-only full CLI driver. It keeps the same native five-minute program,
  real deterministic gates, original generation clock, actual opening/body
  commands, replay/conflict checks and mandatory final caption-authority pass.
  It does not accept creator paths or weaken the workload. Its fixture/driver
  cohort **19/19 PASS1.2997s**, whole TypeScript and focused lint pass. First
  attempt18/19 caught a reversed TEST coverage-helper argument and missing TS
  fixture union type; both fixed. Full captioned CLI execution remains pending;
  V5 is still not the default until combined renderer qualification succeeds.
- Source correction also reaches both actual pure caption compilation presets:
  **5 corrected-text consumer tests PASS0.469s**. TEST words/lineage and original
  file preservation, not actual C0679 listening or rendered-caption quality.
- Explicit UHD/xvYCC source-observation V2: **75 focused tests PASS0.905s suite/
  1.03s wall**. It retains V1 limits/shapes, requires explicit V2 owner/profile,
  preserves exact transfer/chroma/SAR observations and fails unknown transform
  history. No C0679 decode, applicable grade or picture-transform execution.
- New picture-transform compiler remains non-executable pending live exact-source
  gamut/clock proof. **16 pure tests PASS0.058s suite/0.20s wall** and **3 tiny
  actual ramp tests PASS1.679s suite/1.77s wall**, retained at
  `/private/tmp/sniper-picture-transform-0my1m7j7`. Native8×4 independent scalar
  comparison, rational/nonzero clocks and extended-range rejection only; this
  is not a camera/source color qualification or permission to clip gamut.
- Correction transaction:29 focused cases passed1.896s suite/2.08s wall before
  independent review. Actual tiny TEST-store prepare→record→status/replay and
  simultaneous record writers exercised; simulated attestations are not consent.
- Root initial combined correction/source/cut regression:107 PASS11.684s.
  Independent review then found a missing-marker bypass: removing correction
  lineage and rebinding altered text could pass the generic transcript digest.
  Reserved correction paths and named timing-manifest selection now require the
  committed correction route even without that marker.
- Independent review also found that borrowing a longer parent alarm did not
  interrupt blocking work at the declared shorter120s read budget. The scoped
  timer now shortens it and restores only the original absolute parent expiry.
  Updated publisher/consumer cohort24 PASS1.984s, including both reproductions.
  Broader stable-code media/adversarial checks are running; do not call these final.
- Small picture-observation benchmark: same360-frame/12.012s720p bytes,
  old739.728/730.322/734.342ms versus new276.349/276.544/277.801ms.
  Median reduction62.34% for this fixture only. This removes a duplicate full
  decode, not independent selection/approval checks; no longform extrapolation.
  Existing media roundtrips remain required before adoption.
- New [correction command guide](SOURCE_WORD_CORRECTION_WORKFLOW.md) documents
  limits, original-parent preservation and actual read paths. No C0679 correction,
  new cut approval or output qualification has been claimed or applied.
- Correction duration now also matches the selected receipt's actual decoded
  duration, not only a manifest number. Read-only C0679 metadata eligibility
  passed:834.335s on both records. Updated timing/cut/source/publication cohort
  **138 PASS13.806s**. An earlier root invocation named nonexistent
  `test_transcript_timing_correction_safety` and failed collection; that was a
  command-selection error, not a passing test or product failure.
- The stable picture-decoder integration cohort **50 PASS48.221s suite/52.00s
  wall** includes actual tiny opening/body roundtrips. Final retained small
  benchmark median736.949→285.097ms (61.31% less); log
  `/private/tmp/sniper-picture-decode-integration-20260907.log` and measurements
  `/private/tmp/sniper-picture-decode-nxvmbwzz/measurements.json`.
- First real manual-short helper attempt retained at
  `/private/tmp/sniper-manual-short-gxdvuhci`:2PASS/3 readback errors in8.259s.
  Crop pixels/frame count passed but rich receipt nanosecond integers could not
  enter the shared safe-number JSON domain. Fixed by projecting small exact
  fields while retaining the full original receipt's raw SHA, never rounding
  timestamps. Fresh6/6 PASS8.170s at `/private/tmp/sniper-manual-short-4t636jz8`:
  60 NTSC portrait frames, full base/master6.851s and same-base prefix/encode680ms.
  This initial pass is superseded by the expanded cohort below, not erased.
- Expanded manual-short actual-media cohort **8/8 PASS16.332s suite/19.21s wall**:
  `/private/tmp/sniper-manual-short-hzsqhfxw` and
  `/private/tmp/sniper-rotated-manual-short-rug4av8x`. Actual90-degree metadata,
  source-coordinate crop pixels,60frames at30000/1001,96096master samples and a
  native translucent TEST graphic crossing the review endpoint were checked.
  Preparation6.821s; plain prefix631ms; crossing-graphic prefix758ms. Independent
  code/evidence review found no concrete blocker in this bounded slice. This is
  not authenticated full-short CLI, real catalog, full AuditB or creator-framing
  qualification. `resolvedCrop` describes the even-snapped source rectangle;
  later cover/center-crop overflow means it is not the final visible viewport.
- V2 one-to-one source-faithful text correction now shares immutable record,
  publication and cut readback. It preserves timing and unaffected words, removes
  stale ASR confidence only on changed words and rejects ambiguous word aliases.
  Core v1/v2 cohort48 PASS2.830s suite/3.03s wall; publisher/consumer28 PASS2.526s.
  Root's final combined correction/review/cut/promotion cohort **152 PASS9.786s**.
  These overlapping cohorts are not additive and do not fabricate source listening.
- Existing Producer skill now routes timing/text correction to the command guide.
  Its narrow routing update passed the skill validator with the already-installed
  backend environment. The first Sniper-environment validator invocation failed
  because PyYAML is absent; no dependency download or installation was attempted.
- Caption-page integration remains in progress. New explicit profiles must keep
  captions above graphics, retain the combined picture for unchanged AAC copy
  proof, preserve full caption AuditB, avoid duplicate burn and keep original
  clock/64MiPixel limits. This is not yet raw-caption-intent authoring or a real edit.
- Held-caption helper final cohort **16/16 PASS14.499s suite/14.93s wall** at
  `/private/tmp/sniper-held-caption-media-n1wtktil`; log
  `/private/tmp/sniper-held-caption-helper-final-20260907.log`. Actual ordinary
  base/projection11526ms, capture61ms, strong read30ms, new support staging65ms;
  fixture inputs1662ms. Original source and complete render inventory unchanged.
  This reuses actual returned cue shards/pages, not cache-discovered success.
  The earlier15-case pass and preflight-open growth hardening remain retained in
  `scripts/producer/docs/findings/HELD_CAPTION_PROJECTION_REUSES_ACTUAL_ALPHA.md`.
- V5 caption-request authoring is integrated for explicit versioned inputs,
  with the default compiler deliberately stillV4 pending actual captioned
  opening/body qualification. New V5 operation selects captured Producer line
  or karaoke presets for all kept words; no generated timing/text, suppression,
  custom typography, semantic chapters or replacement of existing authority.
  Actual V5 clauses/operation indices remain in candidate/review/history; the
  V4 projection is only an internal reuse of unchanged story/graphics checks.
  Independent review caught inherited enum-array coercion; V5 now rejects it
  without rewriting historical parsers. Initial36-case V4/V5/history cohort
  passed457.8ms; final7-case V5/schema/compiler cohort passed411.2ms. Whole TSC
  and focused lint passed before the final schema-only test addition. Earlier
  integration TSC failures (union narrowing/record return types, then one
  intentionally malformed TEST literal) were corrected, not ignored.

## Previous verified checkpoint — 11:22 UTC (not completion)

- Direct Codex commands cover new admitted-candidate guided project start/status,
  cut/treatment checkpoints, opening and initial body paths. The new bootstrap
  has27focused tests and independent failure-race review, not a real-source
  bootstrap→preview qualification. See `CODEX_COMMAND_WORKFLOW.md` for limits.
- Root combined TypeScript regression:70/70 PASS52.76s, including actual3s
  synthetic preview fixtures with TEST-only writer/critic/attestation data.
  CLI4/4 PASS1.13s separately. Whole TypeScript check PASS1.85s. No full-suite
  or real creative-quality claim follows from these checks.
- Source ASR/ownership/deadline/parser combined Python regression157/157 PASS.
  Normal full C0679 provider PASS153.95s. Original source, admitted transcript
  and unapproved11m25.88s cut remain unchanged.
- Strict token parsing is **experimental, not production default** after its
  real full-source failure. Production preserves explicit row compatibility;
  known acoustic timing limitations remain. Do not advertise improved word
  alignment or silently substitute uncertain token endpoints.
- Actual C0679 opening timing/listening and the ending's resource promise await
  user feedback. Source audio clips are available in Codex; no decision exists.
  A separate source-bound word-timing correction transaction is still missing:
  current timing-review approval cannot itself retime an inflated word span.
- Broad remaining output work includes real cut/intro/body QA, requested
  finishing lanes, scoped revision, representative short, ZIP matched-quality
  comparison and prospective10-minute/two-hour timing. The5-minute synthetic
  body result is not proof of those outcomes. The app goal remains unfinished.

## Goal and preservation

The existing Codex goal already covers the full agreed plan. Its stored state was
`blocked`; a new goal request was refused because that unfinished goal exists.
Do not mark the old goal complete merely to create another. Continue implementation
under the user's explicit renewed authorization; app goal resume is user-controlled.

User renewed the spending constraint during the current run: use subscription-backed
model tools and local processing only. No paid API invocation, purchased credits,
or silent API fallback without explicit approval. If subscription access is exhausted
or its authentication mode cannot be verified, stop that invocation; do not switch
to API billing. A CLI login alone does not prove subscription authentication. The
2026-09-07T03:52Z goal read still returned `blocked`; the user was told to use Resume
on the app's goal row. Do not report that the goal is active without checking it.

Preserve this dirty shared checkout, admitted source snapshots, original footage,
and approved media. Claude's matching session ended with a user interruption at
2026-09-07T02:33:14Z. Source changes newer than the previous review must be verified
and retained, not replaced with the old review's proposed patch.

## Immediate ownership

- Main: direct Codex/Producer skill and Studio CLI handoff, integration tests and log.
- Quality reviewer: local-only ASR admission across distinct workers/ingest/study.
- Latency reviewer: held base/master reuse, private graphics cache and adversarial review.
- Catalog reviewer: shared subscription CLI admission and paid-path exclusion/tests.

Owners must coordinate shared files and preserve other edits. No creator approval
may be synthesized. Synthetic media/critic results remain explicitly TEST-only.

## Sequence and exit evidence

1. Verify/fix the newer recovery, readiness and unpadded-audio changes with negative
   tests. Retain missing/uncertain ownership; do not turn probe failure into absence.
2. Move the actual claim → render → cleanup → readback → selection sequence out of
   test-only orchestration into a shared production service. Exercise failures and
   have the media fixture use that same service.
3. Connect real cut/treatment/opening launch and status to the actual UI, with durable
   launch ownership, no duplicate rendering, original request timing, and safe
   interruption/reopen. A status/media handler test is not UI generation evidence.
4. Complete real C0679 cut review and source-class qualification; then produce its
   opening, obtain genuine human review where required, generate the body, perform
   whole-video QC, and deliver the first reviewable master.
5. Prove one scoped revision and a representative captioned/reframed short; integrate
   the requested finishing lanes without silently disabling unsupported work.
6. Run the remaining agreed regression/quality/performance comparisons. The full
   plan remains `TWO_HOUR_VIDEO_EDITOR_PLAN_2026-09-06.md`, including ZIP adaptations,
   audio/color/temporal review, bounded repair and reuse, and prospective cohorts.

## Starting evidence and limits

- Run14's 617.7-second synthetic opening success is historical mechanism evidence;
  it is not a finished creator video or throughput/quality guarantee.
- C0679 is admitted at `/Users/aaronfigueroa/ProjectSniper-qual-2026-09-07/c0679-20260907`.
  Its current 725.665-second previsual cut is not human-approved; ASR timing anomalies
  still require source-grounded review, not automatic word deletion to satisfy gates.
- No new render, approval, complete-output benchmark, or full-suite PASS is implied
  by this checkpoint. Report focused tests separately from full regressions.
- Record original request/preparation/generation/revision clocks, user wait, all
  failed attempts, hardware/load and cache state. An exhausted budget cannot waive QC.
- Use qualified local media paths; no raw video egress and no new paid service
  integrations. Leave existing unrelated servers and projects alone.

## Session measurements

Implementation re-entry observed at 2026-09-07T02:38:37Z after instruction/current-state
reconciliation. Per-command elapsed measurements and retained media attempt logs
will be added as tests complete; no elapsed value is inferred from a missing record.

### Implemented and bounded-tested this session

- Shared production opening execution now owns claim → worker → protected cleanup →
  strong readback → exact selection; the old media fixture calls this same service.
- Detached opening launch persists an exclusive per-journal intent before spawn,
  strict PID/boot/start activation, explicit parent-lease release, and exclusive
  controller-started marker. Child acquires its own exact checkpoint lease. No
  automatic restart or PID-only recovery; unknown outcomes remain fenced.
- Original admission and parent elapsed floor cross the handoff, with a new local
  child monotonic origin and charged startup/wait. No foreign clock epoch or new
  25-minute allocation is restored. Clock rollback remains poisoned.
- Actual opening launch GET/POST and editor controls added: same-origin bounded
  body, explicit click, persisted retry UUID, no auto-POST, five-second metadata
  polling capped at 25 minutes. Selection/approval/body authority stays separate.
- Known unsupported media profiles and missing sealed executable pins are rejected
  before a permanent launch fence. Python remains the independent media authority.
- Audio fitter now rejects empty/truncated raw input instead of substituting silence.
  Claude's unpadded decode and exact movie/sample timescale changes were preserved.
- Opening recovery now preserves unknown/missing process evidence and rechecks
  ownership at both cleanup CAS guards. Missing media ledgers require manual recovery.
- Readiness gate children stop before return, gate waves await started siblings,
  share a decreasing budget, and clean up in finally only with verified stop.
  New schema2 stop/cleanup evidence is mandatory for current readiness.

Focused evidence (not full-suite or creative qualification):

- Audio: 56 tests passed (45 in 27.07 s wall; source-bus 11 in 12.27 s wall), host
  FFmpeg8.0. No sealed4.4.2 PCM compatibility or actual speech/listening verdict.
- Opening recovery: 36 tests/0.98 s; adjacent claim/selection 7/0.32 s.
- Readiness: 14 tests/2.44 s; legacy runner 4/1.91 s. Stored synthetic source/cut/
  proposal integration 3/3, 39.73 s wall, with TEST gate/critic responses.
- Controller/clock/metadata: 25/25, 0.786 s runner, including actual OS identity
  observation and 29 metadata-only Python parity cases; no media render in cohort.
- UI/client/routes: 18/18, 0.45 s; mounted actual React components with intercepted
  TEST metadata 6.548 s; no browser errors, no duplicate POST, exact retry UUID,
  and test server/browser cleanup. This is not an actual API/render success.
- Whole TypeScript check and focused ESLint passed before the final test-only
  fixture owner registration. Re-run after the media window.

### Actual UI qualification in progress

Isolated supervised server: http://127.0.0.1:3331, cache `.next-guided-20260907`,
workspace `/private/tmp/sniper-guided-ui-20260907.sHJjHk`. Existing port3327 server
was unresponsive in a five-second probe and was left untouched. Initial isolated
start hit sandbox listen EPERM and the supervisor stopped after three bounded
attempts; approved loopback start succeeded. The test instance was then cleanly
restarted with all explicit existing renderer pins. Next added isolated type-cache
includes to tsconfig; optional output cache names are bounded and ignored by Git.

First CLI driver had ESM/CJS named-export mismatch and failed before creating any
fixture. Corrected driver uses the existing registered TS require loader.
First actual setup began03:15:13Z at `test-only-cut-review-647c10c0`. Opening it while
setup was running exposed its missing TEST worker identity: UI orphan recovery
marked it interrupted03:17:05Z, then the cut preview ownership guard failed. Keep
that project as failed evidence; it generated no opening or human approval.
The fixture now registers its REAL controller identity using `markAutoEditWorker`;
the existing cut-pause transaction clears ownership normally. Fresh retry began
03:19:42Z. Do not confuse the failed ownerless harness with a proven production
worker-liveness defect. No creator source or approved artifact was changed.

Fresh corrected setup `test-only-cut-review-c0331568` completed in137.953s with
real readiness gates and8whole-program graphics. Original generation clock
03:21:43.522Z. Actual UI Generate opening click recorded03:23:06.208Z; launch
`b9d7aa49ff6b6d241e42fef02f101563db01641b31d28fdcaffeaeec0f4c93ee`, controller
PID30455, operation`0ed56892-7f8c-4534-8ff2-7db9c271c021`, execution
`b4204a80-2993-4f6a-91d8-152e0b0d9cb2`. Actual UI reload retained the same request,
single execution and original clock; no duplicate POST/render was observed.

That UI run FAILED at03:29:54.284Z after406.591s controller time. Media worker
completed in405.246s, its parent group stopped, and unselected core/review files
exist; required nested media-process ledger was absent. Cleanup correctly blocked
selection with `Opening media process ledger is missing`. Do not invent an empty
ledger, remove its claim, or retroactively qualify this failed run. Preserve all
artifacts; failure is an integration defect under investigation, not a PASS.
Its receipts report241.089s full-base preparation and31.687s whole float master;
the cut_speed substage was101.289s and legacy picture-master substage79.747s.
These nested durations must not be summed twice. No human approval was recorded.

Post-run UI fixes remove contradictory disabled/pending copy, display real operator
opening approval distinctly from body approval, clear transient launch warnings on
terminal metadata, and refresh evidence once on cold terminal reopen (not repeated
full-media polling). Focused display/client/checkpoint tests passed24/24 before the
new mounted-browser cold-reopen regression; final suite counts will be recorded.

UI/client/status/checkpoint cohort now32/32 in0.584s runner time; the prior31/32
had one stale wording assertion, corrected to retain the same no-liveness claim.
Mounted actual React launch lifecycle passed in11.565s, no page errors, exact
retry UUID, one POST, automatic terminal evidence refresh, cleared sticky warning,
and one cold-reopen terminal refresh. Artifacts:
`/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-launch-ui-LuhcQD`.
Focused lint passed. Typecheck caught a TEST environment missing Next's required
NODE_ENV; fixture now includes NODE_ENV:test. Re-run whole types after all edits.

Diagnostic visual review caught another real gap: the1.8018s scoreboard's default
module sequence never displayed its planned tiles/caveat before the cut. Added
the actual-module visible-hold guard and shared lint/probe/TEST-authoring reader.
The corrected TEST fixture owes4.35s for that complete card, not1.8018s. A simple
number card still fits1.8s. No user copy is automatically removed or retimed.
Timing/asset cohort18/18 in0.088s runner, including execution of the actual HTML
module build with recording GSAP stubs. Wider adjacent tests and fresh actual
render were pending at that checkpoint. See
`DEFAULT_ANIMATION_IS_NOT_AN_EMPTY_SCHEDULE.md`.

Subsequent adjacent graphics/catalog timing cohort95/95 passed in8.076s. The corrected
312-second TEST treatment passed real plan/hook/claims/operator gates in0.614s wall;
its pacing/coverage warnings remain warnings, not subjective quality approval.
Independent schedule review found the final scoreboard reveal ends3.10s and needs
4.35s including readable hold. Whitespace-only and Python numeric-underscore parsing
differ conservatively from JavaScript and still need parser parity work.

The missing-ledger integration defect is now fixed prospectively: a pinned Python
media wrapper records worker-started and worker-finished even with zero detached
helpers. New schema2 process outcomes separately hold the completed ledger SHA.
Abrupt exit, forced stop, missing/torn/mutated ledger or unknown nested ownership
remain failures. Historical failed attempts were not repaired or requalified.
Focused evidence:50 TypeScript tests/1.05s and34 Python/receipt tests/4.16s wall;
real zero-helper, nested helper, nested timeout, abrupt exit and post-fork ledger
failure cases included. Whole TypeScript check and focused lint passed before the
fresh source freeze at03:47:31Z. Aggregate recovery probe deadlines remain open.

Fresh setup `test-only-cut-review-030a5e56` completed in161.531s with real readiness
gates,8graphics and no human opening approval. Original generation clock03:49:44.899Z.
Actual editor Generate opening request recorded03:53:46.992Z; controller started
03:53:48.375Z. Launch`caa54da0b98a4d1015ccd1936f009b3af23ab9a7a7562ee6928da0c6cd32a256`,
operation`614b47b1-f734-4dec-80aa-0345c9639575`, execution
`5eb5237c-0dfa-461f-ab72-1da4829c28dd`. Reload retained the same request and clock.
At03:59:48Z the run was still in progress; no PASS or selection yet. Its cut stage
took125.829s and picture-master99.516s, versus101.289s and79.747s in the prior run.
Host load averages during execution were23.31/14.81/12.33; this is not a controlled
performance comparison. The sample remains synthetic test-pattern video/tone audio
with TEST editorial inputs, not C0679 or proof of speech/color/creator quality.

The fresh run subsequently completed `selected-not-approved` at04:02:41.083Z:
controller532.717s (8m52.717s), media worker483.416s, normal stop with complete
schema2 ledger and verified cleanup/readback. Exact selection hash:
`6795a02c7b0033a9485d8ddeed1d8eef4b5c1f249b07053ae66d6b9952dfabc4`.
No human opening or delivery approval. Generation-origin to selection was776.184s
(12m56.184s), including the pre-launch gap; do not report only renderer time.
Full base292.598s + float master32.669s =325.267s full preparation. Seven actual
OCI graphics renders total67.842s; composition31.095s, picture-copy/AAC11.789s,
whole input/result revalidation13.682s. Stages are nested; do not double count.
Core1853frames/2967765presented samples; review2013frames/3224021samples, NTSC
30000/1001,1920x1080. Actual excerpt AAC measured-14LUFS,-5.11dBTP, one audible
AAC encode, no independent excerpt normalization. These are synthetic-signal
technical measurements, not listening or skin/color-quality judgments.
The corrected scoreboard spans45.9459–51.117733s (155frames), exceeding its4.35s
minimum visible schedule. Freeze ended after the successful retained selection.
Post-selection decoded3fps sequence at
`/private/tmp/sniper-selected-opening-qc-20260907.0yitXM/scoreboard-sequence.png`
was visually inspected: all three tiles and the LIMIT caveat now appear and remain
on screen before the cut. This resolves the earlier never-painted-content defect;
it is a coarse sequence check, not the required12/24fps entrance/exit temporal QC,
phone-size readability approval or whole-output craft review.

### Internal body-continuation primitives; not a public body workflow

`holdGuidedBodyInputUnderLease` now binds exact current operator opening approval,
its before/after journal transition and retained approval-time readback, then runs
fresh strong source/media verification under the caller's lease and remaining
budget. It returns immutable held base/master/plan/manifest references and an
unchanged guard; explicitly non-executable, body-not-qualified and not delivery
approval. It does not advance the head or save a plan.11new tests;25/25 related
tests passed in0.350s using an explicitly injected TEST verifier, not new real media.

The ordinary assembler can internally import an exact separately held full-master
selection event/SHA and reuse its original base/source bus/float master into a new
0700 private body directory. It does not recut, remaster or rewrite original
receipts. Whole candidate Audit B and guarded publication remain.7actual tiny-media
tests passed in29.04s plus11ordinary compatibility tests/26.35s. The positive case
retained all opening bytes/inventory and192192presented audio samples, but had no
graphics; it is not public guided-body, graphics-prefix or creative qualification.
Adversarial review found unchecked imported `job.cache_dir` can direct later graphics
writes into the retained opening tree. Derive an attempt-private cache and test
graphics-bearing preservation before connecting this primitive to body generation.

Subscription audit is read-only so far: current opening/body primitives do not call
a model, and the TEST fixture supplies explicit TEST brains. Model-based editor
paths use configured CLIs without a direct SDK fallback, but Codex preflight's
generic logged-in check and saved auth, and Claude's configuration/wildcard environment
inheritance, do not reliably enforce subscription-only mode. Add a fail-closed shared
invocation fence, with authenticated subscription mode and sanitized consistent
environment/configuration. Do not treat this finding as evidence of actual charges.

The same audit found a concrete paid-ASR default: `scripts/local_whisper.py` selects
Deepgram unless local mode/provider is explicit; `producer/ingest.py` can discover
its key from environment files. The manual `_ingest-probe-c0679.ts` does not itself
enforce the local-only claim in its comment. Never execute these paths under the
current authorization without explicit local-provider enforcement. Fix the shared
command/skill path and paid sink before running new source transcription. Existing
reference-study GUI explicitly selects local Whisper; standalone word-lock/semantic
helpers still require audit/guards. Do not run live-Claude harnesses as unit tests.

### Still required; do not mark the goal complete

The opening path currently excludes caption burn, grading, reframing, punch-ins,
transitions, enhancement/gain, speed/J-cuts and opening free-band/hole graphics.
The guided raw-brief/compile/review stages still need a qualified direct skill/CLI
continuation path; custom-web-UI connections are not the current priority.
Full-body continuation, whole audiovisual QC, exact promotion, scoped revision,
real short+long qualification, ZIP/current matched comparison and two-hour evidence
remain unfinished. Readiness still lacks durable cross-request resource ownership
and Docker late-create fencing; its bounded cancellation fix is not complete crash
recovery. Opening ledger completion digests are now fixed/tested for new schema2
attempts; aggregate recovery probe deadlines remain a qualification gap.

Body work should reuse the opening's existing full-program base and held float
master, not remaster/splice the intro. Import its actual provenance into the existing
render graph, preserve exact half-open frame bindings through ordinary assembly,
and introduce a narrowly versioned body successor reader: the original guided
picture-lock reader correctly rejects ordinary ACTIVE/APPROVED_HEAD advancement.
Use real existing full-plan/whole-output critics, not relabeled readiness receipts.
Any repair touching the approved opening/global audio/color needs renewed review.

### Direct Studio and no-spend continuation (after the premature status stop)

The user explicitly directed continued work. The existing blocked app-goal state
does not prevent implementation in this turn; no new goal or completion claimed.
The following evidence supersedes the earlier "read-only subscription audit" and
"private cache still needed" descriptions above only where explicitly tested.

- Studio sync dropped the selected manifest on its assembly handoff and printed
  unquoted paths. Added seven pure regressions: six failed before the fix; all
  seven pass after it. A focused93-line `studio/review_commands.py` now resolves
  paths/argv consistently; `studio_review.py` is269 lines. Explicit override and
  standard sibling-source manifests reach both workers and the printed command.
  Bad explicit paths and `--assemble` without `--apply` fail before subprocesses.
  Failed sync never chains rebuild. This pins the chosen path, not a new cross-
  process hash snapshot or approval authority. Independent review found no blocker.
- Broad Studio cohort:107 tests passed in6.199s, but player class setup failed
  because all default3990–3999 ports were occupied. Existing servers untouched.
  The player test now uses its own41000–41100 range, retains unique screenshots,
  and registers owned-server cleanup before readiness/probe can fail.
  Real pinned Studio player + synthetic fixture tests then passed12/12 in20.998s:
  actual playback, mounted/scaled graphics, base paint and copy/timing/undo checks.
  Root visually inspected all3 player screenshots at
  `/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/studio-player-shots-nnyej7he`.
  These are synthetic structural tests, not dialogue/color or creator approval.
- Existing Codex adapter/canonical Producer skill and Studio command/reference
  now route interactive jobs through local CLIs/Studio without custom UI. Removed
  fixed86s and blanket "everything else $0" claims, paid-ASR guidance, and the
  suggested graphics-ownership receipt bypass. Whole-output audiovisual checks
  replace the insufficient2–3-still-only guidance. One user request is allowed;
  one unverified model pass is not delivery approval.
- Independent skill forward-test caught two real ambiguities and they were fixed:
  an explicit narrow existing-project branch now avoids rerunning new-edit
  proposers/ASR/recompose for copy changes; newly changed auto-graphics plans run
  gates + independent reviews + post-review receipt BEFORE ordinary assembly.
  `sync --apply --assemble` is not a shortcut around that wall. Missing replacement
  copy still requires user clarification; "just do it" does not invent it.
- Skill-creator's Python validator could not import PyYAML in either available
  runtime. No dependency installed. Existing local js-yaml parsed both frontmatter
  blocks and verified name/description/schema/noTODO checks successfully; this is
  a fallback check, not a claim that the unavailable validator passed.
- Imported held preparation now derives a new0700 graphics-cache in its private
  attempt, ignoring caller cache paths. Post-fix cohort16/16 passed27.95s wall
  (26.911s suite): seven actual tiny-media reuse cases, three graphics-destination
  tests with an explicit abort-before-render stub, six publication fault cases.
  Exact192192 presented samples, native picture packets, full master identity,
  unchanged retained opening inventory, no recut/remaster. Evidence:
  `/private/tmp/sniper-held-cache-media-20260907.log` and
  `/private/tmp/sniper-held-preparation-xysbtfyq`.

ASR guards:49/49 final pure/CLI tests passed1.45s wall, including ten actual venv
entrypoints with paid SDK imports, sockets and media/model spawns blocked. Earlier
91-test local study/ASR cohort passed32.00s (includes synthetic media/OCR, no ASR).
Finding: `docs/findings/ASR_KEYS_ARE_NOT_SPENDING_AUTHORITY.md`.
Segmenter/Clipper canonical skills and Producer manual now match local default,
explicit per-invocation spending consent and unchanged local input limitations.
All four edited skill frontmatters passed local js-yaml checks.

Shared CLI subscription guards now pass the bounded independent cohort37/37 in
4.29s wall. The reviewer first reproduced metadata-success returning in29.876ms
while its ignored-stdio child remained alive. The fix requires observed process-
group absence on success and failure; a successful metadata command with an
unexplained descendant is rejected even after cleanup. The same reproduction now
rejects and cleans in26.268ms, with the child absent. All six TS provider sinks use
the shared pinned subscription-auth admission; preflight is asynchronous and
awaited. Three optional legacy Python Claude entrypoints reject execution before
provider/session reads; their streaming bridge is not implemented. No live auth,
models or paid providers were invoked. This does not prove account overage is
disabled, constrain arbitrary Bash/MCP spending, or fix inherited inference-runner
cleanup/crash gaps. Evidence:
`/private/tmp/sniper-subscription-cleanup-independent-20260907.log`.

The direct `scripts/producer/guided-opening.ts` command now exposes the existing
status, launch-status, review-files and launch services without a web server.
Only an exact bounded persisted request can launch; no regenerated retry UUID,
implicit approval, clock reset or automatic retry. Review-files verifies actual
selected file hashes/sizes and rechecks the journal/selection before returning
absolute local paths. It does not requalify source freshness, listen or approve.
Canonical directory validation no longer creates a personal workspace config.
Independent10/10 tests passed1.29s; actual retained TEST-fixture metadata/file
reads worked, with both media hashes checked in165.011ms. No new direct-CLI render
was run. Whole TypeScript check passed after the final test cast fix. Instructions
in `CODEX_COMMAND_WORKFLOW.md` require the repository-root working directory and
explicit approved runtime controls: plain tsx does not load `.env.local`.

Held ordinary assembly now has an exact-frame opt-in tied to the actual retained
master's rate/count. Defaults are unchanged; an unheld or mismatched clock fails
before media work. Actual24fps/NTSC markers previously landed a requested[4,10)
graphic on frames5–10; the opt-in now lands on frames4–9.16focused tests passed
2.20s;25held-media/publication tests passed34.65s, preserving the full-master
identity, retained opening inventory and192192presented samples with one audible
AAC encode. That held full-output smoke has no graphics; the separate marker
tests prove frame endpoints, not full-body visual parity.53adjacent registry/lane
tests passed10.72s. No body launch or delivery authority was enabled. Evidence:
`/private/tmp/sniper-held-frames-media-cohort-20260907.log` and
`docs/findings/HELD_BODY_GRAPHICS_MUST_KEEP_OPENING_FRAME_CLOCK.md`.

Owned isolated Next test server3331 (exec session64057) was stopped normally;
retained fixtures/outputs remain intact. Its accumulated log includes uncaught
`ERR_INVALID_STATE: Controller is already closed` after some media-range requests.
This is an unresolved custom-UI streaming observation, not output corruption
proof; do not count it as a clean whole-UI run or divert from the direct Studio
priority. Existing other servers were not touched.

No new real creator final, fullbody approval, complete short/long regression or
two-hour production qualification is implied by these bounded fixes.

### Continued body prerequisites and direct human review

Direct Codex command now also dispatches `approve` to the existing explicit
human-opening service. All five viewing/listening/decision attestations must
already be true from the operator; the command does not infer or fill any.
Its exact bounded request, original UUID and separate closed operation survive
retries. No body render or delivery approval is chained. Replay only acknowledges
the historical recorded decision; `verificationScope` explicitly distinguishes
it from a new decision's fresh readback.14direct-command tests pass (including
real local help/grammar execution); no human approval was performed in testing.
The independent reviewer found an inherited FIFO-read hang in the approval
service. It now uses the shared no-follow/nonblocking descriptor-bound reader,
deduplicates selected paths while checking every row's SHA/size, and invokes
the original lease/deadline guard before opening, between chunks and after final
identity checks.27related tests passed1.27s wall; a real FIFO child rejected in
0.450s, no live resources. Synchronous kernel I/O is not a hard timeout guarantee.

Python `full_program_frames` now checks ALL body rows rather than returning the
opening intersection. Shared validation binds actual candidate/occurrence JSON,
unique IDs and exact one-to-one ordered catalog operations, integer endpoints,
seconds mapping and presentation. The opening keeps its eight-intersection
limit; the whole-program128-row bound is only structural. TS has a separate
all-row metadata wrapper; it does not replace authenticated sidecar rederivation.
Neither helper changes cuts, deletes unsupported lanes, renders or approves.

New `guided_graphic_template.inspect_full_program_graphics` reuses the SAME
template reader now called by `graphic_intent`, including existing content and
visible-hold checks, full animation duration and actual native canvas. It examines
all rows before any seal/spawn, rechecks template/input bytes and the caller's
budget. A late invalid/too-short card therefore cannot waste earlier body renders.
Tests use actual local catalog HTML with TEST plans, not real graphics renders.
The reviewer reproduced duplicate-order hiding in the TS wrapper and an expiry
during the final Python input hash; both are fixed with dedicated regressions.
Latest bounded cohort:8TS metadata tests/0.598s and31Python body-template/frame/
opening-result tests/0.175s. A separate35-test Studio/result/actual tiny compositor
cohort passed2.997s earlier. These overlapping counts must not be summed as a
unique full-suite total.

The bounded exact PRE-ENCODE compositor-prefix oracle is implemented. Its
26-test cohort covers actual fractional-rate graph pixels, missing/shifted/
reordered graphics, native input workload, byte limits and owned-child cleanup.
Root independently reran all26 plus seven new actual-composition binding tests:
33/33 passed10.591s suite. Retained fixtures include
`/private/tmp/sniper-prefix-oracle-7md3em2n` and
`/private/tmp/sniper-prefix-composition-baql8ast`. The new composition adapter
binds the oracle's exact ordinary compositor command to the actual encode,
retains original caller time and rejects input identity changes before/after it.
It is internal and not yet connected to an executable body worker. Independent
review caught an output-directory replacement race; the original version is
superseded by the descriptor-anchored implementation described below. The tiny
successful picture encode+proof took470.566ms; that is NOT native long-form
throughput.

Independently encoded H264/AAC prefixes need not have equal decoded bytes; do
not turn that false equality assumption into a quality gate. The shared held
float-master proof remains the audio identity authority. Whole delivered-file
QC/listening and actual creator quality/performance qualification remain open.

Body admission now has a separately fenced non-executable claim, exact retained
post-approval snapshot/one known journal edge, replay before new operation
creation, original120-minute clock and proposed55-minute body/25-minute finish
allocation. It cannot spawn, select body media or approve delivery. Independent
review reproduced a deadline expiring during the final CAS read segment, plus
a known clock error that left an operation without its failure result. Both
are fixed;72/72 focused tests pass4.68s independent wall, and the original CAS
exploit now rejects0.55s. Evidence:
`/private/tmp/sniper-body-claim-independent-fixed-20260907.log`.

The72 tests include TEST authority seams, not an unstubbed actual media→approval
→body chain. A real retained run14 read was attempted without mutations: it
correctly refuses because its historical nested-process ledger is missing.
Current readiness also rejects changed pipeline bytes. Old approval is not
fresh body authority; no missing ledger was fabricated or old pipeline resealed.
The newer retained selected run has no human approval. A fresh, explicitly
synthetic integration fixture is being assessed separately.

### Real-source cut-quality defect and active resolution work

Read-only inspection of C0679 exposed an unsafe rule: an overlong ASR timestamp
on the opening function word was described as dead air that must be deleted,
and the critic was told not to restore it. Actual source examples include
`If`0.37–8.64 and restart `if`11.54–13.62. Timestamp/confidence metadata does not
prove silence. The retained TEST cut begins13.62; it is not human-approved.

The deletion direction is fixed: kept and immediately excluded suspicious
opening words require source-grounded review, confidence remains uncertainty
metadata and the forbidden-reintroduction directive is removed.35 Python cut
tests, fake-only TS prompt/contract, type-check and scoped lint pass. This alone
is NOT a usable complete workflow: the gate had no source-bound review decision
that could discharge uncertainty, and deleting another ordinary word could
hide the nearest-neighbor risk. An immutable, exact-source/transcript/candidate/
window-bound review-resolution route is in progress. It must preserve unresolved
decisions, forbid generic waivers and leave other integrity gates unchanged.
No actual creator decision, transcript edit, ASR or provider call was made.

### Later independent findings and current verification

The prefix composition now reserves its output using a held directory descriptor
and exclusive/no-follow relative create, and FFmpeg writes only through that
held regular-file descriptor. Renamed directories cannot redirect writes into
retained media. Actual output bytes get a SHA/size readback; this still does not
decode or approve delivery. The additive bounded `pass_fds` runner defaults to
no inherited descriptors and preserves caller close ownership.

An initial FD transport returnedFFmpeg exit0 but produced corrupt MP4 under
`+faststart`; a new COMPLETE-DECODE test caught this after metadata looked valid.
The private intermediate uses end-of-file moov metadata, preserving every codec/
quality/graph setting. Existing final-delivery muxing retains faststart. Three
actual path/symlink races now fail without altering retained files. Independent
10/10 tests pass6.71s wall, including24exact fractional-rate frames and EVERY
decoded RGB byte matching the ordinary faststart reference. Original race now
rejects before creating output. Finding:
`docs/findings/HELD_OUTPUT_FDS_NEED_REAL_DECODE_TESTS.md`.

Root final combined prefix/runner/frame/template/target cohort:61/61 pass12.894s
suite, with synthetic artifacts `/private/tmp/sniper-prefix-composition-xsh6g6m_`
and `/private/tmp/sniper-prefix-oracle-v3lrq165`. These overlap earlier cohorts.
Current authoring instructions and canonical Producer skill also stop for
unresolved source timing review rather than repeatedly changing cuts until green.
Four related prompt/stream tests, whole type-check and focused lint pass.

Native-size timing observation: seven synthetic repeated catalog occurrences on
the retained311.742-second1080p/NTSC test base passed the prefix oracle in82.0035s.
Complete base decode/count39.8461s and graph comparison41.6055s dominate. It is
not a creator edit, new catalog generation, ten-minute or30-graphic benchmark.
Retained proof `/private/tmp/sniper-native-prefix-cost-p8ficdrx`; `/usr/bin/time`
could not read sandboxed RSS/sysctl and exited1 after the successful script.
No source/audio/visual quality check was removed to improve timing.

Fresh actual integration driver is implemented (no live run yet):
`src/lib/server/__tests__/_guided-body-live-fixture.ts --run`. It accepts NO
existing project, creates the supported300-second1080p TEST fixture with real
gates, invokes productionCLI launch/status/approve, then default unstubbed body
admission/read/replay. TEST attestations explicitly are not genuine human
acceptance/listening.22no-media tests passed4.20s; type-check/lint/control-path
preflight passed. Actual run is deferred until source stability across all
owners; estimate12–18minutes, not an SLA. No body worker is enabled by the test.

### Real-source visual baseline (not delivery QC)

Existing Audit frame extraction sampled the original, ungraded C0679 at15s,
300s and780s. All3 frames extracted and were visually inspected. Evidence:
`/private/tmp/sniper-c0679-source-stills-20260907.XwtSAc/audit_frames/`.
The three samples show broadly consistent lighting/background color, readable
subject framing and natural skin appearance. The780s sample has motion blur in
a moving hand/face; this alone does not justify sharpening or a corrective LUT.
Preserve the existing look unless a source/output comparison demonstrates a
specific improvement. These sparse JPEG samples are not calibrated color
measurements, full-video continuity checks, audio listening, creator acceptance
or finished-output approval. No source/project bytes were changed.

### Timing-review implementation complete; live-chain qualification next

The source-bound review-resolution CLI/consumer is implemented. Decisions are
explicit per-anomaly/per-window submissions; unresolved history, missing tails,
partial appends and concurrent conflicts block. The admitted manifest owns the
review namespace, so the exact cut can move into a generated gate-plan.json and
add only graphicsStyle/graphicsStyleRationale without falsely requiring a second
source audition. Changed sources, transcripts, cut/target semantics or manifest
path do not reuse the fact. Initial plan/manifest bytes are retained through the
cut receipt's final guard. Root independently ran70 Python tests in4.493s and11
focused TypeScript tests in1.455s, all passing; whole type-check and focused lint
also passed. The first lint invocation incorrectly used unsupported --file flags;
the corrected positional-file invocation passed. No live model/listening test
was performed. The stored head detects missing/partial history, not rollback of
every same-user file. Short-mode hook policy remains unchanged.

All source owners are now frozen for the new expensive actual synthetic CLI
chain. It qualifies opening selection→TEST-only approval→body admission/replay,
not body rendering. A future installed body worker changes the pinned pipeline
and will need its own fresh qualification; this run cannot authorize future code
by rewriting its old receipt or non-executable claim.

### Fresh actual chain exposed a real body-verifier budget mismatch

Source-frozen real integration ran06:21:17Z–06:32:17Z in
`/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-body-live-OJXYq0`.
Evidence is in `body-live-evidence/`; project `test-only-cut-review-d64b89b1`.
Actual source admission/preview/readiness, production opening launch/render,
closed cleanup, selected media, and production TEST-only approval all passed.
No actual creator listened or approved. Total driver time660184.9ms includes
the failed body hold. Setup144025.3ms, owned opening engine421834.8ms,
cleanup784.4ms; full-program preparation290159ms contains cut111587.3ms and
base picture finalization85905.4ms. Seven actual OCI renders total53221ms;
range composition25287ms; AAC/picture-copy10413ms. Nested phase timings overlap
their parents and must not be added twice.

Body admission failed2456.4ms into its actual hold because the55-minute body
remainder was passed directly into the opening verifier's25-minute process
class. This was missed by stubbed verifier tests. No body worker spawned or
body claim was successfully admitted. Failed operation/readback evidence and
selected TEST media remain retained; there was no automatic retry or reseal.
The corrective subdeadline is captured once, capped by the original remainder
and existing25-minute verifier ceiling, with decreasing/floored child time and
final checks. Hubble reports32 focused tests including an actual tiny child
passing4.07s, type-check/lint passing; fresh media-chain verification after this
fix is still pending. A future installed body worker will be included in that
next fresh qualification rather than another admission-only rerender.

The next implementation is one foreground owned Codex body command, not another
detached UI/controller subsystem. Separate executable activation, exact process/
Docker cleanup, held base/master, complete template checks, prefix-bound actual
composition and full output QC remain required. V1 body admission stays false/
non-executable. Crashed/ambiguous runs stay fenced without automatic resume.

Root's internal owned-graphics seam is implemented and under independent review:
`AssembleJob`→`GraphicsJob` forwards only live internal callbacks, not JSON flags.
Ordinary placement/caption/frame checks remain; prefix composition deliberately
uses standalone decoded smoothness instead of claiming a missing inline YDIF tap.
Source-float publication also checks the live owner.9 focused tests pass2.186s,
including actual fractional-rate prefix composition and complete picture decode
using TEST markers; placement verifier invocation is stubbed in that tiny test.
56 adjacent tests pass1.163s. This is not yet an executable body/OCI integration.

### Owned graphics adversarial fix and independent verifier regression

Independent review reproduced a real relational-proof bug: the callback encoded
a two-graphic graph instead of the requested three-graphic graph, omitting a
later body graphic. Its opening pixels legitimately matched and its actual
output had the expected path, so the original shallow result check accepted it.
The seam now binds the returned fullGraphHash, exact frame clock/canvas, held
base path and encoded output path to the actual resolved call. Independent
source-byte authority still belongs to the held execution and prefix adapter;
the seam does not invent approval or repeat whole-base hashes at each guard.

Root's67-test owned/frame/graphics/assembly cohort passes3.279s, including a
new REAL oracle-plus-encode regression proving this valid-but-different graph
is rejected. Artifacts: `/private/tmp/sniper-owned-graph-binding-r1dq7t9q`.
Separate adversarial empty-track execution also passed: zero renders, one owned
composition,24 retained frames and empty placement evidence, with no unowned
passthrough. The independent32-test body deadline/authority/claim cohort passes
4.265s, including the actual tiny child process. These are overlapping targeted
cohorts, not a statement that the complete repository or new body worker passes.

Actual synthetic review frames at2s,36s and66s inside rendered graphic windows
were extracted and visually inspected. Those samples are legible with no
obvious text clipping. They do not establish full animation, pacing, calibrated
color, listening quality or creator acceptance. Evidence:
`/private/tmp/sniper-body-live-visuals-20260907.WJqf5p/audit_frames`.

Root added an actual full held-assembly seam test: real empty-prefix composition
feeds the same retained PCM master, one final AAC delivery and whole Audit B.
The retained picture's video packets match final video packets, final audio
decodes completely with exact sample count, and no audit failures remain.
An owner-expiry injected after the REAL audit prevents candidate final/report
publication. Two tests pass20.022s; fixture
`/private/tmp/sniper-held-preparation-xpt4xdys`. This uses tiny generated media,
not OCI graphics, actual body activation, listening or creator approval.
An earlier attempt correctly failed admitted-source drift because root edited
the assembly return metadata while that test was running; the rerun coordinated
source stability and kept the checks intact. An accidental imported test-class
discovery also ran8 extra existing tests in that failed attempt; the test now
imports the fixture module instead and runs only its own2 cases.

The assembly now returns the exact actual `programDeliveryReceipt` path/hash,
allowing the body reader to bind its retained PCM-to-AAC execution receipt
without scanning directories or resealing it. This changes returned metadata
only, not encoding or approval. The foreground TEST driver support now has an
explicit body-command observer class: at most60m10s including protected cleanup
headroom, also bounded by the original request horizon. The actual body worker
still has its own55-minute/original120-minute limits. Eight driver boundary
tests pass0.683s; extension to invoke the completed body service is pending.

### Body continuation integration — 07:33 UTC checkpoint (still in flight)

The foreground body CLI and exact candidate/status contracts now exist. The
TEST driver invokes that real CLI and verifies the exact candidate, actual
Audit B, all replay/status lineage fields, unchanged protected inputs, unchanged
operation inventory and an intentional different-request conflict. It cannot
treat any rejection as that conflict: only a normally stopped child returning
the exact closed `BODY_REQUEST_CONFLICT` response qualifies. A timeout or
unproved cleanup rejects. Eleven no-media driver tests pass independently
(0.48s wall), including the original timeout/unknown-cleanup false-positive
reproduction; whole TypeScript check and focused driver lint passed. The full
fresh body CLI run has NOT yet occurred.

Independent Python integration review found two readback gaps: an incomplete
Audit B could be mistaken for full coverage, and a final support-file mutation
could escape the final check. Dedicated body readers now require applicable
named checks/counts and actual planned-frame coverage, retain warnings, and
hold known cut/timeline/assembly/placement/audio/cover evidence through the
final guard. Independent reruns passed25 focused tests (0.48s wall). Review
also caught an over-strict own-screen contrast requirement that did not match
the actual auditor's applicable checks; that compatibility correction is in
flight, not a reason to weaken actual contrast policy.

A separate real integration miss would have blocked graphics in the private
body directory: ordinary Audit B searched for a nearby `base_final.mp4`, but
the retained whole-program base lives in the original opening directory. The
source-float assembler now passes its exact base as an explicit runtime-only
audit argument, checks its whole SHA before/after QC, and requires its held
preparation SHA. It does not copy the large base, create a symlink, discover a
different reference after failure, or trust a plan-supplied reference path.
The reference reaches both actual frame extraction and placement comparison.
Default omitted-argument behavior remains unchanged for existing callers.

Root's actual one-graphic held assembly plus three boundary tests pass4/4 in
15.663s: `/private/tmp/sniper-held-preparation-prrbinzj`. The real audit contains
31 PASS/0 WARN/0 FAIL, three of three reference frames and graphic presence
changed-pixel ratio0.9854. Full audio decoding passed at−14.01LUFS and−6.04dBTP.
The base remained byte-identical; no PCM remaster occurred. This uses a tiny
synthetic grayscale graphic, actual ordinary placement/prefix/assembly/QC,
not a HyperFrames/OCI graphic or creator approval. Initial test-fixture plans
correctly failed real lint (wrong aspect, missing purpose/spec, too-short
readability hold); the fixture was changed to legal landscape statement-card
intent BEFORE preparation. No gates were skipped. Failed fixtures remain
`/private/tmp/sniper-held-preparation-yea_zl75` and
`/private/tmp/sniper-held-preparation-0anhylj3`.

The earlier two empty-graph actual assembly tests also pass again (including
the one-AAC receipt assertion) in the20.942s cohort that separately reported
the failed one-graphic setup; `/private/tmp/sniper-held-preparation-hnkmh0cm`.
Do not report that mixed cohort as all-green. Adjacent pixel/placement tests
passed independently22/22 in4.10s wall, with no native render.

The existing Producer skill and command guide now document the body command
as implemented but live-unqualified, its original clock, initial own-screen
workload limits, exact request/replay rules, explicit cleanup and separate
creative/listening/delivery decisions. No custom UI or new competing skill was
added. The next qualification remains a fresh frozen-code synthetic complete
CLI chain, followed by real creator decisions and media-specific qualification.

### Fresh frozen CLI chain — 07:40:32 to 07:53:01 UTC, FAILED before body activation

All owners froze before the run. Body/ownership/driver76/76 passed6.61s,
shared guards/history/ledger41/41 passed1.37s, type-check1.71s and lint2.28s
passed. The independent cleanup review found no new blocker; reduced recovery
cannot select media or declare unknown local ownership resolved. Sandbox-only
socket/`ps` denials were retained as unknown failures; scoped-permission reruns
passed. They were not reported as successful absence observations. Ordinary
source-float assembly regression also passed5/5 in24.517s, including retaining
prior output after actual QC failure and a scoped music revision.

The actual local-only full driver ran in
`/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-body-live-DY4yOw`.
The source is driver-generated five-minute1080p TEST media; no provider/model
call or creator listening/approval was performed. Test attestations remain
explicitly synthetic. Initial disk free211GiB, system load averages6.20/6.85/7.22;
this is not a controlled-idle throughput benchmark.

Measured whole driver elapsed748885.672ms (12m28.886s), exit1. Passed: actual
source admission/cut preview/readiness, production opening launch, seven sealed
graphic renders, owned worker return/cleanup, current selection and production
TEST-only approval transaction. Source/preview/readiness took158715.083ms;
opening worker455798.764ms (7m35.799s); cleanup782.640ms. The opening worker's
full-program preparation was312551ms, seven actual graphic renders60289ms,
range composition28985ms, picture-copy/AAC10522ms, whole-input revalidation
12522ms. Nested stages overlap; do not add these to parent totals.

Body continuation's actual opening/source readback passed39351.906ms. Its
foreground CLI then failed normally after45441.221ms:
`BODY_COMMAND_FAILED: Body claim retained whole program references differs
from exact held authority`. Actual outer group stopped normally; no timeout or
forced stop. Body request8f55808d-82ec-4d36-9716-a0c5f71dd07b,
executiona2e2db2b-5d7f-41cd-9a99-50e07b73431e retained only six admission files:
start, held-input, budget admission/precommit, non-executable claim, failed
result. NO activation, body media input/output or process intent exists.
The claim remains retained and non-executable, bodyGenerated:false and
deliveryApproved:false. No retry/replay, adoption, reseal or claim deletion was
performed. No full-body render/cleanup/readback success is claimed.

Independent diagnosis used exact retained bytes: the base path and SHA match,
as do candidate/manifest/master references and held result hashes. The writer
stores `{path, sha256}`, while the historical reader compared that reference
with the entire rich opening-base probe record (eleven additional measured
fields). This is an interoperability bug, not source/media drift. A shared exact
reference projection and rich-probe/actual-retained-reader regression are in
flight; full opening-result SHA and approval/media bindings must remain intact.
The failure demonstrates why mocked happy-path shapes are insufficient, even
when isolated real-media seams and many fault tests pass. A new source-frozen
complete run is still required after this correction.

### Reference interoperability fixed; whole-body eligibility checked earlier

The reference writer and reader now share the same exact `{path, sha256}`
projection. Full held opening-result SHA, approval and media bindings remain
required. The actual DY4yOw historical claim reads successfully in90.205ms
(0.41s wall), independently verified with its journal and six admission files
unchanged. This is read-only regression evidence, NOT permission to activate or
retry the failed attempt. Exact retained/authority/claim tests passed28/28 in
3.87s; broader body tests passed61 with one explicit retained-path opt-in skip.

That preflight exposed a separate TEST recipe problem: its eighth graphic is
an always-presenter-hole takeover, unsupported by the initial full-body
own-screen profile. Opening-only intentionally qualifies just its seven
intersections; it must not reject later intent belonging to another lane.
The body authority now checks all-row metadata BEFORE the costly opening
verifier, retaining its later check too. A realistic eight-row regression
proves this unsupported case invokes the expensive verifier zero times.
Focused tests passed35/35 in4.45s; broader body tests63 passed/one opt-in skip;
type-check and lint passed. No graphic is silently dropped or profile waived.

A separate explicit TEST-only `longform-body-300` source/recipe is being
qualified for a fresh complete run. It authors a spoken numeric comparison
before transcript/admission creation so an own-screen chart can truthfully
illustrate the closing comparison. Existing `longform-96`/`longform-300`
variants remain unchanged. The driver now requires the new marker and checks
all-row metadata/workload before opening work. Driver boundaries11/11 passed
in0.704s, and whole TypeScript check/focused lint passed. Real all-template
preflight and independent recipe review are still in progress; the full body
chain remains unqualified until a new frozen-source run passes.

### Fresh eight-graphic full render — 08:12:35 to 08:30:08 UTC, QC rejected

All owners froze after actual no-media template/gate checks and independent
review. New explicit TEST-only `longform-body-300` source uses311.395s planned
output and347.545s source with eight distinct forms. The previous program and
treatment hashes remain unchanged for both historical variants. Python36/36
passed1.113s; independent new variant6/6 passed0.875s and TS driver/authority
26/26 passed0.436s. Initial disk free204GiB and load10.77/7.79/7.83; this is
again not a controlled-idle ten-minute throughput qualification.

Actual retained workspace:
`/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-body-live-Pc8XlW`.
Whole driver elapsed1052708.219ms (17m32.708s), exit1. Source/preview/readiness
passed148953.681ms. Opening media worker passed393701.482ms (6m33.701s);
opening execution through selection436559.401ms (7m16.559s). Its full-program
preparation took266335ms. The driver completed the real TEST-only approval
transaction, then body admission and separate activation succeeded past the
previous reference bug. Original clock started08:14:42.168Z and was not reset.

Body requestdbba1fea-3828-463a-b751-e7af994873cf, execution
0e983e33-1108-4708-9815-7f174444a760. Its foreground CLI took418666.960ms;
actual body worker366049.199ms, failed normally with groupStopped:true,
forcedStop:false, timedOut:false. All-row templates passed486ms, original
whole-base/master read10948ms, eight actual sealed graphic renders55187ms
in total. Prefix comparison/picture encode logged complete103578ms and
retained-picture copy954ms. These child timings overlap parent assembly;
do not add them to the full worker elapsed. No recut or new master was created.

Actual full Audit B executed against the staged private candidate:
`body-media-output/body-candidate/.source-assembly-v2-ce8s3f7i/audit_report.json`.
It returned33 PASS,3 WARN,2 FAIL. All272 required sample frames and24/24
graphic reference frames were extracted; all eight graphic presence checks
passed. Full audio decode,1920x1080 H.264 High/yuv420p,30000/1001 CFR,48kHz
stereo AAC, faststart and exact observed A/V alignment passed. Measured audio
was−14.0LUFS,−5.0dBTP. These do NOT qualify the overall failed output.

The two failures are concrete:
- Stable synthetic141Hz tone:50.9dB prominence,−24.0dBFS,100% stability over
 230.5s. This is a legitimate detector failure caused by the old TEST harmonic
 bed, not a reason to relax production hum detection.
- Eye-trace applied a fixed720px side-rail band to explicit own-screen
 `nateherk-bullet-bars` with the correct full1920x1080 bbox. The compositor
 already honors the presentation override; the auditor looked up kind geometry
 before its own-screen check. A narrow applicability fix is being tested,
 retaining full-canvas and ordinary rail checks.

Warnings remain for four dark runs inside declared takeovers, eight freezes
(longest4.50s), and rendered16 vs81 planned scene changes in the repeating
synthetic background. They are not suppressed or counted as clean passes.
Root inspected actual graphic midpoints at2.302s,16.133s and79.145s: no obvious
text clipping; the native rail has a large blank right region, and the chart
has no visible value labels at that sampled frame. This is mechanical template
coverage, not evidence of good creator pacing, chart clarity, color grading or
listening. The test source is a calibration signal with a synthetic transcript,
NOT actual speech/ASR or human acceptance.

The run retained failed media and selected no body candidate. Exact cleanup
evidence is under independent review. The failed run has no `body-result.json`:
its successful prefix-stage timing is execution evidence, not an independently
re-readable durable whole-graph proof receipt. Do not reconstruct one or retry
the failed claim. A fresh complete chain is still required after the bounded
audio-fixture and placement-audit corrections.

Independent failed-run reconciliation is now verified read-only: normal media
stop,20 nested process intents/spawns/reaps with no pending child, eight exact
container removal/absence records, and908.656ms owned cleanup. The original
clock and journal remain unchanged. Actual body result/candidate readers reject
the failed worker/missing candidate phase; Python rejects the failure marker.
Status returns candidate:null and no automatic resume. Same-request metadata
replay/different-request conflict are source-inspected here, not actually
executed against this failed attempt. No published final/body-result exists.

The placement applicability correction is atomic: only explicit own-screen
skips free-band rail geometry. Valid finite ordered measurements and exact
full-canvas coverage remain mandatory. All43 focused tests passed, including
read-only actual retained plan/placements; independent5/5 passed too. The
original failed Audit B was not rewritten or relabeled.

The separately named TEST audio branch now uses seeded band-limited modulated
non-speech calibration PCM plus short word markers. Historical branches and
production audio/QC thresholds are unchanged. Before another video run, actual
347.545s audio-only sourceAAC→sharedPCMmaster→deliveryAAC→complete loudness/
decode/audio checks passed39.123s. Evidence is retained at
`/private/tmp/sniper-body-audio-s0t1__9e/TEST-audio-evidence.json`.
FinalAAC measured−14.13LUFS/−2.24dBTP; hum candidate232Hz had11.0dB prominence,
−45.9dBFS,1% coverage and maximum1.0s run. Balance passed; music-disabled
ending was explicitly inapplicable. This proves no video sync, actual speech,
ASR, listening, creator approval or full body qualification. Tiny deterministic
branch-preservation tests and independent review are being finished before the
next fresh frozen-source full run.

### Corrected full media and readback — 08:38:56 to 08:59:59 UTC, final clock transaction failed

After independent audio review and frozen sources, the fresh full driver ran at
`/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-body-live-5ZURW5`.
Elapsed1262896.296ms (21m2.896s), exit1. Source/preview/readiness passed
126312.513ms. Opening media worker371021.616ms; through selection414108.325ms.
Original generation clock08:40:46.284Z remained unchanged. All test approvals
remain synthetic, with no creator listening or acceptance.

Body request123671ae-d379-4628-881b-1338b5719a64, execution
af618505-2c62-44a1-ab50-163b3651ff2e. Full body worker completed normally in
491824.877ms. Eight actual sealed graphics rendered54718ms total. The full
prefix/picture encode completed103631ms, ordinary whole assembly plus Audit B
357329ms, in-worker complete final media/QC read102958ms, and whole-input/result
revalidation8501ms. Parent timings include child stages; do not add them.

Actual full Audit B returned35 PASS,3 WARN,0 FAIL, with272/272 required frames
and all24 graphic reference frames. Hum and eye-trace passed. Warnings remain
for declared dark takeovers, static holds and the repeating synthetic source's
16 detected changes versus81 planned cuts. Final media is1920x1080 H.264 at
30000/1001,9325 frames; the copy step preserved all9325 picture packets.
Audio decoded completely, measured−14.01LUFS/−2.38dBTP, and has14934920
presented48kHz stereo samples plus120 trailing AAC padding samples. Observed
A/V end skew is zero. Final SHA is
`532680888dfdacb0675a85c3855e4e8a9569fe2f4e87f11e090d1a4074e2b749`.
The private file and complete `body-result.json` exist under this attempt.
This time durable prefix proof exists in that receipt; independent provenance
review is in progress. No creator/delivery approval is implied.

Separate actual post-worker Python readback also completed normally in
126506.074ms; its `verified.json` was retained at08:59:57.962Z. However the
foreground body CLI failed after675333.930ms before candidate CAS:
`Generation budget clock-invalid: wall clock moved backwards across attempts`.
The run remains unselected, failure-marked and non-retriable automatically.
Do not promote its valid media receipt into a successful command result.

Two independent traces confirmed a stale timestamp ordering bug, not evidence
of an OS clock adjustment. `bodyClockObservation` captures a timestamp before
its guard runs. The readback supplied a combined ownership/budget guard whose
budget check itself persisted a later clock observation. The outer observation
then compared its older timestamp against that new high-water mark. Actual
order was57.962 verified →57.981/59.168 high-water →59.231 healthy catch clock
→59.279 failure (clockError:null) →59.291 healthy command clock.

An independent no-media reproduction with strictly increasing time reproduced
the exact false failure in21.032ms: outer `.000`, nested `.001`, outer rejected.
Evidence: `/private/tmp/sniper-clock-nested-repro-IeZvb2`. The bounded correction
passes an ownership-only guard to explicit clock persistence while retaining
combined budget/ownership checks around candidate reads/CAS. Genuine backward
clock rejection, original origin, deadline and replay restrictions must remain
unchanged. It is being regression-tested before another fresh full chain.

The bounded clock fix is now atomic/frozen. Four actual durable-clock tests
pass; old wiring reproduced three expected failures plus the explicit old-bug
reproduction. Broader clock/body regressions37/37 passed4.54s, whole TypeScript
check2.10s and lint1.80s passed. Genuine1ms rollback, post-read55-minute expiry
and lost ownership remain failures. The same original origin/UUID and decreasing
work remainder are checked. An independent final wiring review is in progress.
No retained media/approval/journal was edited, and the failed result was not
adopted or retried. The fresh complete CLI success is still outstanding.

### Fresh production CLI chain PASSED — 09:08:18 to 09:29:27 UTC

The source-frozen `longform-body-300` TEST driver completed with exit0 in
1269402.334ms (21m9.402s). Workspace:
`/private/var/folders/3m/8rxbwgds5z7c3r72ccfqb9280000gn/T/sniper-body-live-SnNtCx`.
Project:`test-only-cut-review-ea3dc8bf/producer`. This is a new attempt from new
admitted media, not a retry/adoption of any retained failed claim.

Actual full chain: source/cut preview/readiness125713.243ms; opening media
worker370797.801ms, opening through selection413709.554ms; production TEST-only
opening approval; full-body admission/activation/render; proven worker return
and exact resource cleanup; separate full readback; candidate transaction;
actual status; read-only identical-request replay; exact different-request
conflict. The driver's `0149-cli-body-run.failed.json` is the EXPECTED negative
different-UUID rejection, not a failed run. Top-level `result.json` says
`TEST-chain-passed-private-body-candidate`. No genuine human acceptance,
creative-quality qualification, body approval or delivery approval was created.

Original generation clock09:10:07.283Z remained unchanged. Body request
6f89c139-7290-4540-95d4-a65948d1940b, execution
2856fc8b-013c-4bf5-8f32-084340a5e232. Actual body worker completed491836.925ms
(8m11.837s). Its original-base/master check10783ms; eight actual graphics54774ms
total; prefix/full-picture encode103368ms; whole assembly plus Audit B357193ms;
in-worker full media/QC read103134ms; whole-input/result revalidation8373ms.
Parent stages include child timings; no sums across nested spans are implied.

Full Audit B:35 PASS,3 WARN,0 FAIL,272/272 required frames,24/24 graphic
references and eight actual graphic-presence checks. The same warnings remain
for dark takeover intervals, static holds and the repeating synthetic
background's scene-change count. Final media has9325 frames at30000/1001,
1920x1080,486414278bytes; SHA
`6e044ce1a74891a4f834c0674a7b27c3833b99015bbc8457df8a3fce8dc8d89a`.
All9325 picture packets were preserved into final A/V. Whole audio decode
passed at−14.01LUFS/−2.38dBTP,14934920 presented48kHz stereo samples plus120
trailing AAC padding samples. The output uses the original full master, not
a recut or remaster. Both bodyApproved and deliveryApproved remainfalse.

Independent lifecycle review now passes against actual committed/status
records: candidate fact is in the current journal,167 clock observations retain
the same origin, media/read/cleanup ledgers are closed, and885.134ms cleanup
proves all eight exact registered containers absent. Read-only replay and the
different-request conflict preserve the operation inventory and final SHA.
No public producer/final.mp4, failure markers, extra replay attempt or approval
exists.

This closes the specific fresh full-body CLI mechanical qualification gap.
It does NOT complete the broad editor goal. The initial own-screen/non-caption
profile is narrow; actual C0679/source timing and human cut review, requested
finishing lanes, a captioned/reframed short, scoped revision, matched ZIP/current
comparison, real audio/color/listening and prospective ten-minute timing/quality
qualification remain. An asynchronous question asks the operator whether the
real C0679 trial should preserve story order with conservative cuts or restructure
toward ten minutes.

Final independent QC/evidence review passed0.93s wall: raw/semantic body receipt,
candidate/final/picture references,272 required QC JPEGs and19 support references
rehash correctly. All24 matching graphic-reference JPEGs exist and hash-read;
all eight presence checks pass. Exact opening master event and actual full PCM
rehash match the body delivery. Prefix evidence binds1853 core and2013 review
pre-encode frames, not independently encoded byte parity. No video/audio decode
or render was repeated in this final audit. The initial base's legacy AAC
transport is still honestly recorded; final audible audio comes from the shared
PCM master. This confirms the restricted synthetic mechanical scope only.

### Fresh C0679 admission PASSED — 09:44:35 UTC

The real creator source was freshly admitted without ASR in
`/private/tmp/sniper-c0679-current-20260907.9p06WA/source/asset_manifest.json`.
Actual command exit 0; manifest generated 09:44:35.439951 UTC, completion
observed 09:46:36 UTC. Approximate launch 09:36:12 UTC gives approximately
8m23s to the manifest, not precise internal stage timing.

The retained current-policy receipt proves full decode of 10,280,473,262 bytes,
834.335 seconds, 3840×2160, exact 24000/1001 fps, 20,004 declared video frames,
stereo 48 kHz audio. Networkless, read-only-root, non-root local sealed probe;
no model call, paid API, model/image download or original-file mutation.
Source SHA `06c50ce8c2e9da0a954f9301b1cba0dbfeb62ecebcdd956a034eb1597a301e14`;
media receipt SHA `20a5085dfdebf51a67ca89d34a0eeb6e62b22c3f695f53a5a679fe789b326dc6`;
source-set receipt SHA `ff97c40d4023b040b14e0c43de295fd8ed64c88c406828c3e7ddf22ecf974b1e`.
Detailed scope and observation times are retained in that workspace's
`qualification-notes.md`. No transcript, cut, creator approval or final exists.

Follow-up timing audit found local ASR's prior 3600-second allowance was
per attempt: one CPU fallback could receive another full allowance, while
probe/extraction and source/model hashing were outside it. A narrow aggregate
deadline correction is in progress, with no model/thread/timing-quality change.
Root's first 23 deadline/source/ingest/resume/policy regressions pass (0.084s
wall). Core retry, actual child-group cleanup and CLI/study forwarding are
being independently implemented/tested; not yet a live-ASR qualification.

### Aggregate local ASR qualified; real C0679 timing-quality gap — 10:17 UTC

The narrow aggregate-clock correction is implemented across local Whisper,
ingest/resume, and Study's local transcription caller. All retries, source and
model hashes, probing/extraction, bounded output parsing and publication consume
the original per-source deadline. A verified parent-owned worker group prevents
nested child sessions escaping ordinary cancellation; this does not claim
recovery from arbitrary owner death. Error/garbage/duplicate/post-done NDJSON
cannot be accepted as a successful result. The unchanged provider policy never
downloads a model or falls back to paid ASR.

Final integrated local regressions: **122/122 passed, 4.782 seconds**, including
real child-group deadline/output/descriptor tests. The broad existing paid
transcriber monolith was not refactored. These tests do not measure ASR quality.

The actual fresh C0679 resume command then exited 0: **165.06 seconds wall**
(2m45.06s), with **160.155 seconds** reported for the source transcription
stage. The whole command also includes admission revalidation outside that
per-source clock. Input: 834.335s of real 4K footage. Existing local small.en,
8 threads; provenance reports CPU execution. No paid API, model/image download
or original/admitted-source modification. The reason CPU was selected is not
retained, so no GPU-fallback cause is inferred.

Fresh `source/raw-1.transcript.json` has 162 utterances and 2235 words; SHA
`fa7a789d45efa029c5d10521fa201080caea8ce00b258d251179de3fa41ecd3d`.
Manifest SHA is
`3cb9ea3af7cba3aa9329a9a2184401467a176a8e893e3a1752e2a3921019ccec`.
Independent verification passed result/source bindings and structural timing
checks. Root read the entire transcript before considering editorial cuts.

**Structural timing PASS is not acoustic boundary qualification.** First “If”
spans 0.37–8.64s (8.27s); restart “if” spans 11.54–13.62s (2.08s). Both trigger
the existing opening-timing anomaly classifier. 2204 of 2234 neighboring word
pairs touch exactly; the largest positive gap is only 0.37s. The current parser
uses wrapped row ranges and distributes words within them, ignoring available
token intervals. These timestamps cannot establish actual pauses or safe cuts.

The unapproved retake/pause scanner completed in 0.11s: four retake suggestions,
96.26s total proposed removal, and zero pause suggestions. Zero suggestions is
NOT evidence that the source has no pauses. The full semantic read also found
off-script asides/restarts missed by the deterministic scan, and its 68.12s
closing retake proposal needs editorial review. No cut, human listening record,
creator acceptance, timing override or delivery has been written.

Next is a private cropped-context raw-token comparison using only the existing
model/runtime; the admitted transcript remains unchanged. Independently, the
Codex workflow audit found that later guided services are CLI-callable but a
production new-project/existing-candidate bootstrap adapter is still absent.
The successful TEST bootstrap is not valid authority for real C0679.

### Raw-token experiment and real unapproved cut — 10:35 UTC

A private cropped-context comparison using the same installed model and CPU
completed in19.69s command wall (19.591410s aggregate work, original60s cap).
Extraction0.367404s; ordinary token arm4.441921s; DTW arm4.756802s. Full source
hashes before/after match; current manifest/transcript remain unchanged.
Both arms produced identical text and ordinary intervals:75 rows/73 words.
Actual token envelopes narrow23 words by1.730s total relative to row envelopes,
but **do not fix either stretched opening If**. DTW points disagree with many
ordinary intervals; no DTW default, endpoint fabrication or acoustic approval
was introduced. Evidence and the exact distinctions are documented in
`docs/findings/ASR_ROW_TIMES_ARE_NOT_SPEECH_BOUNDARIES.md`.

The pause proposer now exposes explicit informational timing scope, including
high-touching-boundary warnings, never acoustic silence/absence approval. Its
actual C0679 run took0.08s and preserved all prior proposals. Independent
review found a finite-number subtraction overflow edge; root fixed it and
the final pause/edit/apply-pauses cohort is35/35 PASS in0.063s.

Root authored one **unapproved** original-order produced cut proposal in
`/private/tmp/sniper-c0679-current-20260907.9p06WA/candidate-cut-produced/`:
nine kept ranges, eight evidenced inter-cut removals,685.88s proposed output.
The77.2s resources-retake removal is explicitly a content-choice review, not
dead air: earlier immediate-script examples differ from the final future-series
promise. The complete outro is retained. No ten-minute runtime was forced.
An earlier clean-cut-target sibling is historical only, not the intended
produced workflow or a way to bypass its stricter hook-timing gate.

The actual previsual check failed: four repeated-word errors plus two opening
timing uncertainties; “very very” remains a warning. Candidate and transcript
are preserved, with failure JSON and REVIEW.md. The real `prepare` timing
review freshly hashed source bytes and completed4.89s wall (4829.381ms internal).
Request SHA98017ad5200b99d72e6bb5e0f4d2b69923ca1fdf4b674971f1345a219a1c8b37;
source windows9.54–14.62s and0–13.54s; both decisions unresolved. No listening
record, cut preview, acceptance, treatment clock or delivery exists here.

### Codex treatment command bridge and dead-worker fast failure — 10:44 UTC

New `guided-treatment.ts`/`guided-treatment-status.ts` expose existing v2
accept-cut, raw admission, compilation, readiness and strongest-stage status.
No UI server, writer, invented consent, model override, automatic retry or
new rendering engine. Strict bounded JSON transport rejects duplicate decoded
keys at all depths, nonfinite values, oversized/deep/token-excessive input and
unsafe files. New/adjacent tests46/46 passed; independent rerun1.199s plus3018
seeded JSON string/escape/duplicate checks in62.44ms. Whole TypeScript and
focused ESLint pass. Command scope/replay/consent limits are in the workflow doc.

The external-media waiter now checks the exact launched container ID at bounded
intervals while preserving result-first publication. A confirmed terminated
worker is reread once for a racing result, then fails promptly with its actual
exit/OOM reason; unknown state cannot authorize success. Original remainder
bounds inspect/sleep/late results, and canonical-absence cleanup stays mandatory.
Independent root rerun:37/37 tests PASS0.379s, including virtual-time/failure/
cleanup cases. No real Docker/model/media work occurred in those tests.
The old code's simulated dead-worker wait was1220.462891s; this is a reproduced
wasted-wait bug, not a measured20-minute saving on the successful C0679 admission.

Host waiter changes do not alter the retained source-admission policy/receipt
identity, but do change broad pipeline closure hashes. Existing execution pins
must not be retrospectively re-blessed. Source-policy/version/worker/limits and
isolation were not changed by this slice.

The strict token parser is separately undergoing independent counterexample
review (quote/spacing cases exposed real defects before live qualification).
A production new-only admitted-candidate bootstrap is being implemented through
the existing worker/services; not yet qualified. Broad editor goal remains open.

### Full-source parser counterexample and independent editorial review — 11:00 UTC

The strict ordinary-token parser passed 96 focused tests and independent
counterexamples, but **failed the normal full-source local-provider CLI**.
Actual start 10:50:19.935522 UTC; provider work 153.066588s, aggregate work
158.266476s, command wall 158.33s, exit 1. Retained provider events show the
normal GPU attempt failed (its emitted tail truncates the full return code),
then the existing CPU fallback returned output
which the parser rejected: `lexical token has nonpositive duration`.
This is a real integration failure, not a successful ASR qualification or a
reason to infer word endpoints. No default CPU/DTW/model override was used.

Evidence is retained under
`/private/tmp/sniper-c0679-current-20260907.9p06WA/full-token-provider-1/`.
The original admitted transcript, source and unapproved cut are unchanged.
A separate same-settings raw diagnostic is retaining full token output for
investigation. Do not relax positive whole-word timing or fabricate durations
to make the provider pass; zero-length lexical subpieces must be distinguished
from an entirely zero-length word using actual token grouping.

Independent editorial review read the complete admitted transcript and all
proposed ranges. It found no unique completed lesson lost in the first seven
removals, but the final 77.2s removal changes the resource promise. The actual
workbook/angle-engine capability must resolve that choice, not a later-wins
heuristic. Five additional phrase restarts and several screen/terminology
checks are retained in the candidate's `EDITORIAL_ADVERSARIAL_REVIEW.md`.
This advisory is not a cut-critic receipt, timing decision or human approval.

The new bootstrap's adversarial review reproduced a subprocess registration
race during quiescence. Author is adding narrow bootstrap-only settlement and
strong-reader checks, including runtime-pin equality and forced-stop cleanup
uncertainty. That adapter is not yet qualified and no real project was launched.

### Parser release decision, regressions and real listening aids — 11:10 UTC

Full raw diagnostic completed149.06s command wall /148.959140s aggregate:
0.895162s extraction and137.764011s ASR, same installed runtime/model/settings,
normal failed GPU attempt then CPU.2266rows/2235whitespace words,2719ordinary
tokens. Every row/text/control/order check passed; exactly three full words
have zero token duration: of122.820,a231.230,I801.790. Other2232word groups
pass strict group checks. This is not a clean full strict-parser result.

The proposed subpiece compatibility fix cannot repair these whole words.
Rather than release a known regression, strict parsing is now explicitly
experimental in `local_whisper_token_parser.py`; production restores the
previous row semantics with honest `sniper-whisper-row-uniform-compatibility-v1`
provenance. There is no runtime toggle or silent strict-then-row fallback.
Deadline, process ownership, bounded NDJSON and local-only work remain in place.
The retained full raw result produces exactly the admitted162utterances and
2235words under the production parser; no admitted artifact was rewritten.
Root independently reran157combined core/worker/parser regressions:
PASS4.815s suite /7.75s command wall. The fresh normal-provider smoke test in
`full-compatibility-provider-2/` subsequently passed:153.95s command wall,
153.895689s aggregate and144.557434s provider work, exit0, actual CPU path.
All2235words pass the structural gate; runtime/model/settings and raw provider
result SHA match the original real ASR. Its explicit row parser policy is
retained, and the private result was source-bound without changing admission.
This is normal-provider integration evidence, not acoustic or cut approval.

Real source listening aids were created in10.46s wall /10.381187s aggregate,
including fresh full source hashes before/after and unchanged manifest,
transcript and candidate checks. Files under `source-timing-auditions/` preserve
48kHz stereo24-bit PCM: source0–13.54s (649920frames) and9.54–14.62s
(243840frames). The second clip's proposed cut is2.00s into the clip.
These are source-window aids, not a full cut preview or human decision receipt.
Both were presented in Codex, and explicit listening feedback/resource-promise
clarification were requested asynchronously. No response/approval is assumed.

Existing app goal still read `blocked` at11:05UTC; completion is not claimed.
Implementation continues under the renewed user request, but app Resume remains
user-controlled. The full objective and actual-video quality/throughput work
remain open.

An isolated35s-PCM GPU initialization diagnostic returned **-11 (SIGSEGV)**
after backend loading in0.209712s; it did not reach a transcription result.
Evidence is under `/private/tmp/sniper-gpu-init-diagnostic-fk2qu6f_/` and the
tool transcript. This establishes a GPU-path crash for that invocation, not
its underlying cause or every earlier GPU return code. Earlier emitted
300-character error tails started with `1;` because they truncate the prefix;
they must not be interpreted as proof of positive exit1. Normal bounded CPU
fallback is working. No Homebrew/runtime reinstall, settings override or
speculative GPU fix was applied. Its tiny startup overhead is not the cause
of hours of editing/development latency.

### New-only Codex project bootstrap — final bounded integration

`guided-project.ts` now exposes bootstrap/status from an admitted manifest,
existing previsual cut candidate and actual matching intent. It reuses the
ordinary saved-plan transaction, new job start, detached worker, source
verification, two independent cut reviews and qualified-preview PAUSE. It does
not use the TEST initializer, pretend a writer completed, repair the candidate,
enter saved-plan review or invent human consent. Exact request identity and
saved-version/hash checks preserve all original inputs and intended treatment.

New production modules total699lines across11focused files. New tests27/27
passed independently in1.056s. The independently reproduced late-registration
race now invalidates PAUSE, observes the added group and retains a revocation.
Later unknown cleanup cannot be hidden by an earlier clean failure marker;
marker publication failure retains live lease refusal. Current runtime pins,
held quiescence fact, strongest pending/accepted readers and final journal CAS
are checked. Legacy omission behavior remains unchanged. This is a scoped
bootstrap safety adapter, not a general process-recovery redesign.

Root integration passed70tests in52.737s test duration /52.76s command wall;
the added CLI4tests separately passed1.13s wall. The larger cohort includes
legacy3s synthetic media admission/private-preview creation, with explicit
TEST writer/critic and attestation data, not creator acceptance. Whole TSC
passed1.85s. Root focused ESLint across29touched/new TypeScript files passed
1.90s; targeted diff-check passed. This is not a repository-wide lint result.
No real C0679 bootstrap or subscription critic was launched. The current
candidate is still paused and not eligible simply because the new command exists.
