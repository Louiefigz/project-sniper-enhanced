---
description: Teach Producer reusable edit doctrine and templates from references
argument-hint: <reference-files-or-URLs> <what the system should learn>
---

Invoke the `producer-study` skill.

The operator's request is: $ARGUMENTS

This is a maintainer workflow for improving Project Sniper itself. Measure the
references, extract reusable rules, build or update justified templates, and
verify them. If the operator wants one new video edited like one reference,
route to `/reference-edit` instead.
