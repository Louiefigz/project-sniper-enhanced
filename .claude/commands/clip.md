---
description: Tighten one clip and remove filler at word level
argument-hint: <video-path> <cleanup instructions>
---

Invoke the `clipper` skill.

The operator's request is: $ARGUMENTS

Use the existing transcript, keep-range, clean MP4, loudness, and FCPXML path.
Do not route this to Segmenter or add Producer treatment unless the operator
explicitly asks for a fully produced edit.
