---
description: Split a long recording into topic or guest clips
argument-hint: <video-path> <how to divide it>
---

Invoke the `segmenter` skill.

The operator's request is: $ARGUMENTS

Use the existing transcript-led rough-cut workflow and its single/multicam
export. Preserve operator boundary review. Do not perform word-level polishing
or full visual production unless requested as a separate follow-up.
