PENDING OWNER DECISIONS — why this build is not sellable
========================================================
Recorded 2026-09-17 (rc3). This file exists so the package never implies a decision
that has not been made. A buyer never receives a build that still has it.

SETTLED on 2026-09-17 (reflected in the manual and LICENSE-DRAFT.txt)
  - Price: $97 once. No additional Sniper processing fee.
  - Both Codex and Claude are supported routes.
  - Licence scope: own videos and paid client videos allowed; no reselling,
    sharing or giving away Sniper; no reuse of its code in another paid product.
  - Base update rights: N1, the purchased release only.
  - Refunds: 14 days, unconditional.

RESOLVED BY IMPLEMENTATION in rc3 (no owner action needed)
  - Teacher-named identifiers: migrated to neutral names across code, compositions,
    tests, skills and docs. The material behind some of them is not resolved; see
    STILL OPEN 5.
  - Creator-derived reference libraries: replaced by an original library rendered
    from Sniper's own templates (resources/references).
  - Director hook library from a sibling repository: replaced by an original packaged
    library (resources/director); no sibling repository is read.
  - YuNet model: MIT (OpenCV Zoo), file hash matches upstream; notice shipped.
  - Default music bed: the old bed hummed at 111 Hz; replaced by an original synthesized
    bed (scripts/producer/audio/default_bed.py) that passes the product's hum check.
    A person has not listened to it yet (see below).

STILL OPEN — each blocks release
  1. LICENCE WORDING (input D): LICENSE-DRAFT.txt is not final or reviewed; the
     effect of a refund on the grant and governing law are unsettled.
  2. SUPPORT ROUTE AND RESPONSE COMMITMENT (input D): no support address anywhere.
  3. SUPPORTED PLATFORM BOUNDARY (input R): only a developer Mac on macOS 26 / Apple
     silicon has run this. A clean-Mac install run is needed before any boundary is
     stated to buyers.
  4. HOW FOOTAGE IS ADMITTED: Sniper admits every media file inside a network-less
     Docker sandbox with one approved image, and this package ships no way to provide
     it. As built, this candidate cannot start a Producer edit (Segmenter and Clipper do
     not use this step). install/doctor.command reports
     it as "media admission sandbox". The options are in DECISION-SANDBOX-RUNTIME.md
     beside the release.
  5. CREATOR-DERIVED EDITING DOCTRINE: the saved styles (Restrained, Punch, Slideware)
     and several doctrine studies the editor reads are analyses of specific creators'
     videos or one teacher's taught material. Renaming removed the names, not the
     material. Files: docs/studies/RESTRAINED_STYLE.md, PUNCH_STYLE.md (and
     scripts/producer/docs/findings/PUNCH_STYLE.md), SLIDEWARE_STYLE.md, MODULE_STUDY.md,
     MODULE_CARDS.md, EDITCRAFT_LESSONS.md, SHORTFORM_LESSONS.md,
     REFERENCE_STYLE_STUDY.md. They must be re-authored as original doctrine or
     withheld (dropping the saved styles) before sale.

NOT BLOCKING THIS PRODUCT
  - Sample footage: the first run no longer needs a shipped clip. The manual has the
    buyer record a 45-second test clip from an original script (manual/first-edits.html).
    A shipped sample stays optional; it would need a clip you own and agree to ship.

NOT OWNER DECISIONS — acceptance still outstanding
  - Listen to app/assets/music/default-bed.mp3 once (60 s). It is measured clean, not heard.
  - Claude sign-in on this Mac: the Claude CLI here is not signed in, so no real Claude
    admission or edit could run. One interactive 'install/sign-in.command claude' by the
    owner unblocks it.
  - Clean-Mac install (Z2) and the outside-operator run (Z10/A1).
  - Authenticated delivery of the exact bytes (Z11).
