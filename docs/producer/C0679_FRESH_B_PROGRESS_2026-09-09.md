# Independent C0679 edit B — first benchmark result

This is an evidence record, not delivery approval or completion of Project Sniper.
The active trial directory is:
`/private/tmp/sniper-c0679-fresh-b-20260909.0Ydfxp`.

## Clock and isolation

Started: **2026-09-09T21:26:48.341117+00:00**.
Monotonic origin: **1618505.458083375**.
Two-hour milestone: **2026-09-09T23:26:48.341117+00:00**.
Keep appending `events.jsonl`; never reset this clock after failures.

The new creative input is the original Downloads/C0679.MP4 only, plus the
user's standing brief and installed generic tooling, fonts, skills and catalog.
Original source SHA-256:
`06c50ce8c2e9da0a954f9301b1cba0dbfeb62ecebcdd956a034eb1597a301e14`.
No A transcript, edit, scene, soundtrack, project or source-frame cache is reused.
The operator has prior A exposure, so this is fresh-input, not a blind trial.
Generic guard source contains old clip constants, which were seen but are not
used for B's frame counts, samples, source qualification or creative decisions.

A's archive remains unchanged. A was saved, but not fully Studio-qualified,
before B began. Its later external final-handoff record did not repair the
unresolved Studio/audio limits. This sequencing limitation is not hidden.

## Completed objective preparation

- Fresh source hash, full probe and local CPU Whisper transcript completed.
  Guarded preparation took **135.215 seconds**; no paid transcription service.
- Fresh still/audio inspection established the usable recorded right channel.
  The left channel was effectively dead. The new cut centers the right channel
  into identical stereo channels. No blanket denoising or creative color filter.
- Eleven newly chosen source intervals produce **15,761 frames** at 24000/1001,
  **657.3650416667 seconds** and **31,553,522 samples** at48kHz.
- A-roll preparation took **451.186 seconds** (7m31s), with source preserved.
- Every part and the whole picture passed the actual PTS-lattice checks in the
  source QC worker before its later reporting failure. Full retained audio
  interiors match independently decoded raw source samples exactly. The144-sample
  cut-edge fade zones never increase source magnitude.
- All eleven first cut pictures matched the intended source frame better than
  either adjacent frame. This is boundary identity evidence, not comparison of
  every content frame. The clock check does cover every picture packet.
- Mastering measured **−16.14 LUFS**, **−1.47dB true peak**, identical stereo,
  finite PCM, exact planned sample length. Mastering guard took **45.075s**.
  These are objective measurements, not human listening approval.

## Authored native project

`project/` contains one persistent real presenter video, separate fresh narration,
53 independently editable native sub-compositions, local fonts and local GSAP.
The design is a source-timed annotated notebook with full, split and small
presenter layouts. The fast opening has additional within-scene reveals;
later panels explain the actual worked example and three-stage lesson.

The fully animated build passes installed SDK0.8.31 lint across all54HTML files
with zero errors and zero warnings. The latest Studio test (animated-stock-v6)
has checked all53 settled scene states against actual expected presenter bounds,
active panel identity and item visibility. Opening playback advanced64.992s with
maximum A/V difference4.631ms and zero drops/corruption. That phase's outer guard
still FAILED on a terminal measurement race; separate exact-nonce cleanup recovery
is retained. The corrected guard passed subsequent export and output-QC phases.

Studio uses the coherently realm-corrected private SDK staging, not an unmodified
stock-SDK claim. Stock audio-host behavior is retained: an earlier65-second B
playback advanced65.019 seconds, with maximum A/V difference5.731ms and zero
dropped/corrupt video frames. This did not qualify the then-broken presenter pose.
All these guarded tests defer ancillary sidebar thumbnail requests with503
responses. Thumbnail-enabled production Studio remains unqualified.

Fresh PCM24 browser audio preserves the float master within one24-bit step.
Fresh single-generation AAC measures−16.16LUFS,−1.09dBTP, LRA5.5 and global
aligned SNR34.297dB. The corrected delivery passed finite stereo and all source-bound
local windows, with exact31,553,522 presented samples and14 decoded padding samples.
Neither these numbers nor browser playback are human listening.

## Failures retained on the same clock

- Still scan V1 failed its RGB/YUV conversion negotiation.
- Still scan V2 failed the process-footprint guard during rapid child churn.
  V3 batched the extraction and passed without relaxing resource caps.
- Source QC V1 completed media checks but failed parsing a loudnorm JSON object
  followed by FFmpeg's text trailer. The successful raw measurements were retained
  and parsed during mastering; no recut or hidden restart was used.
- Static Studio V1 stopped safely on new-process identity churn during browser
  startup. All recorded owned children were cleaned up. A bounded measurement
  retry repair is being reviewed; memory limits remain unchanged.
- Native contract/font lint caught missing child timeline registration and
  per-file font declarations/root-relative paths. The generated project was fixed.
- Static Studio V2 failed both resource measurement and source stability:16
  authored HTML files changed during that phase. It is not qualifying evidence.
- Animated V1 failed on non-timeline registry metadata. V2 completed its checks
  but visual review caught the missing root presenter movement; its broad pass
  label is insufficient. Explicit presenter-position assertions were added.
- The SDK relocated external motion.js into the document head, binding selectors
  before the presenter DOM existed. The root timeline is now inline after its
  elements. Literal tween properties are retained, but literalization alone did
  not fix the bug. Never attribute the fix to that unsupported hypothesis.
- V3/V4 revealed preview reload/readiness races. V5's overly strict20ms seek
  tolerance could not accept the30fps Studio preview clock. V6 permits one preview
  frame (40ms bound); final export still explicitly uses24000/1001.
- V5 also retained a missing-footprint/cleanup-error failure. All ten recorded
  children and its supervisor were subsequently absent. Exact-nonce recovery
  archived two successful absence checks before clearing only its active marker;
  the original failed receipt was not changed.

Versioned preparation helpers from independent review have17 passing offline
tests covering exact time-base/start-PTS validation, invalid rates/empty plans,
timeout and launch-error journaling. They do not retroactively certify media.

## Native export and quality result

The full native export used its own15,761-frame fresh PNG cache, with zero transport
errors/fallbacks and complete source-frame coverage. Native CLI time23m38.9s;
guarded export time1430.103s. Maximum owned footprint2.135GiB, no swap growth,
normal kernel pressure throughout; unused-RAM warnings were retained.

The first native MP4 failed audio/color checks: two samples short,79 failing local
audio windows and full-pose color PSNR29.885dB. A new delivery copy replaced the
AAC stream with the fresh single-generation donor and corrected transfer metadata.
Actual H264 picture-slice hash and all30,815 donor AAC packet hashes were preserved;
there was no additional picture or audio encoding. The failed raw export remains.

Corrected MP4:1920×1080,24000/1001,657.3650416667s,15,761frames. SHA256:
`2b2b512eafecea3ef65768a676a78e76bf43bf07c09b0ab5ebad5a7e720846ee`.
Whole decode, every picture packet's rational clock, exact audio presentation,
finite/local-window audio and three independent native screenshot comparisons
passed. Color gates remained MAE≤2 and PSNR≥40dB for whole frame and presenter ROI;
the minimum ROI result40.062dB has little margin. No input-color override was used.

## First playback failed; two-hour benchmark missed

File-level QC completed at109m7.549s. Full playback ended at120m40.653s, before
source re-verification/archiving, so the complete two-hour target was missed.
Playback reached the end with all15,761frames counted, no stalls/seeks/errors or
corrupt frames, but four presentation drops (two near79.21s and two near115.31s).
The unchanged zero-drop and presentation-coverage gates therefore FAILED. Actual
worker exit1, source pins, owned cleanup and lease release are all retained.
No cause is established; a repeatability diagnostic cannot erase this result.

The raw source was rehashed after playback and remains unchanged. The archive
script seals the first result as `file-qc-passed-playback-failed`, not approved.
Later investigation, scoped revisions and product integration must use separate
evidence and copies. Compare A only after this first B package has been sealed.

## Post-seal continuation (separate evidence)

An identical-file, identical-harness whole-playback repeat passed in
666.151788s guarded time: 15,761 presented frames, zero drops/corruption,
maximum media-frame gap0.041709s and no backward clock movement. The first
four-drop failure remains sealed and authoritative for the first benchmark.
The repeat does not establish its cause or recover the missed two-hour target.
Headless playback remained process-muted; it is not audible listening.

The ordinary program-delivery implementation now checks each AAC packet's
sample clock and source-master-relative local audio windows before promotion.
The checked candidate's SHA must match the candidate being promoted. All56
focused offline regressions passed in0.661s after the final metadata change.
The nine actual FFmpeg integration tests passed (9.851137s worker,
21.241308s guarded) immediately before that metadata-only change; these use
short synthetic fixtures, not a new whole-B delivery or native Export button.

A separate real working project is at
`/private/tmp/sniper-c0679-fresh-b-20260909.0Ydfxp/revision-work/project`.
Native Inspector changed panel02's heading to “YOU HAVE AI TOOLS.”, saved it
and reopened it visibly without horizontal clipping. Only panel02 changed;
the UI attempt passed in16.024390s guarded time (9.206s worker). Earlier
selector, native-ID initialization and keyboard-selection failures remain
recorded. One earlier attempt saved concatenated text; the successful attempt
explicitly repaired that recorded working-copy text. Neither archive changed.

Standalone child export then exposed a separate authoring mismatch: 0.8.31's
native render entry dispatch recognizes a template child only when its file
starts with `<template`. Studio accepts the prior full-document wrapper.
Putting GSAP in that document's head was insufficient; putting it inside the
template passed lint but still rendered an inert wrapped root, reporting zero
duration. The failed attempt remained alive until its180s guard stopped and
cleaned its exact owned processes. The bare-template variation then failed
the SDK's contradictory standalone-template lint rule. All direct-child
failures remain; strict lint was not disabled and the SDK was not patched.

A minimal normal native root mounting the exact saved child then exported in
10.032692s guarded time. Its99 picture frames and exact rational clock passed,
but source-snapshot comparison of the graphic region failed at36.67–37.27dB.
An explicit sRGB metadata correction preserved every H264 picture slice and
passed the same whole/graphic gates (minimum graphic42.696dB) in14.038290s
guarded time. This is a silent graphic-child proof, not direct-child-command
support, a revised full master, or final playback acceptance.

Independent creative review also identified three focused improvements:
left-presenter topic/pain comparison, four source-timed quality annotations,
and a range-preserving content-package diagram. See
`C0679_B_CREATIVE_REQUIREMENTS_REVIEW_2026-09-09.md`. These are not yet verified
in a revised master; do not call the current notebook treatment final quality.

On2026-09-10 at00:32:53UTC a separate full original-B human-review copy was
opened in native Studio through localhost41065, with the same declared
thumbnail deferral and realm-corrected preview. The other task verified the
actual UI's10:57 duration,53 panels,657.4s presenter/audio tracks and advancing
playback with volume100%/muteoff. This is not an agent listening claim or user
approval. The guarded session is bounded to45minutes, with competing media
tests paused while the user reviews. Active creative work remains in a
different revision copy; neither reviewed version nor archive is overwritten.

Still unqualified: thumbnail-enabled Studio, saved-path end-user full re-export
with automatic delivery finishing, complete scoped revision/export QC,
representative shortform, audible listening and user creative/publication
acceptance. The broader goal remains unfinished. Never count render-only time
as total editing time.

## Whole-video creative revision: native review in progress

The separate revision now replaces the repeated notebook treatment with
source-motivated full/left/right/corner/absent presenter layouts, open lists,
tool logos, document illustrations, diagrams and varied typography. The opening
establishes Aaron full frame; its first move at3.25325s follows the one-person
workload idea, not a mandatory3s or15s hold. An independent source reviewer
rechecked the whole-body allocation and four corrected answer cues, the actual
workbook glyph, opening01/02 and the one-frame49→50 graphical handoff.

`creative-studio-v1` completed229 scene-state captures,184 transition-state
samples and8 opening/reverse checks in206.325597s guarded time. The overall
phase FAILED before playback: its new absolute-time bridge assertion wrongly
required a1ms match to non-lattice timestamps in the30fps Studio preview.
The next requests use actual preview-frame positions; final export still uses
24000/1001. The working root also changed during native startup because Studio
assigned four new `data-hf-id` attributes to the workbook glyph. Exact before
root SHA:a9117d9f38511dc4b7b12b72f84ba8e402830de91132a9d7e1fc36d1e3c48f45.
All other pinned files were unchanged. The IDs are preserved for the next run;
this explains, but does not convert, the failed source-stability result.

Actual visual review caught the late52 presenter standing/head leaving the crop.
Existing native-source PNGs show him seated through output653s, looking away
and removing the microphone at654s, then rising at655–655.5s. The repair retires
the presenter in place at653.10–653.50 and expands the native endscreen; cuts,
audio and total duration stay unchanged. Revised native checks and full export
remain pending. V1 screenshots contain a scaled canvas and Studio UI; they are
not full-resolution picture/color references. Owned cleanup and lease release
were verified after the failed phase.

The original human-review server expired at01:17:45UTC under its45-minute
deadline; exact owned cleanup was verified without closing the user's browser.
The user subsequently cancelled the separate fresh-input timing run. Finish,
save and time this current creative revision through the ready review handoff.
No follow-on raw-footage edit has begun; neither a new timer nor render-only
time may replace the actual creative-revision elapsed time. See the current
timing instruction in the canonical direction document.

## Current creative export and delivery checks

The full native Studio v2 check passed229 scene states,188 transition states,
eight reverse/opening checks and13 absolute bridge/closing points. Its78-second
moving playback had zero drops/corruption and maximum measured audio/video
clock difference0.007243s; guarded elapsed289.433159s. Strict native lint then
caught five layout-property animations in the endscreen repair. The final
source uses transform-only motion instead. The final panel33 label padding and
panel52 transform-only closing passed targeted native Studio v3 checks in
35.312308s, including4.8s of late playback, zero drops/corruption and maximum
audio/video clock difference0.006890s. These scoped checks are not whole-output
playback approval.

The first full creative export failed strict asset-path lint before rendering,
in14.413509s. Render staging v3 supplies an independent byte-identical prepared
picture at the browser/strict-lint URL while native extraction resolves the
original verified picture path. All54 native files then passed strict lint
with zero errors/warnings. Native reference v3 captured23 exact full-resolution
poses, transitions and closing frames in14.315918s guarded time; compiled SHA
`c8f563db9f980b0c16a3e15635de8c3ba7a4da44179a8be29d1acb97b4a80bec`.
No strict check was disabled and no SDK code was changed for this path repair.

Full creative export v2 started2026-09-10T02:12:12.665752Z, monotonic
1635629.716675916. Its trace verifies one native cache hit, zero misses and all
15,761 prepared source frames. It is still running at this entry; final delivery
finishing, independent file QC, full playback and new Chrome handoff remain.
The delivery adapter reuses unchanged picture-slice/AAC preservation, exact
packet clocks, full decode, local PCM/loudness and fidelity thresholds. It binds
the23 current native references instead of the three older B references. An
independent review found missing scene/panel fields on absolute capture rows;
explicit expected-scene normalization and actual-receipt regressions fixed
that preflight defect. Eleven cheap adapter tests and the23-reference metadata
preflight pass. This is not a rendered-output QC pass.

## Current creative revision: actual export/QC outcomes and timing miss

The authoritative request boundary is **2026-09-10T00:39:13.266Z**, independently
verified from the coordinating user task. There is no monotonic sample for that
boundary; do not manufacture one. The exact two-hour milestone at
02:39:13.272507Z recorded **MISS**. It includes creative work and failed attempts;
02:12:12 is only the first full-render start, not the revision start. The
follow-on fresh-footage trial is cancelled. This is the existing B creative
revision, with transcript/cuts/prepared media reused.

Native export v2 rendered all 15,761 frames, but its wrapper failed after
1,364.324745 seconds because it parsed cache evidence from stdout while the SDK
emitted traces on stderr. The original failed receipt is unchanged. An exact,
separately guarded reconciliation admitted the existing native artifact after
one refused resource admission (7.026960 seconds) and a successful 14.458845-second
check. No rendering was repeated merely to repair the wrapper receipt.

The first delivery QC attempt stopped on a short-lived-process measurement race
after 13.979264 seconds. It was not an excessive-RAM or media-quality failure.
A second check resumed the same stream-preserved candidate and completed clocks,
full decode, motion screen and audio in 50.976282 seconds. It failed one of 23
native-JPEG95 comparisons: panel 13 presenter ROI PSNR 39.727166 dB, below the
unchanged 40 dB gate. All 23 whole-picture comparisons passed. This failure stays
retained; it is not rounded into a pass.

Independent exact lossless-native-PNG captures exposed a second distinction:
the SDK's JPEG95 capture itself is lossy. The PNG v4 attempt failed because its
phase omitted the required transport flag (12.479335 seconds); correctly pinned
v5 captured the same 23 frames in 15.496225 seconds. The CRF15 delivery failed
nine of 23 PNG-relative rows, including a 37.339105 dB panel-13 presenter ROI.
JPEG95-versus-PNG alone was 39.549236 dB there. A native-JPEG-relative pass must
not be described as source/lossless-canvas fidelity.

Six one-frame controlled encoding comparisons took 12.771822 seconds. The
installed SDK's supported **CRF6** setting improved the JPEG-relative result;
no tested control passed the panel-13 PNG-relative 40 dB threshold. No SDK code
or creative source was changed for the subsequent CRF6 full export.

The actual CRF6 full export passed in **1,349.236179 seconds (22m29s)**,
completed 03:25:44.090Z at the worker, and produced 2,881,534,755 native bytes.
It retained the same compiled source, one native cache hit, zero misses and
15,761 frames. Both stdout/stderr are now parsed with independent buffers and
child-close flushing; seven observer regressions cover the earlier failure.
Guarded source, SDK, sandbox, additional-file and owned-cleanup checks passed.

The higher-quality delivery QC passed in **133.523362 seconds**. The finisher
preserves encoded picture slices and the previously qualified AAC packets;
all clock/decode/motion/audio checks and all **23 exact native-JPEG95-relative
samples** passed. Panel-13 presenter PSNR improved from 39.727166 to
**41.713420 dB**, and panel 16 measured 41.843500 dB. Thresholds remain MAE ≤2,
PSNR ≥40 dB with no fitted color/alignment or input color override. The new
candidate remains explicitly **not lossless-canvas qualified** pending the
separate retained PNG diagnostic; no human listening/approval is claimed.

Independent review fixed two QC handoff defects before this successful run:
the cooperative wait now reuses the supervisor's exact `stop_reasons` with its
actual complete snapshot, baseline and policy, and a failed wait is fatal and
permanently prevents a later child launch. Ten cheap regressions passed,
including every cap and failure-followed-by-next-action cases. The real run's
post-command wait journal is retained; these waits are included in 133.523362
seconds and did not weaken measurement, memory, swap, disk or timeout policy.

Native macOS `clonefile` was separately qualified using a disposable font clone:
different inode, exact initial hash, mutation only to the disposable copy, and
unchanged original hash/identity. Node's forced FICLONE helper returned ENOSYS,
but the documented native syscall succeeded. The planned archive copier has no
hardlink or full-copy fallback and checks the same ten-GiB reserve before/after
each clone. No protected source cache or previous archive was deleted. Archive,
whole-output playback and ready Chrome handoff remain pending at this entry.

The actual higher-quality PNG-relative diagnostic completed in9.593096 seconds.
It still **failed four of23 rows**: panel08 presenter39.899605dB, panel13
presenter37.809996dB, panel16 presenter37.939543dB, and closing653 whole-frame
39.871381dB. This is improvement from the previous nine failed rows, not a
lossless-source pass. Its execution/source/cleanup checks completed correctly;
the failed status is the measured fidelity result. The full 10:57 playback phase
started03:32:05.738652UTC. No final playback result is asserted at this entry.

CRF6 export resource evidence:193 samples, peak owned2.223374GiB, no new swap,
all kernel-pressure readings normal. HighQC:19 samples, peak owned0.352539GiB,
no new swap, all pressure normal. Literal-low-unused-RAM warnings remain in all
those samples under the previously qualified cache-aware policy. Eight actual
post-command acknowledgement waits total33.152055 seconds, maximum6.773474.
They are part of the133.523362-second QC result, not excluded overhead.

## Current revision saved and visibly ready for review

Full encoded playback passed in **665.673388 seconds**:15,761 total/presented
frames, zero dropped/corrupt/backward frames, no post-startup faults,6,582
100ms observations and maximum measured media gap0.041709 seconds. Complete
duration657.365042 seconds; all source pins and owned cleanup passed. This was
headless-muted playback, not audible human listening.

The immutable current archive is
`artifacts/c0679-creative-revision-2026-09-09/`. Sealing completed in28.712490
guarded seconds with all pins/cleanup passed. It contains1,716 verified
independent-inode COW files,4,781,197,532 logical bytes, including all68 editable
project files. Manifest SHA:
`497354f15e40e222395e3302a110c02de8f9c28f0a8c598d189467b294cde517`.
Final review MP4:`revision-work/delivery-creative-high-v1.mp4`, SHA:
`13af0af148295aec1299246837dbfe300fb4b5a93966f85fe44a523f4aea2bd6`.
Independent review checked the exact archive file set/sizes, distinct inodes,
selected evidence hashes and manifest limitations. Existing A/B archives and
protected source caches were not modified; about13GiB remained available.

Actual visible Chrome inspection opened
`http://127.0.0.1:41069/#project/project?v=1&t=0&tab=design&rc=0` and confirmed
53 child compositions plus root, enabled Play, **00:00/10:57**, full native
timeline and visible opening presenter. All68 working-project file hashes
were rechecked against the immutable archive **after** the UI opened and still
matched. The tab was marked to remain open. Sidebar thumbnails remain deferred.
The preview server is a bounded45-minute local session, not a restarted
scheduled task and not a fully qualified unmodified-stock Studio workflow.

The exact verified ready-handoff boundary is **03:47:32.864410UTC**, yielding
**11,299.598410 seconds = 3h08m19.598s** from the actual creative request.
The two-hour target was missed; it was not reset after any failed attempt.
Handoff/timing supplements are sibling files outside the immutable archive:
`artifacts/c0679-creative-revision-2026-09-09-handoff.json` (SHA
`fc3387c6901723912eed4a97b65d8bb56e97c2a4060eb473fd7c3636c9b049aa`) and
`artifacts/c0679-creative-revision-2026-09-09-timing.json` (SHA
`c5499fb3ffdd01db4bd2e99ff3621293b0c3038419e10e680fb6558fd7a1f6fb`).

Final cheap regressions:36 Python tests passed in0.043 runner seconds, plus
seven internal native-trace observer checks. Archive-test initial fixture
errors from symlinked `/var` paths were fixed by using explicit `/private/tmp`,
without weakening path safety. The review server also acquires its receipt
exclusively before writing and rejects symlinks in the working project.

This is a **checked native-JPEG-relative review candidate with four retained
lossless-PNG failures**, not lossless-source approval. No human listening,
creative acceptance, publication approval or overall Project Sniper completion
is claimed. No paid APIs, purchases or downloads were used. The separate fresh
trial remains cancelled; the broader goal is unfinished.
