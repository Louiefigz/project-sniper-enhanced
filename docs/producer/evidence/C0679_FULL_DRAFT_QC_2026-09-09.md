# C0679 full editable draft — ongoing evidence, September 9

This is a development checkpoint, not final delivery approval. The existing goal
is unfinished (`usageLimited` when read this turn). Local compute only; no paid
API calls, downloads, scheduling changes, or source-footage modification.

## Verified base and real editing work

Workspace: `/private/tmp/sniper-c0679-produced-review-20260909.FCYcRv`.
The ten frozen normal-speed keeps produce a 683.766417-second, 1920×1080,
24000/1001 H.264 base. The 57 editable graphics remain separate HTML compositions;
they are not burned into this base. First-minute plan contains 14 graphic windows.

`base_final.mp4` is 2,241,414,314 bytes, SHA256
`77352f80bb9c5e6366efdf1d854634d91c6a86fe4ebb795ade6405e86cec745b`.
Full decode passed: 16,394 video frames; 48 kHz stereo AAC;
32,820,788 presented audio samples; zero presentation-vs-video sample drift;
460 decoded trailing padding samples. Ordered cut manifestation passed.
Existing dead-left-channel repair duplicates the source's right channel.
Source color tags remain unchanged; this is not a new grading/mastering claim.

The first attempt spent 430.145 seconds, including 424.698 seconds cutting
picture, then failed the default 2 GiB media-hash budget. The completed picture
was retained. Recovery reused it without another picture encode: 266.749 seconds,
including 38.710 seconds for float-audio/copy-picture mux, 7.975 seconds for
ordered manifestation and 217.039 seconds for full decode/clock QC. The explicit
3 GiB per-call allowance leaves the 2 GiB default and existing safety checks intact.
See `result.json`, `base-recovery-result.json`, `audio-clock.json` and
`cut_manifestation.v1.json` in the workspace. Failed audio intermediates are
preserved in `failed-audio-attempt/`, not deleted.

## Fixed integration failures

- External catalog body JavaScript was being requested from the Studio root and
  returning HTML, leaving pipeline cards blank. The generator now admits and
  inlines bounded, local classic dependencies before instance re-keying.
- Tool icons in the draft needed explicit SVG filenames. V2 preserves those fixes.
- SDK 0.8.31 strips timing attributes from mounted inner roots. Catalog scripts
  then fell back to their default duration, e.g. 4 seconds for a 6–7 second
  statement. Their exit animation made the remaining window blank. Generated
  body scripts now read `root.closest("[data-composition-id]").dataset.duration`:
  the standalone root or surviving mounted host. No copied duration literal,
  new clock, selector wrapper, or tolerance change was introduced.
- The previous text probe checked leaf styles only and falsely passed hidden
  ancestor content. The real-footage probe now checks effective ancestor opacity,
  display and native visibility, including late statement holds.

Native V3 at `http://localhost:41050/#project/studio-v3` passed 57 card checks,
114 boundary checks and 17 statement late-hold checks in 53.846 seconds.
Previously blank cards 20/24/27/30/33/57 were also directly viewed and now show
their actual copy. Remaining visual review is not implied by the automated pass.
V3 generation took 1.097 seconds and launch 5.143 seconds. Its frozen plan SHA is
`b5e50991390b9a119bf200033e404c05a21a5438fab9545a7e8974ce75cf43ad`.
All existing V1/V2 drafts remain preserved.

## Playback is currently a blocking failure

The uninterrupted V3 first-minute test failed in 67.932 seconds: 90 audio seeks,
maximum audio/clock drift 2.283 seconds and video/clock drift 2.999 seconds.
There were 1,458 decoded video frames and one dropped frame. See
`native-playback-qc-v3/result.json`; do not promote the earlier screenshot pass
to a playback or audible-quality pass.

Diagnostic setter tracing identifies SDK `Gl → He → Is` as the repeated-seek
caller. The SDK routes a streaming media element through Web Audio but advances
its master from AudioContext elapsed time instead of that element's playhead.
The actual parent UI re-enables Web Audio on Play; a pre-Play control toggle was
therefore not a valid isolation and was not promoted. Diagnostic tracing is
explicitly excluded from acceptance runs.

An isolated, preview-only single-stream-clock candidate is being tested at
`/private/tmp/sniper-stream-clock-candidate-20260909.eMkFyD`, port 41052.
It changes only the single active connected playing audio-element clock read;
media routing, effects, gain and existing buffer/multiple-source behavior remain
unchanged. The installed SDK and sealed renderer are untouched. Candidate native
acceptance failed: its 61-second rerun took 68.067 seconds, with only one startup
seek/wait per medium and maximum audio/clock drift 14.258 ms, but video/audio drift
reached 166.810 ms, over the unchanged 150 ms limit. Its preparation initially hit Node's
2 GiB single-buffer limit; bounded hashing verified the already copied bytes in
3.223 seconds without copying or encoding the picture again.

The generic `Event: Event` page error is classified: Studio attempts remote
MotionPathPlugin versions 3.12.5 and 3.15.0, blocked by the local-only OS profile.
No MotionPath dependency was found in the authored card source. Do not label the
run zero-errors or install/download a plugin merely to silence the error.

### September 9 follow-up: rejected seek correction and fresh V4

The isolated `W6ryEE` 100 ms playing-video seek correction is rejected. Its first
69.409-second run started before decoded media were ready and eventually paused
at zero; zero measured drift was not accepted. A bounded-readiness rerun failed
in 68.696 seconds with 72 video seeks. Random-access decoding could not catch
the advancing audio target (up to roughly 2.023 seconds video/audio drift).
`__playerReady` is not a can-play guarantee. A test admission gate is not itself
a fix for the native early-Play race.

Fresh `studio-v4` generation took 1.384 seconds. Plan SHA256 is
`9fe4d72daeb70d8719d57bcc891a7b4d5df1d87ac1cdd903068464f6d9c3105f`.
Frozen cuts and base are unchanged. Twelve pipeline cards use the explicitly
declared full-width teaching layout; cards 08/10 use opaque catalog statement
cards; the continued list preserves 5–8 numbering. Native full-V4 review is pending.

The next diagnostic at `/private/tmp/sniper-paired-rate-candidate-20260909.CWhenU`
(port 41055) restores the original playing-video seek exemption and tests a
25 ms deadband / capped ±3% picture-only speed correction. Both its audio clock
and video correction are restricted to a same-source paired base with matched
timing and no loops/effects/groups. It is not a general SDK qualification or an
installed-runtime change. Preparation took 6.587 seconds, launch 5.403 seconds;
an earlier ambiguous insertion-token refusal happened before copying any SDK
or draft files and is retained. Broader scope review identified live-muted,
automation and effective playback-start/rate checks to add before promotion.

### Objective audio measurement

`base-audio-quality.DRAFT.json`: 23.939 seconds total, exact base SHA unchanged.
Existing whole-stream measurement reports −28.17 LUFS and −8.02 dBTP: loudness
qualification fails, and adding the entire loudness difference as raw gain would
exceed the peak limit. Channel balance and A/V boundaries pass; persistent hum
was not detected. This calls for the existing measured mastering/limiting path,
not blanket denoise or speculative picture/color changes. Mastering is pending.

### Native paired-preview and V4 follow-up

`CWhenU` passed the 61-second opening plus two later 8-second playback windows
in 96.481 seconds. Worst video/audio differences were 128.133 / 98.357 / 86.859 ms;
each medium sought only once per start. Preview frame drops remain explicitly
reported: 31/1460, 7/191 and 5/190. This is not zero-drop playback or final-output
quality proof. Media readiness added 8.107 / 0.944 / 1.034 seconds; it does not
fix the separately observed early-Play race.

The tightened preview candidate `qZpqBI` adds live muted-state, automation,
explicit playback-start and default-rate exclusions. Exact runtime SHA256:
`8aba1cf9061c0e95f1083b7dd55e2fd8911bd1e4254d57039be2b91e58140fb3`.
Same-document native controls passed all 12 checks in 23.139 seconds; the actual
foreign-realm mount passed the same checks in 20.597 seconds. Tests include
pause stability, audio mute/unmute, picture always muted, actual 2x advance,
seek/replay and natural end. Prior readiness failures remain retained.

All 57 V4 card checks and 114 boundaries passed in 44.827 seconds, with only
the intentional oversized `#02` background watermark outside the stage. Root
viewed cards 01–26; independent reviewer viewed 27–57 and relevant section cards.
Manual review found low-contrast section titles that the automated visibility
pass did not catch: shared page `--accent` writes leaked between compositions.
The catalog-style AST transform now scopes these accesses to each authored root,
using installed SDK parser dependencies; strings/comments/regexes are untouched.
The final visual rerun is still required, so V4 is not a final visual pass.

New manifests explicitly name `styleScope: catalog-root-style-v1` and
`durationBinding: mounted-host-v1` on their entries. Original saved files without
those markers reconstruct with their original page styles and duration reads;
unknown markers fail closed. This also fixes a legacy-hash regression introduced
by the earlier unconditional duration binding. Earlier unmarked diagnostic views
are retained, not silently relabeled or synchronized. Final dependency/old-view
cohort: 20 passed in 1.813 seconds; 5 AST contracts passed in 62.591 ms. A bounded
runner stdin-contract failure and the failing legacy-hash attempts are retained
in the tool history and not counted as passes. Nine media tests were skipped in
one restricted-PATH cohort and remain due for a properly configured rerun.

The reproducible paired-preview transform and five exact-byte/JavaScript tests
are in the integration mirror only (5 passed in 0.079 seconds). It is not yet
activated through the live cache/server path. Existing user views remain untouched.

Audio mastering first stopped on an exact producer/reader four-decimal rounding
mismatch, then produced qualified PCM but rejected AAC peaks after 225.983 seconds:
PCM −14.18 LUFS / −1.97 dBTP versus AAC −14.19 LUFS / −1.28 dBTP. The unchanged
delivery ceiling is −1.5 dBTP. A separately verified retained-PCM headroom derivative
is being tested; neither the rejected AAC nor a predicted correction is approval.
No global target/config change, source recut or picture reencode was made.

### Qualified audio and corrected native test admission

Headroom recovery passed in 142.696 seconds, reusing the actual admitted PCM
without a source recut. The separately versioned derivative is proven by
reexecuting the parent PCM/filter and comparing decoded sample hashes. Final AAC
is −14.61 LUFS / −1.86 dBTP; full decode passes; all 16,394 picture packets remain
identical and 32,820,788 samples are presented with zero boundary skew. New file:
`mastered-audio-headroom-v1/base-mastered.DRAFT.mp4`, SHA256
`374fcd0e8d276a954c04a4c4bb5a49c82753508c2dd914f2920995b59972ebe2`.
It is a graphics-free qualified base, not a final burned edit or ordinary-master
receipt substitution. General export/read-path integration remains unfinished.

Fresh `studio-v5-mastered` generation took 4.320 seconds (including identity
checks), launch 5.733 seconds, with the same V4 plan and qualified audio bytes.
Its first visual check stopped after 18.773 seconds, reporting an empty late hold
at card08. Raw evidence shows the observed clock was **0**, not requested 37.65:
this was a reset, not that card exiting early. All tracked files stayed unchanged.

The actual SDK toolbar already disables Play until `timelineReady`; its delayed
adapter initializer can legitimately restore the initial playhead before then.
The old probe waited only for runtime `__playerReady` and bypassed that native
guard with direct seeks. Cold zero-progress/seek-reset failures therefore do not
establish a user-facing early-Play bug. Updated probes wait for the actual enabled
native Play control and assert requested versus observed clock (within one frame)
before interpreting card visibility. No speculative SDK UI patch was made.
Corrected visual and uninterrupted full-program results remain pending here.

## Tests and remaining work

- Cut-media regression cohort: 42 passed in 31.094 seconds.
- Dependency/duration tests: 6 passed in 0.042 seconds.
- Generator/project tests: 16 passed in 1.241 seconds.
- Earlier broad LIVE Studio cohort: 195 attempted, 189 passed, 6 OS-permission
  errors (local port binding/process inspection), not 195 passed. Properly
  permissioned rerun remains required; avoid competing with playback timing.
- Independent review found no actionable issue in the bounded byte-budget,
  dependency-inlining or duration-binding deltas.
- Independent agent owns opt-in full-width teaching-layout code, tests and V4
  proposal. Its isolated 2/3/6-node native screenshots were also viewed by root:
  larger labels use the full frame and all instructional copy is visible.
  Whole-program V4 adoption/review remains pending.

Still required: stable real playback and native controls, final visual/audio
review, scoped-revision evidence, applicable integration tests, durable SDK
deployment, short-form proof, final quality-qualified output and an honest
end-to-end two-hour timing verdict. No human listening, exact-word attestation,
creator acceptance, final approval or overall completion has been fabricated.
