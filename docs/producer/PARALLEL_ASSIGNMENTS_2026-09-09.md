# Remaining assisted-edit work and available parallel assignments

Prepared September 9, 2026 from the live implementation record, current source
and explicit ownership confirmation from “Assess Project Sniper enhancements.”
Aaron now reports Claude assignments A (presenter/framing), B (ordinary supported-
source color) and C (caption-only revision composite reuse) complete. Their
deliveries are being reviewed; this is not live integration or blanket feature
acceptance. B has a verified frozen package manifest adding two tests; its report
retains one expected-failure warm-look finding. C is independently verified frozen
at commit20ab28b764fabad3c1525e63171dd22ba1617cab, with all delivery-patch hashes
matching its manifest despite an old in-progress heading. A's expected final
report/export remains unavailable: its package has an earlier matrix and an
incomplete second run, with no exported patches or final manifest. The earlier
matrix includes subsequently corrected fixture/measurement flaws and must not
be presented as a count of established production defects.
Codex retains the integration responsibilities below. Aaron subsequently
requested a fifth Codex agent specifically to reach the timed real-video test.
The coordinating task started `/root/real_footage_pilot` for the bounded pilot
assignment below; no additional app task or competing production owner was
created.

## Fifth agent — pilot preparation completed; edited preview owned by main Codex

The bounded pilot produced a real90-second source-review clip, a fresh67.56-second
unapproved opening candidate, current-schema validation and three actual
transcript/previsual flags. Full handoff:
`/private/tmp/sniper-real-footage-pilot-handoff.md`.

Aaron explicitly agreed to start with the second “if,” remove the repeated
“maybe,” and keep the completed demonstration lead-in. Exact conversation
provenance is retained at
`/private/tmp/sniper-c0679-edit-approval-20260909.XJ1anK/editorial-choices.json`.
That is editorial authorization, not exact acoustic endpoint attestation or
approval of the unchanged candidate or finished video. Main Codex now owns the
next timing/candidate and actual edited-preview execution. The fifth agent's
competing continuation was stopped before any file/media changes. Its read-only
A/C delivery verification is now complete and it has no active native work.
Optional package improvements must not delay the actual edited preview.

The following is the original bounded pilot assignment, retained for context.

User priority: get to testing the approximately two-hour editing workflow. Reuse
existing source/admission/transcription/cut work, prepare an actual reviewable
60–90-second excerpt, and establish exact executable steps for the full edit and
one revision. Report concrete blockers to the main Codex integrator immediately.
Do not replace real-footage testing with another broad test framework.

Ownership is a new private pilot workspace, artifacts, timing record and any
minimal harness proposed separately; no live production or A/B/C owner edits.
Use actual supported calls and existing local assets. Preserve original footage,
requested treatment and real human cut decisions. Record unavailable treatments
and gates explicitly; do not relabel C0679, silently substitute a simpler source,
forge approval or mistake a synthetic/input-only test for the requested edit.

Initial resource reservation: main Codex is running a bounded16-second V10 audio
opening test with a600-second original cap. The fifth agent may inspect sources,
reuse existing transcript/cut artifacts and prepare commands now, but must wait
for a confirmed slot before native/FFmpeg/Docker rendering. External Claude runs
are not directly controlled by the coordinating task.

Report: `/private/tmp/sniper-real-footage-pilot-handoff.md`. Include exact source,
manifest and cut references; proposed excerpt; executable command; produced output
or first concrete failure; source/output duration; and separate preparation,
planning, rendering/queue, failed attempts, human wait, QC, revision and total
timings. The user wants about20minutes of raw footage edited in about2hours total;
the existing10-minute/120-minute post-cut criterion remains a separate clock.
Starting this agent does not establish a completed edit or a benchmark result.

## Current progress and remaining work

Live foundations include HyperFrames SDK/CLI/linter 0.8.31, the 53 measured local
templates, full recorded-catalog discovery, ordinary audio correctness/revision
fixes, ordinary caption-segment reuse, current-word karaoke correction and several
Studio lifecycle/readback/preservation fixes. Delivered caption-selection work
still needs live guided integration; a delivery report is not activation.

The remaining assisted-use milestones are:

1. Real-footage transcript/cut-boundary review and a human-approved conservative
   cut, using actual source admission and the command workflow.
2. Complete guided caption/audio integration and opening/body execution. The
   caption V9 and audio V10 work already exists in isolated integration copies.
3. Connect existing motion, admitted video B-roll and transitions to guided
   execution, preserving graphics order, source timing and the shared audio master.
4. Qualify full presenter to inset/bubble/split and back, including captions and
   graphics, visible framing and sound continuity across opening/body boundaries.
5. Supported-source finishing and output quality, plus remaining Studio preview
   visibility verification. C0679 wide-gamut processing remains a separate gap.
6. Render a real opening and full body, make one actual requested revision, and
   measure picture/sound quality plus the complete workflow time. The 10-minute
   output / approximately two-hour target is still unproven.

Broader one-request autonomy, custom dashboard polish, continuous head-following
and porting every catalog item are not prerequisites added to this assisted-use
scope. A 60–90-second real-footage evaluation should proceed once its actual
source/readiness prerequisites are met; do not wait for every cache optimization.

## Reserved for the main Codex agent

- Existing V9/V10 review, reconciliation and guided activation.
- V10 real-readiness audio timeout/cleanup fix, then actual input to shared-master
  and opening validation. Do not invent another audio version or weaken cut gates.
- Review of the already frozen V11 motion package at
  `/private/tmp/sniper-motion-v11-slice-a-review-20260909.XPQipV/manifest.json`.
  Do not write a competing motion compiler or apply its 12 new files independently.
- Shared contracts/profile reconciliation, live application and final acceptance.

The main agent reports its three prior audit subagents stopped at a subscription
usage limit. They are not active workers. This does not establish the status of
external Claude sessions or authorize a paid/provider fallback.

## Common rules for each parallel agent

You are not alone in this repository. Read applicable repository instructions.
Use an isolated snapshot of the CURRENT dirty project, including relevant
untracked code and required existing local assets. HEAD alone omits ongoing work.
Record baseline and touched-file hashes. Preserve other agents' edits, frozen
packages, source media and original evidence. No reset, cleanup or live patching.

Use existing tools/dependencies read-only. No installs, downloads, paid providers
or catalog refresh. Keep native fixtures short and serial; coordinate substantial
rendering so simultaneous encodes do not consume the same host capacity or make
performance comparisons misleading. Do not start a full repository suite.

Own the isolated files specified below. Keep any proposed change outside those
files in a separate integration patch for Codex, with exact production ownership
and failing reproduction identified. Do not change global profile/source2 gates
to make a local fixture pass. Synthetic media tests mechanisms; record explicitly
whether an actual admitted recording was used and whether human review occurred.

At the first checkpoint write the report named for your assignment, with snapshot
path, baseline, intended owned files and first useful reproduction/result. Preserve
an existing unrelated report and choose a unique replacement path if needed.
At delivery freeze the snapshot and supply patch hashes, focused tests/results,
retained media/frames, observed limitations and integration steps. Preserve failed
attempts. Do not claim live acceptance or hours saved from a frozen package.

## Assignment A — presenter framing and transition qualification

Recommended first. Build a reusable visual regression fixture around the EXISTING
presenter implementation. Initial ownership is new narrowly named test/diagnostic
fixtures and evidence; do not rebuild the renderer or shared guided contracts.

Inspect `scripts/producer/graphics/presenter_layout_graph.py`,
`presenter_layout_geometry.py`, `presenter_layout_contract.py`, existing presenter
media tests and the `guided_presenter_caption_*` consumers. Existing tests already
exercise small synthetic geometry; repeating only those checks is not delivery.

Produce actual full-presenter → inset/bubble and split/presentation → full-presenter
sequences. Check entry, intermediate, hold and exit frames, including cuts and
opening/body boundaries. The presenter must remain in frame whenever shown.
Use explicit protected face/head regions in a controlled fixture, and inspect an
existing admitted real recording where available. This is not continuous tracking.

Include captions and a representative graphic using existing permitted callers.
Check complete caption visibility, overlap, clipped heads, blank/jumped frames,
presentation readability and audio continuity. Use frame/region-specific checks;
pixel counts or frame counts alone cannot prove the correct object or word appears.
Compare a bounded opening/prefix with the corresponding full sequence.

Deliver a concrete pass/fail matrix with viewed frames and exact failing producer
owner for every gap. If a guided combination is currently rejected, retain that
blocker; a low-level render must not be called guided acceptance. Report the
failing production owner before proposing edits. Coordinate contract overlaps;
put any owner-approved focused correction in a separate patch. Do not remove
presenter/body/source2 restrictions as part of fixture construction.

Report: `/private/tmp/sniper-presenter-qualification-handoff.md`.

## Assignment B — ordinary supported-source color qualification

Recommended alongside A. Trace an actually supported ordinary admitted Rec.709
source through existing finishing, preview and export. Initial ownership is new
narrowly named color test/diagnostic fixtures and retained evidence. Existing
color contracts and shared guided authority modules remain with Codex.

Start from `scripts/producer/color/` and the current source/grade media tests;
identify the actual executable caller before claiming a color path works. Some
modules only validate supplied metadata or compile a transform: neither proves
decoder execution, a grade application or source admission.

Compare a good-input unchanged control and an explicitly supported correction.
Inspect representative pixels/scopes, skin/neutral regions where present, clipped
highlights/shadows, transfer/range/primaries tags, frame timing and preview/export
agreement. Keep correction below graphics so captions/cards retain their intended
colors. Verify unchanged audio and original source bytes. Never grade good footage
merely to show activity or infer a camera/look from tags alone.

Use existing admitted real Rec.709 media when available plus controlled reference
fixtures. Do not relabel C0679 as Rec.709 to admit it. If the correction/application
owner is missing or a gate rejects the path, report the exact gap and production
owner; do not bypass it or call metadata-only success finishing. Supply any
proposed narrow production fix separately for Codex reconciliation.

C0679/source2 wide-gamut work is excluded from B: its native reducer still needs
decode/filter-sink, timing, EOF, hash/runtime integration and activation. A separate
native-driver assignment would require its own bounded ownership reservation.

Report: `/private/tmp/sniper-rec709-qualification-handoff.md`.

## Assignment C — caption-only revision composite reuse

Useful optional third worker; speed optimization, not a prerequisite for testing
the first real edit. Own isolated changes to
`scripts/producer/audio/assemble_source_audio.py`, `assemble_publication.py` and
focused tests. Trace existing `captions/caption_assemble.py`, graphics placements,
cache receipts and callers before proposing any helper change; helper/shared
changes belong in a separate integration patch.

The caption-shard fix is already live and must remain intact. The missing part is
reuse of the unchanged caption-free video/graphics composite, with the matching
placement evidence. Copy only the originally held exact prior picture, receipt
and necessary placement dependencies after validation. Use independent inodes.
Do not authorize reuse from a mutable latest pointer, file existence or equal
plan text. Preserve existing dependency checks, public holds and deadlines.

Prove an actual caption-only revision skips unchanged picture work, reuses the
unchanged caption shard and renders the changed one. Compare cached and uncached
controls with identical source, renderer and encoding settings: full video/audio
packet parity, valid placements, complete output audits and preserved parents.
Test changed/missing placement evidence, changed base/graphics/font/tool inputs,
concurrent substitution, linked files and interrupted publication. Preserve
ordinary versus held-preparation versus owned-graphics boundaries.

Record skipped work independently of output-byte equality, and report timings
honestly. The previous four-second shard test showed actual reuse but no total
speedup; do not reuse its result as a composite speed claim or full-video forecast.

Report: `/private/tmp/sniper-caption-composite-reuse-handoff.md`.

## Lower-priority alternative — Studio off-window visibility

The SDK 0.8.31 foreign-HTMLElement-realm predicate issue has a reproduction but no
deployed fix. A separate worker could verify it and prepare a minimal compatible
fix with actual player-control/off-window tests. Reserve its files with Codex
before starting. No runtime URL override, silent installed-node_modules changes,
or invented renderer provenance. Custom UI polish is outside this assignment.
